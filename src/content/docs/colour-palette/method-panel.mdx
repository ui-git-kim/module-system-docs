---
title: Method Panel
description: The four-axis Method panel — Harmony × Style × Impact × Purpose-&-Industry — that biases palette generation through a versioned, evidence-tagged constraint config and a merge engine.
sidebar:
  order: 9
---

import { Aside } from '@astrojs/starlight/components';

The **Method panel** makes Playground generation intelligent without over-claiming. Four optional axes each contribute an OKLCH constraint set; a merge engine composes them into one resolved constraint that biases generation.

<Aside type="caution" title="Associations are conventions, not guarantees">
Colour-emotion research is mostly about what people *associate* with colours, not what colours reliably *make* people feel, and the causal "red impairs performance" line has documented replication failures. So the panel **biases** generation toward conventions — it never promises a psychological effect. UI copy reads "tends to feel…", never "makes users feel…", and every mapping carries an **evidence** tag (peer-reviewed vs convention).
</Aside>

## The four axes

All four are optional and default to **Any**. When at least one is set, the panel drives generation; otherwise the Playground's mode + mood controls do.

| Axis | What it sets | Driven by |
|------|--------------|-----------|
| **Harmony** | The hue-wheel template — Monochromatic, Analogous, Complementary, Split-complementary, Triadic, Tetradic | Hue relationships |
| **Style** | The look — Pastel, Soft, Bright, Vibrant, Neon, Muted, Moody, Dark, Earthy, Elegant, Retro, Warm, Cool, Playful | Mostly **L** and **C**, soft hue window |
| **Impact** | The feel — Calm, Trustworthy, Energetic, Bold, Joyful, Serious, Luxurious, Friendly | Mostly **L** and **C** (valence × arousal), soft hue lean |
| **Purpose & Industry** | The job — Finance/Tech, Healthcare, Food, Eco, Luxury, Children, Data-viz, Illustration, Generic UI | Hue prior + L/C lean + a palette-**size** prior |

Style and Impact lean on **lightness and chroma** because those carry most of the emotional signal and replicate across cultures (Valdez & Mehrabian 1994: Pleasure ≈ 0.69·B + 0.22·S, Arousal ≈ −0.31·B + 0.60·S). Hue is weaker and more culturally variable, so axes only ever apply a **soft** hue lean.

## How axes compose

Each axis value maps to a `ConstraintContribution`:

```ts
{
  L?: { min?, max?, target?, weight? },   // soft range + optional hard bound
  C?: { min?, max?, target?, weight?, relativeToCusp? },
  H?: { biasCenters?: { h, weight }[] },  // soft only
  harmonyTemplate?: SchemeKind,           // Harmony axis only
  distinct?: boolean,                     // Purpose = Data-viz
  paletteSize?: { min?, max?, target? },  // advice, never forced
  rejection?: { minDeltaE?, cvdSafe? }
}
```

The **merge engine** (`resolveMethod`) composes the active axes:

- **Hard bounds intersect** across axes (tightest min/max), in priority order.
- **Soft targets weight-average** and clamp into the resolved range, fed to the generator as a pull toward the preferred L/C.
- **Conflicts resolve by priority: Harmony > Purpose > Style > Impact.** Harmony defines the geometric skeleton; Purpose carries the hardest functional needs; Style and Impact are aesthetic biases that yield first.
- **Relax-to-soft:** if a hard intersection would be empty (e.g. Neon wants high chroma, Luxury wants low), the lower-priority bound is relaxed to a soft target rather than failing — and the relaxation is surfaced in the panel.

The result feeds the existing `generatePalette` / `harmonySetColours` at the Playground's `regenerate()` seam. Locked colours are still respected; the soft bias only steers where inside the feasible region new colours land.

## Honesty & transparency

The panel shows, for each active axis, its "tends to feel…" description and evidence tag; the resolved L/C ranges, hue lean, harmony and suggested size; and any relaxations. The palette-size prior is shown as **advice** — it never resizes the palette for you.

<Aside type="note" title="The numbers are config, not code">
Every semantic → OKLCH mapping lives in versioned JSON config (`METHOD_CONFIG_VERSION`), so ranges can be tuned, A/B-tested, or localised without code changes. The Area 3 (L,C) boxes, the warmth/weight formulas, the Valdez–Mehrabian OKLCH proxy scaling and the priority ordering are **engineering syntheses** — starting points to validate in the Playground, not established standards.
</Aside>

## Culture

Beyond the four axes, an optional **Culture** selector localises generation to a culture/region: it folds a *soft* hue lean toward that culture's positive associations into the hue layer, and surfaces non-blocking cultural **cautions** for the palette's charged colours. It draws on the [Colour Semantics](/colour-palette/colour-semantics/) reference data — information, never a gate. The list is a short curated set of cultures (China & East Asia, India & South Asia, Western, Islamic world, …) rather than every granular context in the data.

## Status

- **v2.17.0** — the Stage 1 skeleton: the four axes, the merge engine, the versioned config, and the Method sheet.
- **v2.18.0** — the per-region soft-hue lean and non-blocking cautions (then labelled "Audience").
- **v2.20.0** — renamed **Audience → Culture** and curated the selector into a short, friendly list.

Still to come (see the [Roadmap](/colour-palette/roadmap/)): the Playground benchmark (50 palettes per Style × Impact preset, target < 20% empty/over-constrained) and empirical validation of the synthesised numbers.

## See also

- [Colour Semantics](/colour-palette/colour-semantics/) — the cultural-association layer that feeds Area 4 hue biases and Area 5 lookups
- [Palette](/colour-palette/palette/) — the Playground the panel drives
- [Roadmap](/colour-palette/roadmap/) — staged build plan and open validation work
