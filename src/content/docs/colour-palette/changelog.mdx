---
title: Changelog
description: Release history for the Colour Palette module.
sidebar:
  order: 5
---

## 2.24.2 — 2026-06-26

### Playground: batched colour-name lookups (fixes the 429 storm + Save), Generator dropdown, scrollable panel

- **One name request per roll, not one per swatch** — generating used to fire a nearest-name lookup for *every* swatch, so even a few rapid Space presses tripped the server's rate limit (HTTP 429). That storm also exhausted the shared rate budget and **broke Save palette**. The Playground now resolves all swatch names in a **single batched, debounced request** per roll (via a new name-only endpoint), and never retries on 429.
- **Generator is a dropdown** — the construction-strategy picker keeps its card look (label + one-line hint) but collapses into a single dropdown to save vertical space.
- **Scrollable Method panel** — the panel body now scrolls on its own, so a tall panel no longer pushes the whole page; the colour columns fill the viewport.

## 2.24.1 — 2026-06-26

### Playground: card-style Method panel + naming / interaction fixes

- **Card-style Method panel** — the Generator is a list of selectable **cards** (label + a one-line hint) and the constraint axes (Aesthetic / Style / Impact / Purpose) are **pill grids**; the Harmony scheme picker is a pill sub-panel and Culture is a full-width dropdown. The detailed "tends to feel…" copy and evidence tags now live in **hover tooltips** rather than tiny inline text.
- **Colour names no longer vanish under rapid rolls** — the thesaurus nearest-name lookup is now **debounced** per swatch and no longer **retries on HTTP 429**, so holding Space stops flooding the endpoint (the cause of the missing names).
- **Functional swatches show a colour name** — they now get the same nearest-name suggestion as hero / accent swatches, instead of the word "functional".
- **Space always generates** — it no longer opens the Generator control; choosing an option returns focus so the next Space rolls.
- **Edge "+" buttons** at the start / end of the row reveal on hover of that end column, matching the add-between buttons.
- **Names + hex sit higher** in each swatch, and the "Press Space" hint **auto-hides** after the first roll.

## 2.24.0 — 2026-06-26

### Method panel: Harmony becomes the Generator axis

Generation gains a first-class **Generator** dimension — *how* a palette is built — separate from the constraint axes that say *what* the colours should be.

- **Generator axis** — a new selector (**Auto**, **Harmony**, **Golden spread**, **Distinct**, **Anchor blend**, **Hue-shift**, **Cosine gradient**, **Scatter**) picks the construction strategy applied *within* the merged constraints. **Auto** keeps the previous varied roll. The old **Harmony** axis folds in as one generator, with a **contextual sub-panel** that swaps per selection (Harmony reveals a scheme select; Anchor blend will reveal its playground).
- **Restored modes + new generators** — the explicit modes removed in 2.22.0 are back (golden / distinct / the harmony schemes), plus three new native-OKLCH strategies: **Anchor blend** (poline-inspired path through anchor colours with an easing curve), **Hue-shift** (the pixel-art ramp) and **Cosine gradient** (Inigo Quilez per-channel cosine).
- **Constraint axes are now four** — Aesthetic × Style × Impact × Purpose-&-Industry. The priority chain drops Harmony: `Aesthetic > Purpose > Style > Impact`. An explicit Generator takes precedence over the Aesthetic's per-role band sampling (which now drives the roll only on **Auto**); the aesthetic still leans hue softly through the merge.
- **Harmony schemes keep their tonal spread** — Auto and the harmony schemes still get the deliberate light/dark lightness ramp most rolls; the other strategies build their own structure.
- **Config-as-data** — `MethodConfig` gains `generators` and `schemes`, extending the 2.23.2 admin-overridable seam. Backend config + the Palette Methods admin page follow in 2.25.0.

## 2.23.0 — 2026-06-26

### Method panel: the Aesthetic axis — colour regions, not fixed palettes

A fifth, optional Method axis that constrains generation to a **vibe**.

- **Aesthetic axis** — pick an aesthetic (**Cyberpunk**, **Retro-Futurism**, **Lunarpunk**, **Goth**, **Cottagecore**, **Minimalist / Scandinavian**, **Mid-Century Modern**, **Old Money / Ivy**) and generation is constrained to that look. The selector is **grouped into six families** (Neon / Futurist, Dark / Romantic, Natural / Cosy, Clean / Modern, Bold / Retro-graphic, Preppy).
- **Regions, not palettes** — each aesthetic is OKLCH **bands** (a hero/base band, an accent band, an optional functional band — L range + C range + hue windows) plus a **selection rule**, not a fixed set of hexes. Re-rolls (Space) sample *within* the bands, so each one looks visibly different but stays on-vibe. Versioned config data (`palette-aesthetics.ts`, `AESTHETIC_CONFIG_VERSION`).
- **Selection rules** — *pick-one* (Goth / Lunarpunk draw exactly one accent hue per roll), *warm-lead + one cool* (Retro-Futurism), and *avoid* (Lunarpunk rejects red / magenta).
- **Priority** — `Harmony > Aesthetic > Purpose > Style > Impact`: a strong driver that still yields to an explicit Harmony. It emits the same `ConstraintContribution` (a soft L/C lean + hue centres) and composes through the merge engine; the real per-role bands ride alongside and are applied at generation time, so a dark base + bright accent don't collapse into one region.
- **Functional colour type** — a third colour type now surfaces in the Playground *up with* hero and accent (backed by the existing functionals model), **absent by default**. Convert a swatch to **Functional** from its role menu; when the active aesthetic defines a functional band, a re-roll recolours those slots from it.
- **Honest by design** — the Aesthetic axis constrains *hue* more firmly than Style/Impact because it encodes a visual-style convention, not a mood effect; overall muted-vs-vibrant is left to the Style axis. Copy stays "tends to read…".
- First of a staged batch: 8 aesthetics across all six families; the remaining ~8 (Steampunk, Dark Academia, Studio Ghibli, Brutalist, Pop Art, Psychedelic, Postmodern/Memphis, Gen-Z Preppy) follow as config-data adds. See the [Roadmap](/colour-palette/roadmap/).

## 2.22.0 — 2026-06-22

### Playground redesign — full-bleed canvas, header controls, docked panels

A cleaner, Coolors-style workspace.

- **Full-bleed canvas** — the in-page toolbar is gone; swatch columns fill the area edge-to-edge (via the starter's `FullBleed`).
- **Header controls** — a palette selector (New playground · search · recent), **Save** (→ **Update** when editing a saved palette) and **undo / redo** sit in the header centre; **Method** (palette icon) and **History** (clock) toggles sit on the right, next to Favourites. All via the header-toolbar registry, route-scoped to the Playground.
- **Docked panels** — Method and History now open as panels docked on the right *inside* the canvas (titled, closeable), up to two side by side — no more app-edge drawers.
- **Generation is Method-only** — the mode/mood selects are removed; the Method panel is the single place generation is tuned (every axis on "Any" still rolls varied palettes).
- **Undo / redo** — proper redo (`Ctrl/Cmd+Shift+Z`), and one Generate is now a single undo step.
- **Add at the ends** — the "+" now also appears before the first and after the last swatch.

## 2.21.1 — 2026-06-21

### Fixes

- **No more near-duplicate swatches in a palette.** A few-offset scheme (complementary, monochromatic) cycling over more slots could land on several near-identical colours (e.g. three almost-the-same teals). Generation now enforces a minimum perceptual separation — a colliding colour keeps its hue (so the scheme survives) but is pushed to a clearly different tone.

## 2.21.0 — 2026-06-21

### Playground generation is now varied, not "the same spread rotated"

Each Generate now rolls a different **structure** instead of always producing an evenly-spread rainbow.

- **Varied structures** — a random named scheme (analogous / complementary / split-complementary / triadic / tetradic / monochromatic), a golden spread, a max-distinct set, or a free scatter — so successive rolls give genuinely different options (a tight blue mono, then a complementary pair, then a spread…).
- **Tonal spread** — most palettes now get light *and* dark members across the lightness band, instead of five takes on one tone — what makes a palette read as "designed".
- Applies to the Method path (within the resolved constraints + culture lean) and the plain mode + mood path. Locked colours still harmonise around them; a set Harmony axis still produces that scheme with a fresh base each roll; distinct/data-viz keeps a flatter ramp.

## 2.20.1 — 2026-06-21

### Fixes

- **Method generation now varies between rolls.** With a hue lean active (e.g. Purpose = Finance/Tech, or a Culture), the golden-angle start was pinned to the bias centre (±18°), so every Generate produced almost the same spread. The start is now a *leaned-random* hue — it still tends toward the bias but explores a different rotation each roll. Harmony schemes likewise pick a fresh leaned-random base when nothing is locked (rather than anchoring on the existing first colour), and the soft L/C pull is capped lower (0.6) so tones vary too.

## 2.20.0 — 2026-06-21

### Method panel: "Culture" replaces "Audience", with a curated list

- **Renamed** the Method panel's **Audience** selector to **Culture** throughout (label, copy, state).
- **Curated the list.** Instead of every granular `context` from the data (~25 entries, with overlaps like "China" / "China / East Asia" and religion-only contexts), the dropdown now shows a short, friendly set — China & East Asia, India & South Asia, Japan, Western, Islamic world, Ireland, Thailand, Latin America & Catholic Europe — each keyword-matched to the relevant records. Only cultures present in the loaded data appear.

The localisation hue-lean and non-blocking cautions are unchanged; they now filter by the selected culture's matching contexts. A structured culture/region field imported with source links (thesaurus-style) is on the [Roadmap](/colour-palette/roadmap/) to replace this in-code curation.

## 2.19.2 — 2026-06-21

### Fixes

- **Thesaurus admin page** no longer sets breadcrumbs by hand — registry-mounted admin pages get them automatically from `DynamicAdminPage`. Removes the redundant call (which also used the wrong `href` field). Matches the Colour Semantics admin page.

## 2.19.1 — 2026-06-21

### Fixes

- **Colour Semantics admin page now actually mounts.** The side-effect import that runs its registration was missing from the feature index, so `/admin/colour-semantics` didn't appear. Also dropped the page's manual breadcrumbs — registry-mounted admin pages get them automatically from `DynamicAdminPage`.

## 2.19.0 — 2026-06-21

### Colour Semantics admin page

A permanent home for the reference data at **`/admin/colour-semantics`** (via `adminPagesRegistry`), so seeding is no longer a console call.

- **Seed reference data** button — idempotently loads the 62 built-in records, with inline pending / success / error feedback (including a clear message when the caller isn't an admin).
- **Browse** — records grouped by Berlin–Kay anchor with scope, valence and confidence, the caution flag highlighted, a "Cautions only" toggle, and record / caution counts.

The underlying `POST /api/v1/colour/semantics/seed` endpoint is unchanged.

## 2.18.0 — 2026-06-21

### Colour semantics wired into the Method panel (Phase 2/3)

The colour-semantics layer now **drives generation and surfaces cautions**, not just stores data.

- **Frontend consumer & per-colour lookup.** A typed API client + TanStack Query hooks (`useColourSemantics`), and lookup helpers (`palette-semantics.ts`) that resolve any generated colour to its Berlin–Kay anchor and pull the matching records, derive the selectable audiences, and compute a palette's charged anchors.
- **Audience selector + localisation (Area 4).** The Method panel gains an **Audience** control (the regional/religious contexts present in the data). Selecting one folds a **soft hue lean** toward that audience's positive associations into generation.
- **Non-blocking cautions (Area 5).** For the selected audience, the panel surfaces quiet, dismissible notes for the palette's culturally-charged colours — e.g. *"White is commonly associated with mourning, death, funerals (negative, high confidence)"* — information, never a gate. With no audience set, a one-line hint lists any charged anchors. Copy reads "commonly associated with…".

Still to come: the full modelled per-colour attribute vector (PAD arousal/valence, nameability, CVD distances), the Method-panel benchmark, and the native-reviewer pass on the regional claims — see the [Roadmap](/colour-palette/roadmap/).

## 2.17.0 — 2026-06-21

### Method panel (four-axis generation) + colour semantics layer

Two foundations for more intelligent — and honest — palette generation.

**Method panel — Harmony × Style × Impact × Purpose-&-Industry.** A new slide-in panel in the Playground with four optional axes, each defaulting to **Any**. Every axis value maps to an OKLCH `ConstraintContribution` held in versioned JSON config, tagged with its **evidence** (peer-reviewed vs convention). A merge engine composes the active axes: hard bounds **intersect**, soft targets **weight-average**, conflicts resolve by **priority** (Harmony > Purpose > Style > Impact), and a lower-priority bound **relaxes to a soft target** when the intersection would be empty. Style and Impact are driven mostly by lightness and chroma — the replication-proof levers (Valdez & Mehrabian 1994) — while hue biases stay soft. When any axis is set it drives generation; otherwise the existing mode + mood controls do. Copy is deliberately honest — "tends to feel…", never "makes users feel…" — and the panel surfaces each axis's evidence tag, the resolved constraints, and any relaxations. New `palette-method.ts` (engine + config); `palette-generators.ts` gains a soft-bias layer and a split-complementary scheme.

**Colour semantics — cultural & symbolic associations (data model).** A new global, admin-curated `colour_semantics` reference table keyed to the 14 Berlin–Kay + extended anchors (red … grey, gold, silver, turquoise). Self-contained (no `userId`, no relations); reads are open to any authenticated user, writes are admin-gated; ships an idempotent seed of 62 built-in records under `/api/v1/colour/semantics`. `source` is intentionally left **empty** (real citations are attached later — nothing is fabricated) and `confidence` is preserved so low-confidence/folkloric rows can be down-weighted. A new Berlin–Kay anchor classifier (`palette-bk-category.ts`) maps any OKLCH colour to its nearest anchor using **lightness and chroma, not hue alone**, so brown, pink and the neutrals are distinguished — the keystone the semantics lookup resolves against.

The frontend semantics consumer, the full per-colour attribute vector, the per-region localisation feed with non-blocking caution surfacing, and a native-reviewer pass on the regional claims are next — see [Method panel](/colour-palette/method-panel/), [Colour semantics](/colour-palette/colour-semantics/) and the [Roadmap](/colour-palette/roadmap/).

## 2.16.0 — 2026-06-20

### Palette builder — roles, generators, View sheet, interactive Profile, favourites & history

The Palette Workspace becomes a real builder: colours flow into palettes by role, the Playground and Palette Profile both generate and edit in place, and colours can be favourited and revisited. Host-verified in a starter-template app.

- **Roles into palettes** — the Colour Profile holder is now a list of role-tagged entries (hero / accent / functional) plus a shortlist. The palette sheet groups pickers by role and offers **Open in Playground**, **Create draft**, and **add to an existing palette**. The legacy single-colour brand-save is removed.
- **Generator toolbox** (`palette-generators.ts`) — OKLCH generation primitives: golden-angle sequencing, distinct / best-candidate sampling, harmony inference + tone-line, `harmonySetColours` (analogous / complementary / triadic / tetradic / monochromatic), and a `generatePalette` orchestrator. Drives the Playground's spacebar regenerate.
- **Playground** — seven generation modes (Random, Distinct, Analogous, Complementary, Triadic, Tetradic, Monochromatic); editable hex per slot; a permanent role icon + role dropdown; seeds from the working colour; a **Save dialog** (name + type: brand / ui / game / illustration / web / marketing / data-viz / exploration); and a **History** panel that restores past generations.
- **Colour View sheet** — a wide right-hand sheet (Analytics / Harmonies / Scales) opened by the eye icon on any swatch; its "+" inserts the chosen harmony or scale colour next to the source.
- **Interactive Palette Profile** — drafts load into the workspace store and edit in place: role dropdown, lock, weight slider, shortlist swap, and View per colour; spacebar regenerate with undo; publishing keeps the published version read-only.
- **Favourites (database-backed)** — a new `colour_favourite` table with its own backend service and routes; a `useFavourites` hook; heart toggles on the Playground, Colour Profile, and View sheet; and a header button (via `headerToolbarRegistry`) scoped to the `/colour` and `/palette` routes.
- **Workspace store widened** — functionals, sub-palettes, reference palettes, context templates, changelog and a browsable history, with checkpoint / restore-snapshot and undo across all of them; legacy fractional weights are normalised on load.

The alts and changelog **data contracts** are in place (`PaletteAlts.custom[]`, extended changelog kinds, checkpoint/restore) but their dedicated UI tabs are not yet wired — see the [Roadmap](/colour-palette/roadmap/).

See [Palette](/colour-palette/palette/).

## 2.12.0 — 2026-06-19

### Playground / Library / Profile — second pass

Iterates the v2.8.0 scaffold into usable pages and migrates the legacy data.

- **Playground** rebuilt as a full-bleed, Coolors-style column layout that auto-seeds from the Colour Profile collection (5 colours by default); spacebar generates, locks survive, "+" adds between.
- **Library** moved to `/palettes` with whole-card-clickable navigation to the Profile, plus a one-time migration of legacy Brand/UI saves into the new `palette` table.
- **Palette Profile** redesigned around a palette strip over tabs (Analytics, Alts & Comparison, Sub-palettes, Examples).
- **Legacy Brand/UI library page removed** — the `colour_palette_saved` table is kept for back-compatibility.
- **Route-order fix** — the palette and favourites routers now register **before** the legacy `/colour` router so their sub-paths resolve (the legacy greedy `GET /:id` would otherwise swallow them).

## 2.8.0 — 2026-06-18

### Palette Workspace — first release (scaffold)

The new multi-colour palette system ships alongside the existing Brand/UI flow (which is unchanged). First pass — pending install verification in a host app.

- **Data model** — new `palette` table: heroes/accents, functionals, sub-palettes, reference palettes, context templates, `structureNodeIds` scoping, and an immutable version chain (`versionChainId`; draft · published). Coexists with `colour_palette_saved`.
- **API** — palette CRUD plus the draft → publish → fork lifecycle at `/api/v1/colour/palettes`.
- **Frontend** — typed service + TanStack Query hooks; the auto-weight algorithm (accent pool `min(n×5, 25)%` with per-group redistribution and weight locks); a workspace store; and three pages — **Library**, **Playground**, and **Palette Profile** — at `/palette-workspace/*`, with a sidebar entry.

See [Palette](/colour-palette/palette/) and the [Roadmap](/colour-palette/roadmap/).

## Docs — 2026-06-18

### Palette Workspace design specification incorporated

Documentation only — no module code or version change.

- **New [Colour](/colour-palette/colour/) hub page** — frames the colour engine: the Colour Profile (Analyse, Gamut, Harmonies, Data Vis, Scales, Adjust, Mix), the maths/processes, and links to the engine references and the Thesaurus.
- **New [Palette](/colour-palette/palette/) page** — the in-development Palette Workspace (Playground, Palette Profile, Library), the hero/accent weighting model and auto-weight algorithm, exports, and the full palette data structure for developers.
- **Thesaurus pages merged** — the v2 design doc and the import format are now a single [Thesaurus](/colour-palette/thesaurus/) page.
- **Overview reframed** around the three-level architecture (Colour engine → Palette workspace → Publish); the shadcn pipeline is repositioned as the post-publish step.
- **Integration** — added Style / Structure / Builder integration points and clarified `PaletteOutput` as the published output contract.
- **Roadmap** — added Active Work (Mix step distribution, chroma allocation), the Palette Workspace build plan, and open questions.
- **Sidebar** — grouped the section under a **Colour** sub-group and wired in the previously-orphaned Mixing Playground page.

## 2.7.0 — 2026-05-01

Current release.

### Mixing Playground (new)

- **New Mix tab structure** — replaced the 7 mode tabs (Pair gradient, Weighted pile, etc.) with 4 goal-oriented tabs: 2 Colour Mix, Multi Colour Mix, Neutrals, Tonal Scales.
- **15 mixing algorithms** compared side by side with the same inputs: OKLCH shorter/longer hue, K-M spectral (3 variants), RYB cube interpolation (Gossett & Chen 2004), OKLab cartesian, OKLab + chroma boost, harmony-mapped gradient, pigmentMix (3 containment strategies), harmonizeMix, neutralFromPair, K-M/OKLCH piles, Gurney hull, K-M/OKLCH complementary neutrals, tinted black scale.
- **SVG visual diagrams** per algorithm: hue wheel arcs, OKLab a,b scatter, L-C gamut cross-sections with cusp/boundary/construction lines, brightness bars.
- **Inline parameters** per algorithm panel — each algorithm has its own controls right next to its output.
- **Perceptual steps research section** — gamut width chart, source/destination gamut triangles, even vs optimal gradient comparison.
- **Chain-walk harmony gradient** — walks through every 0.5-degree intermediate gamut using cusp-line projection, accumulates delta-E, resamples at equal perceptual intervals.
- **Hue distribution bias slider** on the OKLab + chroma boost algorithm for researching gamut-aware step spacing.
- **New utility**: `colour-ryb.ts` — RYB cube trilinear interpolation.
- **New component**: `MixLab.tsx` — the full playground component.
- **New docs page**: Mixing Playground — documents all algorithms, the open perceptual steps research problem, and key files.

## 1.48.95 — 2026-04-29

### Refactor

- **Harmony pipeline rewritten as cusp-line projection.** The previous five-stage pipeline (H-K corrections, warm-lift, neon bell, multi-pass refinement) has been replaced by a single-pass geometric construction. `harmonyMap` decomposes the source colour as `(t, r)` inside a perceptual sub-triangle (vertices: `A` at the threshold-curve × gamut-boundary intersection, `B` at the constant-chroma threshold × gamut-boundary intersection, and `cusp` at the gamut peak). It then reconstructs the destination using the same `(t, r)` in the destination's sub-triangle. The mapping is mathematically self-inverse: `harmonyMap(harmonyMap(x, 180), 180) === x` in float OKLCH, with hex round-tripping limited only by 8-bit channel quantisation.
- **New helper `findThresholdCurveEdge(h, loL, hiL, rising)`.** Binary-searches for the L value where the gamut boundary crosses the chromatic-threshold *curve* (which itself varies with L). Used to place vertex A on the rising side where the gamut narrows fast.
- **Green/cyan neon suppression.** A Gaussian chroma cap centred at H=165° (σ=35°, peak strength 30%) pulls chroma back from the gamut boundary in the green-cyan band only, where sRGB's unusually wide gamut produced overwhelming neon results. Other hues are untouched.
- **Removed:** `CUSP_L_CEILING`, `warmBase` / `warmLiftL` / `warmLiftC`, `neonBell`, `neonRisk`, `coolnessSharp`, `hkDeficit`, the asymmetric Pass 5 reduction, and the dual-cusp pattern. The single-pass cusp-line projection subsumes all of these geometrically.
- **`harmonyMap` no longer calls `gamutMap3DShaded`.** The destination sits inside its own sub-triangle by construction, so containment is guaranteed without a final mapping step.

### Research section redesign

- The Harmonies tab's interactive **Research** view is now a hue slider (0–360° destination hue) instead of a relative angle offset, with three side-by-side gamut cross-sections — Source, Raw bary, and Harmony — at full width. The Harmony triangle shows the three construction lines (cusp→Q, B→edge-A→cusp, A→edge-cusp→B) converging at the destination point, and the values panel surfaces the three preserved ratios (`%cusp`, `%B`, `%A`).

## 1.3.34 — 2026-04-08

### Features

- Import brand colours from Coolors URLs, Adobe Color URLs, hex input, and CSS variable blocks
- Auto-assign brand colours to semantic UI roles (action, structure, emphasis, highlight, background, foreground, neutral)
- Generate all 34 shadcn/ui CSS variables in OKLCH colour space for both light and dark modes
- Tinted neutral generation using brand hue for cohesive surfaces
- WCAG 2.1 AA contrast validation for every surface/foreground pair (with APCA support via `apca-w3`)
- Refinement sliders: neutral warmth, brand saturation, surface contrast, dark mode brightness
- Save, load, activate, and delete palettes per user
- Side-by-side light/dark mode comparison view
- Chart colour generation at 72° hue intervals
- Standalone **Colour Schemes** and **Colour Mix** showcase pages
- Embeds as a tab on Structure project nodes via `nodeTabRegistry`
- Self-contained Prisma schema (`colour_palette_saved`) with no cross-module relations
- Public API mounted at `/api/v1/colour-palette` with full CRUD plus `/active` and `/:id/activate`
- `PaletteOutput` exported as the canonical theme contract for consumer modules
