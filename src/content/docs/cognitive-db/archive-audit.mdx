---
title: Archive Audit
description: Per-session archive-readiness log — unfinished work, loose ends, decisions, and ideas captured before a session is archived
sidebar:
  order: 9
---

import { Aside } from '@astrojs/starlight/components';

A running log of **archive-readiness audits**. Before a working session on the cognitive-db library is archived, it is audited for anything that would be lost — unfinished work, loose ends, decisions, and ideas — and the findings are recorded here so a fresh session can pick them straight up.

<Aside type="note" title="Why this page exists">
Session transcripts get archived and are then awkward to search. This page is the durable, reviewable home for "what was in that session" — decisions and their reasoning, ideas raised in passing, work left mid-flight, and anything the code and the rest of these docs don't already capture. Newest entry first. Nothing here is deleted; when an item is actually done, it's ticked and left in place for provenance.
</Aside>

## How to use this page

At the end of a session you want to archive, run the archive-readiness prompt (below). It audits the session and **appends a new entry at the top** of the log, using the entry template. Then it builds and pushes the docs. Review the entry on the live site; archive the session; start the next one by reading the open items here.

### The prompt

> **Archive-readiness check.** Audit this session end-to-end for anything that would be lost if I archive it: (1) unfinished work — started-not-finished, committed-not-pushed, coded-not-validated, or left WIP (check `git status` and recent commits in every repo touched, including the `docs/` submodule — **and if this is an older session the repo has since moved past, expect HEAD, the version number, and the working tree to look _newer_ than this session's output, and its commits to sit deep in history, absent from a short `git log`/reflog window; that is normal forward progress, not loss. Confirm the work survived by identifying this session's own commits from the transcript and checking each is an ancestor of `origin/main` (`git merge-base --is-ancestor <sha> origin/main`) — reachable from origin means preserved even though HEAD is far ahead. Real loss is only a commit that is on no branch _and_ absent from `origin`; do not infer loss from "HEAD isn't at my version" or "the working tree differs from what I edited"**); (2) loose ends — TODOs, "later" items, deferred fixes, promised follow-ups, checks I said I'd run; (3) decisions & ideas — any decision, trade-off, rejected approach and *why*, or idea raised in conversation not already written in code or docs; (4) undocumented reality — anything we changed or learned the docs don't reflect. Then **append a new entry to the top of the Archive Audit log** (`docs/src/content/docs/cognitive-db/archive-audit.mdx`) using the entry template already on that page. Build the docs, push the docs submodule **and** the submodule pointer. Verify before ticking anything "done" (if a version was bumped, confirm `dist` was rebuilt after the bump; for an older session, confirm `dist` — if gitignored — stamps the **current HEAD** version, not the session's). Finish by telling me plainly whether it's safe to archive.

### Entry template

```mdx
## YYYY-MM-DD — <session focus>

**Verdict:** ✅ Safe to archive  ·  ⚠️ Needs input first  ·  ℹ️ Superseded (work integrated, session now historical)
**Shipped this session:** <version(s), key commits, what went live>

**Unfinished / WIP:** <or "none">
**Loose ends & follow-ups:** <deferred fixes, promised checks, TODOs — or "none">
**Decisions & ideas:** <decisions + why, rejected approaches, ideas raised — or "none">
**Recorded where:** <docs pages / memory files updated, so it's findable>
**Open items for next session:** <the pick-up list — or "none">
```

---

## 2026-08-23 — Retrieve-to-connect + C1 part 2 (surprise, temporal/trend, structural gaps) + condensation reframing; A4 cleared host-side

**Verdict:** ✅ Safe to archive
**Shipped this session:** Four engine releases, all pushed + tagged + docs-updated, all ancestors of `origin/master`. **v1.12.5** — retrieve-to-connect (retrieval-augmented extraction / entity linking, flag-gated `features.retrieveToConnect`) + organic-growth `relationClass` classification (feat `bf007af`, release `ca5d6b3`). **v1.12.6** — defence-in-depth: the `related-entities` context item injection-fenced (fix `8975b84`, release `8de57cd`). **v1.12.7** — C1 part 2: surprise detector storage-aware via the optional `StructureAdapter.expandGraph` (feat `6986d67`, release `7071689`). **v1.12.8** — C1 part 2 complete: the intelligence lane (temporal reminders, trend detection, structural gaps) storage-aware via a shared `intelligence/intelligence-store.ts` read bridge + optional `StructureAdapter.getConnectionsForNodes` (feat `eab72da`, release `834c4d7`). Plus a docs-only condensation reframing (`4eadc8c`). 170 vitest tests green; each release passed a multi-lens adversarial review with findings fixed pre-release (notably a HIGH in v1.12.5 where an invalid model `relationClass` would have discarded the whole extraction — now `.catch(undefined)`).

**Unfinished / WIP:** None in the engine — tree clean (only the two pre-existing untracked `docs/COGNITIVE_DB_REFINEMENTS_*.md`, Kim's do-not-implement notes). Nothing coded-not-committed or committed-not-pushed. Three **handover notes** were written for the host work and sit **uncommitted in `cognitive-ingest/docs/drafts/`** (`engine-cog-ingest-handoff.md`, `retrieve-to-connect-cog-ingest-wiring.md` [superseded pointer], `structure-getConnectionsForNodes-request.md`) — on disk, not at risk of engine-side loss, but commit them in that repo if you want them versioned there.

**Loose ends & follow-ups:**
- **✅ A4 is DONE on cog-ingest (Kim, 2026-08-23)** — extraction now produces a graph end-to-end; the former host-side blocker is cleared. Structure is implementing its side ("doing their stuff").
- **Host activation still pending** (the shipped engine features are dormant until wired): retrieve-to-connect needs cog-ingest to push `scopeRootId` + `unreadableTypes` on `ingest()` and forward `scopeRootId`/`excludeNodeTypes` through `buildStructureAdapter`; **surprise** needs `expandGraph` wired; **structural gaps** needs Structure's new `getConnectionsForNodes` + wiring. **Temporal + trend work as soon as the adapter is injected** (no new host method). Also: persist a proposed `relationClass` onto the Structure connection type; §7 per-source extraction-guidance field. Full detail in `cognitive-ingest/docs/drafts/engine-cog-ingest-handoff.md` + `structure-getConnectionsForNodes-request.md`.
- **Condensation** reframed as host-triggered **data minimisation** and parked (staleness "archive unused" dropped as it violates never-decay; merge/re-parent blocked on a Structure node archive/merge/re-parent API). No engine code change pending a host-side design.

**Decisions & ideas:** Retrieve-to-connect — reused the existing `relationClass` overlay rather than Burgess's branded SST names (neutral-identifiers rule); scope resolved by cog-ingest and pushed via `IngestInput` (engine only forwards — matches engine-scope-boundaries); entity-free-summary query vector (Kim's call over chunk-vectors); folded in organic-growth `relationClass` classification. v1.12.6 — fenced `related-entities` (deviated from the brief's "trusted context" stance on the review's second-order-replay finding, with Kim's explicit go). Surprise / structural gaps — declared `expandGraph` / `getConnectionsForNodes` as **optional** adapter methods, degrade-safe/dormant until the host wires them (same pattern as retrieve-to-connect), and routed the whole intelligence lane through one shared bridge so the v1 path stays byte-identical. **Rejected:** building structural-gap merge / condensation merge without the Structure capabilities (would be dead code); eager temporal/trend porting was earlier deprioritised but then done this session at Kim's request.

**Recorded where:** engine `CHANGELOG.md` + docs-site changelog/roadmap for every version (v1.12.5–v1.12.8); `phase3-status.md` (current state = v1.12.8 + a NEXT PICK-UP pointer), `engine-nextphase-worklist.md` (C1 part 2 done, A4 done, host follow-ups), `MEMORY.md` index; internal `docs/PHASE_E_RESOURCE_OPTIMISATION.md` (condensation reframing note); this entry. Handover prompt in chat.

**Open items for next session:** The handover prompt in chat (session-handover Phase 2). No engine work is blocking — the next substantive engine phase is **B, the rest of the read-lane** (audit which retrieval/discovery/patterns/learning/algorithms services still bind `cog_node` directly, then flip the ones cog-ingest invokes) → toward `cog_node` retirement + **v2.0.0**. Recommended first step: a read-only audit of the remaining cog_node-bound read services.

---

## 2026-08-22 — Phase 3 intelligence-lane sweep (A, C2, C1 core, C3) + extraction-readiness check

**Verdict:** ✅ Safe to archive
**Shipped this session:** Five engine releases, all pushed + tagged + docs-updated, all ancestors of `origin/master`. **v1.12.0** — A: AI Core ops catalogue as the `./operations` export (`COGNITIVE_DB_OPERATIONS`, unblocks extraction on the engine side) + `AiContextItem.role`/`marker` (commits `4c2d109`, `cc11b7f`). **v1.12.1** — C2: storage-aware working-memory consolidation + new `StructureAdapter.updateConnection` (`e58665b`, `8aa12c9`). **v1.12.2** — kebab-case `co-activated` connection type (`efba851`, `a594d3b`). **v1.12.3** — C1 clean core: the proactive Suggestions-Inbox path (contradiction, gaps, temporal reminders, question-answers) storage-aware, with shared `structure-store/node-view` + `type-mapping` bridges and `ensureStructureConnection` (`002bfb2`, `28407be`). **v1.12.4** — C3: aged-content summarisation wired to the `cognitive-db/summarise` op + raw-content archive safety (`fe2f312`, plus the release commit at HEAD `30d2748`). 144 vitest tests green throughout; each substantive change passed a multi-lens adversarial review with findings fixed pre-release.

**Unfinished / WIP:** None in the engine — tree clean (only two pre-existing untracked `docs/COGNITIVE_DB_REFINEMENTS_*.md` files, which are Kim's own do-not-implement notes, deliberately left untracked). Nothing coded-not-committed or committed-not-pushed.

**Loose ends & follow-ups:**
- **⚡ Extraction is blocked host-side (A4), not engine-side.** Verified in `veradai`: it vendors engine **v1.11.1** (pre-`./operations`) and `cog-ingest.ai.ts` (v1.21.10) never seeds the engine's `cognitive-db/*` language ops, seeds `cog-ingest/embed-*` instead of the engine's `cognitive-db/embed-*` (id mismatch), and `buildAiAdapter` frames everything as `context` (ignores `role`). A full extract hard-fails until cog-ingest is re-vendored to v1.12.4 and wired. **I offered to implement the ~30-line cog-ingest fix in veradai; Kim had not greenlit it at handover.**
- **cog-ingest / host follow-ups:** wire `updateConnection` (Structure side confirmed present); pass `userId` to `markAnswered`; seed the `qualifies`/`may_answer`/`answers`/`co-activated` connection types; provision the `cog_content_archive` table + configure the `cognitive-db/summarise` op.
- **C1 part 2 (engine, deferred by Kim):** surprise detector via Structure's `expandGraph` (confirmed to exist — highest-value remaining, on the inbox path); paginated temporal/trend ports; structural gaps (needs a new `getConnectionsForNodes` on Structure; `listNodes` caps at 100).

**Decisions & ideas:** C2 — added `updateConnection` to the adapter to do C2 fully rather than ship a create-only partial (Kim's call; Structure turned out to already have it). C1 — scoped to the "clean core" (inbox path) and used `data.answeredBy` for answered-question state because Structure's `updateNode` can't change `status`; deferred surprise/temporal/trend/structural because they need new host capabilities or pagination. C3 — made `archiveRawContent` throw (was swallowing the error) so raw content is never cleared without a durable archive, and dropped the first-3-sentences fallback in favour of the summarise op. Rejected: porting the age-based temporal/trend detectors eagerly (they truncate to 100 under Structure and aren't on the inbox path — lower value than surprise-via-`expandGraph`). Key discovery: `sourceEngineVersion` in the ops catalogue is now release-stamped from `package.json` + bound by a test, closing the drift class that bit the old JSON.

**Recorded where:** `engine-nextphase-worklist.md` (A/C2/C1/C3 entries + the verified veradai A4 host facts + C1-part-2 host facts), `phase3-status.md` (current state = v1.12.4), `MEMORY.md` index (extraction-blocker flagged); engine `CHANGELOG.md` + docs-site changelog/roadmap for every version; this entry.

**Open items for next session:** The handover prompt in chat (session-handover Phase 2) — priority 1 is the A4 cog-ingest wiring in veradai to unblock extraction (confirm with Kim first, separate repo), priority 2 is C1 part 2 (surprise via `expandGraph`).
