---
title: Code Templates
description: Copy-paste templates for module development
---

import { Aside, Tabs, TabItem } from '@astrojs/starlight/components';

These templates are ready to copy into your module's `src/templates/` directory. Remember to use placeholder patterns — `My Feature` for titles/labels, `my-feature` for kebab-case, `MyFeature` for PascalCase, `myFeature` for camelCase, `my_feature` for snake_case, `MY_FEATURE` for SCREAMING_SNAKE, and `my feature` for lowercase prose. All patterns get replaced with the actual module name during installation. See [Template Placeholders](/module-starter/building-modules/#template-placeholders) for the full reference.

<Aside type="tip">
All templates should include the standard file header with `FILE:` and `PURPOSE:` comments.
</Aside>

<Aside type="caution">
**Backtick escaping:** Templates are template literal strings. If your generated code uses backticks (`` ` ``) or template expressions (`${}`), you must escape them: `` \` `` and `\${}`. This is the most common cause of syntax errors in generated files. See [Escaping Rules](/module-starter/building-modules/#escaping-backticks-and-template-expressions) for examples.
</Aside>

---

## Frontend Templates

### Index (Barrel File)

```typescript
// src/templates/frontend/routes.ts
export const INDEX_TEMPLATE = `// ========================================================================
// FILE: frontend/src/features/my-feature/index.ts
// PURPOSE: Public exports and registry integrations for my-feature
// ========================================================================

// ========================================================================
// REGISTRATION IMPORTS (side effects)
// These MUST come first - they register the module with app registries
// ========================================================================

// Navigation: adds items to sidebar/user menu
import './navigation.registration';

// Collection registrations (uncomment as needed):
import './registrations/dashboard.registration';
import './registrations/preferences.registration';
import './registrations/settings-page.registration';
// import './registrations/integrations.registration';
// import './registrations/personal-info.registration';
// import './registrations/notification-preferences.registration';
// import './registrations/language-options.registration';
// import './registrations/style.registration';
// import './header-toolbar.registration';
import './admin-pages.registration';   // Admin data export/import page (C21–C24)

// Pipeline hooks (uncomment to enable):
// import './filter.registration';
// import './action.registration';

// ========================================================================
// PUBLIC EXPORTS
// ========================================================================

// Routes (for spreading into router)
export { myFeatureRoutes } from './routes.config';

// Pages
export { MyFeaturePage } from './pages/MyFeaturePage';

// Components (export as needed)
// export { MyComponent } from './components/MyComponent';

// Data hooks (TanStack Query wrappers around the service)
export {
  useMyFeatureList,
  useMyFeatureItem,
  useCreateMyFeatureItem,
  useUpdateMyFeatureItem,
  useDeleteMyFeatureItem,
} from './hooks/useMyFeature';

// Types
export type * from './types';
`;
```

### Navigation Registration

The navigation registry supports three areas: main sidebar, secondary sidebar (bottom), and user menu. You can also group items into sidebar sections.

```typescript
// src/templates/frontend/registrations.ts
export const NAVIGATION_REGISTRATION = `// ========================================================================
// FILE: frontend/src/features/my-feature/navigation.registration.ts
// PURPOSE: Register navigation items for my-feature
// ========================================================================

import { navigationRegistry } from '@/config/navigation.registry';
import { Sparkles } from 'lucide-react';

// Main sidebar item
navigationRegistry.registerMain({
  id: 'my-feature',
  title: 'My Feature',
  url: '/my-feature',
  icon: Sparkles,
  order: 30,
  // sidebarSection: 'my-section',  // Optional: assign to a section
  // badge: '3',                     // Optional: badge text/count
  // items: [{ title: 'Sub', url: '/...' }],  // Optional: collapsible sub-items
});

// Secondary sidebar (optional - bottom section)
// navigationRegistry.registerSecondary({
//   id: 'my-feature-secondary',
//   title: 'My Feature',
//   url: '/my-feature',
//   icon: Sparkles,
//   order: 50,
// });

// User menu (optional - user dropdown)
// navigationRegistry.registerUserMenu({
//   id: 'my-feature-menu',
//   title: 'My Feature',
//   url: '/my-feature',
//   icon: Sparkles,
//   order: 30,
// });
`;
```

<Aside type="tip">
Modules can register **sidebar sections** to group navigation items. Use `navigationRegistry.registerSection()` to create a labelled group. See the [Navigation Registry reference](/starter-template/registries/#navigation-registry) for details.
</Aside>

### Routes Configuration

```typescript
export const ROUTES_CONFIG = `// ========================================================================
// FILE: frontend/src/features/my-feature/routes.config.tsx
// PURPOSE: Route definitions for my-feature
// ========================================================================

import { lazy } from 'react';
import type { RouteObject } from 'react-router';

const MyFeaturePage = lazy(() =>
  import('./pages/MyFeaturePage').then((m) => ({ default: m.MyFeaturePage }))
);

export const myFeatureRoutes: RouteObject[] = [
  {
    path: 'my-feature',
    element: <MyFeaturePage />,
  },
];
`;
```

### Page Component

```typescript
// src/templates/frontend/pages.ts
export const PAGE_TEMPLATE = `// ========================================================================
// FILE: frontend/src/features/my-feature/pages/MyFeaturePage.tsx
// PURPOSE: Main page for my-feature
// ========================================================================

import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';
import { Button } from '@/components/ui/button';

export function MyFeaturePage() {
  return (
    <div>
      {/* Page Header */}
      <div className="mb-6">
        <h1>My Feature</h1>
        <p className="mt-1 text-muted-foreground">
          Description of what this feature does.
        </p>
      </div>

      {/* Page Content */}
      <Card>
        <CardHeader>
          <CardTitle>Getting Started</CardTitle>
          <CardDescription>
            Welcome to My Feature.
          </CardDescription>
        </CardHeader>
        <CardContent>
          <p className="text-muted-foreground">
            This is a placeholder. Replace with your feature's UI.
          </p>
          <Button className="mt-4">Get Started</Button>
        </CardContent>
      </Card>
    </div>
  );
}
`;
```

### Detail Page (with Breadcrumbs)

Pages that display a specific entity (e.g., a project, invoice, or user) should set breadcrumbs so the site header shows the correct trail (e.g., "Projects > My Project").

```typescript
// src/templates/frontend/pages.ts
export const DETAIL_PAGE_TEMPLATE = `// ========================================================================
// FILE: frontend/src/features/my-feature/pages/MyFeatureDetailPage.tsx
// PURPOSE: Detail page for a single my-feature item
// ========================================================================

import { useEffect } from 'react';
import { useParams } from 'react-router';
import { useBreadcrumbs } from '@/components/layout';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function MyFeatureDetailPage() {
  const { id } = useParams<{ id: string }>();
  const { setBreadcrumbs } = useBreadcrumbs();

  // TODO: Replace with your data fetching hook
  const item = { name: 'Loading...' };

  // Set breadcrumbs when data loads
  useEffect(() => {
    if (item) {
      setBreadcrumbs([
        { label: 'My Feature', path: '/my-feature' },
        { label: item.name },  // No path = current page (not a link)
      ]);
    }
  }, [item, setBreadcrumbs]);

  return (
    <div>
      <Card>
        <CardHeader>
          <CardTitle>{item.name}</CardTitle>
        </CardHeader>
        <CardContent>
          <p className="text-muted-foreground">
            Detail content for item {id}.
          </p>
        </CardContent>
      </Card>
    </div>
  );
}
`;
```

<Aside type="tip">
Settings and admin pages set breadcrumbs automatically from the registry. You only need `useBreadcrumbs()` in your own custom page components — typically detail/edit pages that show a specific entity name in the header.
</Aside>

### Frontend Service

Every frontend service must use the shared `api` client — never `fetch()` or a custom Axios instance. The `api` client automatically injects auth tokens and points to the correct backend URL.

```typescript
// src/templates/frontend/services.ts
export const SERVICE_TEMPLATE = `// ========================================================================
// FILE: frontend/src/features/my-feature/services/my-feature.service.ts
// PURPOSE: API service for my-feature
// ========================================================================
// Every method accepts an optional AbortSignal so callers can cancel
// in-flight requests. The hooks in ../hooks/useMyFeature.ts thread
// TanStack Query's signal through automatically. Do NOT call this
// service directly from components — use the hooks.
// ========================================================================

import { api } from '@/lib/api';
import type {
  MyFeatureItem,
  MyFeatureListOptions,
  MyFeatureListResponse,
  CreateMyFeatureItemInput,
  UpdateMyFeatureItemInput,
} from '../types';

interface RequestOptions {
  signal?: AbortSignal;
}

export const myFeatureService = {
  // Cursor-paginated list — returns { items, nextCursor, hasMore }, not an array.
  async getAll(
    options: MyFeatureListOptions & RequestOptions = {}
  ): Promise<MyFeatureListResponse> {
    const { signal, limit, cursor } = options;
    const response = await api.get<MyFeatureListResponse>('/api/v1/my-feature', {
      params: { limit, cursor },
      signal,
    });
    return response.data;
  },

  async getById(id: string, options: RequestOptions = {}): Promise<MyFeatureItem> {
    const response = await api.get<MyFeatureItem>(\`/api/v1/my-feature/\${id}\`, {
      signal: options.signal,
    });
    return response.data;
  },

  async create(
    input: CreateMyFeatureItemInput,
    options: RequestOptions = {}
  ): Promise<MyFeatureItem> {
    const response = await api.post<MyFeatureItem>('/api/v1/my-feature', input, {
      signal: options.signal,
    });
    return response.data;
  },

  async update(
    id: string,
    input: UpdateMyFeatureItemInput,
    options: RequestOptions = {}
  ): Promise<MyFeatureItem> {
    const response = await api.patch<MyFeatureItem>(\`/api/v1/my-feature/\${id}\`, input, {
      signal: options.signal,
    });
    return response.data;
  },

  async delete(id: string, options: RequestOptions = {}): Promise<void> {
    await api.delete(\`/api/v1/my-feature/\${id}\`, { signal: options.signal });
  },
};
`;
```

<Aside type="caution">
**Never use `fetch()` or `new axios.create()` in frontend templates.** The shared `api` client from `@/lib/api` automatically injects the user's JWT token on every request. Without it, your API calls will fail with 401 Unauthorised. See [Auth Feature — API Client](/starter-template/auth-feature/#the-api-client) for details.
</Aside>

### Data Hooks (TanStack Query)

The `QUERY_HOOKS_TEMPLATE` generates `hooks/useMyFeature.ts` — a thin per-operation wrapper around the service using the starter template's shared TanStack Query client. Components should consume these hooks rather than calling the service directly.

Key conventions:
- **Key namespacing** — uses `createModuleQueryKeys('my-feature')` from `@/lib/query-keys`. Module keys **must** be namespaced with the module ID to prevent cache collisions between modules.
- **No custom hook factories** — write `useQuery`/`useMutation` directly at each call site. See [Starter Template — Data Fetching](/starter-template/data-fetching/) for the rationale.
- **Signal threading** — TanStack Query's `signal` is passed through to the service's `AbortController` support so unmount/re-fetch cancels in-flight requests automatically.

```typescript
// src/templates/frontend/hooks.ts
export const QUERY_HOOKS_TEMPLATE = `// ========================================================================
// FILE: frontend/src/features/my-feature/hooks/useMyFeature.ts
// PURPOSE: Data hooks for my-feature
// ========================================================================

import { useMutation, useQuery, useQueryClient } from '@tanstack/react-query';
import { createModuleQueryKeys } from '@/lib/query-keys';
import { myFeatureService } from '../services/my-feature.service';
import type {
  CreateMyFeatureItemInput,
  MyFeatureItem,
  MyFeatureListOptions,
  MyFeatureListResponse,
  UpdateMyFeatureItemInput,
} from '../types';

const myFeatureKeys = createModuleQueryKeys('my-feature');

// Queries
export function useMyFeatureList(options: MyFeatureListOptions = {}) {
  return useQuery({
    // options is an object → build from [...root]; key() takes only string/number
    // segments. Valid TanStack key (structural compare); key('list') invalidations still match.
    queryKey: [...myFeatureKeys.root, 'list', options],
    queryFn: async ({ signal }): Promise<MyFeatureListResponse> =>
      myFeatureService.getAll({ ...options, signal }),
  });
}

export function useMyFeatureItem(id: string | undefined) {
  return useQuery({
    queryKey: myFeatureKeys.key('item', id ?? ''),
    queryFn: async ({ signal }): Promise<MyFeatureItem> =>
      myFeatureService.getById(id!, { signal }),
    enabled: Boolean(id),
  });
}

// Mutations
export function useCreateMyFeatureItem() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (input: CreateMyFeatureItemInput) => myFeatureService.create(input),
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: myFeatureKeys.key('list') });
    },
  });
}

export function useUpdateMyFeatureItem() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: ({ id, input }: { id: string; input: UpdateMyFeatureItemInput }) =>
      myFeatureService.update(id, input),
    onSuccess: (updated, { id }) => {
      queryClient.setQueryData(myFeatureKeys.key('item', id), updated);
      queryClient.invalidateQueries({ queryKey: myFeatureKeys.key('list') });
    },
  });
}

export function useDeleteMyFeatureItem() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: (id: string) => myFeatureService.delete(id),
    onSuccess: (_, id) => {
      queryClient.removeQueries({ queryKey: myFeatureKeys.key('item', id) });
      queryClient.invalidateQueries({ queryKey: myFeatureKeys.key('list') });
    },
  });
}
`;
```

<Aside type="tip">
**For infinite-scroll lists**, swap `useQuery` for `useInfiniteQuery` — the service's cursor pagination (`nextCursor`) plugs straight into `getNextPageParam`. An example is included as a commented-out block at the bottom of the generated file.
</Aside>

### Dashboard Widget

<Tabs>
  <TabItem label="Component">
```typescript
// src/templates/frontend/components.ts
export const WIDGET_COMPONENT_TEMPLATE = `// ========================================================================
// FILE: frontend/src/features/my-feature/components/widgets/MyFeatureWidget.tsx
// PURPOSE: Dashboard widget component for my-feature
// ========================================================================
// Widgets are wrapped in React.memo — the dashboard re-renders the widget
// list when the registry emits updates or when layout changes, but the
// widget itself only needs to re-render when its own props change.
// ========================================================================

import { memo } from 'react';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import type { DashboardWidgetProps } from '@/config/dashboard.registry';

function MyFeatureWidgetImpl({ widget, isCompact }: DashboardWidgetProps) {
  return (
    <Card className="h-full">
      <CardHeader className="pb-2">
        {/* CardTitle owns its own scale — don't override it from module code. */}
        <CardTitle>{widget.title}</CardTitle>
      </CardHeader>
      <CardContent>
        {/* data-slot="widget-stat-value" is the starter's foundation Style
            Registry hook (foundation.slots): the scale is applied by the
            design layer and derives from the app's typeset contract, so the
            stat reads correctly under any theme. Never hardcode text-2xl
            font-bold here — it would fight every custom design system. */}
        {isCompact ? (
          <p data-slot="widget-stat-value">42</p>
        ) : (
          <div className="space-y-2">
            <p data-slot="widget-stat-value">42</p>
            <p className="text-muted-foreground">Items processed this week</p>
          </div>
        )}
      </CardContent>
    </Card>
  );
}

export const MyFeatureWidget = memo(MyFeatureWidgetImpl);
`;
```
  </TabItem>
  <TabItem label="Registration">
```typescript
export const DASHBOARD_REGISTRATION = `// ========================================================================
// FILE: frontend/src/features/my-feature/registrations/dashboard.registration.ts
// PURPOSE: Dashboard widget registration for my-feature
// ========================================================================

import { dashboardRegistry } from '@/config/dashboard.registry';
import { MyFeatureWidget } from '../components/widgets/MyFeatureWidget';

dashboardRegistry.register({
  id: 'my-feature-overview',
  title: 'My Feature',
  component: MyFeatureWidget,
  size: '2x1',              // '1x1' | '2x1' | '1x2' | '2x2' | 'full'
  order: 50,                 // 0-100 scale (default: 50)
  category: 'activity',     // Optional grouping
  description: 'Overview of my feature activity',
  canHide: true,            // User can hide this widget (default: true)
  canResize: false,         // User can resize this widget (default: false)
});
`;
```

**Widget Sizes:** `1x1` (small stats), `2x1` (charts), `1x2` (lists), `2x2` (complex), `full` (full-width)
  </TabItem>
</Tabs>

### Settings Page

The settings page registry supports two patterns: **new pages** (add a new section to settings) and **replacement pages** (replace a placeholder like Billing with a real implementation).

<Tabs>
  <TabItem label="Component">
```typescript
export const SETTINGS_PAGE_COMPONENT_TEMPLATE = `// ========================================================================
// FILE: frontend/src/features/my-feature/components/settings/MyFeatureSettings.tsx
// PURPOSE: Settings page content for my-feature
// ========================================================================

import { SectionHeader, Row, ToggleSwitch } from '@/components/settings-blocks';
import { useState } from 'react';

export function MyFeatureSettings() {
  const [enabled, setEnabled] = useState(true);

  return (
    <div className="space-y-6">
      <SectionHeader
        title="My Feature Settings"
        description="Configure my-feature options"
      />

      <Row
        label="Enable Feature"
        description="Turn this feature on or off"
        action={<ToggleSwitch checked={enabled} onChange={setEnabled} />}
      />
    </div>
  );
}
`;
```
  </TabItem>
  <TabItem label="New Page">
```typescript
// Add a completely new page to the settings section
export const SETTINGS_PAGE_NEW_REGISTRATION = `// ========================================================================
// FILE: frontend/src/features/my-feature/registrations/settings-page.registration.ts
// PURPOSE: Register a new settings page for my-feature
// ========================================================================

import { settingsPagesRegistry } from '@/config/settings-pages.registry';
import { Sparkles } from 'lucide-react';
import { MyFeatureSettingsPage } from '../pages/MyFeatureSettingsPage';

settingsPagesRegistry.register({
  id: 'my-feature',
  title: 'My Feature',
  description: 'Configure My Feature settings',
  icon: Sparkles,
  component: MyFeatureSettingsPage,
  order: 50,                 // 0-100 scale (default: 50)
});
`;
```
  </TabItem>
  <TabItem label="Replacement">
```typescript
// Replace a placeholder page (e.g., Billing) with a real implementation
export const SETTINGS_PAGE_REGISTRATION = `// ========================================================================
// FILE: frontend/src/features/my-feature/registrations/settings-page.registration.ts
// PURPOSE: Replace placeholder settings page with real implementation
// ========================================================================

import { settingsPagesRegistry } from '@/config/settings-pages.registry';
import { CreditCard } from 'lucide-react';
import { MyBillingPage } from '../pages/MyBillingPage';

settingsPagesRegistry.register({
  id: 'billing',            // Must match existing placeholder ID
  title: 'Billing',
  description: 'Manage subscription and payments',
  icon: CreditCard,
  component: MyBillingPage,
  order: 50,
  isReplacement: true,      // IMPORTANT: This replaces the placeholder
});

// Replaceable pages: 'billing'
// Non-extensible pages (cannot replace): 'security', 'data'
`;
```
  </TabItem>
</Tabs>

### Header Toolbar

Add buttons, toggles, dropdowns, or custom components to the header toolbar.

```typescript
export const HEADER_TOOLBAR_REGISTRATION = `// ========================================================================
// FILE: frontend/src/features/my-feature/header-toolbar.registration.ts
// PURPOSE: Register header toolbar items for my-feature
// ========================================================================

import { headerToolbarRegistry } from '@/config/header-toolbar.registry';
import { Sparkles } from 'lucide-react';

// Action button (simple click handler)
headerToolbarRegistry.register({
  id: 'my-feature-action',
  type: 'action',           // 'action' | 'toggle' | 'dropdown' | 'button' | 'search' | 'custom'
  position: 'left',         // 'left' (after breadcrumbs) | 'right' (end of header)
  order: 50,                // 0-100 scale (default: 50)
  icon: Sparkles,
  label: 'My Feature Action',
  onClick: () => {
    // Replace with your handler (e.g., open modal, navigate, call API)
  },
});
`;
```

**Header Toolbar Types:** `action` (icon-only click handler), `toggle` (on/off state), `dropdown` (opens menu), `button` (labelled button), `search` (search input), `custom` (full component). See [Header Toolbar Registry](/starter-template/registries/#header-toolbar-registry) for all options.

### Preferences Section

Register a section on the Preferences settings page.

```typescript
export const PREFERENCES_REGISTRATION = `// ========================================================================
// FILE: frontend/src/features/my-feature/registrations/preferences.registration.ts
// PURPOSE: Register preferences section for my-feature
// ========================================================================

import { preferencesRegistry } from '@/config/preferences.registry';
import { MyFeaturePreferences } from '../components/settings/MyFeaturePreferences';

preferencesRegistry.register({
  id: 'my-feature',
  title: 'My Feature',
  component: MyFeaturePreferences,
  order: 50,
});
`;
```

### Integrations

Register a third-party service integration.

```typescript
export const INTEGRATION_REGISTRATION = `// ========================================================================
// FILE: frontend/src/features/my-feature/registrations/integrations.registration.ts
// PURPOSE: Register integration provider for my-feature
// ========================================================================

import { integrationsRegistry } from '@/config/integrations.registry';
import { Cloud } from 'lucide-react';
import { MyFeatureIntegration } from '../components/integrations/MyFeatureIntegration';

integrationsRegistry.register({
  id: 'my-feature',
  name: 'My Feature Service',
  description: 'Connect to My Feature for enhanced functionality',
  icon: Cloud,
  category: 'productivity',  // 'storage' | 'communication' | 'productivity' | 'developer' | 'social' | 'other'
  component: MyFeatureIntegration,
  order: 50,
});
`;
```

### Personal Info Fields

Add custom fields to the Personal Information settings page.

```typescript
export const PERSONAL_INFO_REGISTRATION = `// ========================================================================
// FILE: frontend/src/features/my-feature/registrations/personal-info.registration.ts
// PURPOSE: Register additional fields for Personal Information page
// ========================================================================

import { personalInfoFieldsRegistry } from '@/config/personal-info-fields.registry';

// Register a category for grouping
personalInfoFieldsRegistry.registerCategory({
  id: 'my-feature',
  label: 'My Feature Settings',
  description: 'Additional profile fields for My Feature',
  order: 50,
});

// Text field
personalInfoFieldsRegistry.register({
  id: 'myFeatureUsername',
  type: 'text',              // 'text' | 'textarea' | 'select' | 'custom'
  label: 'My Feature Username',
  description: 'Your username on My Feature platform',
  placeholder: 'Enter username',
  category: 'my-feature',
  order: 10,
  storage: 'preferences',    // Stored in User.preferences JSON
});

// Select field
personalInfoFieldsRegistry.register({
  id: 'myFeatureRole',
  type: 'select',
  label: 'Role',
  options: [
    { value: 'developer', label: 'Developer' },
    { value: 'designer', label: 'Designer' },
    { value: 'manager', label: 'Manager' },
  ],
  category: 'my-feature',
  order: 20,
  storage: 'preferences',
});
`;
```

### Admin Pages

Register pages in the admin panel.

```typescript
export const ADMIN_PAGES_REGISTRATION = `// ========================================================================
// FILE: frontend/src/features/my-feature/admin-pages.registration.ts
// PURPOSE: Register admin pages for my-feature
// ========================================================================

import { adminPagesRegistry } from '@/config/admin-pages.registry';
import { Sparkles } from 'lucide-react';
import { MyFeatureAdminPage } from './pages/MyFeatureAdminPage';

adminPagesRegistry.register({
  id: 'my-feature',              // URL: /admin/my-feature
  title: 'My Feature Data',
  description: 'Export or import your My Feature data',
  icon: Sparkles,
  component: MyFeatureAdminPage,
  order: 50,
  // badge: 'New',               // Optional notification badge
});
`;
```

### Notification Preferences

Register notification toggles that appear in the user's notification settings.

```typescript
export const NOTIFICATION_PREFERENCES_REGISTRATION = `// ========================================================================
// FILE: frontend/src/features/my-feature/registrations/notification-preferences.registration.ts
// PURPOSE: Register notification preference toggles for my-feature
// ========================================================================

import { notificationPreferencesRegistry } from '@/config/notification-preferences.registry';

notificationPreferencesRegistry.register({
  id: 'my-feature-activity',
  label: 'My Feature Activity',
  description: 'Receive notifications about My Feature events',
  defaultValue: true,
  category: 'activity',
  order: 50,                   // 0-100 scale (default: 50)
});
`;
```

### Language Options

Register additional language options that appear in the language selector.

```typescript
export const LANGUAGE_OPTIONS_REGISTRATION = `// ========================================================================
// FILE: frontend/src/features/my-feature/registrations/language-options.registration.ts
// PURPOSE: Register additional language options for my-feature
// ========================================================================

import { languageOptionsRegistry } from '@/config/language-options.registry';

languageOptionsRegistry.register({
  id: 'ja',                    // ISO language code
  label: 'Japanese',
  order: 50,                   // 0-100 scale (default: 50)
  // rtl: false,               // Set true for right-to-left languages
});
`;
```

### Style Registration

Contribute runtime CSS tokens and/or shadcn `components.json` values via the styleRegistry. Runtime fields (`tokens`, `css`) apply immediately; build-time fields (`componentsJson`) require `npm run components:sync` in the host app.

```typescript
export const STYLE_REGISTRATION = `// ========================================================================
// FILE: frontend/src/features/my-feature/registrations/style.registration.ts
// PURPOSE: Register runtime CSS tokens and/or shadcn components.json values
// ========================================================================
// Runtime (hot): tokens + css apply immediately
// Build-time (cold): componentsJson is merged by 'npm run components:sync'
//
// Exclusive fields (only ONE module per app): componentsJson.style,
// componentsJson.tailwind.baseColor, componentsJson.tailwind.cssVariables
// ========================================================================

import { styleRegistry } from '@/config/style.registry';

styleRegistry.register({
  id: 'my-feature.theme',
  order: 50,                   // 0-100 scale — later entries win on overlap

  // Runtime layer
  tokens: {
    // '--primary': 'oklch(0.7 0.15 240)',
  },
  // css: \\\`@layer components { .my-feature-card { ... } }\\\`,

  // Build-time layer (requires 'npm run components:sync')
  // componentsJson: {
  //   // Exclusive (one per app):
  //   // style: 'new-york',
  //   // tailwind: { baseColor: 'neutral', cssVariables: true },
  //   // Additive (many contributors):
  //   aliases: { 'my-feature/blocks': '@/blocks/my-feature' },
  //   registries: { '@my-feature': 'https://registry.myfeature.com/{name}.json' },
  // },
});
`;
```

<Aside type="caution">
If your module sets any exclusive `componentsJson` fields, only **one** module per app may claim them. Documents this in the module's README so app owners know which module owns the base style, base colour, and CSS variable strategy.
</Aside>

<Aside type="note">
For the full API reference including all available methods and hooks, see the [Registry Reference](/starter-template/registries/).
</Aside>

---

## Backend Templates

### Index (Barrel File)

```typescript
// src/templates/backend/routes.ts
export const INDEX_TEMPLATE = `// ========================================================================
// FILE: backend/src/features/my-feature/index.ts
// PURPOSE: Public exports and registry integrations for my-feature
// ========================================================================

import './route.registration.js';
import './service.registration.js';
import './security.registration.js';
import './module.registration.js';     // Runtime module registry (starter v1.48+; no-op on older)

export { myFeatureRoutes } from './my-feature.routes.js';
export { myFeatureService } from './my-feature.service.js';
// Stable service-registry key, for typed cross-module access via
// serviceRegistry.get(MY_FEATURE_SERVICE_KEY). See service.registration.ts.
export { MY_FEATURE_SERVICE_KEY } from './service.registration.js';
export type * from './my-feature.types.js';
`;
```

### Service

```typescript
// src/templates/backend/services.ts
export const SERVICE_TEMPLATE = `// ========================================================================
// FILE: backend/src/features/my-feature/my-feature.service.ts
// PURPOSE: Business logic for my-feature
// ========================================================================

import { prisma } from '../../lib/prisma.js';
import { NotFoundError } from '../../lib/errors.js';
import { createModuleLogger } from '../../lib/logger.js';
import { actionRegistry } from '../../config/action.registry.js';
import type {
  CreateMyFeatureInput,
  UpdateMyFeatureInput,
  MyFeatureListOptions,
  MyFeatureListResponse,
} from './my-feature.types.js';

const log = createModuleLogger('my-feature');

const DEFAULT_PAGE_SIZE = 50;
const MAX_PAGE_SIZE = 200;

// List endpoints return a narrow projection — full objects only via getById.
const LIST_SELECT = {
  id: true, name: true, status: true, createdAt: true, updatedAt: true,
} as const;

export const myFeatureService = {
  // Cursor-paginated — stable under concurrent inserts; relies on the
  // [userId, createdAt] index. Returns { items, nextCursor, hasMore }.
  async getAll(
    userId: string,
    { limit = DEFAULT_PAGE_SIZE, cursor }: MyFeatureListOptions = {}
  ): Promise<MyFeatureListResponse> {
    const take = Math.min(Math.max(1, limit), MAX_PAGE_SIZE);
    const rows = await prisma.my_feature_item.findMany({
      where: { userId },
      orderBy: { createdAt: 'desc' },
      take: take + 1, // one extra to detect the next page
      ...(cursor && { cursor: { id: cursor }, skip: 1 }),
      select: LIST_SELECT,
    });
    const hasMore = rows.length > take;
    const items = hasMore ? rows.slice(0, take) : rows;
    return { items, nextCursor: hasMore ? items[items.length - 1].id : null, hasMore };
  },

  async getById(id: string, userId: string) {
    const item = await prisma.my_feature_item.findFirst({ where: { id, userId } });
    if (!item) throw new NotFoundError('Item not found');
    return item;
  },

  async create(userId: string, data: CreateMyFeatureInput) {
    // Field encryption (hardened+): seal columns declared in encryptedFields with
    // encryptField() from '../security/index.js' before storing. See security.registration.ts.
    const item = await prisma.my_feature_item.create({ data: { ...data, userId } });
    log.info({ userId, itemId: item.id }, 'Item created');
    // doActionAll fans out to sync AND async listeners (doAction runs sync-only
    // and skips addActionAsync consumers). void = fire-and-forget, non-blocking.
    void actionRegistry.doActionAll('my-feature.item.created', { userId, item });
    return item;
  },

  // Atomic ownership check + update in a single transaction.
  async update(id: string, userId: string, data: UpdateMyFeatureInput) {
    const item = await prisma.$transaction(async (tx) => {
      const existing = await tx.my_feature_item.findFirst({ where: { id, userId }, select: { id: true } });
      if (!existing) throw new NotFoundError('Item not found');
      return tx.my_feature_item.update({ where: { id }, data });
    });
    log.info({ userId, itemId: id }, 'Item updated');
    void actionRegistry.doActionAll('my-feature.item.updated', { userId, item, changes: data });
    return item;
  },

  // Single round trip — deleteMany with the compound filter is idempotent.
  async delete(id: string, userId: string) {
    const result = await prisma.my_feature_item.deleteMany({ where: { id, userId } });
    if (result.count === 0) throw new NotFoundError('Item not found');
    log.info({ userId, itemId: id }, 'Item deleted');
    void actionRegistry.doActionAll('my-feature.item.deleted', { userId, itemId: id });
  },
};
`;
```

### Routes

```typescript
export const ROUTES_TEMPLATE = `// ========================================================================
// FILE: backend/src/features/my-feature/my-feature.routes.ts
// PURPOSE: API routes for my-feature
// ========================================================================

import { Router, Request, Response, NextFunction } from 'express';
import { myFeatureService } from './my-feature.service.js';
import { createModuleLogger } from '../../lib/logger.js';

const router = Router();
const log = createModuleLogger('my-feature:routes');

router.get('/', async (req: Request, res: Response, next: NextFunction) => {
  try {
    const { id: userId } = req.user!;
    // Pagination: ?limit=50&cursor=<lastItemId> — returns { items, nextCursor, hasMore }.
    const limit = req.query.limit ? Number(req.query.limit) : undefined;
    const cursor = typeof req.query.cursor === 'string' ? req.query.cursor : undefined;
    const result = await myFeatureService.getAll(userId, { limit, cursor });
    res.json(result);
  } catch (error) {
    next(error);
  }
});

router.get('/:id', async (req: Request, res: Response, next: NextFunction) => {
  try {
    const { id: userId } = req.user!;
    const item = await myFeatureService.getById(req.params.id, userId);
    res.json(item);
  } catch (error) {
    next(error);
  }
});

router.post('/', async (req: Request, res: Response, next: NextFunction) => {
  try {
    const { id: userId } = req.user!;
    const item = await myFeatureService.create(userId, req.body);
    res.status(201).json(item);
  } catch (error) {
    next(error);
  }
});

router.patch('/:id', async (req: Request, res: Response, next: NextFunction) => {
  try {
    const { id: userId } = req.user!;
    const item = await myFeatureService.update(req.params.id, userId, req.body);
    res.json(item);
  } catch (error) {
    next(error);
  }
});

router.delete('/:id', async (req: Request, res: Response, next: NextFunction) => {
  try {
    const { id: userId } = req.user!;
    await myFeatureService.delete(req.params.id, userId);
    res.status(204).send();
  } catch (error) {
    next(error);
  }
});

export const myFeatureRoutes = router;
`;
```

<Aside type="note">
The `SERVICE_TEMPLATE` and `ROUTES_TEMPLATE` snippets above are abbreviated for readability. The **shipped** templates also generate the default admin data export/import surface (C21–C24): the service's `exportUserData`/`importUserData` methods and the `requireAdmin`-gated `GET /export` + `POST /import` routes. See the [Conformance Contract](/module-starter/conformance-contract/) for what C21–C24 require.
</Aside>

### Route Registration

```typescript
// src/templates/backend/registrations.ts
export const ROUTE_REGISTRATION = `// ========================================================================
// FILE: backend/src/features/my-feature/route.registration.ts
// PURPOSE: Register routes with the route registry
// ========================================================================

import { routeRegistry } from '../../config/route.registry.js';
import { myFeatureRoutes } from './my-feature.routes.js';

routeRegistry.register({
  path: '/my-feature',
  router: myFeatureRoutes,
  feature: 'my-feature',
  versions: ['v1'],           // Mounted at /api/v1/my-feature
  requiresAuth: true,
  description: 'My Feature management',
  // middleware: [],           // Additional middleware
  // deprecated: false,        // Mark as deprecated
});
`;
```

### Service Registration

Uses TypeScript **type augmentation** for type-safe access to services across features.

```typescript
export const SERVICE_REGISTRATION = `// ========================================================================
// FILE: backend/src/features/my-feature/service.registration.ts
// PURPOSE: Register service with the service registry
// ========================================================================

import { serviceRegistry } from '../../config/service.registry.js';
import { myFeatureService } from './my-feature.service.js';

// The single, exported key for this module's entry in the service registry.
// Cross-module callers resolve the service through this constant —
// serviceRegistry.get(MY_FEATURE_SERVICE_KEY) — instead of a magic string.
export const MY_FEATURE_SERVICE_KEY = 'my-feature' as const;

// Type augmentation enables: serviceRegistry.get(MY_FEATURE_SERVICE_KEY).
// The interface key must repeat the string LITERAL — a TS key can't be a
// computed const — so keep the two in sync.
declare module '../../config/service.registry.js' {
  interface ServiceTypes {
    'my-feature': typeof myFeatureService;
  }
}

serviceRegistry.register(MY_FEATURE_SERVICE_KEY, myFeatureService, {
  feature: 'my-feature',
  description: 'My Feature business logic',
  version: '{{moduleVersion}}',   // substituted from module.ts — never hardcode
});
`;
```

<Aside type="tip">
For plugins or runtime-loaded modules, use `serviceRegistry.registerDynamic()` and `serviceRegistry.getDynamic<T>()` instead of type augmentation. See [Service Registry reference](/starter-template/registries/#service-registry) for details.
</Aside>

### Types

```typescript
// src/templates/backend/types.ts
export const TYPES_TEMPLATE = `// ========================================================================
// FILE: backend/src/features/my-feature/my-feature.types.ts
// PURPOSE: TypeScript types for my-feature
// ========================================================================

export interface CreateMyFeatureInput {
  name: string;
  description?: string;
}

export interface UpdateMyFeatureInput {
  name?: string;
  description?: string;
  status?: 'active' | 'inactive' | 'archived';
}

// List / pagination types (returned by the paginated getAll)
export interface MyFeatureListOptions {
  limit?: number;   // Page size — clamped to [1, 200] server-side. Default: 50
  cursor?: string;  // ID of the last item from the previous page
}

export interface MyFeatureListItem {
  id: string;
  name: string;
  status: string;
  createdAt: Date;
  updatedAt: Date;
}

export interface MyFeatureListResponse {
  items: MyFeatureListItem[];
  nextCursor: string | null;
  hasMore: boolean;
}
`;
```

---

## Prisma Schema

<Aside type="caution">
Module schemas must be **self-contained** with **no `@relation` directives**. Use plain foreign keys with indexes instead.
</Aside>

```typescript
// src/templates/schema.ts
export const SCHEMA = `
// ========================================================================
// My Feature Models
// ========================================================================
// Self-contained — NO @relation to the User model (avoids requiring changes
// to User). Use plain foreign keys + indexes. To add referential integrity,
// add the relation fields to your own User model.

model my_feature_item {
  id          String   @id @default(cuid())
  userId      String
  name        String
  description String?
  status      String   @default("active")
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  // Supports "items per user, newest first" without an in-memory sort.
  @@index([userId, createdAt])

  // Security integration (see security.registration.ts):
  // - Field encryption: keep a sensitive column as String and store the
  //   encryptField() envelope in it; declare it in encryptedFields.
  // - Neon RLS: add tenantId String (+ @@index([tenantId])) and list it in
  //   the manifest's ownedModels. tenantId == userId in solo mode.
}

// User settings — separate table (recommended over editing the User model).
model my_feature_settings {
  id        String   @id @default(cuid())
  userId    String   @unique          // @unique already creates the index
  enabled   Boolean  @default(true)
  config    Json     @default("{}")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
`;
```

### Why No Relations?

Modules are installed independently and may be uninstalled. Using `@relation` would:
1. Require modifying the `User` model (which modules shouldn't do)
2. Create migration dependencies between modules
3. Cause issues when uninstalling modules

Instead, use plain foreign keys with indexes for performance.

---

## Next Steps

- [Auth Feature — API Client](/starter-template/auth-feature/#the-api-client) — How the shared API client works (must-read for frontend services)
- [Lifecycle Hooks](/module-starter/lifecycle-hooks/) — Filters and actions
- [CLI Reference](/module-starter/cli-reference/) — Full command documentation with flags and examples
- [File Reference](/module-starter/file-reference/) — File organisation and naming conventions
- [Building Your Module](/module-starter/building-modules/) — Template authoring and configuration
- [Registry Guide](/module-starter/registry-guide/) — How to register with each registry
