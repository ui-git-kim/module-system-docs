---
title: Code Templates
description: Copy-paste templates for module development
---

## Navigation Registration

```typescript
// navigation.registration.ts
import { navigationRegistry } from '@/config/navigation.registry';
import { Icon } from 'lucide-react';

navigationRegistry.registerMain({
  id: '[module]',
  title: '[Module]',
  url: '/[module]',
  icon: Icon,
  order: 50,
});
```

---

## Dashboard Widget

```typescript
// components/widgets/ModuleWidget.tsx
import type { DashboardWidgetProps } from '@/config/dashboard.registry';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function ModuleWidget({ widget }: DashboardWidgetProps) {
  return (
    <Card>
      <CardHeader className="pb-2">
        <CardTitle className="text-sm font-medium">{widget.title}</CardTitle>
      </CardHeader>
      <CardContent>
        <div className="text-2xl font-bold">Value</div>
      </CardContent>
    </Card>
  );
}
```

```typescript
// registrations/dashboard.registration.ts
import { dashboardRegistry } from '@/config/dashboard.registry';
import { ModuleWidget } from '../components/widgets/ModuleWidget';

dashboardRegistry.register({
  id: '[module]-widget',
  title: '[Module] Overview',
  component: ModuleWidget,
  size: '1x1',
  order: 50,
});
```

---

## Settings Page

```typescript
// pages/ModuleSettingsPage.tsx
import {
  SettingsSection,
  SettingsRow,
  SettingsLabel,
  SettingsDescription,
  SettingsControl,
} from '@/components/settings';
import { Switch } from '@/components/ui/switch';

export function ModuleSettingsContent() {
  return (
    <SettingsSection title="Settings">
      <SettingsRow>
        <div>
          <SettingsLabel>Enable Feature</SettingsLabel>
          <SettingsDescription>Turn on this feature</SettingsDescription>
        </div>
        <SettingsControl>
          <Switch />
        </SettingsControl>
      </SettingsRow>
    </SettingsSection>
  );
}
```

```typescript
// registrations/settings-pages.registration.ts
import { settingsPagesRegistry } from '@/config/settings-pages.registry';
import { ModuleSettingsContent } from '../pages/ModuleSettingsPage';
import { Icon } from 'lucide-react';

settingsPagesRegistry.register({
  id: '[module]',
  title: '[Module]',
  description: 'Configure [module]',
  icon: Icon,
  component: ModuleSettingsContent,
  order: 70,
});
```

---

## Backend Service

```typescript
// [module].service.ts
import { prisma } from '../../lib/prisma.js';
import { NotFoundError } from '../../lib/errors.js';

export const moduleService = {
  async getAll(userId: string) {
    return prisma.module.findMany({
      where: { userId },
      orderBy: { createdAt: 'desc' },
    });
  },

  async getById(id: string, userId: string) {
    const item = await prisma.module.findFirst({ where: { id, userId } });
    if (!item) throw new NotFoundError('Not found');
    return item;
  },

  async create(userId: string, data: CreateInput) {
    return prisma.module.create({ data: { userId, ...data } });
  },

  async delete(id: string, userId: string) {
    await this.getById(id, userId);
    await prisma.module.delete({ where: { id } });
  },
};
```

---

## Backend Routes

```typescript
// [module].routes.ts
import { Router } from 'express';
import { moduleService } from './[module].service.js';

const router = Router();

router.get('/', async (req, res, next) => {
  try {
    const data = await moduleService.getAll(req.user!.id);
    res.json(data);
  } catch (error) {
    next(error);
  }
});

router.post('/', async (req, res, next) => {
  try {
    const item = await moduleService.create(req.user!.id, req.body);
    res.status(201).json(item);
  } catch (error) {
    next(error);
  }
});

router.delete('/:id', async (req, res, next) => {
  try {
    await moduleService.delete(req.params.id, req.user!.id);
    res.status(204).send();
  } catch (error) {
    next(error);
  }
});

export const moduleRoutes = router;
```

---

## Route Registration

```typescript
// route.registration.ts
import { routeRegistry } from '../../config/route.registry.js';
import { moduleRoutes } from './[module].routes.js';

routeRegistry.register({
  path: '/[module]',
  router: moduleRoutes,
  feature: '[module]',
  versions: ['v1'],
  requiresAuth: true,
});
```

---

## Service Registration

```typescript
// service.registration.ts
import { serviceRegistry } from '../../config/service.registry.js';
import { moduleService } from './[module].service.js';

declare module '../../config/service.registry.js' {
  interface ServiceTypes {
    '[module]': typeof moduleService;
  }
}

serviceRegistry.register('[module]', moduleService, {
  feature: '[module]',
});
```

---

## Prisma Schema

```prisma
// schema.additions.prisma
model Module {
  id        String   @id @default(cuid())
  userId    String
  name      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  user User @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
}
```
