---
title: Testing & CI
description: The template's Vitest suite (unit, cross-user isolation, registry) and the GitHub Actions pipeline, including the opt-in Neon-branch database tests.
---

import { Aside, Steps } from '@astrojs/starlight/components';

The template ships a minimum test suite ([Vitest](https://vitest.dev)) and a GitHub Actions pipeline so regressions are caught before they reach production — the coverage the foundation itself relies on, and a starting point for your app.

## What's covered

| Test | File | Needs a DB? |
|------|------|-------------|
| **`owned()` scoping** — the isolation key can't be overridden by a client-supplied `tenantId` | `backend/src/lib/owned.test.ts` | No |
| **Cross-user isolation** — user A can't read/update/delete user B's rows through any path (Neon RLS) | `backend/src/__tests__/isolation.test.ts` | **Yes** |
| **Registry smoke** — features register their security manifest + deletion handlers | `backend/src/__tests__/registries.test.ts` | No |
| **Email seam** — provider adapter auto-wires from env; registrations land; unwired sends fall back to the console log | `backend/src/features/email/email.test.ts` | No |
| **Jobs seam** — handler registry + `enqueueJob` entry point; inline runner runs once; a declared-but-unwired runner stays visible | `backend/src/features/jobs/jobs.test.ts` | No |
| **Notifications registration** — manifest, deletion handler, job handler + service entry land from their side-effect imports; `notify()` input validation | `backend/src/features/notifications/notifications.test.ts` | No |
| **Module registry** — first-writer-wins, validation, stable enumeration, and the dependency presence-check (range recording) | `backend/src/features/modules/modules.test.ts` | No |
| **Module init ordering** — dependency policy + boot-order enforcement of module `init()` | `backend/src/features/modules/modules.init.test.ts` | No |
| **Admin module actions** — enable/disable write-guards (env / production gating) | `backend/src/features/modules/modules.admin-actions.test.ts` | No |
| **Step-up re-verification** — a code verifies once, wrong codes burn it, codes and grants expire; `requireStepUp` answers 403 `step_up_required` until a grant arrives; the browser retries once with the grant | `backend/src/features/security/step-up.test.ts`, `frontend/src/lib/__tests__/step-up.test.ts` | No |
| **Device tokens** — single-use enrolment, rotation, refresh-token reuse detection, retention prune; a device token is never admin and never passes a web-session-only route | `backend/src/features/auth/devices.service.test.ts`, `backend/src/middleware/auth.middleware.test.ts` | No |
| **Route guards** — device tokens refused on account deletion, export and security-alert settings; step-up on deletion and on email settings; preference and avatar size caps | `backend/src/features/user/user.routes.test.ts`, `backend/src/features/email/email.routes.test.ts` | No |
| **Account deletion + export** — an unfinished erasure is never reported as done and can't re-create the account; the personal-data export collects every handler | `backend/src/features/user/account-deletion.test.ts`, `backend/src/features/user/security.registration.test.ts`, `backend/src/features/security/export.test.ts` | No |
| **Audit trail** — security hooks reach the audit sink and record the acting admin | `backend/src/features/admin/audit.registration.test.ts` | No |
| **Origin gate + SMTP secret** — a refused origin gets a direct 403; the stored SMTP password is only reused for the same server | `backend/src/middleware/origin.middleware.test.ts`, `backend/src/features/email/email.config.test.ts` | No |
| **Frontend helpers** — `getApiErrorMessage` and the light/dark colour-scheme helpers (including blocked storage) | `frontend/src/lib/__tests__/api-error.test.ts`, `frontend/src/lib/__tests__/color-scheme.test.ts` | No |
| **BaseRegistry lifecycle** — register / unregister / order / admin overrides | `frontend/src/config/__tests__/base-registry.test.ts` | No |

<Aside type="note" title="On “module install/uninstall”">
The roadmap's *module install/uninstall integration test* is exercised here as the **registry register→unregister lifecycle** (BaseRegistry), which is the mechanism modules plug into. A full scaffold-a-throwaway-module test belongs to the **module starter**, not the app template.
</Aside>

## Running tests

```bash
npm test                 # both packages (root)
cd backend && npm test   # backend only
cd frontend && npm test  # frontend only
```

The **cross-user isolation** test is DB-backed and **skips unless `TEST_USE_DB=1`** and a real Postgres is configured — so `npm test` is green locally with no database. It provisions a sample owned table with `FORCE ROW LEVEL SECURITY` + a `current_setting('app.tenant_id')` policy — the exact pattern owned models use (see [User-data isolation](/starter-template/database/#user-data-isolation-neon-rls)) — and drives it through the real `withTenantContext` helper.

## Continuous integration

`.github/workflows/ci.yml` has four jobs:

- **backend** — `npm install` → `prisma generate` → typecheck → unit tests.
- **frontend** — `npm install` → **test** → typecheck + build. A cloned app builds against its own shadcn `components/ui/*`. The bare template ships none (they are generated per app), so there CI first generates the [shadcn baseline](/starter-template/scripts/#shadcn-baseline) with a non-interactive `shadcn init`. A template change that breaks the type-check or build is caught in the template, not in every app after `template:update`. The build uses placeholder `VITE_API_URL` / `VITE_NEON_AUTH_URL` values unless you set repository variables with those names (CI builds are never deployed). Lint runs only where an eslint config exists (the template ships none).
- **audit** — an allowlist-aware `npm audit` gate across backend + frontend. **Warn-only** unless enabled (see below).
- **isolation** — the DB-backed test against a **throwaway Neon branch**. **Opt-in** (see below).

### When CI runs

- **The template repo:** on every push to `main` and every pull request (v2.13.0). None of the default jobs need secrets (the isolation job stays opt-in).
- **A cloned app:** auto-runs stay **off** until you opt in. Add the repository variable **`CI_AUTO_RUN`** = `true` once the app's GitHub settings are ready (see the isolation job below). Until then, pushes don't fail on missing settings.
- **Anywhere:** you can start the whole pipeline by hand from the **Actions** tab (`workflow_dispatch`).

### Enabling the Neon-branch isolation job

<Steps>

1. In the repo's **Settings → Secrets and variables → Actions**, add secrets `NEON_API_KEY` (Neon account settings → API keys) and `NEON_PROJECT_ID` (your Neon project).
2. Add a **repository variable** `RUN_DB_TESTS` = `true`.
3. In a cloned app, also add `CI_AUTO_RUN` = `true` so the pipeline runs on push (see [When CI runs](#when-ci-runs)).

</Steps>

CI then creates a fresh Neon branch per run, points the isolation test at it (`TEST_USE_DB=1`), and **deletes the branch afterwards** (even on failure). Without the variable set, the job is skipped and the rest of the pipeline still runs.

<Aside type="tip" title="Why a real branch, not a mock">
Isolation is enforced by **Postgres RLS**, not app code — so the test only means something against a real database. A throwaway Neon branch gives production parity (the real adapter + policies) and costs nothing to spin up and tear down.
</Aside>

### The dependency-audit gate

The **audit** job runs `npm audit` across `backend` and `frontend` through `scripts/audit-gate.mjs`. A plain `npm audit` gate is unusable here — some advisories can't be fixed in this repo (Prisma 7's `deepmerge-ts` chain, whose only npm fix is a Prisma downgrade), so it would fail forever and get ignored. Instead the gate reads **`.audit-allowlist.json`** and splits high/critical advisories into **known** (allowlisted) vs **NEW**, reporting only new risk.

It is **warn-only by default** — it never fails CI unless you set the repository variable **`AUDIT_GATE_ENFORCE`** = `true`. The allowlist holds the Prisma `deepmerge-ts` chain and `GHSA-g38m-r43w-p2q7`, the better-auth advisory that Neon's managed auth server still carries (see [Dependency vulnerabilities](/starter-template/roadmap/#dependency-vulnerabilities)); curate it as advisories are triaged, then flip `AUDIT_GATE_ENFORCE` on once it's clean. Run it locally with `node scripts/audit-gate.mjs`.

The gate **fails closed** (v2.13.0): an audit that could not run (no lockfile, a registry or network error) is reported as a failed audit, never as clean. With `AUDIT_GATE_ENFORCE` on, that fails the job.

<Aside type="caution" title="First run surfaces what the lockfiles carry">
The gate is a mirror, not a fix — on first run it reports every high/critical advisory in the committed lockfiles, including stale-lockfile pins that already have patched versions in range. Refresh with `npm run deps:lockfile`, then allowlist (or fix) what genuinely remains before enforcing.
</Aside>

## Enabling tests on an existing app (after `template:update`)

The test **files** and the **CI workflow** are template-owned and force-sync into your app on update. Since v1.51.0/v1.54.0 the rest is automatic too: `sync-deps` adds the `vitest` devDependency to `frontend/` and `backend/`, and `sync-package-scripts` reconciles the `test`/`test:watch`/`type-check` (and backend `pretest`) scripts into **all three** `package.json` files — nothing to do by hand.

If your app's checked-out update script predates those reconciler legs (see the [one-update lag caveat](/starter-template/updating/#post-merge-reconciliation)), the manual fallback is:

<Steps>

1. Install the runner in each package: `cd backend && npm i -D vitest` and `cd frontend && npm i -D vitest` — or run `node scripts/sync-deps.mjs && npm run install:all`.
2. Run `node scripts/sync-package-scripts.mjs` so each package's `package.json` gets `"test": "vitest run"` and friends.

</Steps>
