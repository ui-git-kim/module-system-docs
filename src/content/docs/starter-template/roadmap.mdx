---
title: Roadmap
description: Planned features and future direction for the starter template
lastUpdated: 2026-07-09
---

import { Aside, Card, CardGrid } from '@astrojs/starlight/components';

This page outlines planned features and improvements for the starter template. Items are grouped by priority and complexity.

<Aside type="note">
This roadmap reflects current plans and may change based on community feedback and module requirements.
</Aside>

---

## Planned

Features planned for upcoming releases.

### Security floor (v2.0.0)

**Priority:** Highest — the foundation security architecture every app inherits. **Philosophy: delegate to expert providers, don't build security.** The starter is the thin, correct wiring of **Neon, Cloudflare, a managed KMS, and standard libraries**; bespoke code is a short, named allow-list only (app-layer field encryption, the deletion-cascade wiring, a thin `owned()` defence-in-depth). Landing in phases under a **v2.0.0** umbrella. See the [Security feature](/starter-template/security-feature/) for the full delegation map.

Phases (each implemented, reviewed, and stopped for sign-off before the next):

- ✅ **P1 — Security feature shell + `securityRegistry` + tier config** (v1.28.0). The declaration layer + `securityProfile` (`standard`/`hardened`/`strict`, default `hardened`) → `SecurityConfig` with env overrides.
- ✅ **Auth hardening (Neon Auth, no DIY)** (v1.29.0). Locked-down API CSP (helmet) + documented edge-CSP/WAF guidance + reliance on Neon's short-lived/refresh tokens + cookie-mode-where-possible + MFA/step-up by tier — *not* a custom session/cookie/CSRF system. (An earlier custom-cookie attempt was reverted as DIY.)
- ✅ **Isolation floor — Neon RLS + the `tenantId` convention** (v1.30.0). Owned models carry `tenantId` (isolation key) + `userId` (actor); `req.user.tenantId` (= user id in solo); `withTenantContext` (sets `app.tenant_id`) as the RLS seam + `owned()` keyed on `tenantId` as defence-in-depth; Database docs reframed to the RLS floor with the `FORCE RLS` + `tenantId` policy template. `tenantId` is present from the start (forward-compat) so going multi-user is a data change, not a schema rewrite. **Database-per-tenant** (hard isolation) remains a deferred opt-in capability — see below.
- ✅ **Field encryption (app-layer AES-256-GCM)** (v1.31.0). `encryptField`/`decryptField` over Node's `crypto` (random IV per call, GCM auth tag verified), plus `hashSecret`/`verifySecret` (SHA-256 + constant-time) for high-entropy tokens. Key from a provider abstraction — **`env` adapter shipped; `kms` adapter as an interface** (`setKeyProvider`); per-tenant keys + crypto-shred are a KMS-adapter capability (strict). The key is **never sent to the database** (app-layer, not `pgcrypto`), so Neon only ever holds ciphertext. Off at `standard`, on at `hardened`+.
- ✅ **Deletion cascade (provider-API wiring)** (v1.32.0). `deleteAccount()` runs registered deletion handlers in a fail-safe, classification-driven order (`credential` crypto-shred → `owned` hard-delete → `referenced` unlink; `preserved` deferred as a legal hold), then removes the **Neon Auth identity last** via the `AuthIdentityDeleter` seam (default `unconfigured` = visible, not silent — closes the gap where `deleteUser` left the identity behind), and returns a consent + classification compliance bundle. Modules register handlers via `securityRegistry.registerDeletionHandler`; both the self-service and admin deletion paths run the cascade. A retention job to erase `preserved` holds when they lift remains out of scope (app-level).
- ✅ **Field-encryption key custody (AWS KMS)** (v1.33.0–v1.35.0). Resolves the `kms` adapter the field-encryption phase shipped as an interface. **Envelope encryption**: KMS holds the root key, the app unwraps a data key **once** at startup and caches it, AES-256-GCM runs locally (KMS isn't called per field); the plaintext key is never in env, the DB, or a Neon dump. `createAwsKmsKeyProvider({region,keyId,wrappedDek})` reference adapter at `@/features/security/key-providers/aws-kms`; `@aws-sdk/client-kms` is an **optional, app-installed** dep (dynamically imported, so non-KMS apps build without it). One-command `npm run security:setup-kms` (v1.34.0) provisions the wrapped key + injects the env; the provider then **auto-wires from env at bootstrap** (v1.35.0 — no `app.ts` edit, survives `template:update`). Crypto-shred = disable/delete the KMS key (global); per-tenant crypto-shred is the documented growth path. Guide: [Field encryption with AWS KMS](/getting-started/aws-kms-setup/).
- ✅ **Cloudflare edge** (v1.36.0). The perimeter (DDoS, WAF, bot, edge rate limiting, TLS/HSTS, the frontend XSS-CSP, optional Zero Trust) delegated to **Cloudflare** — [setup guide](/getting-started/cloudflare-edge/) + a per-tier edge checklist. The code half is a **startup posture guardrail** (`runStartupPostureCheck()` in `features/security/posture.ts`, wired at the end of `app.ts` bootstrap): it evaluates the resolved posture once every feature + module has registered — edge declaration, trust-proxy sanity, field-encryption key readiness, auth-identity deleter, module `minimumTier` requirements, production hardening — logs each finding, and **halts at `strict` in production** on an error finding (override: `SECURITY_POSTURE_ENFORCE`). A one-shot `edgePostureMiddleware` warns when the first real request bypasses the edge (no `cf-ray`/`cf-connecting-ip`). New env knobs `SECURITY_EDGE` + `SECURITY_POSTURE_ENFORCE`; non-throwing `getKeyProviderStatus()` readiness probe. **The edge is additive — its absence warns, never fails; the app-layer floor stands alone.**

**Deferred (no provider exists / heavier capability):**

- ⏳ **Zero-knowledge encryption — "the operator can't read the data".** No provider offers this: at-rest and field encryption still let the key-holder decrypt, and Neon Data Masking doesn't hide data from the project owner. The only mechanism is **end-to-end / client-side encryption** keyed by the user's secret, with hard tradeoffs (no server-side search/processing/AI on content; difficult key recovery). **Opt-in, per-app, designed later.** Today's baseline for the same intent is ciphertext-in-DB via app-layer field encryption (above) + access controls.
- ⏳ **Database-per-tenant control-plane.** The provisioning (Neon API), tenant→connection routing, and fan-out-migration machinery that makes database-per-tenant operable at scale for multi-tenant apps.

### Observability (core): System admin page + Audit/Errors

**Priority:** High — **decision (2026-06-25): audit + error logging live in the starter core, not a separate module.** Consolidates the former *Database Health Page*, the *Audit & Errors module*, and the error-console half of *Error Pages & Offline Handling*. The seams already exist in core — 9 lifecycle action hooks (`user.*` / `admin.*`), the single error-middleware choke point, `securityRegistry.getAuditEvents()` (declaration layer, already shipped), and the `SECURITY_AUDIT_MODE` knob — so the foundation belongs here. Compliance-grade depth stays an **opt-in sink** so apps that don't need it carry no weight.

**Architecture — one pipeline, swappable sink** (mirrors the `setKeyProvider` pattern):

- `AuditSink` interface — `recordEvent()` / `recordError()` / `queryRecent()`, with `setAuditSink()` as the swap seam.
- **`MemoryAuditSink`** (default, always on) — bounded ring buffers (~200 errors / ~500 activity), ephemeral, zero-config; powers the live console.
- **`PrismaAuditSink`** (optional, `hardened`+) — `AuditEvent` / `ErrorLog` models; survives restarts, supports retention. A composite fans out to memory + DB so the live console always works.
- **Compliance sink** (opt-in; strict / `SECURITY_AUDIT_MODE=compliance`) — immutable / hash-chained, no TTL, export hooks. The former "module", reframed as this sink.

**Capture (almost entirely additive):**

- **Errors** — one line in `backend/src/middleware/error.middleware.ts` after `log.error()` → `auditSink.recordError(...)`. Optional `POST /api/v1/admin/system/client-error` from a React error boundary.
- **Activity** — a new `backend/src/features/audit/audit.registration.ts` subscribes the sink to the 9 existing hooks via `actionRegistry.addActionAsync(hook, 'audit', cb)` — no changes to the emitting services. `securityRegistry.getAuditEvents()` is the canonical filter set.

**The `/admin/system` page** (mirrors the v1.37.0 Security page — `*-admin.routes/service/types` + page + `adminPagesRegistry`, admin-gated reads):

- **Database** — `checkDatabaseHealth()` (connected / latency / adapter) + last migration (`_prisma_migrations`) + table row counts.
- **Services / connections** — reuses `evaluateStartupPosture()` (the 7 startup findings) + `getKeyProviderStatus()`: Neon Auth/JWKS, KMS readiness, edge, DB adapter. A *window*, not a reimplementation.
- **Console** — errors + activity tabs reading `auditSink.queryRecent()` via `GET /api/v1/admin/system/errors` and `.../activity`, rendered with the existing `AdminDataTable`.

**Security / redaction:** all reads are **admin-only** (posture + errors expose internals). Redaction reuses `securityRegistry.getEncryptedFields()` / `getHashedSecrets()` to scrub activity `changes`; stack traces are server-side / dev only; secrets shown set-or-not. DB-backed audit is owned data → `tenantId` / RLS applies.

**Phasing:**

- ✅ **P1 (shipped v1.39.0)** — `/admin/system` page + sink abstraction + `MemoryAuditSink` + the error-middleware hook + activity listeners (**errors and activity** console) + DB / services health. Ephemeral, in-memory, no schema change. See [System page](/starter-template/system/).
- ✅ **P2 (shipped v1.40.0)** — durable `SqlAuditSink` persisting to a **self-provisioned** `"AuditLog"` table (raw SQL `CREATE TABLE IF NOT EXISTS`, no Prisma model / migration / propagation — a deliberate change from the original model-based plan to avoid the 5-file schema hand-sync tax) + TTL retention (`SECURITY_AUDIT_RETENTION_DAYS`) + a History tab. Opt-in via `SECURITY_AUDIT_SINK=database`; best-effort (degrades to memory).
- ✅ **P3 (shipped v1.40.0)** — `ComplianceAuditSink`: immutable, SHA-256 hash-chained (the exact canonical payload is stored per row, so verify is round-trip-safe), advisory-locked, never pruned; `audit/verify` + `audit/export` endpoints + a System-page integrity check. Auto-selected at the strict tier / `SECURITY_AUDIT_MODE=compliance`.
- **P4** (optional) — client-error boundary capture into the console.

**Supersedes** the former separate *Audit & Errors module* and *Database Health Page* entries. The rate limiter stays unchanged — 429s become visible in the console instead of being silenced.

### Integrations / connector module (separate module)

**Priority:** Medium — out of starter scope; consumes the starter's credential-encryption primitive (P5). Connections (OAuth/API auth + credential storage + external read/write) as the core, automation as an optional embeddable add-on. Built elsewhere.

### Per-app security tiering

**Priority:** Per-app — assign each app its tier via the flags (e.g. Veradai → `strict`, Theme Editor → `standard`, most → `hardened`). Done in each app repo, not the template.

### Multiple Layouts

**Priority:** High

Currently the template has a single dashboard layout with sidebar navigation. This enhancement will add:

- **Builder/Editor Layout** - Full-bleed canvas for visual editors (already have `FullBleed` component)
- **Wizard Layout** - Step-based flows for onboarding or multi-step forms
- **Focus Layout** - Minimal chrome for focused tasks (reading, writing)
- **Landing Layout** - Marketing pages without app navigation

**Implementation approach:**
- Layout registry for registering layout components
- Route-level layout selection
- Nested layouts for flexibility

**Related:** the `styleRegistry` (implemented alongside — see changelog for the landing version) is the sibling feature for module-contributed styling; the two should compose (a module may want to ship both a layout and a matching style profile).

### In-App Component Reinstall for Style Registry

**Priority:** Medium

The `styleRegistry` landed with a terminal-only reinstall flow — when a module's `componentsJson` contributions change (new `style`, `baseColor`, `cssVariables`, added registries, etc.), admins need to run `npm run components:sync` in a terminal to apply the changes. This enhancement brings that flow into the admin UI.

**Scope:**

- Backend endpoint that spawns `scripts/sync-components.{ps1,sh}` as a child process.
- Streams stdout/stderr back to the admin UI over Server-Sent Events or WebSocket.
- Admin page surfaces: "Reinstall N components to apply changes? [Reinstall] [Keep staged]".
- "Keep staged" lets an admin batch multiple `styleRegistry` changes before a single reinstall.
- Progress feedback (which component is currently being reinstalled, any errors).

**Depends on:** [Admin Module Management](#admin-module-management) landing first. That feature will establish the node-spawn-and-stream pattern for module install/update/uninstall, which this reinstall flow will reuse rather than reinvent.

**Constraint:** some deploy environments (locked-down hosting, containers without a writable filesystem) will block shelling out to `npx`. This feature is therefore opt-in — apps that can't support it can continue using the terminal sync.

### Dashboard Widget Admin

**Priority:** High

Admin panel page for managing dashboard widgets:

- View all registered widgets
- Toggle widget visibility globally (hide from all users)
- Reorder widget default positions
- Set default visibility for new users

### Permissions Module Integration

**Priority:** High

Prepare the template for granular permissions beyond basic roles:

- `managed` role support is already in place
- Hook points for permission checks in navigation
- Permission-aware UI components
- Backend middleware integration points

### Admin Module Management

**Priority:** High

Replace CLI-based module operations with an admin panel UI. Currently modules are installed, updated, and removed via terminal commands (`npx my-module init`, `update`, `uninstall`). This feature brings that workflow into the app itself:

- **Browse & install** — Discover available modules (from a registry or local packages) and install them from the admin panel
- **Configure** — Module-specific settings and prompt answers managed through a UI instead of interactive CLI prompts
- **Deactivate / activate** — Toggle modules on or off without removing files (replaces `npx my-module disable/enable`)
- **Uninstall** — Remove modules cleanly with confirmation, data export options, and rollback (replaces `npx my-module uninstall`)
- **Update** — Check for module updates and apply them with a diff preview (replaces `npx my-module update`)
- **Module status dashboard** — View installed modules, versions, health, and dependencies at a glance

**Implementation considerations:**
- Backend service that wraps scaffold operations (init, update, uninstall, enable, disable)
- Admin page registered via `adminPagesRegistry`
- Module manifest (`.module-manifests.json`) read/write from the backend
- Restart or hot-reload mechanism after module changes take effect
- Permission-gated to admin role

**Trust boundary (flagged in the v1.26.0 audit):** installing a module from the admin UI means executing untrusted code — arbitrary side-effect imports run on the frontend at registration time, and the backend would spawn `npx`. There is no sandboxing or isolation today, and none is yet designed. Before this feature ships, decide what runs untrusted module code and how it is contained (signed modules / allowlist registry / out-of-process execution). Also note today's **build-time coupling** that blocks true runtime install-uninstall: features are wired via static side-effect imports in `app.ts` and `routeRegistry.mountAll()` is one-shot (it sets `mounted = true` and warns that late registration "may not work"). Runtime module management needs a dynamic mount/unmount story, not just a UI over the CLI.

### Email OTP / magic link sign-in

**Priority:** Medium — builds on v1.23.0's custom auth pages.

v1.23.0 shipped with email+password and Google OAuth. Adding OTP brings the starter back up to feature parity with the original Neon `AuthView` without reintroducing the Neon UI package or its Tailwind preset. Scope adds one more page (OTP code verification step) and a sign-in-with-OTP toggle on `SignInPage.tsx`. The underlying client already supports OTP via `authClient.emailOtp.*` methods.

### Finalise the forgot-password / reset-password flow

**Priority:** High — blocks production for v1.23.0-based apps.

v1.23.0 shipped `ForgotPasswordPage.tsx` and `ResetPasswordPage.tsx` as custom shadcn forms that call `authClient.requestPasswordReset()` and `authClient.resetPassword()` respectively — the method names from Better Auth 1.6. Neon's server is on Better Auth 1.4.6 until their upgrade lands ("coming weeks" per the Neon team on Discord, 2026-04-15) and their docs explicitly flag SDK methods for password reset as "not fully supported yet." The forms are wired but unverified end-to-end. Before any v1.23.0-based app ships to production:

- Trigger a reset from the live app, confirm the email arrives.
- Click the link, confirm it lands on `/auth/reset-password` with a valid `?token=...`.
- Submit a new password, confirm it's actually rotated on the server (sign in with the new password).

If any step fails, fall back to Neon's standalone `<ForgotPasswordForm>` / `<ResetPasswordForm>` components under a scoped CSS loader (to keep the Tabs fix intact on every other route) until Neon's 1.6 upgrade lands and the SDK path works cleanly.

### Template update mechanism review

**Priority:** High — every point fix to date has been a patch on top of the wrong abstraction.

`npm run template:update` uses a git three-way merge as its update mechanism. That choice has cost real time across the v1.22.0 → v1.23.1 arc: v1.22.2 leaked a withdrawn experimental change into a downstream app, v1.23.0's merge left stale imports behind, the docs submodule at various points copied its contents into apps that had no business tracking docs, and every template release has required a post-ship patch to unwind one or another merge surprise. The v1.23.1 post-merge force-sync is a patch on the symptom, not the cause.

The cause is that **git merge is the wrong tool for this job.** Merge assumes bidirectional collaboration between peers — two branches that have legitimately diverged and want to reconcile. Template updates are unidirectional: the template is authoritative, the app is a consumer, and the app's job is to receive updates, not negotiate with them. Every scenario where git's three-way resolution "helpfully" keeps a local line is a scenario where the template lost.

This entry is a **review, not an implementation plan** — the point is to step back and question every assumption before writing more code. Concrete deliverable: a design doc (not a PR) that answers the questions below with data from real update scenarios, then proposes an architecture.

**Questions the review should answer:**

- **What are the actual scenarios `template:update` has to handle?** Enumerate every real case — fresh app off `create-app`, app that's merged every release, app that skipped several releases, app that pulled a withdrawn experimental release, app that forked a core file legitimately (e.g. custom admin dashboard), app with user-owned files that the template also ships (`components.json`, `schema.prisma`), app that has `.claude/` / `docs/` leaked in from a prior bad merge, app on a disconnected network with a local template path, app tracking multiple modules with their own update cadences. What does each need?
- **Which scenarios does today's git-merge-based flow actually handle correctly?** The evidence so far says the happy path works and almost everything else takes manual intervention. Let's write this down with examples rather than guess.
- **Is there a reason we're using git merge beyond "git is available"?** The update flow doesn't need most of what merge provides (conflict markers, interactive resolution, rebase compatibility). It needs: "replace template-owned files with the new versions, leave user-owned files alone, stop pretending these two categories are negotiable."
- **Could a simpler mechanism replace it?** Candidates to evaluate:
  - **Copy-then-sync** — `template:update` fetches the template at the target version, walks a manifest of template-owned paths, copies each into the app's working tree. No merge. User-owned paths are never touched. File deletions in the template become deletions in the app. This is how GitHub's "Template repository" feature works in spirit. Simpler, harder to mess up, easy to preview accurately. Cost: loses git's conflict detection for apps that have forked a template file — need an opt-in mechanism (the `.template-ignore` from the sibling roadmap entry).
  - **Versioned patches** — the template publishes a patch file per release (`patches/v1.22.3-to-v1.23.0.patch`). `template:update` applies the patch sequentially. Reproducible, reviewable, auditable. Cost: patch files drift if the user's files diverge; needs a reject-file review step when a patch doesn't apply.
  - **Manifest-driven per-file sync** — the template ships `.template-manifest.json` listing every file it owns and its current content hash. `template:update` compares app file hashes against the manifest and syncs the diff. User-owned files are absent from the manifest, so they're never touched. Clean separation of concerns. Cost: manifest has to be kept in sync with the template tree — solvable with a pre-commit hook on the template side.
  - **Hybrid** — force-sync for template-authoritative files (the majority), git merge for shared-ownership files like `schema.prisma`, skip for user-owned files. Essentially today's v1.23.1 behaviour formalised with explicit categorisation in a manifest rather than a hardcoded allow-list.
- **What testing harness does this need?** The update script currently has zero automated coverage. Every new scenario is discovered by a downstream user in production. At minimum we should have a test that spins up a simulated app at version N, runs `template:update` to N+1, and asserts specific files are in specific states. Running the full matrix (every supported starting version × every current version) on every release is how we catch the v1.22.2 class of bug before it ships.
- **How does preview mode accurately represent what will happen?** Today's `--preview` shows a git diff between branches — but the actual merge behaviour diverges from a naive diff. Preview should run the full update in a throwaway worktree and show the true before/after for the app's tree, including post-merge force-syncs and path removals.
- **What's the rollback story?** If `template:update` goes wrong, there's no clean undo beyond `git reset --hard`, which the script can't offer because it'd also discard uncommitted user work. `template:revert-to v<N>` should be a first-class command — snapshot the app's tree before the update, restore on request.
- **How does the app signal "I've genuinely customised this template file and I know what I'm doing"?** The `.template-ignore` roadmap entry is one answer. There might be others — per-file YAML frontmatter markers, a fork registry, whatever.
- **What's the relationship to the Module Starter's update flow?** Modules already have an update path (`npx my-module update`). Does the template update mechanism share code / principles / primitives with it? If not, should it? A unified "update-something-from-upstream" primitive used by both would be worth the refactor cost.

**Scope for the review:**

- Write the design doc answering the above. Publish to `docs/src/content/docs/starter-template/update-mechanism-review.mdx` with a draft banner.
- Implement a test harness for `template:update` covering at least the five known-problematic scenarios from this session (v1.22.2 → v1.23.0 stale import, docs submodule leak, .claude leak, user-customised `schema.prisma`, fresh unrelated-histories merge).
- Propose an architecture change based on the design doc's conclusions. Plan the migration from today's git-merge flow to the chosen architecture.

**Out of scope for the review** (explicitly — keep it bounded):

- Shipping the new architecture in the same release as the design doc. This is a review, not a rewrite. Ship the doc first, discuss, then plan the implementation.
- Refactoring `create-app` in the same pass — different concerns, can come later.

**Expected outcome:** at minimum, an honest catalogue of what works and what doesn't. At best, a clear architectural direction that closes this class of bug permanently so we stop patching the symptom.

### Auto-merge schema field additions in `template:update`

**Priority:** High — fills the last piece of the "no manual changes per release" promise.

v1.25.0 added the `appTheme` field to the `AppSettings` model. The update script already detects and auto-adds **new models** (via `Extract-ModelSection` in `update-template.{ps1,sh}`), but doesn't handle **new fields on existing models**. Downstream apps that pull v1.25.0 have to manually add `appTheme Json?` to their local `AppSettings` and run `prisma migrate dev` — small, but the kind of friction this template's versioning philosophy keeps saying shouldn't exist.

**What this would do:**

- **Parse both schemas after merge.** For each model the template ships (User, AppSettings, plus anything future releases add), diff the field lists. Field present in template but not in local → "missing field."
- **Insert missing fields into the local schema** with their full Prisma definition (type, modifiers, default, attributes). Preserves any local fields the user has added to the same model.
- **Set `$needsMigration = true`** so the existing auto-migrate step (`prisma migrate dev --name template_update_vX_X_X`) fires and generates the migration for the downstream app's DB.
- **Handle the "field removed in template but user added it locally" case** — leave the user's field alone, warn about the mismatch at the end of the update so the user can decide.

**Edge cases:**

- **Field renames** — template changes `oldName String` to `newName String`. Diff algorithm sees "remove old, add new" — not a rename. Ship a data-migration SQL snippet alongside the schema change for renames; document as a breaking change.
- **Type changes** — template changes `field Int` to `field BigInt`. Diff sees same field name, different definition. Warn the user, don't auto-overwrite. Type changes force a major bump anyway per the [deprecation policy](/starter-template/versioning/).
- **Multi-file schema mode** — the `Extract-ModelSection` approach works per-file; extend to iterate `backend/prisma/schema/*.prisma` in multi-file mode.
- **User has removed the model entirely** — if downstream deleted `AppSettings` (unusual but possible), respect that; the existing new-model auto-add kicks in. Don't force a re-add.

**Why this belongs in the update script, not a codemod:**

Codemods (per the separate [Deprecation codemods](#deprecation-codemods-shipped-with-templateupdate) roadmap item) are for consumer-code changes — rewriting imports and JSX shapes. Schema additions are infrastructure changes the downstream app needs regardless of what its own code looks like. Handling them in the update script keeps the flow atomic: one `template:update` run pulls the release and makes the downstream whole.

**Target: v1.25.1 or v1.26.0.** The `appTheme` field already requires a manual schema step for apps pulling v1.25.0 — documented in the changelog. Shipping this fix in v1.25.1 means apps that update in the intervening window do the manual step once; apps that jump v1.24.X → v1.25.1 get it for free.

### Structure-only styling migration

**Priority:** High — retires the ~170 legacy style-in-template violations the v1.24.7 audit catalogued.

v1.24.7 codified the rule that template components use layout + semantic tokens only (no typography scale, font weights, colour literals, decorative shadows). The rule protects downstream apps from fighting the template's design decisions — but the template itself has accumulated violations over the pre-1.24.7 releases:

- **~158 font-size / font-weight occurrences** across 30 files (`text-sm`, `font-semibold`, etc.) in `settings-blocks/`, `page-blocks/`, feature pages, sidebar + header components, auth pages.
- **~12 colour-literal occurrences** in 5 files — admin role badges (`bg-red-100 text-red-800` for admin, blue for user, purple for managed), plan status badges (green-100), security status pills (green/blue), personal-info verified badge (green), table badges (blue).
- **A handful of `shadow-*` / `tracking-*` / `leading-relaxed`** occurrences.

Migration plan (one or two entries per release until cleared):

- **Typography pass** — replace `text-sm` / `font-medium` / `font-semibold` usage with structural-only classes. Where the class was genuinely setting a design-level size or weight (e.g. `text-lg` on a page title), move the styling into a Style Registry entry that targets the appropriate shadcn data-slot (`data-slot="card-title"` etc.) so the style module can own the scale.
- **Status colour pass** — remove hardcoded `bg-red-100` / `bg-green-100` / `bg-blue-100` badge styles. Depends on shipping `success` / `warning` / `info` tokens in the template's stub globals.css (signals intent; shadcn init's output overrides when consumers want different colours). Badge components move to using the new tokens.
- **Shadow pass** — remove `shadow-sm` / `shadow-md` from template code; expose via Style Registry entries contributed by a "card elevation" style module.
- **Audit script** — ship `scripts/audit-template-styling.{ps1,sh}` that greps for the banned class patterns and reports violations. Run as part of CI so regressions get caught.

Each release in the migration plan ships a **non-breaking** change — existing visual output is preserved through Style Registry entries registered on template bootstrap, consumers see no difference. Once the audit script reports zero violations, the rule becomes enforceable via CI.

### Deprecation codemods shipped with `template:update`

**Priority:** Medium — reduces the manual-migration burden even further once deprecation shims retire.

The v1.24.6 [deprecation policy](/starter-template/versioning/) ensures downstream apps never break at head — public exports go through a shim cycle before removal. That solves the "broke in the field" problem, but downstream apps still need to do a one-time find-and-replace when they want to move off a deprecated export (rename imports, swap JSX shapes, etc.). The template can do that work for them.

**What this would explore:**

- **Codemods per deprecation** — when a release deprecates a public export, ship a jscodeshift (or ts-morph) codemod alongside the shim that rewrites consumer code to use the replacement. Codemod lives at `codemods/<release>/<export-name>.ts` in the template tree.
- **`template:update` prompts to run them** — after pulling a release that deprecates something, the update script detects any codemods shipped by that release and prompts: "v1.24.4 deprecated `TabBar`. Run the migration codemod now? [y/N]". Running it does the rewrite across the downstream app's `src/` and stages the changes for review.
- **Skip-if-already-migrated** — codemod is idempotent, so running it twice is safe. If the downstream app has already migrated (no TabBar imports found), codemod reports "nothing to do."
- **Catalogue page in docs** — `/starter-template/codemods/` lists every codemod the template has ever shipped, what it does, and which release introduced it. Downstream apps catching up from multiple versions behind can see the full migration sequence.

**Scope note:**

- Codemods only for public-export shape changes (rename, prop signature change, file-path move). Not for semantic behaviour changes — those still need human review.
- Downstream apps opt-in per codemod at `template:update` time. Never auto-run without confirmation.
- Module-contributed codemods are a follow-up — modules that ship their own deprecations can contribute codemods via a module manifest field once the primitive is in place for the starter template itself.

**Why this is "planned" not "now":**

Useful, but the deprecation shim + warning flow already covers the core "don't break downstream" requirement. Codemods are a second-order UX improvement that makes migrating off deprecated exports less tedious. Land [per-change / per-hunk template updates](#per-change--per-hunk-template-updates) first — that addresses the more common daily friction.

### Per-change / per-hunk template updates

**Priority:** Medium — complements the [Template update mechanism review](#template-update-mechanism-review).

Today's `template:update` is whole-file in both directions: a conflict on any part of a file surfaces as "the whole file is in conflict," and the force-sync branch replaces the whole file with the template's current version even if only one line of that file actually changed between releases. Both behaviours are blunt.

A downstream user hit this after v1.24.4 pulled the new `AppSettingsPage.tsx`: they'd made no local changes to that file, but the three-way merge still surfaced the whole file as unresolved because git saw "both sides modified." The user-owned allow-list preserves their version; the force-sync replaces with the template's — fine for template-owned files, but a maintenance overhead for anyone tracking local churn.

**What this would explore:**

- **Per-hunk apply** — instead of letting git merge the whole file, walk the diff of template-changed files between the old merge-base and the new template HEAD, and apply each hunk individually with `git apply --3way` (or equivalent). A hunk that doesn't apply cleanly leaves that specific range of the file in conflict; unaffected ranges stay pristine. Means: a 2-line template tweak to a 400-line file doesn't surface as "the whole file is conflicted."
- **Skip-identical optimisation** — if the app's version of a file is byte-identical to the template's pre-change version (no local edits since the last merge), just swap to the new version. No merge needed.
- **Whole-file opt-out** — for files where per-hunk doesn't make sense (large JSON configs, serialised data, generated code), mark them in a manifest to fall back to today's whole-file behaviour.

**Why this is "planned" not "now":**

This is essentially "reinvent `git merge` with a narrower contract," and the per-hunk application logic is the hard bit — needs robust handling of context-lines, binary files, whitespace-only changes, deleted-then-recreated files, and so on. The sibling [Template update mechanism review](#template-update-mechanism-review) roadmap item is the right place to weigh per-hunk apply against the other candidate architectures (copy-then-sync, versioned patches, manifest-driven per-file sync). Land the review's design doc first, then pick.

### `.template-ignore` for per-app merge customisation

**Priority:** Medium — follow-up to v1.23.1's force-sync behaviour.

v1.23.1's `template:update` force-syncs every template-changed file to the template's current version, using a hardcoded user-owned allow-list. Works for every known case today, but there's no way for an app to say "I've customised `features/admin/pages/AdminDashboardPage.tsx` on purpose, please don't overwrite it." The escape hatch should be a file in the app root:

```
# .template-ignore — extends the user-owned allow-list for this app
frontend/src/features/admin/pages/AdminDashboardPage.tsx
frontend/src/features/dashboard/
```

Each line is a path (file or directory prefix) that's added to the user-owned allow-list at merge time. `update-template.{ps1,sh}` reads this file if present and extends `USER_OWNED_PATHS` / user-owned directory checks accordingly.

**Scope:**

- Read `.template-ignore` from the app root before the post-merge force-sync step.
- Treat each non-comment, non-blank line as a user-owned path. Support glob syntax (`frontend/src/features/admin/pages/*.tsx`) for flexibility.
- Warn when an app adds paths that the template has actually changed since the last merge — so the user knows they might be missing a fix.
- Document in the [Updating guide](/starter-template/updating/) with worked examples.

**Why not by default:** force-sync as the default is the right call — most apps will never need to diverge from a template-owned file. Making `.template-ignore` the opt-in keeps the common case clean and the escape hatch explicit.

### Archive removed files during template update

**Priority:** Medium — safety net for major refactors like v1.23.0.

When `npm run template:update` pulls a template release that deletes files (e.g. `frontend/src/features/auth/components/AccountSettings.tsx` in v1.23.0), the merge straight-deletes them locally. If the downstream app had customised the deleted file, the customisation is gone — recoverable from git history, but only if the user knows to look there and hasn't already committed the deletion. There's no diffable record in the working tree.

This feature borrows the "non-destructive uninstall" pattern from the Module Starter's [uninstall flow](/module-starter/building-modules/) — instead of hard-deleting, move removed files to an archive tree under `.template-archive/v<version>/<original-path>`. The working tree stays clean (the archive is gitignored by default), users have a two-week window to review what went and salvage customisations, and a follow-up `template:archive:clean` command drops the archive after review.

**Scope:**

- **Update `update-template.{ps1,sh}`** — before running the git subtree merge, detect files the incoming release deletes (via `git diff --name-only --diff-filter=D <old>..<new>`). For each, copy the current working-tree version to `.template-archive/v<previous-version>/<original-path>` before the merge runs. The merge then deletes the original.
- **Gitignore entry** — `.template-archive/` added to the root `.gitignore` shipped by the starter. Users who want to archive into a committed path can opt out.
- **Archive manifest** — write `.template-archive/v<version>/manifest.json` listing what was moved, why (which template commit deleted it), and the suggested action (review / delete / merge into new location). LLM-friendly so `claude-code` / CI bots can read it.
- **`npm run template:archive:clean`** — drops archives older than N versions (default: keep the last 2). Lets users maintain a small review window without unbounded archive growth.

**Out of scope:**

- Archiving *modified-then-removed* files differently from unmodified removals. Treat them all the same — the archive IS the diff-from-what-the-template-had. Users can use `git diff` against the previous template tag to find their customisations.
- Restoring from archive automatically. That's a manual `cp` back into the working tree — we don't want magic that re-creates deleted files unless the user explicitly asks.

**v1.23.0 retrospective:** `AccountSettings.tsx` was deleted and `auth.css` (which only ever existed in the withdrawn v1.22.2) was also removed. Neither was customised in any known downstream app, so no loss occurred this time — but the risk exists for every future refactor. Archive-on-removal should land before the next large restructuring change (custom settings pages / layout registry / similar).

### Neon Plugin Integration & Live DB Audit

**Priority:** Medium

Install Neon's official Claude Code plugin (`neon@claude-plugins-official`) and run a full database audit against the actual Neon project — not just the public docs. Parked after v1.22.2 because the plugin install hit SSH-auth friction on a fresh machine and the doc-only audit (see v1.22.2 changelog) already confirmed the backend stack matches Neon's published recommendations.

**What the plugin unlocks (once installed):**

- Query the actual project via MCP tools rather than inferring from docs.
- Verify `DATABASE_URL` vs `DIRECT_DATABASE_URL` resolve to the `-pooler` and direct endpoints Neon expects.
- Compare the configured `NEON_AUTH_URL` against the project's JWKS endpoint and trusted domains list.
- List branches and check for schema drift between `main` and any preview branches.
- Read the current Auth settings (email verification on/off, "Allow Localhost" toggle, custom SMTP configured, custom OAuth creds) against the production checklist added in v1.22.2.
- Inspect any existing RLS policies — relevant if modules start adopting the Data API pattern documented in [Database & Prisma](/starter-template/database/#alternative-the-neon-data-api).

**Install note:** on machines without GitHub SSH keys, set `git config --global url."https://github.com/".insteadOf git@github.com:` before `claude plugin install`, otherwise the marketplace clone fails with `Permission denied (publickey)`.

**Expected outcome:** either "current setup matches project state, nothing to change" (most likely) or a short punch-list of Neon Console configuration items to update for a specific environment. No template code changes anticipated.

### Error Pages & Offline Handling

**Priority:** Medium

Proper error boundaries and user-facing error pages:

- Network failure page (no internet connection — currently auth silently fails)
- Auth error page (JWKS fetch failure, token expired, invalid session)
- 404 page (replace current plain `<div>404</div>` with styled page)
- 500 / unexpected error boundary with retry action
- React Router `errorElement` integration on protected routes
- Offline detection banner or toast notification
- *Note:* the React error boundary here can also report caught client errors into the admin **System** page console — see the **Observability (core)** entry above (P4).

### Deployment Guide

**Priority:** Medium

Documentation page covering production deployment:

- Pre-deployment checklist (env vars, migrations, build verification)
- Deployment steps (backend first, then frontend)
- Post-deployment verification (health check, auth flow, critical paths)
- Security hardening checklist (rate limiting, CORS, monitoring)
- Error monitoring integration patterns (Sentry, structured logging)

### Internationalisation (i18n)

**Priority:** Medium

Add foundation for multi-language support:

- Language options registry (already exists)
- Translation file structure
- React context for current locale
- Backend response localisation hooks

### Notification System

**Priority:** Medium

Built-in notification infrastructure:

- Toast notifications (already using sonner)
- Persistent notification centre
- Notification preferences (registry exists)
- Backend notification service hooks

### Command Palette

**Priority:** Low

Global keyboard-driven navigation:

- `Cmd+K` / `Ctrl+K` trigger
- Search across navigation items
- Recent items and actions
- Extensible via registry

### styleOverrides Bootstrap Cache Fix

**Priority:** Medium

Module-registered style entries apply synchronously at module-load time; `AppSettings.styleOverrides` is fetched asynchronously during React mount. If an admin disabled an entry whose tokens visibly change the shell, users see a brief flash from the "on" state to the "off" state during the fetch.

**Fix:**

- Cache the last-known `styleOverrides` map in `localStorage` from `AppSettingsContext` on every update.
- In `main.tsx`, read the cache before React mounts and call `styleRegistry.applyOverrides(cached)` synchronously — same pattern as the existing theme-class init that prevents the dark/light mode flash.
- On server response, reconcile and re-apply if the server-side map differs from the cache.

**Scope:** ~15 min. Fully backwards-compatible — users without a cached value still get the current behaviour.

Related: [Style System § Known limitation](/starter-template/style-system/#known-limitation--first-paint-flash), [Changelog v1.21.0](/starter-template/changelog/#v1210).

### Frontend Env Var Build-Time Validation

**Priority:** Low

The backend uses Zod to validate env vars at startup; the frontend currently doesn't validate `VITE_*` vars at all. A missing or malformed `VITE_NEON_AUTH_URL` surfaces as a runtime auth failure, not a build error.

**Fix:** add a Vite plugin (or a `tsx` preflight) that parses `import.meta.env` against a Zod schema and fails the build if required vars are missing. Mirrors the backend's `config/env.ts` pattern.

### App Documentation

**Priority:** Low

Optional documentation setup for apps created from the template:

- Create script prompts: "Would you like to set up documentation?"
- Scaffolds Astro/Starlight docs folder (same setup as module system docs)
- App-specific docs, not linked to module system shared docs
- Pre-configured with app name and basic structure
- Separate from the shared module system documentation

**Current state:** Apps inherit the `docs/` submodule from the template, which points to the shared module system docs. Apps should either have no docs folder or their own dedicated docs.

### Production security hardening

**Priority:** Medium. Most items landed in **v1.27.0**; the remaining one is tracked here. Overlaps the security checklist in [Deployment Guide](#deployment-guide).

- **Explicit Content-Security-Policy** — Helmet still runs on defaults; set explicit CSP directives (the existing `app.ts` TODO). Not yet shipped.

✅ **Shipped in v1.27.0:** `validateParams` / `validateQuery` middleware + admin `:userId` validation, `trust proxy` (`TRUST_PROXY_HOPS`), `Cache-Control: no-store` on API responses, HTTP request timeout (`REQUEST_TIMEOUT`), `/api/docs` prod-gating, and the client-side `RoleProtectedRoute` on `/admin/*`.

### Runtime enable/disable for collection registries

**Priority:** Medium. Partially shipped in **v1.27.0**.

v1.27.0 added a generic admin enable/disable gate to the frontend `BaseRegistry` (`getActive()` / `applyAdminOverrides()` / `isAdminHidden()`) and wired it to the **dashboard** registry, persisted in `AppSettings.dashboardOverrides`. Navigation and toolbar already had admin hide via `navigationOverrides` / `toolbarOverrides`. Remaining: extend the gate to other collection registries as needed, and ship the admin UI for toggling widgets (see [Dashboard Widget Admin](#dashboard-widget-admin) — the backend + registry plumbing now exists). Backend **route** registry gating stays blocked on the build-time-coupling work (see [Admin Module Management](#admin-module-management)).

### Documentation–code sync follow-ups

✅ **Resolved in v1.38.0** — the 2026-06-25 [documentation audit](#known-documentation-issues) surfaced five places where the **code** should change to match the docs. All five shipped in the v1.38.0 minor bump (see the [changelog](/starter-template/changelog/#v1380)); kept here as the record of what changed:

- **`TabBar` removal version is stale in code.** `frontend/src/components/settings-blocks/components/TabBar.tsx` (file header, `@deprecated` JSDoc, and the runtime `deprecationWarning()` string) and `frontend/src/components/settings-blocks/index.ts` still say *"scheduled for removal in v1.25.0"* — now in the past while the shim still ships. Reconcile to the real target (e.g. v1.26.0+) or remove the shim per the deprecation policy. The runtime string is shipped to every downstream dev console.
- **`LOG_LEVEL` comment is misleading.** `backend/src/config/env.ts` comments the default as *"'info' in production, 'debug' in development"*, but `logger.ts` passes `env.LOG_LEVEL` verbatim with no dev override — the effective default is always `info`. Fix the comment.
- **No `type-check` npm script (optional).** Docs now point users at `npm run build` (which type-checks via `tsc`). If a dedicated command is wanted, add `type-check` (`tsc -b --noEmit` / `tsc --noEmit`) to `frontend/package.json` and `backend/package.json`.
- **`userService` not in the user feature barrel (optional).** Docs import it from the real path `@/features/user/services/user.service`. Re-export it from `frontend/src/features/user/index.ts` if a barrel import is preferred.
- **JSDoc-only deprecations don't warn at runtime (optional).** `navigationRegistry.unregisterMain/unregisterSecondary/unregisterUserMenu` carry `@deprecated` but emit no `deprecationWarning()`. Add the runtime warning for full deprecation-policy compliance (the type-only `SidebarNavItem` alias can't warn at runtime).

### Security manifest back-port + deletion handlers (Structure, cog-ingest)

**Priority:** High — closes the account-deletion gap for modules that predate the [deletion cascade](/starter-template/security-feature/#deletion-cascade) (shipped v1.32.0).

The cascade infrastructure and `securityRegistry.registerDeletionHandler` already ship. What's missing is the per-module **back-port**: several modules still lack a `security.registration.ts` manifest and an account-deletion handler, so they don't participate in `deleteAccount()`.

- **Structure** and **cog-ingest** need a manifest + an `owned` deletion handler. Cog-ingest currently only logs on `structure.node.deleted` and retains ingest jobs — see [Cog Ingest → Integration](/modules/cog-ingest/integration/).
- **Builder** is the reference implementation; **Billing** has a scaffolded manifest to copy from.
- **Decision item (separate):** cog-ingest's node-delete **retain-vs-cascade** policy. Node deletion may legitimately retain ingest jobs as history, but **account/tenant deletion must purge them regardless** — the retain policy is scoped to node deletion only.
- **Conformance:** the Module Starter template should emit a `security.registration.ts` stub so new modules are born participating in the cascade.

### Minimum testing + CI pipeline

**Priority:** High — the template has no automated coverage today; every regression is found downstream in production.

- **User-boundary isolation tests** — assert one user cannot read or write another's owned rows through any access path (the "cross-tenant" tests are user-boundary tests under the single-tenant model).
- **Registry-registration smoke test** — boot the app and assert every expected registration is present (pairs with the Module Starter's "no validation for duplicate registrations" known issue).
- **Module install / uninstall integration test** — scaffold a throwaway module, install it, assert it registers, uninstall it, assert clean removal.
- **GitHub Actions pipeline** — lint + typecheck + test + build on every push, so the above (and the styling audit script) run automatically rather than by hand.

### Observability & ops hardening

**Priority:** Medium — complements the shipped [System page + audit sinks](/starter-template/system/) (v1.39.0–v1.40.0).

- **Sentry error tracking (wired).** Initialise Sentry as early as possible in the backend bootstrap and register `Sentry.setupExpressErrorHandler(app)` **after** routes are mounted; feed the frontend error boundary into it too. Complements the in-app audit console rather than replacing it.
- **Verify the middleware order is actually mounted.** Confirm `helmet` → CORS → `express-rate-limit` → routes is wired in `app.ts` (helmet first, then CORS, then rate limit), plus liveness/readiness **health-check** endpoints the rate limiter skips.
- **Backup / restore runbook.** A documented procedure built on **Neon instant point-in-time restore** — a 1–30 day history window, LSN-granularity, restore into a branch off the root — covering how to restore, how to verify, and the retention window per tier.

### Docs-alignment pass

**Priority:** Medium — tracks the documentation reconciliation pass this entry is part of (in progress).

- Single-tenant **tenancy framing** reconciled on the [Module Roadmap](/modules/roadmap/) and [LLM Rules](/getting-started/llm-rules/): product is single-tenant; isolation is keyed on `tenantId`, 1:1 per user.
- **Icon Picker** bundle-size / lazy-chunk constraints documented; **cog-ingest** scaffold placeholders (`initMyFeature` → `initCogIngest`) fixed; **Registry Reference** confirmed to list `securityRegistry`.
- **Pickers module retired** — pages replaced with deprecation stubs pointing to the starter Icon Picker and the Colour Palette / Style modules; sidebar and cross-references updated.

---

## Known Security Issues

Findings from the v1.26.0 security & architecture audit (backend + frontend, audited against live code). Application-layer access control is sound for the current surface — the `User` model is the only user-owned data and every access path is scoped to `req.user.id` — but the items below must be closed before any deployment that stores sensitive or multi-tenant data, especially the legal/evidence-grade apps on this stack.

<Aside type="tip" title="Mostly addressed in v1.27.0">
  The error-leak, rate-limiting, TOCTOU-race, and CORS items below were fixed in **v1.27.0**, and the user-data isolation backstop now ships (`owned()` + opt-in RLS). Remaining: the frontend `better-auth` Critical (coupled to Neon's auth upgrade — see Dependency vulnerabilities) and explicit CSP (see [Production security hardening](#production-security-hardening)). Per-item status is noted below.
</Aside>

### Database-level user-data isolation backstop

**Priority:** Critical (strict tier) — the headline gap for legal/evidence apps.

**Status:** ✅ Addressed in **v1.27.0** — `@/lib/owned` ships `owned(userId, where)` for app-layer scoping and `withUserContext()` for the opt-in RLS backstop; the strict-tier RLS profile is documented. Adopt both for any user-owned model.

User-data isolation is enforced **only** at the application layer (JWT → `req.user.id` → `where: { userId }`). There is no Postgres Row-Level Security and no reusable ownership-check helper shipped by the template. The only user-owned model today is `User`, and every path is correctly scoped — but the commented example relation (`prisma.post.findMany({ where: { userId } })` in `user.service.ts`) is the pattern downstream developers copy by hand, so the first user-owned model added is one forgotten `where` clause away from full cross-user leakage.

- Ship an ownership-scoped query helper (e.g. `owned(userId, where)`) so isolation is not hand-rolled per query.
- Document the **RLS floor**: `FORCE ROW LEVEL SECURITY` plus a per-request `set_config('app.tenant_id', …)` inside `withTenantContext` (the Prisma table-owner connection bypasses RLS otherwise). See [Database & Prisma → User-data isolation](/starter-template/database/#user-data-isolation-neon-rls).

### Error-message leakage in production

**Priority:** High. **Status:** ✅ Fixed in **v1.27.0** — non-operational errors now return a generic message in production.

`backend/src/middleware/error.middleware.ts` returns `err.message` to the client for **all** errors including unexpected 500s; only `stack` is gated behind `NODE_ENV === 'development'`. Unwrapped Prisma/internal errors leak schema and query detail to clients in production. Fix: when the error is not an operational `AppError`, return a generic message and log the real one server-side only.

### No global rate limiting

**Priority:** High. **Status:** ✅ Fixed in **v1.27.0** — global `express-rate-limit` on `/api` (env-tunable, health checks skipped).

`backend/src/app.ts` carries only a commented-out `express-rate-limit` TODO; the sole limiter anywhere is the 1-per-hour data-export check in `user.service.ts`. Auth, role-change, and delete endpoints have no throttle. The `TooManyRequestsError` + `Retry-After` plumbing already exists — only the limiter middleware is missing. (New dependency goes through the `create-app` set, not a direct install.)

### Non-atomic role checks (TOCTOU races)

**Priority:** High. **Status:** ✅ Fixed in **v1.27.0** — both guards now run in Serializable transactions (`withTransaction({ isolationLevel: 'Serializable' })`).

Two role invariants are checked non-atomically (`count()` then mutate):

- **First-user-becomes-admin** (`user.service.ts` `getOrCreateProfile`) — concurrent first logins can mint multiple admins; a pre-admin attacker who calls the API first gains admin.
- **Last-admin guard** (`admin.service.ts` `updateUserRole` / `deleteUser`) — concurrent demotions/deletes can leave zero admins and lock the system.

Fix: wrap each in `withTransaction` with row locking, or enforce via a DB constraint / unique partial index.

### CORS allows no-origin requests with credentials

**Priority:** High. **Status:** ✅ Fixed in **v1.27.0** — no-origin requests are rejected in production.

`backend/src/app.ts` returns `callback(null, true)` for requests with no `Origin` header while `credentials: true` is set (an acknowledged, unresolved `DECISION NEEDED` in the code). The API authenticates via `Authorization: Bearer`, so classic CSRF exposure is currently limited — but this is the wrong default and becomes critical once any cookie/session auth is in play (`better-auth` uses session cookies). Reject no-origin requests in production.

### Dependency vulnerabilities

**Priority:** High. **Status:** ⚠️ Partially fixed in **v1.27.0** — backend now reports **0 high / 0 critical**; the frontend `better-auth` Critical remains (see below).

Original audit (`npm audit`, lockfile-only): frontend 1 critical + 15 high, backend 10 high. v1.27.0 bumped `axios` (`^1.18.0`), `react-router-dom` (`^7.18.0`), `ws` (`^8.21.0`) and regenerated all lockfiles — the existing `^` ranges already permitted the patched versions, so the highs were stale-lockfile pins. Result: **backend 0 high / 0 critical** (3 moderate dev-tooling remain); frontend highs reduced.

**Still open:** the **`better-auth`** Critical (2FA bypass, OAuth `state`/PKCE) is transitive via `@neondatabase/neon-js` and pinned to Neon's Better Auth 1.4.6 server — it tracks the same Neon upgrade as the [forgot-password flow](#finalise-the-forgot-password--reset-password-flow) and **cannot be unilaterally bumped without breaking auth**. The remaining frontend highs are that auth cluster (`fastify` / `kysely` / `fast-uri`) or dev-only build tooling (`vite` / `rollup`). Re-run `npm audit` after the Neon upgrade lands, and add a recurring audit gate (CI).

---

## Known Performance Issues

Findings from the v1.20.4 performance audit. Grouped by where the cost actually lands.

<Aside type="danger" title="Blocker before production">
  **Group C — Backend list perf** is a pre-production blocker. The `getAllUsers()` endpoint returns every user row in the database with no `take`/`skip` pagination, and `User` has no index on `createdAt` (used for ORDER BY in the admin users list) or `role` (used for the "last admin" check). This is fine at dev scale but will degrade quickly past a few thousand rows and must be fixed before shipping to real traffic.
</Aside>

**Status as of v1.20.7:**
- ✅ Group A — Context + callback hygiene (v1.20.5)
- ✅ Group B — Bundle + debounce (v1.20.6)
- 🚧 **Group C — Backend list perf (pre-production blocker)**
- ✅ Group D — Correctness cleanup (v1.20.7)

### Context & callback hygiene

**Priority:** High — quick wins, zero behavioural risk

- **`AppSettingsContext.value` is not memoised** ([`frontend/src/features/admin/context/AppSettingsContext.tsx:159`](https://github.com/ui-git-kim/unique-react-fullstack-starter/blob/main/frontend/src/features/admin/context/AppSettingsContext.tsx)) — the context wraps the entire app shell; every render of `AppSettingsProvider` creates a new object identity, forcing every consumer to re-render. Fix: wrap in `useMemo`.
- **`useNavigationOverrides.save` and `useToolbarOverrides.save` depend on `[overrides]`** — the callbacks are recreated on every toggle/swap. Pages memoise their toolbar `items` against these callbacks via `useAdminToolbarActions`, which triggers a registry unregister→register cycle on every interaction. Fix: pass the overrides to save as an argument, not a closure dep.
- **`NavItem` is not `React.memo`-wrapped** ([`frontend/src/features/dashboard/components/sidebar/NavMain.tsx`](https://github.com/ui-git-kim/unique-react-fullstack-starter/blob/main/frontend/src/features/dashboard/components/sidebar/NavMain.tsx)) — sidebar re-renders all items on every parent render. Low impact today (fewer than 50 items), worth fixing for module authors who might add many items.

### User-visible responsiveness

**Priority:** High

- **`AdminDataTable` search has no debounce** ([`frontend/src/features/admin/components/AdminDataTable.tsx:143`](https://github.com/ui-git-kim/unique-react-fullstack-starter/blob/main/frontend/src/features/admin/components/AdminDataTable.tsx)) — every keystroke triggers a full TanStack table re-filter. Jank at 100+ rows. Fix: 200ms debounce.
- **Admin pages are in the main bundle** ([`frontend/src/features/admin/admin-pages.registration.ts`](https://github.com/ui-git-kim/unique-react-fullstack-starter/blob/main/frontend/src/features/admin/admin-pages.registration.ts)) — the route already wraps in `<Suspense>`, so switching to `React.lazy()` is a clean win for the 98% of users who aren't admins.

### Backend list scalability

**Priority:** Medium — only matters past a few thousand rows, but cheap to fix now

- **`admin.service.ts -> getAllUsers()` has no pagination** ([`backend/src/features/admin/admin.service.ts:321`](https://github.com/ui-git-kim/unique-react-fullstack-starter/blob/main/backend/src/features/admin/admin.service.ts)) — returns every user row on every admin-users-page visit. Fix: add `page`/`limit` params and return `{ users, total }`; update `adminService` and `useAdminUsers` in lockstep (breaking API change → minor version bump, downstream modules that call this need updating).
- **`User` model missing indexes on `createdAt` and `role`** ([`backend/prisma/schema.prisma`](https://github.com/ui-git-kim/unique-react-fullstack-starter/blob/main/backend/prisma/schema.prisma)) — `getAllUsers` orders by `createdAt` and `updateUserRole` counts admins by `role`. Add `@@index([createdAt])` and `@@index([role])`. Ships as a migration downstream apps need to run.

### Duplicate fetches & cleanup

**Priority:** Low — correctness more than performance, but should be fixed

<Aside type="tip" title="Systemic fix landed in v1.22.0">
Adopting TanStack Query as the [standard data fetching layer](/starter-template/data-fetching/) supersedes most of the individual fixes in this section. Providers that migrate to `useQuery` get deduplication (no more double-fetch), built-in cancellation (no stale `setState` on unmount), and a shared cache as a matter of course. Migrate remaining hooks (`useAdminUsers`, `useNavigationOverrides`, `useToolbarOverrides`) as they're next touched — the reference pattern is `AppSettingsContext`.
</Aside>

- ✅ **`AppSettingsPage` fetches app settings twice** — page now reads from the global context (already landed); the context itself is backed by a shared `useQuery` since v1.22.0, so any other consumer that later mounts `['app-settings']` hits the cache.
- **Fetch effects without `AbortController`** in remaining hooks (`useAdminUsers`, `useNavigationOverrides`, `useToolbarOverrides`) — stale `setState` after unmount. Preferred fix: migrate each to `useQuery` / `useMutation`, which handles cancellation and re-use for free.
- **`matchMedia` listener in `main.tsx` is never removed** — module-level, runs once in production; HMR in dev can accumulate listeners. Low impact but worth fixing.

---

## Known Documentation Issues

<Aside type="tip" title="✅ Resolved 2026-06-25 — accuracy audit of 2026-06-24 (against v1.37.0)">
Full re-audit of every Starter Template, Reference, and Getting Started doc page against the live codebase — a 25-cluster multi-agent sweep with adversarial verification. **135 of 136 findings fixed** across 31 files (54 Incorrect + 16 Outdated + 65 Incomplete), plus the stale `docs/CLAUDE.md` structure listing. 1 finding was a no-op (already correct); 6 candidates were rejected on verification as false positives. The audit also surfaced **5 code-side follow-ups** — tracked under [Documentation–code sync follow-ups](#documentationcode-sync-follow-ups) in Planned. The backlog below is kept as the resolved log (each item marked `[DONE]`). Supersedes the v1.21.1 audit.
</Aside>

Confirmed factual errors — wrong APIs, import paths, code examples, endpoints, env vars, and defaults — plus stale/missing-behaviour sections. Fix **Incorrect** first, then **Outdated**.

| Page | Incorrect | Outdated | Incomplete |
|------|:---:|:---:|:---:|
| `dashboard-feature` | 5 | 1 | 4 |
| `logging` | 6 |  | 2 |
| `architecture` | 4 | 1 | 6 |
| `create-script` | 2 | 3 | 4 |
| `reference/types` | 3 | 1 | 1 |
| `user-feature` | 4 |  | 3 |
| `header-toolbar` | 4 |  | 2 |
| `shell-layout` | 4 |  | 2 |
| `reference/registries-api` | 3 |  | 2 |
| `auth-feature` | 2 | 1 | 1 |
| `configuration` | 3 |  | 4 |
| `updating` | 1 | 2 | 2 |
| `getting-started/documentation` | 1 | 2 |  |
| `index` | 2 |  |  |
| `reference/base-registry-api` | 2 |  | 2 |
| `security-feature` |  | 2 | 1 |
| `getting-started/neon-setup` |  | 2 |  |
| `reference/cli-commands` | 2 |  | 2 |
| `versioning` | 2 |  | 3 |
| `developer-workflow` | 2 |  | 1 |
| `features` | 1 |  |  |
| `hooks` |  | 1 | 6 |
| `database` | 1 |  | 3 |
| `base-registry` |  |  | 3 |
| `registries` |  |  | 3 |
| `admin-feature` |  |  | 3 |
| `icon-picker` |  |  | 1 |
| `scripts` |  |  | 4 |
| `getting-started/llm-rules` |  |  | 1 |
| **Total** | **54** | **16** | **66** |

Full per-finding backlog below (each line is one tracked item — mark `[done]` as fixed):

```text

architecture
  [DONE] Incorrect — Database Schema
      ISSUE: The schema example shows a separate model Profile { ... } with displayName, avatarUrl, avatarColor etc., and a User model with a profile Profile? relation and role Role @default(user) typed against a Role enum.
      FIX:   Replace the Profile model block with the actual User fields (name, avatarUrl, bio, preferences Json) and change role Role @default(user) to role String @default("user"). Remove the separate Profile model entirely or note that profile data is denormalised onto User.
  [DONE] Incorrect — Database Schema — AppSettings model
      ISSUE: AppSettings is shown with id String @id @default("singleton"), logoDisplayMode String @default("both"), and only the fields appName/logoUrl/logoDisplayMode/navigationOverrides/toolbarOverrides.
      FIX:   Change the id default to "default", logoDisplayMode default to "logo-and-name", and add the missing fields (showDashboardInNav, enableMarketingPages, styleOverrides, dashboardOverrides, appTheme) or note the example is abbreviated.
  [DONE] Incorrect — Overview (ASCII diagram)
      ISSUE: The bottom of the architecture diagram labels the database as 'User · Profile · AppSettings tables'.
      FIX:   Change to 'User · AppSettings tables'.
  [DONE] Incorrect — Request Flow
      ISSUE: Step 2 of the request flow shows api.get('/api/v1/user/profile').
      FIX:   Change step 2 to api.get('/api/v1/user').
  [DONE] Outdated — Overview (ASCII diagram) — Backend features
      ISSUE: The backend features list in the diagram shows only user/ and admin/. The frontend list shows only auth, user, dashboard, admin.
      FIX:   Add security/ to the backend feature list, and add security/ and support/ to the frontend feature list in the Overview diagram (the detailed tree further down does include security, but support is still missing everywhere).

auth-feature
  [DONE] Outdated — Backend Middleware → Extended Request Type
      ISSUE: The declare global { namespace Express { interface Request { user?: { ... } } } } block lists only id, email, and role? on req.user. It omits the tenantId field.
      FIX:   Add tenantId?: string; to the documented Request.user interface, e.g.: typescript user?: { id: string; // Neon Auth user ID (JWT 'sub' claim) email: string; // User's email (JWT 'email' claim) role?: UserRole; // Populated by requireAdmin/requireRole/attachRole tenantId?: string; // Isolation key for data scoping (RLS + owned()); equals id in solo apps, set by requireAuth };  and add a sentence noting requireAuth sets tenantId to the user id by default.
  [DONE] Incorrect — Troubleshooting → OAuth Not Working (step 3)
      ISSUE: Step 3 instructs: "Enable the provider in NeonAuthUIProvider".
      FIX:   Remove the NeonAuthUIProvider reference. Replace step 3 with: "Enable the provider in the Neon Console (Settings → Auth)" — OAuth provider configuration is entirely Neon-side, not in template code.
  [DONE] Incorrect — Production Considerations → Neon Auth Production Checklist (closing Aside)
      ISSUE: The note says to watch Neon's changelog for "new features worth wiring into NeonAuthUIProvider — passkey and 2FA props are likely candidates".
      FIX:   Drop the NeonAuthUIProvider reference. Reword to: "...new features worth wiring into the custom auth pages or surfacing via authClient — passkey and 2FA are likely candidates...".

configuration
  [DONE] Incorrect — Variable Reference → Backend, DIRECT_DATABASE_URL row (line 228)
      ISSUE: The backend Variable Reference table marks DIRECT_DATABASE_URL as Required = "Yes".
      FIX:   Change DIRECT_DATABASE_URL Required from "Yes" to "No", and note it falls back to DATABASE_URL but is required for Neon migrations (use the non-pooled URL).
  [DONE] Incorrect — Variable Reference → Backend, FRONTEND_URL row (line 233)
      ISSUE: The backend Variable Reference table marks FRONTEND_URL as Required = "Yes".
      FIX:   Change FRONTEND_URL Required from "Yes" to "No", with default http://localhost:5173 (must match the frontend origin for CORS in non-dev environments).
  [DONE] Incorrect — Variable Reference → Frontend (line 216) vs Environment Files → Frontend (line 27)
      ISSUE: The two frontend tables contradict each other on VITE_API_URL: the Environment Files table (line 27) lists Required = No with default http://localhost:3100, while the Variable Reference table (line 216) lists Required = Yes.
      FIX:   Change VITE_API_URL Required from "Yes" to "No" in the Variable Reference table (line 216) so it matches the Environment Files table and the code default.

create-script
  [DONE] Outdated — Step 5: Backend Port / Step 6: App Remote (prompt ordering)
      ISSUE: The walkthrough jumps from 'Step 5: Backend Port' straight to 'Step 6: App Remote (Optional)', implying those are consecutive prompts. It never documents the two prompts the script asks in between.
      FIX:   Insert two new steps after 'Step 5: Backend Port': a 'Prisma Schema Mode' step (single file vs multi-file in prisma/schema/, default single, recommended multi if installing modules) and a 'Vector Database (pgvector)' step (default No; adds the pgvector extension + Embedding model), then renumber 'App Remote' accordingly.
  [DONE] Outdated — Confirmation
      ISSUE: The documented confirmation summary only shows Location, Backend port, Database, Auth, and App remote.
      FIX:   Update the confirmation block example to include 'Schema:' and 'Vectors:' lines, and note the skipped status reads 'Manual setup required'.
  [DONE] Outdated — What Happens Next
      ISSUE: The numbered 'What Happens Next' list omits the AWS KMS field-encryption setup that the script offers, and the steps don't reflect that the script runs in 6 labelled phases ([1/6]…[6/6]).
      FIX:   Add a step describing the optional AWS KMS field-encryption key custody prompt (runs scripts/setup-kms.mjs / npm run security:setup-kms) between the database setup and initial commit.
  [DONE] Incorrect — Step 3: Database URLs (multiline input)
      ISSUE: The prompt example shows a single-line prompt 'Paste your Neon snippet (press Enter when done):' implying one Enter finishes input. The 'Getting the Neon Snippet' Steps say 'Paste the entire snippet … Press Enter to confirm'.
      FIX:   Clarify that the snippet is read line-by-line and the user presses Enter on an empty line to finish (i.e. paste both lines, then press Enter on a blank line).
  [DONE] Incorrect — Troubleshooting → 'shadcn init failed'
      ISSUE: The manual recovery command lists components: 'sidebar breadcrumb separator dropdown-menu avatar collapsible badge card dialog tooltip' and uses 'init --yes --defaults'.
      FIX:   Update the troubleshooting command to the full component list and the actual init invocation. Add the components: table checkbox sheet button input label radio-group switch tabs alert alert-dialog, use --yes --overwrite on add, and note init is interactive (choose Radix).

dashboard-feature
  [DONE] Incorrect — Widget Configuration (table)
      ISSUE: The widget-config table lists requiredRole with type UserRole as a property: "| requiredRole | UserRole | No | Role-based visibility |".
      FIX:   Rename the row to minRole with type string and note it is reserved for future use (not yet enforced): "| minRole | string | No | Minimum role to see widget (reserved, not yet enforced) |". Remove the UserRole type reference.
  [DONE] Incorrect — File Structure
      ISSUE: The file-structure diagram lists two page files: pages/DashboardPage.tsx # Main layout (shell) and pages/DashboardHome.tsx # Home page with widget grid. It also lists components/widgets/ExampleWidgets.tsx correctly but shows components/header/ and components/sidebar/ only as bare folders.
      FIX:   Remove the DashboardHome.tsx line. Annotate DashboardPage.tsx as exporting both DashboardPage (shell layout) and DashboardContent (home widget grid). The doc's later code examples that define function DashboardHome() should be relabelled DashboardContent to match the real export.
  [DONE] Outdated — Registry Hooks / User Widget Preferences
      ISSUE: The doc describes widget visibility purely as a per-user preference (hide/show via user.preferences.dashboardWidgets) and never mentions admin-level global disabling. getVisible is described only in terms of user prefs.
      FIX:   Add a subsection documenting admin overrides: getActive() (all widgets minus admin-disabled, sorted) vs getAll() (everything, for admin management UIs), applyAdminOverrides(ids) and isAdminHidden(id). Note that getVisible()/useVisibleDashboardWidgets filter through the admin-active set first, so an admin-disabled widget is hidden for everyone regardless of user preference.
  [DONE] Incorrect — DashboardGrid Component -> Props
      ISSUE: The DashboardGrid props table types userVisibility as UserWidgetPreference[] and presents UserWidgetPreference as if it were an importable/public type.
      FIX:   Either keep UserWidgetPreference[] as the literal prop type but clarify it is structurally compatible with the public WidgetVisibility[], or document the prop type as WidgetVisibility[]. Do not imply UserWidgetPreference is an importable type.
  [DONE] Incorrect — Example Widgets
      ISSUE: The Example Widgets snippet shows WelcomeWidget using useUserProfile() to greet the user (Welcome back, {profile?.name ...}) and shows QuickStatsWidget rendering hardcoded 1,234 / +12% from last month.
      FIX:   Replace the Example Widgets snippet with the real WelcomeWidget (QuickLinks layout), QuickStatsWidget (3-stat grid), and mention RecentActivityWidget (which demonstrates the isCompact prop). Note the registered welcome widget's title is 'Getting Started'.
  [DONE] Incorrect — Using the Hook / Widget Visibility UI / DashboardGrid Basic Usage
      ISSUE: Multiple code examples define function DashboardHome() and import/use it as the home component, e.g. <DashboardGrid widgets={visibleWidgets} />.
      FIX:   Rename the example component to DashboardContent. Fix the 'Using the Hook' example: either render widgets manually after calling useVisibleDashboardWidgets, or just use <DashboardGrid userVisibility={...} /> (DashboardGrid accepts userVisibility, not widgets).

database
  [DONE] Incorrect — Runtime Client Setup
      ISSUE: The lib/prisma.ts code example references env.DATABASE_ADAPTER, env.DATABASE_URL, etc., but the example never imports env — it imports PrismaClient, PrismaNeon, PrismaPg, neonConfig and ws only. As written the snippet would throw env is not defined.
      FIX:   Add import { env } from '../config/env.js'; to the example imports (and ideally note the real implementation wraps adapter selection in a createAdapter() helper and caches the client on globalThis for hot-reload).

developer-workflow
  [DONE] Incorrect — Testing Changes → 3. Check Type Errors
      ISSUE: The doc instructs: 'In the test app (not the template), run: npm run type-check' to check for type errors.
      FIX:   Replace npm run type-check with the actual type-checking command. For the frontend that is npm run build (runs tsc -b); for the backend npm run build (runs tsc). Either change the example to npm run build, or add a type-check script to the template's package.json files first and document that.
  [DONE] Incorrect — Key Principles (item 4)
      ISSUE: Key Principle #4 states: 'Forward momentum — No backwards compatibility layers. Clean breaks with major versions.'
      FIX:   Rewrite principle #4 to reflect the real policy: 'Deprecate, don't delete — public exports get a backwards-compatibility shim plus a dev-mode deprecationWarning, and are removed no sooner than one minor version later. See the versioning/deprecation policy.' Remove the 'No backwards compatibility layers / clean breaks with major versions' framing.

features
  [DONE] Incorrect — Intro / Core Features
      ISSUE: States 'The starter template includes four core features' and the auth card says 'Neon Auth with JWT tokens, email OTP, and role-based access control.'
      FIX:   Either keep 'four core user-facing features' but acknowledge the security (foundation) and support features, and change 'email OTP' to 'email + password and Google OAuth'.

getting-started/documentation
  [DONE] Incorrect — Editing Documentation → File Structure (FileTree)
      ISSUE: The FileTree lists create-script.mdx as a child of getting-started/ (alongside index.mdx, neon-setup.mdx, llm-rules.mdx, documentation.mdx).
      FIX:   Remove the create-script.mdx line from the getting-started/ node of the FileTree. If you want to reflect the real getting-started contents, list aws-kms-setup.mdx and cloudflare-edge.mdx instead, and note create-script lives under starter-template/.
  [DONE] Outdated — Editing Documentation → File Structure (FileTree)
      ISSUE: The getting-started/ node enumerates a fixed list of pages (index, neon-setup, create-script, llm-rules, documentation) with no ellipsis, implying it is complete.
      FIX:   Add aws-kms-setup.mdx and cloudflare-edge.mdx to the getting-started/ node of the FileTree (or append an ellipsis line to signal the list is non-exhaustive).
  [DONE] Outdated — Architecture (ASCII diagram)
      ISSUE: The Documentation Site ASCII diagram lists the Getting Started column as - Introduction / - Neon Setup / - Create Script / - LLM Rules / - Documentation.
      FIX:   In the diagram's Getting Started column, drop Create Script (it belongs under Starter Template) and reflect the current getting-started pages (Introduction, Neon Setup, AWS KMS, Cloudflare Edge, LLM Rules, Documentation), or genericise the column so it doesn't imply an exact page list.

getting-started/neon-setup
  [DONE] Outdated — Step 4: Configure Environment Variables → Backend (.env) (lines 64-88)
      ISSUE: The backend .env example lists Database, Auth, Server, Frontend and a few optional knobs (DEBUG_PRISMA, DB_QUERY_TIMEOUT, SHUTDOWN_TIMEOUT) but contains no SECURITY_* variables at all.
      FIX:   Add a # Security block to the backend .env example showing at least SECURITY_PROFILE=hardened and SECURITY_EDGE=none, with a pointer to the Configuration page's Security tier section for the full knob list.
  [DONE] Outdated — Step 4: Configure Environment Variables → Frontend (.env) (lines 90-101)
      ISSUE: The frontend .env example lists only VITE_NEON_AUTH_URL, VITE_API_URL and VITE_APP_NAME.
      FIX:   Add VITE_SECURITY_PROFILE=hardened to the frontend .env example with a comment noting it should match the backend SECURITY_PROFILE.

header-toolbar
  [DONE] Incorrect — Registering Items / Item Interface
      ISSUE: The button type examples and the ToolbarItem interface use label for the visible button text, variant for the button style, and loading/disabled as the loading/disabled props. E.g. the dynamic-toolbar example registers { type: 'button', label: 'Save Changes', variant: 'default', disabled: !hasChanges }, and the interface lists variant?: string, loading?: boolean, disabled?: boolean.
      FIX:   Update all button examples and the interface to use buttonLabel, buttonVariant (union of the 5 shadcn variants), buttonSize, and isLoading (boolean | (() => boolean)). Keep label documented as tooltip/aria-label only. Note disabled and isActive are boolean | (() => boolean).
  [DONE] Incorrect — Item Types
      ISSUE: The Item Types table lists required props per type: button → label, onClick, variant; dropdown → icon, component, label. It also implies action requires label.
      FIX:   Correct the button row to reference buttonLabel/buttonVariant/buttonSize, and clarify these are conventional (not type-enforced) requirements since only id, type, and position are mandatory.
  [DONE] Incorrect — Dynamic Toolbar Actions
      ISSUE: The useAdminToolbarActions example registers a button with { id: 'nav-save', type: 'button', label: 'Save Changes', position: 'right', order: 1, variant: 'default', disabled: !hasChanges, onClick: handleSave }.
      FIX:   Change the example to { id: 'nav-save', type: 'button', position: 'right', order: 90, buttonLabel: 'Save Changes', buttonVariant: 'default', disabled: !hasChanges, onClick: handleSave }.
  [DONE] Incorrect — Admin Overrides
      ISSUE: States hidden toolbar items are "respected by the sidebar rendering logic."
      FIX:   Change "respected by the sidebar rendering logic" to "respected by the header toolbar rendering logic (HeaderToolbar component)."

hooks
  [DONE] Outdated — Available Backend Hooks > Admin Service Hooks
      ISSUE: The Admin Service Hooks table lists only admin.settingsUpdated, admin.navigationUpdated, admin.toolbarUpdated, plus the shared user.roleUpdated and user.deleted. It omits two action hooks the admin service now fires.
      FIX:   Add two rows to the Admin Service Hooks table: admin.styleOverridesUpdated | doAction | { overrides }, and admin.dashboardOverridesUpdated | doAction | { overrides }.

index
  [DONE] Incorrect — What You Get / Tech Stack / Built-in Features
      ISSUE: Multiple places state authentication is 'Neon Auth with JWT tokens, email OTP, and role-based access' and the Tech Stack table lists Auth as 'Neon Auth (JWT + email OTP)'.
      FIX:   Replace 'email OTP' with 'email + password and Google OAuth' throughout (What You Get bullet, Tech Stack Auth row).
  [DONE] Incorrect — Getting Started — Once complete
      ISSUE: Lists routes as 'Sign-in page at /sign-in'.
      FIX:   Change 'Sign-in page at /sign-in' to '/auth/sign-in'.

logging
  [DONE] Incorrect — Logger Setup
      ISSUE: The setup example imports and calls a createAppLogger function: import { createAppLogger, createModuleLogger } from '../lib/logger.js'; and const log = createAppLogger(); described as the "Root logger (used in app.ts and middleware)".
      FIX:   Remove createAppLogger from the import and example. Show the actual API: import { logger, createModuleLogger } from '../lib/logger.js'; then // Root logger (used in app.ts and middleware) const log = logger; and // Module-scoped logger (adds module name to every entry) const log = createModuleLogger('user');
  [DONE] Incorrect — Log Output (Production tab)
      ISSUE: The production JSON example shows the module field as "name":"user" — {"level":30,...,"name":"user"}.
      FIX:   Change "name":"user" to "module":"user" in the production JSON example.
  [DONE] Incorrect — Module Loggers
      ISSUE: The comment in the Module Loggers example states: // All log entries include { name: "billing" }.
      FIX:   Change the comment to // All log entries include { module: "billing" }.
  [DONE] Incorrect — Log Output (Development tab)
      ISSUE: The development pretty-printed example renders the module as INFO (user): — pino-pretty's (name) parenthesised prefix syntax, implying a name field.
      FIX:   Either show the module as a regular key line (e.g. [14:23:01] INFO: Profile created for first user with a module: "user" line among the fields), or — if the (name) header is desired — note that it requires a name field which the template does not set. Do not present (user) as current behaviour.
  [DONE] Incorrect — Using Loggers in Features
      ISSUE: The feature example shows const log = createModuleLogger('user'); and logs log.info({ userId, role, audit: true }, 'Profile created for first user'); inside getOrCreateProfile, presented as backend/src/features/user/user.service.ts.
      FIX:   Align the example with the real code: const log = createModuleLogger('user-service'); and log.info({ email }, 'First user created as admin');. If the doc wants to illustrate audit tagging, point to a call that actually uses it (e.g. line 185 log.info({ audit: true, userId, previousRole, newRole }, 'User role updated')).
  [DONE] Incorrect — Using Loggers in Features
      ISSUE: Prose: "The { audit: true } tag marks security-sensitive operations" is attached to the 'Profile created for first user' example, implying first-user creation is audit-tagged.
      FIX:   Keep the audit-tag explanation but attach it to a log call that is actually tagged, or move it to the dedicated Audit Logging section rather than the first-user example.

reference/base-registry-api
  [DONE] Incorrect — Class Signature
      ISSUE: The constructor parameter table states the defaultOrder parameter default is 10 ("| defaultOrder | number | 10 | Default order for items that don't specify one |").
      FIX:   Change the default cell from 10 to 50: "| defaultOrder | number | 50 | Default order for items that don't specify one |".
  [DONE] Incorrect — Order Convention
      ISSUE: The Order Convention table uses a 1-9 (Core/system), 10 (Default order), 11-49 (Early feature), 50-99 (Standard feature), 100+ (Third-party/plugin) scale, and the surrounding prose implies default order is 10.
      FIX:   Replace the table with the code's convention — 0-25 Core, 26-50 Feature, 51-75 Secondary, 76-100 Utility — and state the default is 50, the cap is 100. This also aligns with the (correct) table in starter-template/base-registry.mdx.

reference/cli-commands
  [DONE] Incorrect — Type Checking
      ISSUE: Documents npm run type-check with the description "Run TypeScript type checking across both projects" as a runnable command.
      FIX:   Remove the entire "Type Checking" section, or replace the row with the commands that actually type-check: frontend uses cd frontend && npm run build (tsc -b && vite build) and backend uses cd backend && npm run build (tsc). If a real type-check alias is desired, add it to the scripts first.
  [DONE] Incorrect — Database (Prisma) — Aside tip
      ISSUE: The tip states "The Prisma CLI config is in backend/prisma/cli.config.ts. It configures the adapter (neon or pg) and sets the database URL."
      FIX:   Change the path to backend/prisma.config.ts and correct the description: "It sets the Prisma schema path, migrations path, and the database URL (DIRECT_DATABASE_URL for migrations). The runtime adapter (neon or pg) is configured separately in backend/src/lib/prisma.ts."

reference/registries-api
  [DONE] Incorrect — dashboardRegistry → DashboardWidget interface
      ISSUE: The DashboardWidget interface comment states order?: number; // Default: 10.
      FIX:   Change the interface comment to order?: number; // Default: 50.
  [DONE] Incorrect — settingsPagesRegistry → SettingsPage interface
      ISSUE: The SettingsPage interface comment states order?: number; // Default: 100.
      FIX:   Change the interface comment to order?: number; // Default: 50.
  [DONE] Incorrect — filterRegistry → register signature comment and examples
      ISSUE: The filterRegistry section uses colon-delimited hook names: the register signature comment reads hook: string, // Hook name (e.g., 'user:displayName') and the example registers/applies 'user:displayName'.
      FIX:   Change 'user:displayName' to 'user.displayName' in the register comment and both examples so the reference matches the dot-notation convention.

reference/types
  [DONE] Outdated — Environment Variables Schema
      ISSUE: The documented envSchema lists only 11 variables (DATABASE_URL, DIRECT_DATABASE_URL, DATABASE_ADAPTER, DEBUG_PRISMA, DB_QUERY_TIMEOUT, NEON_AUTH_URL, PORT, NODE_ENV, FRONTEND_URL, SHUTDOWN_TIMEOUT) and omits roughly a dozen variables that now exist in the schema.
      FIX:   Update the documented envSchema block to include the request-timeout/proxy vars (REQUEST_TIMEOUT, TRUST_PROXY_HOPS), the rate-limit vars (RATE_LIMIT_WINDOW_MS, RATE_LIMIT_MAX), the full SECURITY_* tier/knob set, and LOG_LEVEL — matching backend/src/config/env.ts.
  [DONE] Incorrect — Environment Variables Schema
      ISSUE: The doc shows DEBUG_PRISMA: z.string().default('false'), implying the parsed value is a string.
      FIX:   Document the transform: DEBUG_PRISMA: z.string().default('false').transform((val) => val === 'true'), // -> boolean.
  [DONE] Incorrect — Error Types → Error Subclasses (table)
      ISSUE: The Default Message column lists UnauthorizedError → Unauthorised (British spelling with 's').
      FIX:   Change the table cell to Unauthorized (with a z) to match the actual default message.
  [DONE] Incorrect — Error Types → Error Subclasses (table)
      ISSUE: The Default Message column lists ServiceUnavailableError → Service unavailable.
      FIX:   Change the table cell to Service temporarily unavailable to match the actual default message.

security-feature
  [DONE] Outdated — Security tier (securityProfile) — tier table footnote ²
      ISSUE: Footnote ² on the tier table (line 73) states: "Only the env key adapter ships working today; the KMS adapter is an interface wired per-deployment (the crypto phase resolves it and falls back to env with a warning at non-strict tiers)." This implies no working KMS adapter ships and that you must hand-wire one per deployment.
      FIX:   Update footnote ² to say the AWS KMS adapter now ships and auto-wires from env (npm run security:setup-kms writes the vars; the feature registers createAwsKmsKeyProvider with no app.ts edit). Keep the accurate part: when kms is selected but no adapter is wired (env incomplete), resolution falls back to env with a warning at non-strict tiers and throws at strict. e.g. "The AWS KMS adapter ships and auto-wires from SECURITY_KMS_* env; if kms is selected but unwired, resolution warns and falls back to env at non-strict tiers (throws at strict). Override with SECURITY_KEY_PROVIDER=env."
  [DONE] Outdated — Intro version Aside (line 17) + whole page
      ISSUE: The version-tracker Aside lists shipped phases up to "the Cloudflare edge + startup posture guardrail (v1.36.0)" and the page never mentions the admin Security page or its env knob. The page presents itself as the full picture of the foundation security feature.
      FIX:   Extend the version Aside to include "the admin Security page (posture dashboard + env-knob editor, v1.37.0)" and add a short subsection (or a cross-link) covering the admin Security page and the SECURITY_ADMIN_ALLOW_ENV_WRITE knob (off by default; required to write backend/.env in production).

shell-layout
  [DONE] Incorrect — Root providers (main.tsx)
      ISSUE: Doc states main.tsx wraps the app in two providers: (1) NeonAuthUIProvider — auth context and built-in auth UI, and (2) TooltipProvider.
      FIX:   Replace the numbered list with the real root providers: (1) QueryClientProvider — outermost, shared TanStack Query cache; (2) TooltipProvider — root Radix tooltip context. Remove the NeonAuthUIProvider bullet entirely and note that auth uses authClient/useSession with no provider wrapper (the Neon Auth UI provider was removed in v1.23.0).
  [DONE] Incorrect — Overview (DashboardPage.tsx example)
      ISSUE: The DashboardPage.tsx code example shows the provider tree as AppSettingsProvider > UserProfileProvider > SidebarProvider > AppSidebar/SidebarInset, omitting any breadcrumb or full-bleed providers.
      FIX:   Add <BreadcrumbProvider> and <FullBleedProvider> to the Overview code example between UserProfileProvider and SidebarProvider so it matches the actual DashboardPage composition.
  [DONE] Incorrect — Built-in Toolbar Items
      ISSUE: The built-in theme-toggle registration example shows order: 100 and includes label: 'Toggle theme'.
      FIX:   Change order: 100 to order: 95 and remove the label: 'Toggle theme' line from the built-in toolbar example.
  [DONE] Incorrect — Breadcrumbs → Auto-Clear on Navigation
      ISSUE: Doc states: 'Dynamic breadcrumbs automatically clear when the route changes, so pages don't need to manually clean up. The BreadcrumbProvider listens for location.pathname changes and resets to null.'
      FIX:   Replace this subsection. State that the provider does NOT auto-clear on navigation; instead the component that sets dynamic breadcrumbs must clear them via its own useEffect cleanup (return () => setBreadcrumbs(null)). Explain the ordering reason (parent effect fires after child effects, so provider-level clearing would clobber a child's set call).

updating
  [DONE] Incorrect — What Gets Updated vs Preserved → Updated (template infrastructure)
      ISSUE: The 'Updated (template infrastructure)' table lists CLAUDE.md | 'AI assistant instructions' (line 115), implying CLAUDE.md is force-synced from the template on update. The same page's user-owned allow-list (line 143) lists README.md, CLAUDE.md as preserved — a self-contradiction.
      FIX:   Remove the CLAUDE.md row from the 'Updated (template infrastructure)' table (lines 114-115). CLAUDE.md is user-owned and preserved — it already appears correctly in the 'User-owned allow-list' table at line 143. If a note is wanted in the Updated section, it should say CLAUDE.md is NOT auto-updated.
  [DONE] Outdated — After Updating (Aside: 'Most of this is automated since v1.22.1')
      ISSUE: The Aside says the script runs npx prisma migrate dev --name template_update_v<version> 'when new Prisma models were merged in' (line 207), implying migration only fires when a new model is added.
      FIX:   Update the Aside to state the auto-migration runs whenever the template changed the Prisma schema at all (new models OR new fields on existing models), not only 'when new Prisma models were merged in'.
  [DONE] Outdated — After Updating
      ISSUE: The 'After Updating' section documents only three automated post-merge actions (install:all → prisma generate, and prisma migrate dev). It omits the foundation-script and env reconciliation steps that the update scripts now run after every merge.
      FIX:   Add a subsection to 'After Updating' (or a new 'Post-merge reconciliation' block) explaining that the update reconciles foundation npm scripts into package.json (committed automatically) and appends any missing foundation env keys into frontend/.env + backend/.env (uncommitted, for you to review/fill). This is the documented behaviour that explains why npm run deps:update stops reporting 'Missing script' after an update.

user-feature
  [DONE] Incorrect — User Profile → Profile Data Structure
      ISSUE: The documented UserProfile interface includes updatedAt: string; as a field returned to the frontend.
      FIX:   Remove the updatedAt: string; line from the documented frontend UserProfile interface, or add updatedAt to the frontend type if it is meant to be available. As written, code has id, email, role, name, avatarUrl, bio, preferences, createdAt only.
  [DONE] Incorrect — User Preferences → UserPreferences interface
      ISSUE: The documented UserPreferences interface lists emailNotifications?: boolean;, marketingEmails?: boolean;, and productUpdates?: boolean; as the notification fields, and dashboardWidgets?: WidgetVisibility[].
      FIX:   Replace the notification fields with emailSecurityAlerts?, emailProductUpdates?, emailWeeklyDigest?, emailMarketing? (all boolean), and change dashboardWidgets?: WidgetVisibility[] to dashboardWidgets?: DashboardWidgetPreference[]. The full code interface also includes an index signature [key: string]: unknown;.
  [DONE] Incorrect — User Preferences → Updating Preferences
      ISSUE: The example calls updatePreferences({ theme: 'dark', emailNotifications: false }).
      FIX:   Change the example to use a real field, e.g. updatePreferences({ theme: 'dark', emailMarketing: false }).
  [DONE] Incorrect — GDPR Compliance → Data Export / Account Deletion
      ISSUE: Both GDPR examples use import { userService } from '@/features/user';.
      FIX:   Change the import to import { userService } from '@/features/user/services/user.service'; (matching the actual module path), or add export { userService } from './services/user.service'; to the feature barrel and keep the docs as-is.

versioning
  [DONE] Incorrect — Currently deprecated exports
      ISSUE: The table lists only TabBar, and the line immediately below states "Nothing else is currently deprecated. If an upcoming release adds entries to this table, they'll land here before the release ships."
      FIX:   Either add rows for these deprecated exports to the table or replace the "Nothing else is currently deprecated" sentence with the real state. The page's own policy (step 4 / "Docs trail") requires every @deprecated public export to appear in this table, so the table is currently in violation of its own rule.
  [DONE] Incorrect — Currently deprecated exports (TabBar row + 'Why TabBar removal was deferred' aside)
      ISSUE: The page states the TabBar removal target is "Deferred — v1.26.0 at the earliest" and dedicates an aside to explaining the deferral past v1.25.0.
      FIX:   Reconcile the two sources of truth. If the removal is genuinely deferred to v1.26.0, update the JSDoc/header/warning string in TabBar.tsx and index.ts to say v1.26.0. The runtime deprecationWarning(...) message at TabBar.tsx:96 ships "scheduled for removal in v1.25.0" to every downstream dev console, contradicting the docs.
```

---

## Under Consideration

Ideas being evaluated for feasibility and demand.

### Audit Logging

Track important actions:

- User activity logging
- Admin action audit trail
- Configurable log levels per feature
- Integration with external logging services

### Real-time Updates

WebSocket infrastructure for live features:

- Connection management
- Channel subscriptions
- Presence indicators
- Event broadcasting hooks

### CLI Scaffolding

Code generation for common patterns:

- `npm run generate:feature` - Create feature folder structure
- `npm run generate:registry` - Add new registry
- `npm run generate:page` - Add settings/admin page

---

## Completed

Features that have been implemented and released.

| Feature | Version | Description |
|---------|---------|-------------|
| Documentation accuracy audit (135 fixes) | docs-only | 2026-06-25: re-audited all Starter Template, Reference & Getting Started pages against v1.37.0 (25-cluster multi-agent sweep + adversarial verification). Fixed 54 Incorrect, 16 Outdated, 65 Incomplete across 31 files and corrected the stale `docs/CLAUDE.md` structure. 6 candidate findings rejected as false positives; 1 no-op. 5 code-side follow-ups fixed in v1.38.0 (TabBar removal retargeted to v2.0.0, LOG_LEVEL comment, type-check scripts, userService barrel export, unregister* runtime warnings). Supersedes v1.21.1. |
| `AppSettings.appTheme` field + `setAppTheme` mutator | v1.25.0 | Added a dedicated `Json?` field on the `AppSettings` singleton for module-owned theme configuration. Shape is opaque (starter enforces no type) — the consuming style module (e.g. useFoundry) defines and validates its own schema. Exposed on the frontend as `AppTheme` type + `setAppTheme(config)` on the `AppSettingsContext` (optimistic update + PATCH + refetch). Backend: Zod accepts `z.unknown()`, service handles `null` → `Prisma.DbNull` for clearing. Downstream apps pulling v1.25.0 need a one-time manual schema step (add `appTheme Json?` to their `AppSettings` model + `prisma migrate dev`); auto-merge roadmapped as a follow-up. TabBar shim removal deferred past v1.25.0 per the deprecation policy. |
| Structure-only styling policy (rule 7) | v1.24.7 | Codified the rule that template components under `frontend/src/` (outside `components/ui/`) use layout utilities + shadcn semantic tokens only — no typography scale, font weights, colour literals, or decorative shadows. Visual design lives in globals.css, shadcn variants, or Style Registry entries. LLM rules page rule 7 added with the full allow/deny list. CLAUDE.md critical rules + dedicated "Structure-Only Styling Policy" section. Audit caught ~158 font-size/weight occurrences and ~12 colour-literal occurrences — migration planned as a dedicated roadmap entry (see "Structure-only styling migration"). No code changes in this release — policy only, enforced on new code going forward. |
| Deprecation policy + `TabBar` shim + versioning docs | v1.24.6 | Codifies the template's "deprecate, then remove" contract for public exports. New `@/lib/deprecation` utility emits dev-only, once-per-id warnings (tree-shaken in production). `TabBar` removed cold in v1.24.4 is now restored as a deprecation shim wrapping shadcn `<Tabs variant="line">` under the old API — downstream `import { TabBar } from '@/components/settings-blocks'` keeps working with zero changes. Removal target: v1.25.0 (next minor). New `/starter-template/versioning/` docs page with the full policy, release cadence, shim pattern, and currently-deprecated table. LLM rules page rewritten — old "no backwards compatibility" rule retired in favour of deprecate-then-remove. CLAUDE.md summary + Common Mistakes entry updated. |
| Frontend TS cleanup + conflict-branch user-owned paths + unified `AppSettings` | v1.24.5 | Three bundled cleanups. 1) Extended `update-template.{ps1,sh}` conflict branch to auto-resolve user-owned paths with `git checkout --ours` (mirrors the success-branch allow-list; no more hand-resolving `globals.css`/`package.json`/etc. every release). 2) Unified the two parallel `AppSettings` interfaces (service had createdAt/updatedAt, context had styleOverrides — neither was a superset, causing TS2345 in AppSettingsPage). Service is now the canonical source; context imports and re-exports. 3) Frontend TS/lint cleanup: unused imports in AdminDashboardPage and NavigationManagementPage; LucideIcon→ReactNode cast in AdminComponents; canonical class names in AdminDataTable + 4 auth pages; `UserRole \| null` accepted in NavMain+NavSecondary `hasRoleAccess`; React 19 `useRef` initial value in PersonalInformationPage. |
| Drop custom `TabBar`; AppSettingsPage uses stock shadcn Tabs | v1.24.4 | Removed the hand-rolled `TabBar` from settings-blocks (predated shadcn's `radix-nova` style which ships `variant="line"` on `TabsList` out of the box). AppSettingsPage now uses stock `Tabs` / `TabsList variant="line"` / `TabsTrigger` / `TabsContent` from `@/components/ui/tabs`. Docs updated across admin-feature, architecture, user-feature, and module-starter building-modules to point consumers at the shadcn primitive. Consumers with `TabBar` imports will get a build error on merge — intentional, replacement snippet in the changelog. |
| `AuthBackground` slabs use blend modes (true shades/tints of primary) | v1.24.3 | Follow-up to v1.24.2 which went bold but used `bg-black/N` / `bg-white/N` overlays that desaturated the primary toward grey. v1.24.3 switches the strategy to `bg-primary` slabs with `mix-blend-multiply` (darker shade: primary × primary) and `mix-blend-screen` (lighter tint: 1 − (1 − primary)²) over a `bg-primary` base. Hue is mathematically preserved across every facet because the only colour in play is `--color-primary`. Works in every shipping browser since 2016 — much broader support than the color-mix() alternative. |
| `AuthBackground` goes bold — primary fill + tonal facets | v1.24.2 | Reworked the auth-page background to match the bold geometric hero references in the template's design brief. Previous versions (v1.23.0 at 0.03-0.07 opacity, v1.24.1 at 0.08-0.15) expressed primary as "hint of tint on grey." v1.24.2 fills the whole page with solid `bg-primary` and overlays angular clip-path slabs in tonal shades — `bg-black/25`, `bg-black/15`, `bg-white/10` — which mix with whatever primary the user has configured. Vivid primaries get rich facets; near-black primaries get restrained monochrome depth. Same `.auth-page-background*` Style Registry override hooks preserved. |
| Scrub template-level style overrides | v1.24.1 | Removed the `@theme` stub, `.lucide { stroke-width: 1 !important }`, `* { border-color }`, and `body { ... }` rules that the template had been shipping in `globals.css`. All were either Tailwind v3 patterns, template-author preferences, or rules that fought shadcn init's output anyway. `globals.css` in the template is now a single `@import 'tailwindcss'` — `shadcn init` (run by `create-app`) produces the authoritative version on each downstream app, protected from future force-syncs by the v1.23.2 allow-list. Also bumped the `AuthBackground` slab opacities from 0.03–0.07 to 0.08–0.15 so vivid primaries actually read as primary (the previous values were calibrated for zinc's near-black primary and looked gray for anything more colourful). |
| Public branding endpoint + `BrandingProvider` split | v1.24.0 | Fixes a v1.23.0 regression — auth pages crashed on sign-out because `useAppSettingsValue()` was called outside `AppSettingsProvider`. Split the public-facing subset of settings (appName, logoUrl, logoDisplayMode, styleOverrides) into a new `BrandingProvider` mounted at the app root, backed by a new public `GET /api/v1/branding` endpoint (no auth required). Auth pages now use `useBranding()` — forgiving hook that returns `DEFAULT_BRANDING` instead of throwing when the provider isn't available, with a 5-minute stale window so TanStack Query serves the admin's branding on returning visits. `AppSettingsProvider` stays inside `DashboardPage` owning nav/toolbar/styleOverrides admin surface. |
| Ship `.vscode/settings.json` + `.vscode/extensions.json` for Tailwind v4 | v1.23.4 | Every downstream app hit the same VS Code warnings after `shadcn init` — built-in CSS linter flagging `@theme`, `@custom-variant`, `@apply` as unknown at-rules. The Tailwind IntelliSense extension loads the project fine but doesn't replace the stock linter. Shipped `.vscode/settings.json` with `"files.associations": { "*.css": "tailwindcss" }` (Tailwind Labs' own recommendation) plus `"editor.quickSuggestions": { "strings": "on" }` for className completions, and `.vscode/extensions.json` recommending `bradlc.vscode-tailwindcss` + `Prisma.prisma`. Also un-ignored `.vscode/settings.json` in `.gitignore`. |
| Auto-resolve `docs`/`.claude`/`.template-archive` conflicts | v1.23.3 | Extended `update-template.{ps1,sh}` conflict handling so that modify/delete conflicts on the template-only excluded paths auto-resolve by removing them (with the submodule-registration cleanup for `docs`). Previously the post-merge strip only ran after a clean merge, so every release that bumped the docs submodule pointer left downstream apps stuck mid-merge with a `docs` conflict. Same shape as the existing `create-app.*` auto-removal in the conflict branch. |
| Force-sync allow-list adds `globals.css` | v1.23.2 | Fixes a regression introduced by v1.23.1. The post-merge force-sync was overwriting downstream apps' `frontend/src/styles/globals.css` with the template's minimal stub on every `template:update` — wiping the light + dark token palette, `@custom-variant dark`, and font imports that `shadcn init` writes during `create-app`. Added the path to the user-owned allow-list in both `update-template.ps1` and `update-template.sh`, same semantics as `components.json`. Affected apps can recover with `cd frontend && npx shadcn@latest init --yes --defaults --force`, then `npm run template:update` to pick up v1.23.2 — from then on the allow-list protects it. |
| `template:update` enforces template authority | v1.23.1 | `update-template.{sh,ps1}` now force-syncs every template-owned file to the template's current version after the git merge finishes, with a small user-owned allow-list (components.json, schema.prisma, migrations, package.json, lock files, .env, README, CLAUDE.md) preserved as-is. Fixes a class of merge bug where git's three-way resolution could leave downstream apps with stale code from withdrawn releases (discovered when v1.22.2 → v1.23.0 left the withdrawn `auth.css` import in one app's `features/auth/index.ts`). Also strips `docs/`, `.claude/`, and `.template-archive/` from the working tree if the merge pulls them in. Running `template:update` retroactively heals any app stuck in the v1.22.2 merge state. `.template-ignore` for per-app opt-outs is roadmapped as a follow-up. |
| Custom shadcn auth pages | v1.23.0 | Replaced Neon's pre-built `AuthView` / `AccountView` / `SignedIn` / `SignedOut` / `UserButton` / `RedirectToSignIn` components and the `NeonAuthUIProvider` wrapper with four custom shadcn pages (`SignInPage`, `SignUpPage`, `ForgotPasswordPage`, `ResetPasswordPage`) and an `AuthBackground` decorative component. All four call `authClient` methods directly (`signIn.email`, `signUp.email`, `signIn.social`, `requestPasswordReset`, `resetPassword`). Removed the `@neondatabase/neon-js/ui/tailwind` preset from `globals.css` — which fixed the shadcn `Tabs` layout bug that v1.22.2 failed to scope via CSS cascade layers. Forgot/reset password flows are wired but Neon's server is on Better Auth 1.4.6 until their 1.6 upgrade — those two flows need end-to-end verification before production (tracked in the "Finalise the forgot-password / reset-password flow" roadmap entry). |
| TanStack Query Data Fetching Layer | v1.22.0 | TanStack Query wired as the app-wide server-state layer. Shared `QueryClient` at the app root (outermost provider), opinionated defaults in `lib/query-client.ts` with per-default rationale, app-level + module-scoped query key convention in `lib/query-keys.ts`, `AppSettingsContext` refactored as the reference `useQuery`-in-Context pattern. New authoritative [Data Fetching](/starter-template/data-fetching/) doc page, LLM rules section, and Common Mistakes entries. Devtools mounted in dev only. |
| Documentation Accuracy Audit | v1.21.1 | Audited all 23 starter-template doc pages against v1.21.0 code. Fixed 19 findings: stale API endpoints, missing schema fields, outdated `main.tsx` code sample, lazy/static admin page patterns, style registry absent from llm-rules and architecture tree, theme.mdx reframed as low-level primitive, admin-feature Style Overrides section, configuration frontend env var detail, scripts TOC. Added `useStyleRegistry` hook for consistency with every other registry. New cross-cutting **Style System** page under the starter-template sidebar. |
| Style Registry + components.json sync | v1.21.0 | `styleRegistry` for runtime CSS (tokens + raw CSS) and build-time shadcn `components.json` contributions. Exclusivity enforced on `style` / `tailwind.baseColor` / `tailwind.cssVariables`. `AppSettings.styleOverrides` persists per-entry admin toggles. `scripts/sync-components.{ps1,sh}` + `npm run components:sync` merge module manifests and reinstall components. Pipeline also fixes a latent stale-token leak in `applyThemeVariables`. |
| Remove dead `postcss.config.js` | v1.20.4 | Tailwind v3 leftover (empty `{ plugins: {} }`). Tailwind v4 uses `@tailwindcss/vite` directly — no PostCSS needed. |
| TS 7.0 `baseUrl` Deprecation Fix | v1.20.3 | Removed `baseUrl` from `frontend/tsconfig.json`. `paths` resolves relative to tsconfig since TS 5.0, and Vite resolve.alias is unchanged — so no behaviour change, just silences the TS 6.x deprecation warning. |
| Dependency Update Scripts | v1.20.2 | `deps:check` / `deps:update` / `deps:update:latest` npm scripts + `scripts/update-deps.{ps1,sh}`. Root, frontend, and backend updated in one command — semver-safe by default, opt-in for major bumps via npm-check-updates. |
| Root TooltipProvider | v1.20.2 | Single `TooltipProvider` mounted in `main.tsx` so shadcn tooltip additions work anywhere without wrapping. Nested providers still override for custom `delayDuration`. |
| Theme & Style Hook Points | v1.20.1 | `theme.ts` utility with `applyThemeVariables()` / `resetThemeVariables()`; `theme.cssVariables` filter hook; `theme.changed` action hook. Prepares for [Style module](/modules/roadmap/#style). |
| Action Registry Hook Points | v1.19.1 | 10 action hooks wired across user and admin services; modules can hook into `user.created`, `user.deleted`, `admin.settingsUpdated`, etc. |
| Structured Logging | v1.19.0 | Pino structured logging replacing all `console.*` calls; request ID correlation; `LOG_LEVEL` env variable; module loggers |
| Unified Order Convention | v1.19.0 | 0-100 scale (Core/Feature/Secondary/Utility), default 50, applied to all registries |
| Version Script Sync | v1.19.0 | `bump-version.ps1` now keeps root and backend `package.json` in sync with `VERSION` file |
| Dynamic Breadcrumbs for Settings & Admin | v1.18.0 | Settings and admin pages auto-set breadcrumbs from registries; detail page template with `useBreadcrumbs` |
| Multi-File Prisma Schema Option | v1.17.0 | Create-app prompt for single or multi-file schema; update scripts auto-detect mode |
| Personal Info Fields Registry Fix | v1.16.0 | Module-registered preference fields now save correctly; debounced text fields; `onChange` returns `Promise<void>` |
| Unified Settings & Page Block Libraries | v1.15.7 | Shared `settings-blocks` and `page-blocks` component libraries for settings and admin pages |
| Reusable Admin Components | v1.15.0 | Admin component library, data tables, toolbar actions |
| Update Script Auto-Install | v1.15.1 | Update scripts automatically install dependencies after merge |
| Dynamic Breadcrumbs | v1.14.8 | Dynamic breadcrumb context for page-level breadcrumb overrides |
| Unified Navigation Overrides | v1.13.0 | Merged section overrides into navigation overrides |
| Registry-Based Sidebar Sections | v1.12.0 | Sections defined in navigation registry with admin overrides |
| Sidebar Sections Admin | v1.11.0 | Admin page for organising nav items into sections |
| Toolbar Overrides Admin | v1.10.0 | Admin page for hiding toolbar items |
| Drag-and-Drop Navigation | v1.9.0 | Admin panel navigation reordering with dnd-kit |
| Filter & Action Registries | v1.8.0 | Hook points for data transformation and side effects |
| Header Toolbar Registry | v1.7.0 | Extensible header actions |
| Admin Feature | v1.6.0 | Branding, navigation overrides |
| Settings Pages Registry | v1.5.0 | Modular settings sections |
| Dashboard Widgets | v1.4.0 | Widget grid system |
| User Avatars | v1.3.0 | Priority-based avatar display |
| Service Registry | v1.2.0 | Cross-feature communication |
| Route Versioning | v1.1.0 | API versioning support |
| Core Template | v1.0.0 | Auth, navigation, base architecture |

---

## Related Roadmaps

- [Module Roadmap](/modules/roadmap/) — Complete inventory of all modules: active, planned, and under consideration
- [Module Starter Roadmap](/module-starter/roadmap/) — Planned features for the scaffolding tool
- [Structure Module Roadmap](/modules/structure/roadmap/) — Feature-level roadmap for the Structure module

---

## Contributing Ideas

Have a feature request or improvement idea?

1. **Check existing items** - Review this roadmap and [GitHub issues](https://github.com/ui-git-kim/unique-react-fullstack-starter/issues)
2. **Open an issue** - Describe the feature and use cases
3. **Discuss** - Engage with feedback to refine the proposal
4. **Contribute** - Pull requests welcome for approved features

<Aside type="tip">
When proposing features, consider how they fit the modular philosophy. Features that can be implemented as optional modules are preferred over core additions.
</Aside>
