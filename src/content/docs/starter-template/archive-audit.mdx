---
title: Archive Audit
description: Per-session archive-readiness log for the starter template — unfinished work, loose ends, decisions, propagation, and ideas captured before a session is archived
lastUpdated: 2026-07-30
sidebar:
  order: 100
---

import { Aside } from '@astrojs/starlight/components';

A running log of **archive-readiness audits** for the **Starter Template**. Before a working session on the starter is archived, it is audited for anything that would be lost — unfinished work, loose ends, decisions, ideas, and downstream propagation still owed — and the findings are recorded here so a fresh session can pick them straight up.

<Aside type="note" title="Why this page exists">
Session transcripts get archived and are then awkward to search. This page is the durable, reviewable home for "what was in that session" — decisions and their reasoning, ideas raised in passing, work left mid-flight, and anything the code and the rest of these docs don't already capture. Entries are ordered by **session date, newest first**. Nothing here is deleted; when an item is actually done, it's ticked and left in place for provenance.
</Aside>

<Aside type="caution" title="This repo is a template, not a runnable app">
The starter has **no `node_modules`, no build, and no `dist`** — it is cloned by `create-app` and updated into existing apps by `template:update` (a git merge from `main`). So the audit's "did it ship" test is different from a normal repo: there is nothing to rebuild here. What matters is the **`VERSION`** file and whether the work reached **`main` on the `unique-react-fullstack-starter` remote** — apps pull from `main`, so a fix stranded on a branch is invisible downstream. "Shipped" = committed to `main` + `VERSION` bumped for any code change; "propagated" is then the app's job (`npm run template:update`, and `prisma migrate dev` when a model changed).

Two consequences worth stating once: **the remote is not called `origin` here** (`origin` is the docs submodule's remote), and **nothing in this repo is runtime-verified where it is written** — type-checks and tests only run against a cloned app (the `use-foundry-ui` / `veradai` overlay technique). An entry that says "review-validated" means exactly that.
</Aside>

---

## Open items across all sessions

<Aside type="note" title="Seeded from repo state, not from the session audits">
This register was seeded on **2026-07-29** by checking the repository at **v2.0.0** — it is *not* the output of the per-session audits below, which have yet to be written. Treat it as a starting baseline: each audit run should correct, extend or tick these as the sessions actually show.
</Aside>

1. **Deprecation removals are overdue — v2.0.0 shipped without them.** The [deprecation table](/starter-template/versioning/#currently-deprecated-exports) targets **v2.0.0** for four entries, and the major landed on 2026-07-25 with all of them still exported: `TabBar` / `TabBarProps` / `TabItem` (since v1.24.4); `navigationRegistry.unregisterMain` / `unregisterSecondary` / `unregisterUserMenu` (since v1.24.x); the six admin page barrel exports from `@/features/admin` (since v1.56.0 — these defeat the admin panel's `React.lazy` code-splitting, so removal is also a bundle-size win); and `useAppSettings` (since v1.63.0, target "v1.65.0+", now two majors late). Also long overdue on a "next minor" target: `withUserContext` (v1.27.x) and the `SidebarNavItem` type alias (v1.24.x). **Either do the removals in a v2.1.0 sweep, or re-target the table** — right now the policy and the code disagree, which is worse than either choice. (The current **v2.0.0** targets for `TabBar`/`unregister*`, and the table's completeness — the `SidebarNavItem` / `withUserContext` / `unregister*` rows — were set in **v1.38.0**'s doc-code sync; see the 2026-06-27 log entry.)

2. **Module-starter follow-up for v2.0.0 boot-order enforcement.** Owed in the module-starter repo before the new gating means anything downstream: `npm run contract:sync` + `contract:check` (its copy is still on contract **v1.2.0** while the starter ships **v1.3.0**), and an `init` callback in the generated `module.registration.ts` — until then **every module is enforced but reports as `ungateable`**. Its template comment ("Ranges are recorded, not yet validated") is now false and is emitted verbatim into host app source.

3. **CI database tests are opt-in and may still be off.** The workflow gates the isolation suite on `vars.RUN_DB_TESTS == 'true'` plus the `NEON_API_KEY` / `NEON_PROJECT_ID` secrets. Those are GitHub-side settings this repo cannot verify — **confirm they are set**, or the cross-user RLS isolation test has never actually run in CI. *(Audit note, 2026-07-10 Phase 5 session: separately, the **entire** backend CI job failed at type-check from v1.43.0 until the v1.45.1 fix, so **no** backend test — DB or otherwise — ran in that window. That outage is closed; the DB-opt-in question above still stands.)*

4. **Question still open with Neon.** Whether Neon's managed Better Auth server carries the fix for **GHSA-g38m-r43w-p2q7** (account takeover via OAuth auto-link, CVE-2026-53516 — fixed in better-auth ≥ 1.6.11). The starter meets the vulnerable preconditions exactly, and the vulnerable logic runs on Neon's server where it isn't observable from the client tree.

5. **Two audit env keys never reached the `create-app` scripts.** `SECURITY_AUDIT_SINK` and `SECURITY_AUDIT_RETENTION_DAYS` (v1.40.0, opt-in durable/compliance audit) are in `env.ts` and `sync-env.mjs` but **not** in `create-app.ps1` / `create-app.sh` — the embedded-copy tax, deliberately deferred as low-value (opt-in optional knobs; new apps still get the commented placeholders via `sync-env` on their first `template:update`). Still open at v2.0.0. Low severity — close by adding a commented block to both `create-app` `.env` writers to match `sync-env`.

### Standing constraints (not bugs — know them)

6. **Snyk cannot run in this environment** (the known `SNYK-0003` auth failure). Sessions have substituted a dedicated adversarial security review dimension. It is environmental — don't rabbit-hole on the token.
7. **Docs are not build-verified locally.** The `docs/` submodule has no `node_modules`, so MDX errors (an unimported component, a broken anchor) only surface on the deploy. Check the live page after pushing.
8. **`.claude/settings.local.json` is permanently dirty**, deliberately left unstaged by every session — usually a CRLF-only diff, sometimes also session-local permission-allowlist entries a run auto-accumulates (`Bash(...)`, `pwsh`/`node` command grants). Not a problem; just don't be alarmed by it.

### Doc drift spotted while setting this page up

9. **`CLAUDE.md`'s structure-only styling section is stale.** It claims "~158 font-size/weight occurrences and ~12 colour-literal occurrences cataloged for migration". The actual count in template code today is **4** — all in `UserAvatar.tsx`, all covered by the blessed "proportional text tied to a sized box" exception. The migration completed in v1.61.0; the CLAUDE.md paragraph never caught up.

10. **`IconRenderer` imports the full Lucide barrel — latent bundle risk.** `frontend/src/components/icon-picker/IconRenderer.tsx` does `import { icons as lucideIcons } from 'lucide-react'` (still true at v2.0.0; surfaced in the 2026-07-10 Phases 0–4 session's `PROJECT_DECISIONS` reconciliation). It is currently only consumed inside the dynamically-imported picker, so it sits in the lazy chunk — consistent with the roadmap's "Icon Picker now lazy-chunked". The risk is **latent**: the moment `IconRenderer` is imported into the **eager** graph to render saved-by-name icons (nav / dashboard / config from JSON or DB), it drags the entire icon set into the main bundle. Guard before that — switch it to `lucide-react/dynamic` (`DynamicIcon`) or a curated name→component map. Low priority while it stays inside the lazy picker.

---

## How to use this page

At the end of a session you want to archive, run the archive-readiness prompt below. It audits that session and **inserts an entry into the log in session-date order** using the entry template. Then it pushes the docs. Review the entry on the live site, archive the session, and start the next one by reading the open items above.

Sessions are audited **in whatever order you get to them**, not necessarily newest-first — an older session can be audited long after a newer one. The prompt accounts for that: it places the entry by date rather than blindly at the top, and it never assumes it is the last audit to run. The `✅ Reviewed through …` marker moves only when a sweep genuinely completes a contiguous run.

### The prompt

> **Archive-readiness check (Starter Template).** Audit this session end-to-end for anything that would be lost if I archive it: **(1) unfinished work** — started-not-finished, committed-not-pushed, coded-not-validated, or left WIP (check `git status` and recent commits in every repo touched, including the `docs/` submodule — and if this is an older session the repo has since moved past, expect HEAD, the `VERSION` file, and the working tree to look newer than this session's output, and its commits to sit deep in history, absent from a short `git log`/reflog window; that is normal forward progress, not loss. Confirm the work survived by identifying this session's own commits from the transcript and checking each is an ancestor of the starter's `main` — note the remote here is **`unique-react-fullstack-starter`**, not `origin` (`git merge-base --is-ancestor <sha> unique-react-fullstack-starter/main`) — reachable from that remote means preserved even though HEAD is far ahead. Real loss is only a commit that is on no branch and absent from the remote; do not infer loss from "HEAD isn't at my version" or "the working tree differs from what I edited". There is **no `node_modules`, build or `dist`** in this repo, so do not look for a rebuild: the analogue is the **`VERSION`** file — confirm any change under `backend/src/**`, `frontend/src/**`, `scripts/**` or the root shell scripts bumped it, because that is what signals an app that a `template:update` is available); **(2) loose ends** — TODOs, "later" items, deferred fixes, promised follow-ups, checks I said I'd run, and **downstream propagation still owed**: which apps need `npm run template:update`, whether that update then needs `prisma migrate dev` (any new model), whether a new env key reached all of `env.ts` + `sync-env.mjs` + **both** `create-app` scripts (the embedded-copy tax), and whether the module-starter needs a `contract:sync` — flagging that the real type-check gate is a cloned app, since this repo can't `tsc`; **(3) decisions & ideas** — any decision, trade-off, rejected approach and why, or idea raised in conversation not already written in code or docs; **(4) undocumented reality** — anything we changed or learned the docs don't reflect, specifically whether every reported downstream issue moved from **Open** to **Completed** on [`upstream-issues.mdx`](/starter-template/upstream-issues/) with the fix version, and whether the [changelog](/starter-template/changelog/), [roadmap](/starter-template/roadmap/) and the [deprecation table](/starter-template/versioning/) are current — including removals a major release was supposed to perform.
>
> Then add an entry for this session to the Archive Audit log (`docs/src/content/docs/starter-template/archive-audit.mdx`) using the entry template already on that page, and refresh the **Open items across all sessions** register at the top with anything this session adds, closes or contradicts. **Sessions are audited out of order, so this is probably not the last audit and may not be the newest session:** insert the entry in **session-date order** (newest first) rather than blindly at the top; if the log already has an entry for this session, replace it instead of adding a duplicate; leave other sessions' entries alone; and only move the `✅ Reviewed through …` marker if this run genuinely completes a contiguous stretch back to it — otherwise leave the marker where it is. Push the docs submodule **and** the submodule pointer in the starter repo (docs-only work needs no version bump). Verify before ticking anything "done". Finish by telling me plainly whether it's safe to archive.

### Entry template

```mdx
## YYYY-MM-DD — <session focus>

**Verdict:** ✅ Safe to archive  ·  ⚠️ Needs input first  ·  ℹ️ Superseded (work integrated, session now historical)
**Shipped this session:** <VERSION change(s), key commits, what went live — this repo ships the TEMPLATE; there is no dist>
**Propagation:** <which apps need `template:update`, whether a migration follows, env keys owed — or "n/a (docs-only)">

**Unfinished / WIP:** <or "none">
**Loose ends & follow-ups:** <deferred fixes, promised checks, propagation owed, TODOs — or "none">
**Decisions & ideas:** <decisions + why, rejected approaches, ideas raised — or "none">
**Recorded where:** <docs pages + memory files updated, so it's findable>
**Open items for next session:** <the pick-up list — or "none">
```

---

## Log

## 2026-07-14 — Per-module migration runner (host + scaffold), update-script hardening, module-fleet rollout · v1.51.0–v1.53.0

**Verdict:** ✅ Safe to archive · ℹ️ Superseded — a large multi-day session (**2026-07-11 → 07-14**); every commit across five repos is confirmed an **ancestor of its remote's `main`**, and later sessions built directly on top (the raw `_module_migrations` design here produced two downstream issues both since fixed; the contract moved v1.2.0 → **v1.3.0** at v2.0.0). Repo is now at **v2.0.0** — HEAD/`VERSION` far ahead, as expected. Nothing lost.
**Shipped this session:** Four starter releases, `VERSION` **1.50.2 → 1.51.0 → 1.52.0 → 1.52.1 → 1.53.0**, all commits ancestors of `unique-react-fullstack-starter/main`:
- **v1.51.0** (`b3c033c`, 07-11) — `update-template.{ps1,sh}` `-Continue`/`--continue` resume flag (finish the post-merge pipeline after a hand-resolved merge conflict; refuses mid-merge and when behind the template head), `scripts/sync-deps.mjs` (presence-only `FOUNDATION_DEPS` reconciler, third leg beside sync-package-scripts/sync-env; seeds `vitest`), and a `vite.config.js`-shadow **warning** at end-of-update + the up-to-date fast path.
- **v1.52.0** (`db2188d`, 07-14) — the **host half** of the migration runner: contract **v1.2.0** in `module-contract.ts` (`MODULE_MIGRATIONS_TABLE`/`_COLUMNS`/`_DDL`, `parseMigrationFilename`, `compareMigrations`), read-only `getModuleMigrationStatuses()` folded into `getModulesOverview()`, and a **Migrations** section on `/admin/modules`. **No new Prisma model** (the `_module_migrations` table is raw SQL, read via `$queryRawUnsafe`), **no new env key**.
- **v1.52.1** (`f50c1bf`, 07-14) — cleaned a stale `FIXME` in `DataPrivacyPage.tsx` (the test-mode toggle was already `import.meta.env.DEV`-gated).
- **v1.53.0** (`921b432`, 07-14) — `scripts/check-vite-shadow.mjs`: a `predev`/`prebuild` guard that **auto-removes** a shadowing `frontend/vite.config.{js,mjs,cjs,d.ts}` when git confirms it untracked (keeps + warns on a tracked config / non-git checkout; never exits non-zero); reconciled into apps via `FOUNDATION_SCRIPTS`.

Cross-repo (all preserved on their own remotes): **module-starter scaffold 1.17.0 → 1.18.0** (`d52deaf`, `origin`, 07-14) — the runner itself: `src/scaffold/migrations.ts` applies SQL + programmatic **TS backfills** at CLI time (`init`/`update`/new `migrate-db`), each file in its own transaction *with* its `_module_migrations` row (atomic), TS transpiled + loaded in-process via **esbuild**, raw **postgres** on `DIRECT_DATABASE_URL`, checksummed/append-only/forward-only/stop-and-report+resume; `ModuleDefinition.migrations` field; `contract:sync`'d to v1.2.0. **Module fleet onto scaffold v1.17.0 + the `module.registration.ts` hand-port:** `structure` 1.15.1 → **1.15.2** (`4332b37`), `billing` 1.2.0 → **1.2.1** (`46af4f7`; the repo has since moved to `ReactApps/Modules/billing`), `cog-ingest` 1.0.0 → **1.0.1** (`197e4fc`; retrofitted a scaffold link via `git merge --allow-unrelated-histories -s ours`). **Validation** (no `tsc` here): cloned-app overlays (billing/structure `npm ci` + `tsc` + `tsup`), the `use-foundry-ui` overlay for the host half (`InstalledModule` row confirmed on boot), and **31 assertions against real Postgres via PGlite** (in-process — Neon never touched) for the runner.
**Propagation:** apps pull v1.51.0–v1.53.0 via `template:update`. **No `prisma migrate dev` owed** from any of these (no new model). **No env-key reconciliation owed** (no new key — `env.ts`/`sync-env.mjs`/both `create-app` `.env` writers untouched). Module-starter **`contract:sync` was done this session** (v1.2.0); the later v1.2.0 → v1.3.0 drift is register item 2, a v2.0.0-era matter, **not** opened by this session. The raw `_module_migrations` table (a signed-off design choice) later required host/app follow-ups downstream — both since fixed, see below.

**Unfinished / WIP:** none. All commits pushed and ancestor-confirmed. Working tree clean apart from the standing `.claude/settings.local.json` diff and the ` M docs` submodule-pointer dirtiness (the repo moved to v2.0.0 past this session — not this session's WIP). The `use-foundry-ui` test artifacts (`.module-state.json`, `structure/index.ts`, new `module.registration.ts`) were left **uncommitted at the user's explicit request** ("leave them") — a legitimate `structure` 1.15.2 update in a linked dev app, not loss.
**Loose ends & follow-ups:** All now closed by later sessions or this audit:
- Two downstream consequences of the raw-table design, both **already Completed** on [upstream-issues](/starter-template/upstream-issues/): the `/admin/modules` read logged a scary `prisma:error 42P01` where the table doesn't exist yet (my M1 `$queryRawUnsafe`) → **fixed v1.54.0** (`to_regclass()` probe); and `prisma migrate dev` drift where the table exists in the DB but not migration history → **fixed v1.58.1** (`prisma.config.ts` external tables).
- The **veradai `vite.config.js` shadow** diagnosed this session (the `@sentry/react` dev-scan break) — **this audit added it to [upstream-issues](/starter-template/upstream-issues/) as Completed (v1.53.0)**; the earlier v1.51.0 warning + v1.53.0 auto-heal are the fix, and veradai's manual `Remove-Item` one-liner was handed to the user.
- A reusable **module-alignment prompt** was authored to bring the *other* modules (builder/pickers/style/colour-palette/document-management/ai-core/cognitive-db) onto scaffold v1.18.0 + the registration hand-port; module-repo work, tracked outside the starter (later sessions clearly advanced the fleet — structure 1.24, billing 2.2).
**Decisions & ideas:** Migration-runner design **signed off via AskUserQuestion** (recorded in `module-starter/migrations.mdx` + the `migration-runner-SIGNED-OFF` memory): **TS backfills IN v1** (the user *overrode* the SQL-only recommendation); a **raw `_module_migrations` table, not a Prisma model** (dodges the multi-site embedded-schema tax — the same call made for the security `AuditLog` table; this is precisely the choice that produced the two downstream follow-ups above, the trade-off playing out as the sign-off anticipated); **per-module trigger only** (`npx <module> update`/`migrate-db`, never `template:update`); stop-and-report + resume, `--dry-run`, forward-only; **TS executed via an in-process esbuild loader** (raw pg transaction, not the host Prisma client). Also: "build M2 first, push M1+M2+docs together" so contract and runner landed in lockstep; billing's dead-file cleanup (rehomed `MODULE_REGISTRATION` into its monolithic `index.ts`, deleted 11 `my-feature` split files the real merge dragged in); the vite-shadow **tracked-vs-untracked delete rule**; **PGlite** as the runner's real-Postgres E2E harness. The vite-shadow *automation* the user asked about became v1.53.0.
**Recorded where:** starter changelog v1.51.0/v1.52.0/v1.53.0; module-starter changelog scaffold v1.18.0; roadmap (migration runner ✅ shipped, both halves); new `module-starter/migrations.mdx` + `cli-reference` `migrate-db`; `modules-feature.mdx` migration section; `upstream-issues.mdx` (vite-shadow Completed, this audit); memories `migration-runner-SIGNED-OFF`, `modules-foundation-progress`, `template-update-gotchas`.
**Open items for next session:** none owed by this session. This audit **adds** the vite-shadow Completed entry to `upstream-issues`; it opens, closes and contradicts **nothing** in the register above (its items are all v2.0.0-era; register item 2's contract drift post-dates this session's correct v1.2.0 sync). The `✅ Reviewed through …` marker stays unset — **v1.48.0–v1.50.2** (modules foundation) sits un-audited between the 2026-07-10 entries and this one, so the run is not contiguous.

## 2026-07-11 — Module system as a foundation feature: contract, registry, dependency resolution, admin dashboard · v1.48.0–v1.50.2

**Verdict:** ✅ Safe to archive · ℹ️ Superseded — the session (**2026-07-10 overnight → 07-11**) that promoted the module system into its own foundation feature and produced the parked proposal + queued fixes the **2026-07-14 · v1.51.0–v1.53.0** entry (directly above) then implemented. Every commit across the starter, module-starter and docs is a confirmed **ancestor of its remote's `main`**; the repo is now at **v2.0.0** (HEAD/`VERSION` far ahead, as expected — the v2.0.0 boot-time WARN→FAIL enforcement sits directly on the `moduleRegistry`/`computeModuleOrder()` seams laid here). Nothing lost. This closes the gap the entry above flagged.
**Shipped this session:** Starter `VERSION` **1.47.1 → 1.50.2**, all commits ancestors of `unique-react-fullstack-starter/main`:
- **v1.48.0** (`fb49237`) — the **`modules` foundation feature** (`backend/src/features/modules/`): the deliberately self-contained **`ModuleContract`** (identity + `depends` + lifecycle, zero imports so a hash-synced copy can be the module-starter's single source of truth), the runtime **`moduleRegistry`** installed modules register with at import time, a new **`InstalledModule`** Prisma model + startup **state-sync** that diffs registry-vs-table and emits `module.installed`/`updated`/`missing` audit events. The `ModuleSecurityManifest` stays deliberately separate (per-feature `security.registration.ts`).
- **v1.49.0** (`302d7df`) — **dependency resolution**: contract **v1.1.0** carries pure `compareSemver`/`satisfiesRange` (documented range subset); `validateDependencies()` range-checks; `computeModuleOrder()` = Kahn topological order + cycle **paths** + a `blocked` partition — all **warn-only** this release. An adversarial review that *executed* counterexamples caught two real semver bugs (`>= 1.2.0` operator-whitespace mis-tokenised; bare operators failing open) — both fixed pre-push.
- **v1.50.0** (`7ffca85`) — read-only **`/admin/modules`** + `GET /api/v1/admin/modules/overview` (admin-gated, side-effect-free `getModulesOverview()`), mirroring the Security page's blocks; a review-caught missing `/api/v1` prefix that would have 404'd the page forever was fixed pre-push.
- **v1.50.1** (`fa7b509`) + **v1.50.2** (`72ea95a`) — field fixes from the first real app (`use-foundry-ui`) updating across this train: friendly remediation when the Prisma client predates `InstalledModule`, then **update-script hardening** — explicit `npx prisma generate` (**Prisma 7 dropped the v6 postinstall auto-generate**), a Neon **P1001** cold-start retry, and a Vite 8 `@sentry/react` stub alias.

Cross-repo (preserved on their remotes): **module-starter scaffold 1.15.0 → 1.16.0 → 1.17.0** (`2645be5`, `ccc68ad`, `origin`) — consume the contract via the banner-stamped synced copy + `contract:sync`/`contract:check`, the install-time `depends` presence + semver-range **warning**, and a generated `module.registration.ts`. Docs commits `0f2b2dd`/`928b55f`/`7108526`/`ada9578`/`71f3a81`. Each phase passed a multi-dimension adversarial review before push (~30 findings across phases b–e incl. the semver, route-prefix, `Prisma.DbNull`-clears-dropped-`depends`, and concurrent-boot-upsert bugs); the semver + Kahn logic was additionally **executed** against the real implementation in a scratch harness (90+ assertions) and `scaffold:update` was E2E-validated on a `structure` clone. Review-validated + scratch-executed (no `tsc`/runtime in this repo).
**Propagation:** apps pull v1.48.0–v1.50.2 via `template:update`; because v1.48.0 adds the **new `InstalledModule` model**, that update **then runs `prisma migrate dev`** (additive — no `tenantId`, no FK; auto-added by both `update-template` scripts + embedded in `create-app` multi-file mode; **all five embed sites** reconciled). **No new env key** (`env.ts` / `sync-env.mjs` / both `create-app` `.env` writers untouched). Module-starter **`contract:sync` was done this session** (to v1.1.0). `use-foundry-ui` is fully migrated (client regenerated, `InstalledModule` present on boot, its shadowing `vite.config.js` removed). **Module fleet adoption was owed at session end** — since done by the v1.51.0–v1.53.0 session above (`structure` 1.15.2, `billing` 1.2.1, `cog-ingest` 1.0.1 hand-ported `module.registration.ts` onto scaffold v1.17.0+).

**Unfinished / WIP:** none — every commit ancestor-confirmed on its remote. Working tree clean apart from the standing `.claude/settings.local.json` diff and the ` M docs` submodule-pointer dirtiness (the repo has moved to v2.0.0 well past this session — not this session's WIP).
**Loose ends & follow-ups:** all three raised-here loose ends **since shipped** (kept for provenance, and audited in the entry directly above): the queued update-script fixes — `-Continue` resume after a hand-resolved merge conflict, `scripts/sync-deps.mjs` (the missing third reconciler leg), and a `vite.config.js`-shadow warning — landed **v1.51.0** (`b3c033c`, ancestor-confirmed); the parked **per-module migration-runner** proposal was signed off and built as contract **v1.2.0** + scaffold **v1.18.0** in the same v1.51–v1.53 session; the boot-time dependency enforcement (WARN→FAIL) this release deliberately left as warn-only shipped in **v2.0.0** (contract v1.3.0). The field diagnosis behind v1.50.1/.2 is written up in the `template-update-gotchas` memory; the Prisma-7-generate / Neon-P1001 fixes were diagnosed-and-fixed **same-session** (never stranded Open), and the vite-shadow is already **Completed** on [upstream-issues](/starter-template/upstream-issues/) (added by the audit above).
**Decisions & ideas:** (1) the admin Modules page is **read-only by design** — install/update/enable from the UI executes untrusted module code, an undecided **trust boundary**; deferred, not forgotten. (2) **Evidence-based dependency semantics** originate here — a `missing` dependency is excused when its feature folder exists (old-vintage / frontend-only modules that never register), which became v2.0.0's "prove absence before failing" rule. (3) `InstalledModule` chosen as **runner-agnostic deployment state** (no `tenantId`, never-deleted rows, `missingSince` tombstone, `enabled` reserved for future runtime gating) over a per-tenant table — the same "raw/deployment table, not per-account" instinct the migration runner later applied to `_module_migrations`. (4) The per-module **migration-runner proposal** (parked here for sign-off) recommended SQL-only v1 — the user later *overrode* that to include TS backfills when it was built. (5) Two systemic update-pipeline gaps ("the update doesn't finish after a merge conflict"; "it doesn't install new deps") were the **user's own diagnosis** — both became v1.51.0. (6) The `ModuleContract` was made a **zero-import self-contained file** specifically so the module-starter could carry a byte-for-byte hash-verified copy (`contract:check`) rather than depend on the starter as a package.
**Recorded where:** starter changelog **v1.48.0 / v1.49.0 / v1.50.0 / v1.50.1 / v1.50.2**; new [Modules feature](/starter-template/modules-feature/) page + `registries.mdx` (`moduleRegistry`) + roadmap (module-system-as-foundation ✅, Admin Module Management dashboard ✅ read-only); module-starter `building-modules`/`scaffold`/`file-reference`/`lifecycle-hooks`/`starter-integration`/`versioning`/`roadmap`/`changelog`; memories `modules-foundation-progress` + `template-update-gotchas`.
**Open items for next session:** none owed by this session — all pick-up items (module-fleet adoption, migration-runner sign-off) were actioned by the **v1.51.0–v1.53.0** session above. This audit opens, closes and contradicts **nothing** in the register (its items are all v2.0.0-era; register item 2's contract-drift post-dates this session's correct v1.1.0 sync). The `✅ Reviewed through …` marker stays unset — v1.41.0–v1.47.0 (the Phase 1–5 preprod / email-jobs-notifications sessions) remain un-audited below v1.48.0, so the run is still not contiguous.

## 2026-07-10 — `create-app.sh` pgvector macOS (BSD sed) fix · v1.47.1

**Verdict:** ✅ Safe to archive · ℹ️ Superseded — the v1.47.1 fix shipped and is an ancestor of `main`; **v1.65.0** (`49e8f18`) later replaced `create-app.sh`'s whole pgvector path with the declaration-only `scripts/ensure-pgvector.mjs` primitive, so the `awk` block this session added is gone from HEAD (superseded, **not** reverted to the broken `sed`). Nothing lost. **Scope:** another slice of the 2026-07-10 marathon (v1.41.0 → v1.51.0) — a standalone session doing only this one create-script fix, distinct from the same-day **v1.44.1** (`update-template.sh` parity, entry below) and **v1.47.0** (notifications) sessions.
**Shipped this session:** `VERSION` 1.47.0 → **1.47.1** — bump `f0b3ab9`, fix `5906ced` (18:13–18:14 +1000), both confirmed **ancestors of `unique-react-fullstack-starter/main`**. Fixed a real BSD/macOS bug in `create-app.sh`'s pgvector block: the darwin branch inserted the datasource `extensions` line with `sed 's/…/…\n…/'`, but BSD/macOS `sed` does not interpret `\n` in an `s///` replacement — it emitted a literal `n`, producing a broken schema (`provider = "postgresql"n  extensions = …`). Replaced the darwin/Linux `sed -i` split with the portable `awk` insertion `update-template.sh` had already adopted in **v1.45.2** (gate the generator/datasource blocks; insert `previewFeatures` after `output =` and `extensions` after `provider = "postgresql"`) — one code path for both platforms, both scripts now sharing the logic. Linux was never affected (GNU `sed` honours `\n`); `create-app.ps1` was never affected (PowerShell regex handles the newline). Changelog v1.47.1 added (docs `189c5c3`). Review-validated (no `tsc`/runtime here), and the `awk` was exercised against the real template `schema.prisma` in a scratchpad before commit — both inserts landed in the right blocks; `bash -n` clean.
**Propagation:** **n/a — nothing owed, no app acts.** `create-app.sh` runs only at **new-app creation** from the template; `update-template.{sh,ps1}` explicitly `rm`/`git-rm` `create-app.{sh,ps1,bat}` out of every app on each `template:update` ("Removed template-only file"), so the fix reaches only fresh clones and never propagates to an existing app. No new Prisma model (no `migrate dev`), no new env key (`env.ts` / `sync-env.mjs` / the `create-app` `.env` writers untouched), no module-starter `contract:sync`.

**Unfinished / WIP:** none — clean working tree apart from the standing `.claude/settings.local.json` diff (this run left session-local permission-allowlist entries in it, as expected; unstaged).
**Loose ends & follow-ups:** none owed by this session. Swept all root `.sh` scripts for the same `s///` + `\n` pattern — the pgvector `extensions` line was the only occurrence. Snyk skipped (shell isn't a Snyk Code language; the environmental `SNYK-0003` applies regardless).
**Decisions & ideas:** chose to replace the **entire** darwin/Linux `sed -i` split with one `awk` path rather than patch just the darwin `\n` (e.g. the literal backslash-newline `sed` form) — rationale: portability across GNU/BSD in a single branch, and parity with `update-template.sh`'s v1.45.2 approach. History vindicated the parity instinct: v1.65.0 folded both scripts onto the shared `ensure-pgvector.mjs` primitive.
**Recorded where:** changelog **v1.47.1** (still present at HEAD); this entry. No memory file — fully shipped, no live follow-up, and the code path has since been superseded.
**Open items for next session:** none — this audit opens and closes **nothing** in the register above; it contradicts only the "CRLF-only" wording of standing-constraint 8 (this run's `settings.local.json` also carries real permission-allowlist additions), refined there.


## 2026-07-10 — Phase 5 foundation seams: email, jobs, notifications · v1.45.0–v1.47.0

**Verdict:** ✅ **Safe to archive** — historical. Fully shipped and preserved on `main`; the repo has since advanced to **v2.0.0**, and later sessions built directly on this work (the **admin Email page** in v1.58.0 sits on this email seam; the notifications bell/page were recomposed onto canonical shadcn primitives in v1.62.0). This is the "email-jobs-notifications" slice the 2026-07-11 entry flagged as un-audited. Nothing is owed that isn't already tracked below.
**Shipped this session:** `VERSION` 1.44.1 → **1.47.0**, three foundation seams — each its own backend feature on the auth/user/admin pattern, built in dependency order so the composite came last:
- **v1.45.0 email** (`5fc900f`) — `backend/src/features/email/`: never-throwing `sendEmail()`, a console default that reports `sent:false` (never fakes success), four optional **app-installed** adapters (Resend / SendGrid / SES / Nodemailer-SMTP) via the variable-specifier dynamic-import idiom, env auto-wire. Plus **v1.45.1** (`a005da8`) — a real find: the backend CI job had been **red since v1.43.0** (a `ToolbarOverrides` `TS2352` cast in `admin.service.ts`), so the whole Vitest suite had *never run* in CI; casting through `unknown` fixed it and turned CI green for the first time.
- **v1.46.0 jobs** (`a73f49a`) — `backend/src/features/jobs/`: import-time `registerJobHandler()` (verified that module `onActivate` runs at CLI time, not server runtime), never-throwing `enqueueJob()`, an honest in-process default, and an optional **pg-boss** durable adapter. Also introduced the foundation **`app.shutdown`** drain seam in `lib/prisma.ts` (bounded by `SHUTDOWN_TIMEOUT`, reentrancy-latched) with `http.close` + `jobs.drain` handlers.
- **v1.47.0 notifications** (`246edda`) — `backend/src/features/notifications/` + a frontend bell/page: server-side `notify()` composing the email + jobs seams, an owned **`Notification`** model, `owned()`-scoped read-state API, `notification.created` hook. This fixed a **latent critical**: `lib/owned.ts`'s generic constraint was a TypeScript *weak type* that failed `tsc` the moment `owned()` was first called with a real second argument — widened to `W extends object`.

All eight code commits carry paired `chore: bump` commits, and every one — plus the four docs pushes (`73f3558`, `43bf7dc`, `cd93141`, `a1e2c43`) — is a confirmed **ancestor of `unique-react-fullstack-starter/main`** (verified via `merge-base --is-ancestor`; HEAD is ~84 commits ahead at v2.0.0). Each seam passed a multi-dimension adversarial review workflow before push (**31 distinct findings confirmed and fixed** across the three — incl. a live-network test hazard, a pg-boss v12 named-export break, shutdown reentrancy, and cursor-pagination tie skips), and each release ran green in CI. Review-validated + CI-green; no `tsc`/runtime here.
**Propagation:** apps pull via `npm run template:update`; because v1.47.0 adds a **new `Notification` model**, that update then runs `prisma migrate dev` (additive — no FK, mirrors `Space`). **New env keys, all four sites reconciled** (re-verified in the current tree): `EMAIL_PROVIDER` / `EMAIL_FROM` / `RESEND_API_KEY` / `SENDGRID_API_KEY` / `SMTP_URL` (v1.45.0) and `JOBS_RUNNER` (v1.46.0) each reach `env.ts` + `sync-env.mjs` + **both** `create-app` scripts, and the `Notification` model reaches all **five** embed sites (`schema.prisma`, both `create-app`, both `update-template` auto-add blocks). Optional adapter SDKs stay app-installed, never template deps. Module-starter `contract:sync`: n/a.

**Unfinished / WIP:** none. Clean working tree at session end apart from the standing `.claude/settings.local.json` diff.
**Loose ends & follow-ups:** none owed by this session. (The one spun-off task — the v1.44.1 `Space` auto-add for `update-template.sh` — was a *separate* 2026-07-10 session; it is audited below and shipped.)
**Decisions & ideas:** (1) **Seam order email → jobs → notifications** (user-approved) so the composite is built on finished foundations, not stubs. (2) **`EmailMessage.attachments` included now** (billing receipts) rather than deferred; **console default returns `sent:false`** (visible, honest) over a silent no-op or a per-send warn — both user-approved. (3) **pg-boss over BullMQ** for the durable runner — Postgres-backed, so no new infrastructure, matching the provider-delegation philosophy. (4) **SES adapter deliberately rejects attachments** (raw-MIME assembly is out of seam scope) — documented, points at the SMTP adapter instead. (5) **Auth emails stay with Neon Auth** — the email seam intentionally does *not* wire verification/reset (a Neon dashboard SMTP setting); its consumers are notifications, receipts, module mail. (6) **Realtime push (WebSocket/SSE) for notifications deferred** to a future foundation seam — the badge polls; recorded on the roadmap + the notifications page.
**Recorded where:** three new feature pages — [Email](/starter-template/email-feature/), [Jobs](/starter-template/jobs-feature/), [Notifications](/starter-template/notifications-feature/); changelog **v1.45.0 / v1.45.1 / v1.46.0 / v1.47.0**; roadmap promotion-candidates marked **✅ Shipped (v1.45.0–v1.47.0)**; the `hooks.mdx` Foundation Seam Hooks table (`email.outbound` / `email.sent` / `app.shutdown` / `notification.created`); `configuration.mdx` + `features.mdx`; and the `preprod-audit-progress` memory (Phase 5 marked complete).
**Open items for next session:** none from this session. This audit adds only a clarifying note to register item 3 (the full-backend-CI outage this session closed); it opens, closes and contradicts nothing else (the register's items are all v2.0.0-era). Phases 1–4 (v1.41.0–v1.44.0) of the same pre-production audit remain un-audited below.

## 2026-07-10 — `update-template.sh` parity: pgvector opt-in + extension pre-create · v1.45.2

**Verdict:** ✅ Safe to archive · ℹ️ partially superseded — the v1.45.2 code shipped and is an ancestor of `main`; **v1.65.0** (`49e8f18`) later folded the schema-declaration step onto the shared `scripts/ensure-pgvector.mjs` primitive, so the inline `awk` this session added is gone from HEAD — but the interactive prompt, the `Embedding` model write, and the pre-migrate `CREATE EXTENSION` all **remain live**. Nothing lost. **Scope:** the middle slice of the same-day 2026-07-10 marathon — sits by commit time between **v1.44.1** (Space parity, entry below) and **v1.47.1** (`create-app.sh` sed fix, entry above), both of which reference this session.
**Shipped this session:** `VERSION` 1.45.1 → **1.45.2** — bump `4b3fde3`, port `0230315` (`update-template.sh`, +88 lines; 16:43–16:44 +1000); docs changelog `b86ec8d`. All three confirmed **ancestors of their remote mains** (`unique-react-fullstack-starter/main` for the starter, `origin/main` for docs) — preserved though HEAD is now v2.0.0. Ported the interactive **pgvector** section from `update-template.ps1` into `update-template.sh`: an opt-in *"Enable vector database? (y/N)"* prompt that adds `previewFeatures = ["postgresqlExtensions"]` + `extensions = [pgvector(map: "vector", schema: "public")]` to the generator/datasource blocks and writes a starter `Embedding` model (multi-file → `schema/embedding.prisma`; single-file → appended), plus a pre-migration `CREATE EXTENSION IF NOT EXISTS vector;` before `prisma migrate dev`. **Closed the last `.ps1`/`.sh` divergence** — the exact task the v1.44.1 session (below) had spawned. Review-validated (no `tsc`/runtime here); the `awk` inserts were exercised against the real template `schema.prisma` in a scratchpad and the `Embedding` heredoc rendered and byte-compared to the `.ps1`'s before commit; `bash -n` clean.
**Propagation:** **normal `template:update` only.** Unlike the v1.44.1 `Space` model below, the `Embedding` model here is **opt-in/interactive** (written only if the user answers *y*), **not** auto-added like `AppSettings`/`Space` — so it forces **no `prisma migrate dev`** on update. No new env key (`env.ts` / `sync-env.mjs` / both `create-app` scripts untouched — `git show 0230315 --stat` = `update-template.sh` + the docs gitlink only), no module-starter `contract:sync`.

**Unfinished / WIP:** none.
**Loose ends & follow-ups:** one at the time, **now closed** — spawned a task flagging that `create-app.sh`'s macOS/BSD `sed` branch used `\n` in an `s///` replacement (emits a literal `n` on macOS, corrupting the datasource block when vectors are enabled). Fixed same-day in **v1.47.1** (`5906ced`), audited in the entry directly above. Nothing open.
**Decisions & ideas:** (1) used **`awk`, not `sed -i`**, for the generator/datasource edit — GNU vs BSD `sed` diverge on both `-i` and `\n`-in-replacement (the same portability class the v1.44.1 `head -n -1` → `sed '$d'` fix addressed); (2) kept the embedded `Embedding` text **byte-identical to the `.ps1`'s**; (3) **later superseded (not loss):** **v1.65.0** replaced the inline `awk` declaration across `create-app` / `update-template` / `enable-vectors` with the shared `ensure-pgvector.mjs` primitive — because the append-only `awk` could emit a *duplicate* `previewFeatures`/`extensions` attribute (which Prisma rejects) when such a list already existed; and a later session added a **`VECTORS_JUST_ENABLED`** guard to the pre-create step, closing a gap this version had (it only pre-created when the *pre-prompt* schema already used pgvector, so a database enabled *during* the run wouldn't get `CREATE EXTENSION` run ahead of `migrate dev`).
**Recorded where:** changelog **v1.45.2**; `scripts.mdx` (documents both the interactive `update-template` pgvector path and its v1.65.0 supersession by `ensure-pgvector`); this entry. No memory file — the pgvector-primitive lineage lives in the `ensure-pgvector-primitive` memory. **Note for future passes:** the **v1.44.1 changelog entry still literally reads "the bash script has no pgvector section yet"** — correct-at-the-time provenance, superseded by the v1.45.2 changelog entry directly above it and not mirrored on any roadmap/register/live surface, so **don't re-flag it**.
**Open items for next session:** none — this audit opens, closes and contradicts **nothing** in the register above; its one follow-up closed in v1.47.1.


## 2026-07-10 — `update-template.sh` parity: Space model auto-add (macOS/Linux) · v1.44.1

**Verdict:** ✅ Safe to archive · ℹ️ historical — the v1.44.1 code shipped and is an ancestor of `main`; the repo has since moved to **v2.0.0**, and later *same-day* sessions built on it (the v1.47.0 notification-model auto-add is literally *"mirrors the Space auto-add above"*). Nothing lost. **Scope:** 2026-07-10 was a marathon day (**v1.41.0 → v1.51.0**, several distinct sessions); this entry covers only the **v1.44.1** slice — the email / jobs / notifications / modules-foundation / update-template-resume releases are separate sessions that get their own dated entries when audited.
**Shipped this session:** `VERSION` 1.44.0 → **1.44.1** — bump `f600a0b`, fix `d5033e3` (both 14:24–14:25 +1000), confirmed **ancestors of `unique-react-fullstack-starter/main`**. Ported the `Space` model auto-add block from `update-template.ps1` into `update-template.sh` (multi-file → writes `backend/prisma/schema/space.prisma`; single-file → inserts before the `APP SETTINGS MODEL` banner, stepping back over the `// ====` opener exactly as the `.ps1` does), so **macOS/Linux apps updating via bash finally receive the `Space` model** shipped in v1.41.0 — `grep Space update-template.sh` had returned nothing before. Fixed a real BSD/macOS bug found alongside: `extract_model_section` used GNU-only `head -n -1` → portable `sed '$d'` (the old form silently blanked the AppSettings single-file extraction on a Mac and warned instead of adding the model). Changelog v1.44.1 added (docs `9ebc633`). Review-validated (no `tsc`/runtime here), plus exercised against three fixtures — single-file-with-banner, single-file-no-anchor → append, multi-file-dir — all inserting correctly, with the `sed '$d'` output confirmed to drop the end-marker line.
**Propagation:** apps pull via `npm run template:update`; `Space` is a **new model**, so that update then runs `prisma migrate dev` — additive, no FK to `User`, safe for every app. Fixes the **bash** path only; Windows/PowerShell apps had `Space` since v1.41.0. **New env keys:** none. **Module-starter `contract:sync`:** n/a.

**Unfinished / WIP:** none — clean working tree apart from the standing `.claude/settings.local.json` CRLF-only diff.
**Loose ends & follow-ups:** one at the time, **now closed** — I spawned a task to port the interactive **pgvector/Embedding** offer (plus its pre-migrate `CREATE EXTENSION` step), the last `.ps1`/`.sh` divergence; a later same-day session shipped it in **v1.45.2** (`0230315`), and `pgvector` is present in `update-template.sh` at HEAD. Nothing open.
**Decisions & ideas:** (1) kept the `.sh` embedded `Space` text **byte-identical to the `.ps1` block** (itself already two prose-comment lines shorter than `backend/prisma/schema.prisma`) — parity over prose-fidelity; (2) spun the pgvector port into its **own task** rather than fold an interactive feature into a one-line parity fix; (3) skipped Snyk — shell isn't a Snyk Code language, and the environmental `SNYK-0003` failure applies regardless.
**Recorded where:** changelog **v1.44.1**; the fix itself in `update-template.sh` (the Space block + the `sed '$d'` line), both still at HEAD. No memory file needed (routine parity fix, fully captured by changelog + code).
**Open items for next session:** none from this v1.44.1 session — this audit opens, closes and contradicts **nothing** in the Open-items register above.


## 2026-07-10 — Pre-production audit Phases 0–4: Space model · Sentry/ops · testing+CI · audit manifest · v1.41.0–v1.44.0

**Verdict:** ℹ️ Superseded (work integrated, session now historical) — safe to archive. This is the **foundational** 2026-07-10 session; the four 2026-07-10 entries above descend from it — the **v1.44.1** `update-template.sh` Space auto-add fixes a gap *this* session left, and the **v1.45.0–v1.47.0** Phase 5 seams execute a hand-off prompt this session wrote. Repo is now at **v2.0.0**; every commit verified an ancestor of `unique-react-fullstack-starter/main`.
**Shipped this session:** `VERSION` 1.40.0 → **1.44.0** in four minors, all on `main`:
- **v1.41.0** (`164b38f`) — Phase 1: the `Space` model — a user's per-account isolation boundary ("space"), auto-provisioned 1:1 in `getOrCreateProfile`. Solo product, **no teams**; `Space.id === User.id === tenantId`; standalone (no FK).
- **v1.42.0** (`095adff`) — Phase 2: **optional/dynamic Sentry** (backend `instrument.ts` + a frontend root `ErrorBoundary`), health-probe rate-limit-skip hardening, a Neon PITR runbook. Plus `b0ee45e` (docs pointer: admin-cleanup roadmap entry + a Sentry-on-update note).
- **v1.43.0** (`b90e694`) — Phase 3: **Vitest** suite (incl. the DB-backed cross-user **RLS isolation** test) + a GitHub Actions pipeline.
- **v1.44.0** (`e372d34`) — Phase 4: the `audit` **security manifest** (closes the foundation-feature manifest gap).

Docs commits `b78bacd` / `3164f97` / `b7e243a` / `43f5663` / `a8c7dc0`. Each phase was adversarially reviewed pre-push (caught 3 real breakers: a Vite build-break in P2; `shadcn add` + Neon `username` CI failures in P3). Review-validated only — no `tsc`/runtime here.
**Propagation:** apps updating through this range need `npm run template:update`, and because Phase 1 added the **`Space` Prisma model**, that update then runs `prisma migrate dev` (additive, no FK). Sentry env keys (`SENTRY_DSN` / `VITE_SENTRY_DSN`) reached **all four** sites (`env.ts` + `sync-env.mjs` + both `create-app` scripts) — re-verified in the current tree. All folded into the update-to-v2.0.0.

**Unfinished / WIP:** none — four phases shipped + pushed; working tree clean bar the standing `.claude/settings.local.json` diff.
**Loose ends & follow-ups:**
- **Phase 1 missed `update-template.sh`.** The `Space` auto-add went into `update-template.ps1` only, so bash-updated (macOS/Linux) apps didn't receive `Space` — **fixed same day in v1.44.1** (entry above).
- **Phase 3's backend CI was born red.** The new `type-check` CI step surfaced a **pre-existing** `ToolbarOverrides` `TS2352` cast in `admin.service.ts`, so the backend job (hence the whole Vitest suite) failed from v1.43.0 until the **v1.45.1** fix — this session's tests never actually ran green in CI during its own window. Now closed; see register item #3's note.
- **Test-script propagation to existing apps** (backend/frontend `package.json`) — flagged as a `sync-package-scripts` (root-only) gap; **later fixed in v1.54.0** (per-target reconciliation).
- **CI DB isolation test is opt-in** — needs `RUN_DB_TESTS=true` + `NEON_*` secrets (**register item #3**), still unverified GitHub-side.
- **`IconRenderer` full-barrel** — surfaced in the `PROJECT_DECISIONS` reconciliation; still true at v2.0.0 → added as **register item #10** (latent).
**Decisions & ideas:** solo product = **one user ≈ one space, no teams**; "space" not "workspace"; `Space` standalone. Stripe / plan / subscription belong to the **billing module**, not `Space` (user's call). Sentry = **optional + dynamically imported** (the KMS pattern), off unless a DSN is set. CI DB tests use a **throwaway Neon branch** (user's pick over a Postgres service container). Manifests **stay separate** — `ModuleSecurityManifest` (runtime, per-feature) vs `ModuleDefinition` (identity/lifecycle) — with `ModuleDefinition` to become a `modules` foundation feature the module-starter consumes (subsequently built as v1.48.0–v1.50.2 → v2.0.0).
**Recorded where:** roadmap (P1–P4 marked shipped; new "Module system as a foundation feature" + "Admin console cleanup" entries), changelog v1.41.0–v1.44.0, four new docs pages (`getting-started/product-model`, `getting-started/error-tracking`, `getting-started/backup-restore`, `starter-template/testing`), memory `product-model-solo-space` + `preprod-audit-progress`.
**Open items for next session:** confirm the CI Neon secrets (register #3). Phase 5 + the modules-feature hand-offs are done (audited above); their prompts remain in `preprod-audit-progress` for provenance. This audit **adds** register item #10 and fills the v1.41–v1.44 slot; it does **not** move the `✅ Reviewed through …` marker — **v1.32.0–v1.37.0** (the 2026-06-22 deletion/KMS work + the transcript-less v1.36/v1.37) remains un-audited between the 2026-06-27 entry below and the pre-v1.38 range, so the run still isn't contiguous.

## 2026-06-27 — Docs accuracy audit + Observability (System page: P1 console, P2 durable, P3 compliance) · v1.38.0–v1.40.0

**Verdict:** ✅ Safe to archive · ℹ️ superseded — every commit shipped and is an ancestor of `main`; the repo has since moved to **v2.0.0** and a later "admin console cleanup" (`e91b8e0`) relocated the audit sink from `lib/` into `features/admin/`. Nothing was lost. (Session ran 2026-06-24 → 06-27.)
**Shipped this session:** `VERSION` 1.37.0 → **1.40.0** across three minors, all on `unique-react-fullstack-starter/main`:

- **v1.38.0** (`ac3c1ef`) — remediated the documentation accuracy audit (135 confirmed findings fixed across 31 doc pages via a 25-cluster multi-agent sweep + adversarial verification) plus five code-side follow-ups: `TabBar` deprecation retargeted v1.25.0 → **v2.0.0**, `env.ts` `LOG_LEVEL` comment corrected, `type-check` npm scripts added (root/frontend/backend), `userService` re-exported from the `@/features/user` barrel, and runtime `deprecationWarning()` added to `navigationRegistry.unregister{Main,Secondary,UserMenu}`.
- **v1.39.0** (`a18cf20`) — admin **System** page at `/admin/system` (DB health + services/posture + an in-memory errors/activity console) and the audit/error sink seam (`AuditSink` + `MemoryAuditSink`, an error-middleware tee, activity listeners on the nine existing hooks).
- **v1.40.0** (`7bb39f7`) — **P2** durable `SqlAuditSink` (self-provisioned `"AuditLog"` table + retention prune) and **P3** `ComplianceAuditSink` (append-only, SHA-256 hash-chained over a stored canonical payload, advisory-locked, never pruned) + verify/export endpoints and a History tab.
- Also created the starter-template **Upstream Issues** page (seeded empty) and recorded the "observability into core" design in the roadmap — docs-only commits `e792a4c`, `d9ec8da`.

**Propagation:** apps pull all three minors through the ordinary `template:update` (now subsumed by the v2.0.0 update). **No `prisma migrate dev`** — P2/P3 deliberately use a runtime-provisioned table, not a Prisma model, so there is no schema/migration/`create-app` propagation for the audit store. **Env-key gap (still open at v2.0.0):** `SECURITY_AUDIT_SINK` + `SECURITY_AUDIT_RETENTION_DAYS` reached `env.ts` + `sync-env.mjs` but **not** the two `create-app` scripts (open item 5).

**Unfinished / WIP:** none — everything committed and pushed; working tree clean apart from the permanently-dirty `.claude/settings.local.json`.

**Loose ends & follow-ups:**
- The env-key → `create-app` gap above — deliberately deferred (opt-in optional knobs, and editing the PowerShell/bash `.env` blocks blind, with no compiler here, was judged riskier than the benefit; new apps still get the commented placeholders via `sync-env` on their first update).
- **Not compiler-validated.** No `node_modules` in the template; the v1.38–1.40 TypeScript was verified by a 5-lens adversarial review standing in for `tsc`, not by a build. The real gate is a cloned app (`use-foundry-ui` / `veradai`).
- **Snyk Code** was org-disabled (a 403 "Snyk Code not enabled", distinct from the environmental `SNYK-0003`) — a manual adversarial security review substituted.
- The P2/P3 raw SQL (DDL, advisory-lock chain, prune) was written but never executed against a live DB; the design is best-effort (degrades to memory on any DB error), so an untested SQL path fails safe rather than breaking the app.
- `GET /api/v1/admin/system/audit/export` is API-only (no UI button) — intentional, for scripted archival.

**Decisions & ideas:**
- **Audit + error logging live in the starter CORE, not a separate module** — reverses the roadmap's earlier "dedicated `@unique-icon/audit-errors` module" plan. Rationale: every seam (the nine `user.*`/`admin.*` action hooks, the single error-middleware choke point, `securityRegistry.getAuditEvents()`, the `SECURITY_AUDIT_MODE` knob) already lives in core; compliance-grade depth stays an opt-in sink. Recorded in the roadmap "Observability (core)" entry.
- **P2 pivot — a self-provisioned `"AuditLog"` table via raw SQL, not a Prisma model.** A new Prisma model is a 5-file hand-sync tax (`schema.prisma` + `create-app` ×2 + `update-template` ×2), high-risk to author blind with no compiler. Raw SQL + `CREATE TABLE IF NOT EXISTS` compiles in every app regardless of schema, needs no migration/propagation, and degrades to memory on error. Recorded in roadmap P2 + `system.mdx` + changelog.
- **Rate limiter left unchanged** — rejected a dev-mode bypass; the 429 storm was useful signal (it surfaced a downstream fetch loop), so the fix is to make 429s *visible* via the console, not silence them.
- The dashboard 429 storm was traced to the **structure module's** new public API (its v1.15.0) — a module issue handed to a separate chat, explicitly **not** logged on the starter Upstream Issues page (it is not a starter-repo issue).

**Recorded where:** docs — new `system.mdx` + `upstream-issues.mdx`; `configuration.mdx` (audit env knobs); `changelog.mdx` (v1.38/39/40); `roadmap.mdx` (Observability-core entry, the Known Documentation Issues backlog + the five code-sync follow-ups, P1/P2/P3 shipped markers); `versioning.mdx` (deprecation table completed + `TabBar` target); plus the 31 audit-fixed pages. Code — the System page + audit sink (since relocated to `features/admin/`). **No memory files were written** this session.

**Open items for next session:** the env-key `create-app` gap (open item 5); and note that this session set the **v2.0.0** removal target for `TabBar`/`unregister*`, so those removals are still owed (open item 1).


## 2026-06-22 — Stale `.env.example` cleanup + Manual-Setup env docs · v1.36.1

**Verdict:** ✅ Safe to archive — historical. Fully shipped and preserved on `main`; the repo has since moved through **v2.0.0**, and nothing from this session is unfinished or stranded.
**Shipped this session:** `VERSION` 1.36.0 → **1.36.1** (patch). Starter commits `becbbcf` (delete both `backend/.env.example` + `frontend/.env.example`, rewrite the README **Manual Setup** section), `f71bfeb` (version bump), `38584f4` (docs-pointer) — **all three ancestors of `unique-react-fullstack-starter/main`**, so preserved even though HEAD is far ahead. Docs commit `d9b1c5b` (changelog `v1.36.1`) is an ancestor of `origin/main`. The changelog `## v1.36.1` entry and the rewritten README are both still present at current HEAD.
**Propagation:** existing apps pick up the two `.env.example` deletions + README rewrite on their next `npm run template:update`. **No migration** (no Prisma model), **no new env key** (existing keys only *documented* — `env.ts` / `sync-env.mjs` / **both** `create-app` scripts untouched, so no embedded-copy tax), **no module-starter `contract:sync`**. The repo has since shipped through v2.0.0, so any app updated after 2026-06-22 already carries it. Review-validated only (Markdown + a file deletion — nothing to `tsc`).

**Unfinished / WIP:** none. Clean working tree apart from the standing `.claude/settings.local.json` CRLF-only diff (open item #7), left unstaged.
**Loose ends & follow-ups:** none owed. During the session the three starter commits were intentionally left unpushed (the docs workflow pushes only the submodule); they have **since reached `main`** — verified as ancestors — so that is closed.
**Decisions & ideas:** (1) **Deleted the orphaned files + repointed the README** rather than regenerate/sync them — the two `.env.example` files were read by no scaffolding script and had drifted to ~v1.19.0, missing the whole v1.27.0–v1.36.0 security env surface; deletion removes a *fourth* place that would otherwise need hand-syncing. (2) README's Manual Setup now documents only the **stable core connection keys inline** and delegates the **churning security surface** to `node scripts/sync-env.mjs` — deliberately, so it can't re-stale the same way; `create-app.{ps1,sh}` + `sync-env.mjs` are named as the single source of truth. (3) Deliberately **did not** add `TRUST_PROXY_HOPS` / `REQUEST_TIMEOUT` to the README env blocks — they are `SECURITY_PROFILE`-derived defaults that neither generator emits, so the README mirrors the canonical generators rather than invent keys they don't write (rationale previously only in the session summary — captured here). (4) Mid-session slip, already fixed + pushed: the changelog header was first written `v1.37.0`, then corrected to `v1.36.1` to match the patch bump (docs commit `d9b1c5b`).
**Recorded where:** [changelog](/starter-template/changelog/) `## v1.36.1`; the README **Manual Setup** section; this entry. No memory file needed — fully captured by the changelog + repo.
**Open items for next session:** none from this session. The register above is unchanged by this work — it neither opens, closes, nor contradicts any item.

## 2026-06-21 — Security floor: audit + v1.27.0 → v1.31.0 (provider-delegation reorientation)

**Verdict:** ℹ️ Superseded (work integrated; session now historical) — and ✅ safe to archive. The repo is far ahead at **v2.0.0** (`56c2993`); every commit below is a verified ancestor of the remote.
**Shipped this session:** v1.27.0 (security hardening + isolation-backstop seed + dashboard overrides) · v1.28.0 (security feature shell + `securityRegistry` + `securityProfile` tier config) · v1.29.0 (auth hardening — locked-down API CSP, *no* DIY sessions) · v1.30.0 (isolation floor — Neon RLS + the `tenantId` convention; `owned()`→`tenantId`, `withTenantContext`, `withUserContext` `@deprecated`) · v1.31.0 (field encryption — app-layer AES-256-GCM + `hashSecret`/`verifySecret`, env key provider + KMS interface). Parent commits `9ad11c1`, `20ffff6`, `5b82149`, `d9e831a`, `1772eb4` (+ docs-pointer commits `fac1466`/`ffd4585`/`f856698`/`3fdd002`) — **all ancestors of `unique-react-fullstack-starter/main`**. Docs: 7 commits, tip `0aa3055`, all ancestors of `origin/main`. `VERSION` bumped for every code phase; docs-only commits correctly did not. Began with a 13-agent verification of a full security/isolation audit whose Critical/High findings became the v1.27.0 fixes.
**Propagation:** cumulative — apps get all of this via `npm run template:update` (the repo is now v2.0.0, so an update pulls everything since). **No Prisma model added** this session (a WIP `Session` table was reverted) → **no `prisma migrate dev` owed**. New env keys (`SECURITY_*`, `SECURITY_ENCRYPTION_KEY`, `VITE_SECURITY_PROFILE`) reached `env.ts` + **both** `create-app` scripts; `sync-env.mjs` didn't exist yet (added v1.33.0) — **verified it now carries them**, so existing apps get them appended on update.

**Unfinished / WIP:** none. A custom cookie/session/CSRF layer was started then **deliberately reverted** as DIY-security (nothing left in tree; the reversion is the intended state).
**Loose ends & follow-ups:** none owed by this session that later work didn't absorb. Everything is **review-validated only** (no `node_modules`) — the v1.31.0 crypto passed a focused adversarial review but, like all of this session, was never runtime-tested here (standing constraint). Minor drift: the [deprecation table](/starter-template/versioning/#currently-deprecated-exports) labels `withUserContext` "v1.27.x" — it was *introduced* v1.27.0 but only *deprecated* v1.30.0 (when `withTenantContext` landed).
**Decisions & ideas:** the pivotal one — **reoriented the whole effort from a bespoke security framework to provider-delegation** ("don't roll your own security"): delegate to Neon Auth / Neon RLS / managed KMS / Cloudflare + standard libs; the starter is thin wiring + the `securityRegistry` declaration layer; bespoke is a short named allow-list only. Consequences locked here: **field encryption is app-layer AES-256-GCM, not `pgcrypto`** (pgcrypto would send the key to Postgres, defeating "the DB can't read it"); **`tenantId` is a forward-compat column** beside `userId` (solo: `tenantId === userId`, so going multi-user later is a data change, not a schema rewrite); isolation is **Neon RLS** (FORCE RLS + `app.tenant_id`) with `owned()` as defence-in-depth; **database-per-tenant** (Neon project-per-tenant; branch-per-tenant judged off-label) and **zero-knowledge E2EE** ("operator can't read" — no provider offers it, and Neon Data Masking doesn't hide from the project owner) were **deferred to the roadmap**; the work landed under a **v2.0.0 umbrella** (since cut). The deep Veradai/Neon isolation exploration (control-plane cost; the "operator can't see" DB-access-vs-zero-knowledge fork) is captured at outcome level in the roadmap + security-feature page; the full reasoning is transcript-only.
**Recorded where:** docs — `security-feature.mdx` (delegation map + field encryption + isolation), `auth-feature.mdx` (hardening / no-DIY note), `database.mdx` (Neon RLS floor), `roadmap.mdx` (Security floor v2.0.0 phase tracker + deferred items), `changelog.mdx` (v1.27.0–v1.31.0), `versioning.mdx` (`withUserContext`). Memory — `security-provider-delegation` (philosophy), `security-floor-progress` (resume point), `dependency-propagation`, `embedded-schema-sync`. **Note:** the authoritative `SECURITY_DESIGN.md` lives in the user's **Downloads — not the repo, and not maintained**; the docs site is the source of truth.
**Open items for next session:** none unique to this session — its trajectory continued (deletion cascade v1.32.0, AWS KMS v1.33.0, …) and is folded into v2.0.0. See the register above for live open items.

<Aside type="tip" title="Reviewed through — not started">
The log now holds entries spanning **2026-06-21 → 2026-07-14** (with several same-day **2026-07-10** slices) — not yet a contiguous reviewed run: the **v1.41.0–v1.44.0** Phase 1–4 pre-production sessions between v1.40.0 (2026-06-27) and the 2026-07-10 fixes are still un-audited, so the marker stays unset. Sessions for this repo run from **2026-06-21** (v1.27.0, the first retained transcript) to the present; anything before that predates transcript retention and lives in the [changelog](/starter-template/changelog/) and git history. Two releases have **no retained transcript** and will never get an entry: **v1.36.0** (Cloudflare edge + startup posture guardrail) and **v1.37.0** (admin Security page), both 2026-06-22 — they are covered by the changelog and the `security-floor-progress` memory instead.

Once a contiguous run of sessions has been audited, replace this note with `✅ Reviewed through <date> · <version>` so the next sweep only has to read the entries above it.
</Aside>
