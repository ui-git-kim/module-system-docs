---
title: Configuration
description: Installation prompts, database schema, API endpoints, and node type configuration
sidebar:
  order: 1
---

import { Steps, Tabs, TabItem, Aside } from '@astrojs/starlight/components';

This page covers everything about how Structure is configured — from the CLI prompts during installation to the database schema and API endpoints.

---

## Installation Prompts

`npx structure init` asks only two questions — both about opt-in semantic embeddings. There is **no** node-type onboarding wizard: node types are defined *after* installation via the admin **App Structure** page, or by importing a JSON config (`POST /types/import`).

### Cognitive Foundations (Optional)

Opt-in vector embedding support for semantic search. Requires a Postgres database with the pgvector extension available (Neon ships it). See [Cognitive Foundations](/modules/structure/cognitive/) for the full feature.

| Prompt | Default | Condition | Description |
|--------|---------|-----------|-------------|
| `enableEmbeddings` | No | — | Add embedding columns to `structure_node` and generate the semantic search service |
| `embeddingDimensions` | 1024 | enableEmbeddings=true | Embedding vector dimensions — 1024, 512, 1536, or 3072 |

To opt in later, run `npx structure reconfigure --set enableEmbeddings=true`. That records the new answer, re-syncs the generated files and schema, and fires the embeddings heal hook to provision the pgvector infrastructure — no hand-editing of `.module-state.json`. Changing dimensions after installation requires a manual migration.

---

## Post-Installation

<Steps>
1. **Generate Prisma client**
   ```bash
   npx prisma generate
   ```

2. **Run migrations**
   ```bash
   npx prisma migrate dev
   ```

3. **Add routes** to `frontend/src/routes.tsx`
   ```typescript
   import { structureRoutes } from '@/features/structure';

   // In protected routes — spread at the END so more specific app
   // routes take precedence over Structure's catch-all patterns.
   ...structureRoutes,
   ```
</Steps>

There is **no backend init step.** Structure sets `hasInit: false` — the API routes and the backend service self-register through side-effect imports (`route.registration.ts` / `service.registration.ts`) that run the moment the feature's backend `index.ts` is imported. You do not call `initStructure()` anywhere.

Sidebar navigation populates automatically — when the feature module loads post-login, it fetches node types and syncs the registry. If your app imports the feature before auth is ready, mount the optional `<StructureInit />` component (exported from `@/features/structure`) inside your authenticated layout for an explicit re-prime.

<Aside type="note" title="Gotchas">
- **No backend init call.** Structure sets `hasInit: false` — there is no `initStructure()`. Routes and the service self-register via side-effect imports (`route.registration.ts` / `service.registration.ts`) when the feature is imported.
- **Route ordering matters.** Structure's protected routes use catch-all patterns (`/:nodeType`, `/:nodeType/:nodeId`). Spread them at the end of your route table — anything after them with a specific path will never match.
- **Node types are not auto-seeded.** A fresh install has no node types — create them via Admin > App Structure or a JSON import. Modules that register their own node types should set `moduleId` (and typically `userId: null` for system-level types) for ownership tracking.
</Aside>

---

## Node Types

Structure does **not** auto-seed any node types. A fresh install has an empty structure — there is no `initStructure()` and no default Container/Primary/Folder/Tag types. Create node types via the admin **App Structure** page, or import a JSON config (`POST /types/import`).

### System vs User Node Types

Node types have two flags that control ownership and visibility:

| Flag | Value | Meaning |
|------|-------|---------|
| `userId` | `null` | System-level type — visible to all users |
| `userId` | `string` | User-created type — visible to that user only |
| `isSystem` | `true` | Protected type — only admins can edit, cannot be deleted by users |
| `isSystem` | `false` | Regular type — users can edit and delete |

Module-registered node types should set `isSystem: true` and `userId: null` so they are protected system types. User-created types from the admin panel have `isSystem: false` and the creating user's ID.

<Aside type="note">
When a module registers a node type with `moduleId` set, the system tracks ownership. If the module is uninstalled, its node types can be cleaned up automatically.
</Aside>

---

## Database Schema

Structure creates nine tables in your database, plus a tenth (`structure_chunk`) when embeddings are enabled.

### structure_node

The universal entity table — every node is stored here.

| Column | Type | Description |
|--------|------|-------------|
| `id` | String | CUID primary key |
| `userId` | String | Owner |
| `nodeType` | String | References node type slug |
| `name` | String | Display name |
| `slug` | String? | URL-safe identifier |
| `description` | String? | Rich text description |
| `data` | JSON | Custom field values |
| `icon` | String? | Custom icon override |
| `color` | String? | Custom colour override |
| `parentId` | String? | Parent node (self-referential) |
| `path` | String? | Materialised path for tree queries |
| `depth` | Int | Tree depth (0 = root) |
| `sortOrder` | Int | Position among siblings |
| `fromNodeId` | String? | Source node for connections |
| `toNodeId` | String? | Target node for connections |
| `connectionType` | String? | Connection relationship label |
| `status` | String | `active` / `archived` / `deleted` |
| `stage` | String? | Current stage slug (null if no stage configured) — column mapped to `workflowState` |
| `classification` | String? | Per-node classification override (null = inherit the node type's governance default) |
| `isFavorite` | Boolean | Marked as favourite |
| `isPublished` | Boolean | Publishing workflow state |
| `publishedSlug` | String? | Public URL slug |
| `settings` | JSON | Node-specific settings |
| `metadata` | JSON | Module extensibility data |
| `embedding` | vector? | Embedding vector (opt-in — only when embeddings enabled) |
| `embeddingText` | String? | Exact text that was embedded, used for staleness checks (opt-in — only when embeddings enabled) |
| `embeddingModel` | String? | Model that produced the embedding; `null` means the node needs embedding (opt-in — only when embeddings enabled) |
| `searchVector` | tsvector? | Always-on full-text search vector over name + description + data values; trigger-maintained and GIN-indexed (created by the module migration, not opt-in) |
| `createdAt` | DateTime | Creation timestamp |
| `updatedAt` | DateTime | Last update timestamp |
| `observedAt` | DateTime? | Bi-temporal — when the fact was true in the world, distinct from `createdAt`; nullable, populated by the cognitive engine, for time-scoped filters only (never a ranking signal) |

<Aside type="note" title="Self-referential relations">
The schema uses multiple self-referential `@relation` directives to disambiguate `parentId`, `fromNodeId`, and `toNodeId` foreign keys. This is required by Prisma when a model references itself through multiple fields.
</Aside>

### structure_node_type

Configuration for each node type.

| Column | Type | Description |
|--------|------|-------------|
| `id` | String | CUID primary key |
| `userId` | String? | Owner (null = system default) |
| `slug` | String | Unique identifier |
| `name` | String | Display name |
| `namePlural` | String | Plural form |
| `description` | String? | Type description |
| `icon` | String? | Default Lucide icon |
| `color` | String? | Default theme colour |
| `moduleId` | String? | Owning module ID |
| `rules` | JSON | Hierarchy rules |
| `superClasses` | JSON | `string[]` of parent type slugs for is-a inheritance (default `[]`) — see [Type Hierarchy](/modules/structure/type-hierarchy/) |
| `features` | JSON | Enabled features |
| `fields` | JSON | Custom field definitions |
| `builtInFieldConfig` | JSON | Merge templates / `editableBy` for built-in name, description, and slug fields |
| `conditionalColor` | JSON | Colour rules based on a field value |
| `permissions` | JSON | Node-level create / edit / archive / delete overrides |
| `primaryScope` | JSON | Primary-node scope — `{ isPrimary?, scopedTypes? }` for the header scope selector |
| `governance` | JSON | Governance — `{ classification?: { levels?, default? } }` for data classification |
| `fieldTemplateIds` | JSON | `string[]` — attached shared field-template ids (`structure_field_template`) |
| `navigation` | JSON | Navigation config |
| `labels` | JSON | Admin label overrides (the `getLabel` keys — see `NodeTypeLabels`) |
| `stageConfig` | JSON | StageConfig — stages, groups, transitions, defaultStage — column mapped to `workflowConfig` |
| `contextHints` | String? | Free-text hints for AI context assembly, returned by `GET /nodes/:id/context` |
| `contextConfig` | JSON | Context provider config — `includeAncestors`, `ancestorDepth`, `includeConnections`, `includeChildCounts` |
| `aiFields` | JSON | AI Core prompt-assembly config — `{ promptSegments?, outputExamples? }` (storage-only; consumed by AI Core) |
| `groupSlug` | String? | References `structure_node_type_group.slug` for sidebar grouping |
| `cardTemplateId` | String? | FK → structure_template for card view |
| `listItemTemplateId` | String? | FK → structure_template for list view |
| `detailHeaderTemplateId` | String? | FK → structure_template for detail header |
| `overviewTemplateId` | String? | FK → structure_template for overview |
| `templateOverrides` | JSON | Local overrides on assigned templates |
| `integrations` | JSON | Admin control of module tabs/widgets/extensions |
| `isSystem` | Boolean | Protected from user editing |
| `isEnabled` | Boolean | Whether the type is active |
| `sortOrder` | Int | Display order |
| `proposalStatus` | String? | `null` = live type; `pending` = awaiting admin approval |
| `proposalMeta` | JSON | Proposal metadata (proposing module, reason, source context) |

An `@@index([proposalStatus])` supports the admin proposal queue. See [Cognitive Foundations](/modules/structure/cognitive/) for the proposal workflow.

### structure_template

Reusable rendering templates for views. A template defines how nodes render in a specific view (card, list, detail header, overview) and can be assigned to multiple node types with local overrides.

| Column | Type | Description |
|--------|------|-------------|
| `id` | String | CUID primary key |
| `userId` | String | Owner |
| `name` | String | Display name |
| `slug` | String | URL-friendly identifier |
| `description` | String? | Template description |
| `type` | String | `card`, `listItem`, `detailHeader`, or `overview` |
| `config` | JSON | View-specific configuration |
| `isDefault` | Boolean | Default template for this type |
| `sortOrder` | Int | Display order |

### structure_field_template

Reusable shared field sets. A field template defines a `NodeTypeField[]` that can be attached to many node types (via `structure_node_type.fieldTemplateIds`). Its fields merge into each attaching type's resolved fields — and are included by the secret-sealing and `editableBy` walkers — but are never persisted into the type's own `fields` column (define once, reference many).

| Column | Type | Description |
|--------|------|-------------|
| `id` | String | CUID primary key |
| `userId` | String | Owner |
| `name` | String | Display name |
| `slug` | String | URL-friendly identifier (unique per user) |
| `description` | String? | Template description |
| `fields` | JSON | `NodeTypeField[]` — the shared field set |
| `sortOrder` | Int | Display order (default 0) |
| `createdAt` | DateTime | Creation timestamp |
| `updatedAt` | DateTime | Last update timestamp |

### structure_settings

User-specific preferences and UI state.

| Column | Type | Description |
|--------|------|-------------|
| `id` | String | CUID primary key |
| `userId` | String | Unique owner reference (`@unique`) |
| `settings` | JSON | Flexible settings object |
| `createdAt` | DateTime | Creation timestamp |
| `updatedAt` | DateTime | Last update timestamp |

### structure_node_type_group

Groups node types under collapsible sidebar section headers. Purely organisational — groups are labels for the sidebar, not containers that hold nodes.

| Column | Type | Description |
|--------|------|-------------|
| `id` | String | CUID primary key |
| `userId` | String? | Owner (null = system default) |
| `slug` | String | Unique identifier |
| `name` | String | Display name (shown as sidebar section header) |
| `icon` | String? | Lucide icon for the group |
| `color` | String? | Theme colour |
| `sortOrder` | Int | Display order (default 50) |

### structure_connection_type

Configuration for typed connections between nodes. Controls which node types can connect, cardinality, and optional field schemas on connections.

| Column | Type | Description |
|--------|------|-------------|
| `id` | String | CUID primary key |
| `userId` | String? | Owner (null = system default) |
| `slug` | String | Unique identifier (e.g., `related`, `depends-on`) |
| `name` | String | Display name |
| `namePlural` | String | Plural form |
| `description` | String? | Type description |
| `moduleId` | String? | Owning module ID |
| `icon` | String? | Default Lucide icon |
| `color` | String? | Default colour |
| `fromTypes` | JSON | `string[]` — allowed source node types. Empty = any, `'*'` = any, and listed types admit their subtypes — see [Type Hierarchy](/modules/structure/type-hierarchy/) |
| `toTypes` | JSON | `string[]` — allowed target node types. Same rules as `fromTypes` |
| `bidirectional` | Boolean | If true, A→B and B→A are the same pair — a `unique` type then allows only one connection per unordered pair, in either direction (default true). Traversal is undirected regardless. |
| `maxConnections` | Int? | Max connections of this type per node (null = unlimited) |
| `isUnique` | Boolean | One connection per pair only (default false) |
| `fields` | JSON | `NodeTypeField[]` — data fields on connections (same rich field system as node types) |
| `fieldTemplateIds` | JSON | `string[]` — attached shared field-template ids (`structure_field_template`) |
| `isSystem` | Boolean | Protected from user editing |
| `isEnabled` | Boolean | Whether the type is active |
| `sortOrder` | Int | Display order (default 50) |

### structure_api_key

Per-user API keys for the public, key-authed [REST API](/modules/structure/public-api/). Only the SHA-256 hash of the key is stored (via the security feature's `hashSecret`) — the raw key is shown once at creation and never persisted.

| Column | Type | Description |
|--------|------|-------------|
| `id` | String | CUID primary key |
| `userId` | String | Owner |
| `name` | String | Human label (e.g. `Zapier integration`) |
| `keyHash` | String | `hashSecret(fullKey)` — lookup + verify; `@unique`; raw key never stored |
| `keyPrefix` | String | First chars of the key, for display only (e.g. `sk_3f9a1c`) |
| `scopes` | JSON | `string[]` — `read` and/or `write` |
| `lastUsedAt` | DateTime? | Best-effort usage timestamp |
| `expiresAt` | DateTime? | Optional expiry (null = never) |
| `revokedAt` | DateTime? | Set when revoked (null = active) |
| `createdAt` | DateTime | Creation timestamp |
| `updatedAt` | DateTime | Last update timestamp |

### structure_function

Admin-defined named-expression library for the `formula` field type (tier 2 of the formula function library). Each row is a reusable named expression — parameters plus a safe-eval body — callable from formula fields and from other functions. Dev-registered code functions (math, similarity) live in a separate frontend registry, not here. Self-contained: `userId` is a plain FK (`null` = system/dev-seeded).

| Column | Type | Description |
|--------|------|-------------|
| `id` | String | CUID primary key |
| `userId` | String? | Owning scope (null = system/dev-seeded) |
| `name` | String | Callable identifier (unique per scope) |
| `params` | JSON | `string[]` — ordered parameter names |
| `expression` | String | The safe-eval expression body |
| `description` | String? | Description |
| `category` | String? | Grouping label for the admin UI |
| `createdBy` | String? | Plain FK to user id (audit only) |
| `createdAt` | DateTime | Creation timestamp |
| `updatedAt` | DateTime | Last update timestamp |

A `@@unique([userId, name])` keeps function names unique per scope; `@@index([category])` supports the admin grouping. The table is mapped to `structure_functions` in the database.

### structure_chunk (opt-in)

Created **only when embeddings are enabled** (added by the chunk-storage module migration, not the base schema). A dedicated pgvector table holding per-node text chunks and their embedding vectors for chunk-level semantic search. See [Cognitive Foundations](/modules/structure/cognitive/) for its columns and the chunk search service.

### Raw SQL access notes

If your code queries these tables directly (outside of Prisma), note two things:

- **Pluralised table names.** Prisma models are singular (`structure_node`) but `@@map` pluralises them in the database (`structure_nodes`, `structure_node_types`, `structure_node_type_groups`, `structure_connection_types`, `structure_templates`, `structure_field_templates`, `structure_api_keys`, `structure_functions`). Use the plural names in raw SQL.
- **`@map`-ed columns.** Two Prisma fields map to differently-named database columns (kept for historical reasons, no data move): `structure_node.stage` → column `workflowState`, and `structure_node_type.stageConfig` → column `workflowConfig`. Raw SQL must use the `workflowState` / `workflowConfig` column names.
- **Unique constraints on `structure_node`.** Beyond the primary key, there are two compound uniques that the API surfaces as `400` errors:
  - `@@unique([userId, slug])` — a user can't have two nodes with the same slug
  - `@@unique([userId, parentId, name])` — a user can't have two siblings with the same name under the same parent

---

## Node Type Configuration

The full TypeScript interface for node type configuration:

### NodeTypeConfig

```typescript
interface NodeTypeConfig {
  slug: string;
  name: string;
  namePlural: string;
  description?: string;
  icon?: string;
  color?: string;
  moduleId?: string | null;
  groupSlug?: string | null;
  isSystem?: boolean;
  isEnabled?: boolean;
  sortOrder?: number;
  rules: NodeTypeRules;
  features: NodeTypeFeatures;
  fields?: NodeTypeField[];
  // Merge templates / editableBy for built-in name, description, slug fields
  builtInFieldConfig?: Record<string, unknown>;
  // Colour the node based on a field value
  conditionalColor?: {
    field: string;
    rules: Array<{ value: string; color: string }>;
    fallback?: string;
  };
  // Node-level action overrides
  permissions?: {
    create?: 'anyone' | 'admin';
    edit?: 'anyone' | 'admin';
    archive?: 'anyone' | 'admin';
    delete?: 'anyone' | 'admin';
  };
  navigation: NodeTypeNavigation;
  // Template assignment — reference reusable templates per view
  cardTemplateId?: string | null;
  listItemTemplateId?: string | null;
  detailHeaderTemplateId?: string | null;
  overviewTemplateId?: string | null;
  // Shared field templates — attached reusable field-set ids (structure_field_template)
  fieldTemplateIds?: string[];
  templateOverrides?: NodeTypeTemplateOverrides;
  labels?: NodeTypeLabels;
  integrations?: NodeTypeIntegrations;
  // Stage machine (requires features.stages: true)
  stageConfig?: StageConfig;
  // Context provider — hints for AI and assembled context endpoint
  contextHints?: string;
  // AI Core prompt-assembly config (storage-only; consumed by AI Core's AI tab)
  aiFields?: AiFieldsConfig;
  contextConfig?: {
    includeAncestors?: boolean;
    ancestorDepth?: number;
    includeConnections?: boolean;
    includeChildCounts?: boolean;
  };
}
```

### Rules

```typescript
interface NodeTypeRules {
  isRoot?: boolean;                 // Can exist at root level (no parent)
  allowedParents?: string[];        // Slugs of types this can nest inside
  allowedChildren?: string[];       // Slugs of types this can contain
  maxDepth?: number;                // Max nesting depth
  maxChildren?: number;             // Max number of direct children
  isHierarchical?: boolean;         // Can form hierarchies
  allowMultipleParents?: boolean;   // Tag-like behaviour
  isConnection?: boolean;           // Is this a connection-type node?
  connectionConfig?: {
    fromTypes?: string[];
    toTypes?: string[];
    bidirectional?: boolean;
  };
}
```

<Aside type="note">
**Enforced server-side since v1.24.0.** `createNode` and `moveNode` validate these rules — a type with `isRoot: false` requires a parent, `allowedParents`/`allowedChildren` gate placement, and `maxChildren`/`maxDepth` are honoured. Each rule is **opt-in**: an unset field imposes no constraint, so a type with no rules configured behaves exactly as before. `allowedParents`/`allowedChildren` compare type **slugs**; `maxDepth` is 0-based (root = 0). (Not yet enforced on `bulkCreateNodes` / `POST /nodes/bulk`.)
</Aside>

### Primary scope

Mark a node type as the app's **primary scope** (e.g. Matter) so a header dropdown lets users pick the current instance and other types' lists filter to that instance's subtree. Configured on the admin **Hierarchy** tab. Added in v1.24.0.

```typescript
// On NodeTypeConfig:
primaryScope?: {
  isPrimary?: boolean;      // This type is the primary scope; adds the header picker
  scopedTypes?: string[];   // Which type slugs the selection filters.
                            // empty or ['*'] = every type; a list = only those types
};
```

The selected instance's id is stored per user in `structure_settings` (`currentScopeNodeId`) via the `useCurrentScope()` store, and `NodeListPage` scopes matching lists by injecting `pathPrefix` (the selected node's materialised-path prefix). **`scopedTypes` is an admin choice:** leave it empty (or use `'*'`) to scope the whole app to the primary node, or list specific slugs to scope only those and leave global/reference types (e.g. legislation) unscoped. The primary type's own list is never filtered by its own selection. Built for one primary scope now, shaped so multiple/secondary scopes can be added later.

### Governance (data classification)

A generic, extensible per-type **governance** block. Its first rule type is **classification** (sensitivity / privilege). Configured on the admin **Hierarchy** tab. Added in v1.25.0.

```typescript
// On NodeTypeConfig:
governance?: {
  classification?: {
    // Ordered sensitivity levels — lower rank = less sensitive.
    levels?: Array<{ value: string; label: string; rank: number; color?: string }>;
    default?: string;   // Level applied to nodes of this type with no override
  };
};
```

- **Per-field sensitivity.** Any field (`NodeTypeField`) can carry `sensitivity?: string` — a level `value` — for field-level redaction. It survives type-hierarchy resolution into `resolved.fields`.
- **Per-node override.** `structure_node.classification` (nullable) overrides the type default for a single node; `null` inherits the default. Accepted on create / update (in-app and the public REST API); a change fires the `structure.node.classificationChanged` action hook.
- **Evaluators (structure stores + evaluates; the consumer enforces).** Exported from the feature root on both backend and frontend:
  - `getEffectiveClassification(node, nodeType)` → per-node override → type default → `undefined` (an empty override is treated as no override).
  - `isExportSafe(node, nodeType, level)` → `true` when the node's effective classification `rank` ≤ the export threshold `level`'s rank. An **unclassified** node is unrestricted (fail-open); an explicitly-set level not in the configured `levels`, or an unknown threshold, **fails closed**.

<Aside type="note">
Classification levels are **per node type** and are **not** inherited through `superClasses` (own-config only, like the hierarchy rules). For an app-wide scheme, configure consistent levels across the types that need it. The per-node override is **API-only** for now (no UI picker yet), and export-pipeline enforcement is a consuming module's responsibility — structure only evaluates.
</Aside>

### AI Fields

Per-type prompt-assembly config, stored by Structure and consumed by AI Core — Structure never interprets it. Surfaced on the node type **AI** tab. Added in v1.30.0. `promptSegments[].type` maps to AI Core's `ConsumerSegment.type` (`task` | `context` | `external`).

```typescript
// On NodeTypeConfig:
aiFields?: AiFieldsConfig;

interface AiFieldsConfig {
  // Structured prompt segments a consumer supplies to AI Core on a run.
  promptSegments?: Array<{
    type: 'context' | 'task' | 'external';
    label?: string;
    content: string;
  }>;
  // Output examples that guide the model (rendered by the consumer).
  outputExamples?: Array<{
    label?: string;
    input?: string;
    output: string;
  }>;
}
```

### Features

Each flag turns on an optional capability for entries of this type. Used by the admin UI (Properties tab) and checked at runtime by the API.

```typescript
interface NodeTypeFeatures {
  slug?: boolean;            // Allow custom URL slug
  description?: boolean;     // Show rich description field
  color?: boolean;           // Allow custom colour override
  icon?: boolean;            // Allow custom icon override
  favorites?: boolean;       // Allow marking as favourite
  archiving?: boolean;       // Allow archive (soft delete)
  publishing?: boolean;      // Enable draft/published workflow
  manualSort?: boolean;      // Allow drag-and-drop reordering
  stages?: boolean;  // Enable the stage machine (populate stageConfig)
  // Expose entries of this type via the public REST API
  publicApi?: { enabled?: boolean };
  // Opt entries of this type into cognitive processing (embeddings, ingest)
  cognitiveIngest?: { enabled?: boolean };
}
```

`cognitiveIngest` appears as the "Cognitive Processing" toggle on the node type Properties tab and controls which types the embeddings service targets by default — see [Cognitive Foundations](/modules/structure/cognitive/).

### Navigation

```typescript
interface NodeTypeNavigation {
  mainNav?: {
    enabled: boolean;
    label?: string;
    icon?: string;
    order?: number;
    showCount?: boolean;
  };
  sidebarNav?: {
    parentType?: string;
    maxDepth?: number;
    showEmpty?: boolean;
    collapsible?: boolean;
    defaultExpanded?: boolean;
    showEntries?: boolean;
    entryFilter?: 'favorites' | 'recent' | 'all' | 'pinned';
    maxEntries?: number;
    userOverridable?: boolean;
  };
  pageNav?: {
    style: 'tabs' | 'sidebar' | 'none';
    // Each built-in tab can be a simple boolean (show/hide) or an object
    // with per-tab overrides like a custom label or widget layout.
    builtInTabs?: {
      overview?: boolean | { label?: string; widgetLayout?: 'single' | 'two-column' };
      info?: boolean | { label?: string };
      children?: boolean | { label?: string };
      connections?: boolean | { label?: string };
      settings?: boolean | { label?: string };
    };
    tabOrder?: string[];       // Explicit tab ordering by ID
    hideTabs?: string[];       // IDs of module-provided tabs to hide
    defaultTab?: string;
  };
  listPage?: {
    defaultView?: 'grid' | 'list' | 'table';
    defaultSort?: string;
    showFilters?: boolean;
    showSearch?: boolean;
    groupBy?: string;
    card?: {
      showDescription?: boolean;
      showIcon?: boolean;
      showColor?: boolean;
      showFields?: string[];
    };
    columns?: Array<{
      fieldId: string;
      label?: string;
      width?: string;
      sortable?: boolean;
    }>;
    filterableFields?: string[];
  };
}
```

### Fields

```typescript
interface NodeTypeField {
  id: string;
  type: string;        // See built-in types below
  label: string;
  description?: string;
  placeholder?: string;
  required?: boolean;
  options?: Array<{ label: string; value: string }>;
  validation?: {
    min?: number;
    max?: number;
    pattern?: string;
    message?: string;
  };
  order?: number;
  section?: string;
  width?: 'full' | 'half';
}
```

**Built-in field types:** text, textarea, number, date, datetime, time, select, multiselect, chained-select, toggle, boolean, url, email, phone, address, name-field, secret, hidden, section-break, node-reference, field-group. See the [Field Types Reference](/modules/structure/fields/) for the full list with configuration and examples.

**New field types:**
- `boolean` — renders as Yes/No radio buttons (not a toggle switch)
- `field-group` — repeating field group (repeater). Contains sub-fields defined via the `fields` array (a `NodeTypeField[]` on the field-group field). Each entry adds a row with all sub-fields. Edited in admin via a dedicated sub-fields editor.
- `node-reference` — supports `multiple: true` via its `nodeReferenceConfig` object (alongside `allowedTypes`), not in `options`, for multi-select node references

### Labels

```typescript
interface NodeTypeLabels {
  addNew?: string;
  addNewShort?: string;
  editItem?: string;
  viewItem?: string;
  deleteItem?: string;
  searchItems?: string;
  filterItems?: string;
  notFound?: string;
  notFoundInTrash?: string;
  emptyState?: string;
  parentItem?: string;
  allItems?: string;
  archiveAction?: string;
  restoreAction?: string;
  publishAction?: string;
  unpublishAction?: string;
  favoriteAction?: string;
  unfavoriteAction?: string;
}
```

### StageConfig

Optional per-node-type stage machine. Enable with `features.stages: true`.

```typescript
interface StageConfig {
  stages: Stage[];
  groups?: StageGroup[];
  defaultStage: string;             // slug of the initial stage
  transitions?: StageTransition[];
}

interface Stage {
  slug: string;
  name: string;
  color?: string;    // hex colour for badge
  icon?: string;     // Lucide icon name
  group?: string;    // references StageGroup.slug
  sortOrder?: number;
}

interface StageGroup {
  slug: string;
  name: string;
  sortOrder?: number;
}

interface StageTransition {
  from: string;      // from-stage slug
  to: string[];      // allowed target stage slugs
}
```

When a node is created, the service auto-sets `stage` to `stageConfig.defaultStage`. Stage changes fire the `structure.node.stageChanged` action hook with `{ userId, node, oldStage, newStage }`. When `transitions` are defined, `updateNode` rejects a stage change the map does not permit — an unset from-stage (or an empty map) is unconstrained, so transition enforcement is fully opt-in.

### ConnectionTypeConfig

Configuration for typed connections. Managed via the Connection Types admin tab or `structureRegistry.register()`.

```typescript
interface ConnectionTypeConfig {
  slug: string;
  name: string;
  namePlural: string;
  description?: string;
  icon?: string;
  color?: string;
  moduleId?: string | null;
  fromTypes?: string[];        // allowed source node types (empty = any, '*' = any; listed types admit their subtypes)
  toTypes?: string[];          // allowed target node types (same rules as fromTypes)
  bidirectional?: boolean;     // default true
  maxConnections?: number;     // per node (null = unlimited)
  unique?: boolean;            // one per pair only
  fields?: NodeTypeField[];    // same rich field system as node types
  fieldTemplateIds?: string[]; // attached shared field-template ids (structure_field_template)
  isSystem?: boolean;
  isEnabled?: boolean;
  sortOrder?: number;
}

// Connection fields use the full NodeTypeField shape (see Fields above).
// `ConnectionTypeField` is retained only as a @deprecated alias of NodeTypeField.
type ConnectionTypeField = NodeTypeField;
```

---

## API Endpoints

All endpoints are prefixed with `/api/v1/structure`.

### Nodes

| Method | Path | Description |
|--------|------|-------------|
| GET | `/nodes` | List nodes (with filters) |
| GET | `/nodes/:id` | Get single node |
| POST | `/nodes` | Create node |
| PATCH | `/nodes/:id` | Update node |
| DELETE | `/nodes/:id` | Soft delete |
| POST | `/nodes/:id/move` | Move to different parent |
| POST | `/nodes/bulk` | Bulk create nodes (per-row atomic, partial success — see note below) |
| POST | `/nodes/:id/duplicate` | Duplicate node (subtree copy) |
| POST | `/nodes/:id/archive` | Archive |
| POST | `/nodes/:id/restore` | Restore from archive |
| POST | `/nodes/:id/favorite` | Toggle favourite |
| GET | `/nodes/:id/path` | Get ancestor path |
| GET | `/nodes/:id/children` | Get direct children |
| GET | `/nodes/:id/descendants` | Get all descendants |
| GET | `/nodes/:id/context` | Get assembled node context (node, type, ancestors, breadcrumb, counts, connections) |
| PATCH | `/nodes/:id/metadata` | Atomic namespaced metadata write |

<Aside type="note" title="Bulk create is partial-success, not all-or-nothing (v1.19.0)">
`POST /nodes/bulk` creates each row atomically on its own and returns `{ created, errors }`, where
`errors` carries the index and message of each row that failed. Rows that succeed are committed even
if others fail.

This corrects a real defect rather than changing a design: the endpoint previously ran the whole
batch inside one interactive transaction *while* catching per-row errors. A failed statement aborts
an interactive transaction at the database, so every subsequent row failed and the commit became a
rollback — yet the caught results still reported the earlier rows as created, and lifecycle hooks
fired for rows that were never persisted. Per-row atomicity is what the documented
per-node error collection actually requires. If you need all-or-nothing semantics, validate before
calling, or delete the created ids on a non-empty `errors` array.
</Aside>

### Connections

| Method | Path | Description |
|--------|------|-------------|
| GET | `/nodes/:id/connections` | Get connections for a node (`?status=`, `?origin=` filters; active-only by default) |
| GET | `/connections` | List connections with pagination (`?status=`, `?origin=`, `?connectionType=`, `?page=`, `?limit=`) |
| POST | `/connections` | Create a connection (optional `origin` and `status` body fields) |
| POST | `/connections/bulk` | Bulk create connections — per-row atomic, partial success; fires one `structure.connections.bulkCreated` (v1.27.0) |
| POST | `/connections/:id/promote` | Promote a pending connection to active |
| DELETE | `/connections/:id` | Remove a connection |

Connection `origin`/`status` semantics, pending connections, and promotion are covered in [Cognitive Foundations](/modules/structure/cognitive/).

### Tree

| Method | Path | Description |
|--------|------|-------------|
| GET | `/tree` | Get tree structure (optional `?type=` filter) |
| POST | `/reorder` | Batch reorder nodes within a parent |

### Node Types

| Method | Path | Description |
|--------|------|-------------|
| GET | `/types` | List all types (add `?all=true` for disabled) |
| GET | `/types/:slug` | Get type config |
| POST | `/types` | Create type (admin only) |
| PATCH | `/types/:slug` | Update type (admin only) |
| DELETE | `/types/:slug` | Delete type (admin only, fails if nodes exist) |
| GET | `/types/export` | Export **node types only** as JSON (admin only) |
| POST | `/types/import` | Import types from JSON — `merge` or `replace` mode (admin only) |

The `/types/export` endpoint returns node types alone (`{ version, exportedAt, nodeTypes }`). The bundled configuration export — connection types, groups, and field templates alongside node types — is a **frontend admin-UI feature** (the App Structure page's **Export** button), assembled client-side, not this endpoint.

Since v1.18.0, the list and get responses include two additive keys: `superClasses` (the raw parent slugs) and a read-only `resolved` block (`fields`, `features`, `ancestors`) with inheritance applied. See [Type Hierarchy](/modules/structure/type-hierarchy/).

### Node Type Proposals

Modules can propose new node types for admin approval — see [Cognitive Foundations](/modules/structure/cognitive/) for the workflow.

| Method | Path | Description |
|--------|------|-------------|
| POST | `/types/proposals` | Propose a node type (any authenticated user) |
| GET | `/types/proposals` | List pending proposals (admin only) |
| POST | `/types/proposals/:slug/approve` | Approve a proposal — body `scope` of `user` (default) or `system` (admin only) |
| POST | `/types/proposals/:slug/reject` | Reject and delete a proposal, freeing the slug (admin only, returns 204) |

### Connection Types

| Method | Path | Description |
|--------|------|-------------|
| GET | `/connection-types` | List all connection types |
| GET | `/connection-types/:slug` | Get connection type config |
| POST | `/connection-types` | Create connection type (admin only) |
| PATCH | `/connection-types/:slug` | Update connection type (admin only) |
| DELETE | `/connection-types/:slug` | Delete connection type (admin only) |

### API Keys

Session-authed management of keys for the [public REST API](/modules/structure/public-api/).

| Method | Path | Description |
|--------|------|-------------|
| GET | `/api-keys` | List the current user's keys (no secrets) |
| POST | `/api-keys` | Create a key (raw key returned once) |
| DELETE | `/api-keys/:id` | Revoke a key |

The public, API-key-authed data endpoints live under `/api/v1/public/structure/:type` — see the [Public REST API](/modules/structure/public-api/) page.

### Groups

| Method | Path | Description |
|--------|------|-------------|
| GET | `/groups` | List all node type groups |
| GET | `/groups/:slug` | Get group by slug |
| POST | `/groups` | Create group (admin only) |
| PATCH | `/groups/:slug` | Update group (admin only) |
| DELETE | `/groups/:slug` | Delete group (admin only) |

### Templates

| Method | Path | Description |
|--------|------|-------------|
| GET | `/templates` | List templates (optional `?type=` filter) |
| GET | `/templates/:id` | Get single template |
| POST | `/templates` | Create template |
| PATCH | `/templates/:id` | Update template |
| DELETE | `/templates/:id` | Delete template |

### Field Templates

Reusable shared field sets (`structure_field_template`) attached to node types via `fieldTemplateIds`.

| Method | Path | Description |
|--------|------|-------------|
| GET | `/field-templates` | List field templates |
| GET | `/field-templates/:id` | Get single field template |
| POST | `/field-templates` | Create field template (admin only) |
| PATCH | `/field-templates/:id` | Update field template (admin only) |
| DELETE | `/field-templates/:id` | Delete field template (admin only) |

### Settings

| Method | Path | Description |
|--------|------|-------------|
| GET | `/settings` | Get user settings |
| PATCH | `/settings` | Update user settings (merges) |

### Query Parameters

| Param | Description |
|-------|-------------|
| `type` | Filter by node type slug |
| `types` | Comma-separated list of type slugs |
| `includeSubtypes` | `true` to expand the `type`/`types` filter to each type plus all of its subtypes (v1.18.0+) — see [Type Hierarchy](/modules/structure/type-hierarchy/) |
| `parentId` | Filter by parent (use `null` for roots) |
| `status` | `active`, `archived`, `deleted`, or `all` |
| `isFavorite` | `true` or `false` |
| `search` | Search name and description |
| `page`, `limit` | Pagination |
| `sortBy` | `name`, `createdAt`, `updatedAt`, or `sortOrder` |
| `sortOrder` | `asc` or `desc` |
| `includeChildren` | `true` to include child nodes |
| `includeChildrenDepth` | Depth of children to include |
| `includePath` | `true` to include ancestor path |
| `includeConnectionCounts` | `true` to include connection counts |
| `pathPrefix` | Hierarchical scoping — return only descendants whose path starts with this prefix |
| `dataFilters` | JSON array of `DataFilter` objects for filtering on custom field data (see below) |
| `dataSortField` | Sort by a custom field in the `data` JSON column |
| `dataSortType` | Type hint for data sort: `text`, `number`, or `date` |
| `stage` | Filter by exact stage slug |
| `stages` | Comma-separated list of stage slugs |
| `stageGroup` | Filter by stage group slug |

### Data Filters

The `dataFilters` query parameter accepts an array of filter objects for advanced filtering on custom field values stored in the `data` JSON column:

```typescript
interface DataFilter {
  fieldId: string;
  operator: DataFilterOperator;
  value?: unknown;
}

type DataFilterOperator =
  | 'equals' | 'not_equals' | 'contains' | 'starts_with'
  | 'gt' | 'gte' | 'lt' | 'lte' | 'between'
  | 'in' | 'not_in' | 'is_empty' | 'is_not_empty';
```

Operators `is_empty` and `is_not_empty` do not require a `value`. The `between` operator expects `value` to be `[min, max]`. The `in` and `not_in` operators expect `value` to be an array.

---

## Request Validation

All POST and PATCH endpoints validate request bodies using Zod schemas. Invalid requests return a `400 Bad Request` with a descriptive error message.

| Schema | Used by | Key validations |
|--------|---------|----------------|
| `createNodeSchema` | POST `/nodes` | `nodeType` and `name` required, max lengths |
| `updateNodeSchema` | PATCH `/nodes/:id` | All fields optional, max lengths |
| `moveNodeSchema` | POST `/nodes/:id/move` | `newParentId` required (nullable) |
| `reorderNodesSchema` | POST `/reorder` | `nodeIds` array required (min 1) |
| `createConnectionSchema` | POST `/connections` | `fromNodeId`, `toNodeId`, `connectionType` required; optional `origin` (lowercase-kebab) and `status` (`active` or `pending`) |
| `createNodeTypeSchema` | POST `/types` | `slug` regex validated, `name`/`namePlural` required |
| `updateNodeTypeSchema` | PATCH `/types/:slug` | All fields optional |
| `proposeNodeTypeSchema` | POST `/types/proposals` | Everything in `createNodeTypeSchema`, plus required `proposal.proposedBy` (kebab-case) |
| `approveNodeTypeProposalSchema` | POST `/types/proposals/:slug/approve` | Optional `scope` — `user` (default) or `system` |
| `createTemplateSchema` | POST `/templates` | `name`, `type` required, `slug` regex validated |
| `updateTemplateSchema` | PATCH `/templates/:id` | All fields optional |

---

## CLI Reference

Complete list of subcommands exposed by `npx structure <command>`.

| Command | Flags | Purpose |
|---------|-------|---------|
| `init` | `--force`, `--no-interactive`, `--dry-run`, `--skip-dep-check` | Install the module. `--force` overwrites existing files; `--no-interactive` skips prompts and uses defaults; `--dry-run` previews what would be generated without writing; `--skip-dep-check` proceeds even when required modules are missing or outside the declared version range |
| `update` | `--dry-run`, `--force`, `--skip-schema`, `--set <assignments...>` | Update templates and schema. `--dry-run` previews, `--force` proceeds through conflicts, `--skip-schema` updates templates only, `--set <assignments...>` also changes install-time answers (delegates to `reconfigure`) |
| `reconfigure` | `--dry-run`, `--force`, `--set <assignments...>`, `--skip-schema` | Change install-time answers (toggle gated features) and re-sync files/schema, e.g. `--set enableEmbeddings=true` |
| `migrate` | `--dry-run` | Add version headers to existing files (for installations that pre-date version tracking) |
| `migrate-db` | `--dry-run` | Run the module's database migrations (SQL + TS backfills). `--dry-run` lists pending migrations without applying |
| `uninstall` | `--force`, `--keep-data`, `--export <path>` | Remove the module. `--keep-data` preserves tables, `--export <path>` writes data to JSON before removal |
| `disable` | `--force` | Temporarily disable without removing files |
| `enable` | `--force` | Re-enable a disabled module |
| `info` | — | Print module name, version, features, and available commands |

<Aside type="caution" title="Module migrations need a reachable database">
Structure ships module SQL migrations (the first is `1.16.0__cognitive-foundations.sql`). Both `init` and `update` apply them, so they need a reachable database — set `DIRECT_DATABASE_URL` (or `DATABASE_URL`) in `backend/.env` before running either command.
</Aside>

### Updating

```bash
npx structure update            # Update templates and schema
npx structure update --skip-schema  # Update templates only
npx structure update --dry-run  # Preview changes without applying
npx structure update --force    # Force update through conflicts
```

The update command pulls the latest template versions. If the schema has changed, it updates your Prisma schema block. Run `npx prisma generate` and `npx prisma migrate dev` after updating.

### Uninstalling

```bash
npx structure uninstall
npx structure uninstall --keep-data             # Keep database tables
npx structure uninstall --export ./backup.json  # Dump data first
```

This removes generated files and the schema block. You will need to manually clean up any references in your `routes.tsx` and `server.ts`.

---

## Next Steps

- [Usage](/modules/structure/usage/) — Admin page, pages, components, and hooks
- [Cognitive Foundations](/modules/structure/cognitive/) — Embeddings, semantic search, connection provenance, and node type proposals
- [Field Types Reference](/modules/structure/fields/) — All 22 built-in field types with configuration and examples
- [Merge Fields & Conditional Logic](/modules/structure/merge-fields/) — Auto-compute field values and control visibility
- [Integration](/modules/structure/integration/) — How other modules extend Structure
- [Registries Guide](/modules/structure/registries-guide/) — Comprehensive reference for all 10 registries, module fields, and hooks
- [JSON Import & Export](/modules/structure/json-reference/) — Import and export Structure configurations
- [Database docs](/starter-template/database/) — Starter template Prisma setup and migration commands
