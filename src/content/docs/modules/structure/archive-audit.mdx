---
title: Archive Audit
description: Per-session archive-readiness log — unfinished work, loose ends, decisions, and ideas captured before a session is archived
lastUpdated: 2026-07-24
sidebar:
  order: 12
---

import { Aside } from '@astrojs/starlight/components';

A running log of **archive-readiness audits**. Before a working session on the Structure module is archived, it is audited for anything that would be lost — unfinished work, loose ends, decisions, and ideas — and the findings are recorded here so a fresh session can pick them straight up.

<Aside type="note" title="Why this page exists">
Session transcripts get archived and are then awkward to search. This page is the durable, reviewable home for "what was in that session" — decisions and their reasoning, ideas raised in passing, work left mid-flight, and anything the code and the rest of these docs don't already capture. Newest entry first. Nothing here is deleted; when an item is actually done, it's ticked and left in place for provenance.
</Aside>

## How to use this page

At the end of a session you want to archive, run the archive-readiness prompt (below). It audits the session and **appends a new entry at the top** of the log, using the entry template. Then it builds and pushes the docs. Review the entry on the live site; archive the session; start the next one by reading the open items here.

### The prompt

> **Archive-readiness check.** Audit this session end-to-end for anything that would be lost if I archive it: (1) unfinished work — started-not-finished, committed-not-pushed, coded-not-validated, or left WIP (check `git status` and recent commits in every repo touched, including the `docs/` submodule — **and if this is an older session the repo has since moved past, expect HEAD, the version number, and the working tree to look _newer_ than this session's output, and its commits to sit deep in history, absent from a short `git log`/reflog window; that is normal forward progress, not loss. Confirm the work survived by identifying this session's own commits from the transcript and checking each is an ancestor of `origin/main` (`git merge-base --is-ancestor <sha> origin/main`) — reachable from origin means preserved even though HEAD is far ahead. Real loss is only a commit that is on no branch _and_ absent from `origin`; do not infer loss from "HEAD isn't at my version" or "the working tree differs from what I edited"**); (2) loose ends — TODOs, "later" items, deferred fixes, promised follow-ups, checks I said I'd run; (3) decisions & ideas — any decision, trade-off, rejected approach and *why*, or idea raised in conversation not already written in code or docs; (4) undocumented reality — anything we changed or learned the docs don't reflect. Then **append a new entry to the top of the Archive Audit log** (`docs/src/content/docs/modules/structure/archive-audit.mdx`) using the entry template already on that page. Build the docs, push the docs submodule **and** the submodule pointer. Verify before ticking anything "done" (if a version was bumped, confirm `dist` was rebuilt after the bump; for an older session, confirm `dist` — if gitignored — stamps the **current HEAD** version, not the session's). Finish by telling me plainly whether it's safe to archive.

### Entry template

```mdx
## YYYY-MM-DD — <session focus>

**Verdict:** ✅ Safe to archive  ·  ⚠️ Needs input first  ·  ℹ️ Superseded (work integrated, session now historical)
**Shipped this session:** <module version(s), key commits, what went live>

**Unfinished / WIP:** <or "none">
**Loose ends & follow-ups:** <deferred fixes, promised checks, TODOs — or "none">
**Decisions & ideas:** <decisions + why, rejected approaches, ideas raised — or "none">
**Recorded where:** <docs pages / memory files updated, so it's findable>
**Open items for next session:** <the pick-up list — or "none">
```

---

## 2026-07-29 — Embeddings/reconfigure trust-fixes (v1.23.1–v1.23.4), scaffold 1.29→1.30, and the veradai requirements audit + build plan

**Verdict:** ✅ Safe to archive
**Shipped this session:** four patch releases + two scaffold pulls, all pushed; the veradai planning arc captured in docs + memory. (Phase 1 / v1.24.0 was executed by a follow-on session from the plan below — noted for continuity, not this session's commits.)
- **v1.23.1 — `onReconfigure` embeddings heal.** A same-version `reconfigure` doesn't fire `onUpdate`, so flipping `enableEmbeddings` on skipped `healEmbeddingsSetup`. Added an `onReconfigure` hook gated on the off→on transition. **Live-tested in veradai** (Neon+pgvector): terminal showed `No version change - skipping onUpdate hook` then `Running onReconfigure hook… Embeddings infrastructure ready`. Commits `af938b6` + release + `c8ddacd`.
- **v1.23.2 — conditional pgvector-datasource advisory.** The heal now reads the host schema and only advises the datasource `pgvector` declaration when it is actually missing (silent once declared), pointing at the starter's `enable-vectors`/`ensure-pgvector` rather than a manual edit. `495cd28`. No generated-output delta (host tsc N/A).
- **v1.23.3 — computed built-in fields populate.** Closed the last `builtInFieldConfig` trust-fix item: `NodeForm` resolves name/description/slug `computedTemplate` via the now-exported `useResolvedTemplate`; `updateNode` stops locking `computedTemplate` built-ins so the client-computed value persists (Option A — same trust model as custom computed fields). `84e54d7`. **Host-tsc-validated in veradai: 0 errors in `features/structure`** (the 14 other errors pre-existing in billing/document-management).
- **v1.23.4 — removed vestigial `routes.registration.ts`.** A comment-only dead file (routes go via `structureRoutes` + `protectedRoutes`), the one true C2 hit once the linter was fixed. `497bfd4`. `conformance:check` now clean.
- **Scaffold 1.24.1 → 1.29.0 → 1.30.0.** 1.29.0 (`c8a3784`) needed a manual conflict merge (package.json) + restoring a silently-dropped `sync-contract.cjs`. 1.30.0 (`c342966`) shipped my two upstream reports — the **C2 conformance-linter barrel fix** (13 false positives → 0) and the **dropped-scaffold-file backfill** — and retired `sync-contract`.
- **Upstream feedback shipped:** the onUpdate↔onReconfigure pairing docs (module-starter 1.24.1), the `ensure-pgvector` starter primitive (spec'd here, shipped in the starter), and the C2 + backfill fixes (scaffold 1.30.0).
- **Veradai requirements arc:** mapped the veradai app's structure requirements against code (5-agent audit) — most already shipped; produced the phased **Build Plan** doc (`/modules/structure/build-plan/`), the roadmap's **Veradai-Driven Build Sequence**, rescoped **Custom Detail Page Layouts** to merge-field exposure (useFoundry owns presentation), and added the **shared field templates** candidate.

All this session's commits are on `origin/main` (module HEAD `bbb02e4`; docs `be8063c`); working trees clean; `dist` rebuilt after each bump. Module now at v1.24.0 (Phase 1, follow-on session) plus these docs commits.

**Unfinished / WIP:** None unpushed.

**Loose ends & follow-ups:**
- **`ensure-pgvector` advise→auto-enable FLIP (structure side) — NOT done.** The starter shipped `ensure-pgvector` (`npm run ensure-pgvector`, verified present in veradai); structure's heal still only *advises*. The flip (detect missing declaration → invoke `ensure-pgvector`, advisory fallback) is written up and ready (~15 lines) but unbuilt.
- **Live reconfigure P3009 test on a NON-pgvector Postgres** — the one heal branch veradai (Neon) can't exercise; deferred.
- **Phase 1 (v1.24.0) deferred follow-ups** (follow-on session): `bulkCreateNodes` skips hierarchy enforcement; `moveNode` `maxDepth` checks only the moved node not each subtree descendant; scoped **tree** (`getTree` `pathPrefix`) untouched.
- **The build plan (Phases 2–6) is the forward queue:** (2) AI fields tab [blocked on ai-core prompt-assembly contract], (3) generic rules/sensitivity, (4) field UX (collapsible + the `field-group` repeater `allData` bug at `components.ts:1974`), (5) merge-field exposure, (6) shared field templates [UX not designed].

**Decisions & ideas:**
- **pgvector datasource ownership = STARTER, not a module.** Generator/datasource blocks are app-owned (the scaffold strips them from module schemas); pgvector is one-per-DB shared substrate. NOT ai-core (its docs forbid owning columns; empty `depends`), NOT cog-ingest (a consumer). The starter's `ensure-pgvector` primitive (non-interactive, declaration-only, no demo `Embedding` model) is the clean fix; structure detects + triggers.
- **Computed built-ins — Option A** (client computes, server accepts) over server-side compute: consistent with how custom computed fields already work; smaller; server keeps `system`/`admin` enforcement.
- **`workflow` → `status` rename** (decided): keep it first-class because the status-changed hook is the automation / task-module plug-in point (not "just a field").
- **Custom Detail Layouts rescoped:** structure exposes merge fields; the user's **useFoundry** visual builder owns presentation (JetEngine / Dynamic.ooo ↔ Elementor). Structure's net-new = a merge-field metadata/discovery API + exporting the pure resolver.
- **Shared field templates verified absent:** `structure_template` is layout-only; `superClasses` is is-a inheritance; module fields are code-only. Net-new = a `structure_field_template` entity + attach point + merge into `resolved.fields`.
- **C2 conformance false-positives were a linter limitation** (hardcoded barrel path), not a structure bug — fixed upstream.
- **Primary scope: scoped-types is an admin choice** (a list; empty/`*` = everything); build for 1 primary, shape config for multiple later.

**Recorded where:** Docs — changelog v1.23.1–v1.23.4; roadmap (Veradai-Driven Build Sequence, Custom-Detail rescope, Completed rows); `cognitive.mdx` (onReconfigure heal + conditional advisory + Opting-in-later); **new `build-plan.md`**. Memory — `module-starter-reconfigure-arc`, `pgvector-datasource-ownership`, `scaffold-1-29-conformance`, `trust-fix-v1-23-0` (computedTemplate ticked), `veradai-requirements-and-plan` (full phased plan + shared-field-templates). The 5-agent requirements audit + 3-agent build-plan grounding survive only in the transcript; their load-bearing outcomes are in the build plan + memory.

**Open items for next session:** **Phase 2 — AI fields tab** (see the continuation prompt / build plan). Then Phases 3–6, the `ensure-pgvector` flip, and the Phase-1 deferred follow-ups.

## 2026-07-25 — Chunk storage (v1.22.0) + trust-fix release (v1.23.0) + roadmap verification + module-starter reconfigure gap

**Verdict:** ✅ Safe to archive
**Shipped this session:** Two releases, both pushed and host-validated.
- **v1.22.0 — Chunk storage.** Dedicated `structure_chunk` pgvector table (gated on `enableEmbeddings`, same opt-in as node embeddings) + six chunk methods on the `structureEmbeddings` service (`replaceNodeChunks`, `setChunkEmbedding`, `listChunksNeedingEmbedding`, `getNodeChunks`, `deleteNodeChunks`, `semanticSearchChunks` — joins `structure_nodes` for source-node status/type filtering), a partial HNSW index (`1.22.0__chunk-storage.sql` + runtime `ensureInfrastructure` + install heal), a gated `structure.chunks` erasure handler, `chunks` on the capability probe, and two action hooks. Commits `f24cd49` (feat) + release + docs. Backend validated via an isolated Prisma-7.9 harness (0 errors).
- **v1.23.0 — Trust-fix release.** Six "declared-but-dead" admin controls made real: `builtInFieldConfig` editableBy (NodeForm read-only gating + server-side `updateNode` enforcement, ignore-not-throw), custom-field editableBy in the form loop, `NodeChildrenTab` create gated on `permissions.create`, server-side data-field sort (`dataSortField`/`dataSortType`, in-memory type-aware) with `NodeTable` driving it, conditional-colour option-label Select + stale warning, chained-select admin editor. Dropped dead `renderCompact` + `inputMask`. Commits `4a8a837` (release) + `eb71f30` (feat) + docs `bf95210`. **Host-tsc-validated in veradai (structure 1.23.0): 0 errors in `features/structure`, backend + frontend** (all errors were pre-existing in billing/document-management).
- **Completeness sweep** (docs `5fed60e`/`3ee8aa2`): reconciled three stale action-hook counts to 38 and added the chunk hooks to `registries-guide` — the `nodeTypeGroup`-hook gap flagged in the entry below was already closed by `ad06ae6`.
- **Roadmap verification workflow** (18 agents, code-checked) sized every open item — the basis for the trust-fix batch selection.
- Corrected the stale `roadmap.mdx` Searchable-Encrypted-Fields line (the starter's per-tenant HKDF `{ tenantId }` blocker is **resolved as of starter v1.64.0**; only the blind-index primitive remains).

All commits are on `origin/main` in both the module (HEAD `8f5bfbc` = v1.23.0) and `docs/`; working trees clean; `dist` rebuilt after the v1.23.0 bump.

**Unfinished / WIP:** None shipped-but-unpushed. Two v1.23.0 sub-items were **deliberately deferred** (below), not left half-done.

**Loose ends & follow-ups:**
- **Custom Detail Page Layouts** — the flagged **next trust-fix**. Admin pipeline ships + persists but `NodeDetailHeader`/`NodeOverviewTab` ignore the resolved config (same trust-bug class). Needs a **design decision first**: reconcile the two overview-config paths (`navigation.pageNav.builtInTabs.overview.widgetLayout` read vs `templateOverrides.overview.layout` written) — pick one canonical. Documented on the roadmap.
- **`builtInFieldConfig.*.computedTemplate` auto-compute for built-in fields** — a computed built-in is read-only, but its value isn't auto-derived yet (the fiddly react-hook-form + async-resolve piece).
- **Module-starter reconfigure arc** — logged as a module-starter upstream issue (docs `ab68d0b`): no first-class way to toggle an init prompt (e.g. `enableEmbeddings`) during `update` — hand-editing `.module-state.json` is the only path. The module-starter session has since **shipped** the arc (a `reconfigure` command, `onReconfigure` hook, prompt sections + `defaultPrompts`, `update --set`, gate-off env comment-out, path-collision guard, `init --dry-run`) — all scaffold infra, so structure pulls it via `scaffold:update`. **Not yet pulled/validated here** (next session, task A).

**Decisions & ideas:**
- **Chunk table rides the `enableEmbeddings` opt-in** (not a new prompt) — chunks need pgvector, meaningless without embeddings. So an app without embeddings (e.g. veradai) correctly has **no** `structure_chunk` and no schema change on update — expected, not a miss.
- **Extend `structureEmbeddings`** rather than a new `structureChunks` service — reuses vector helpers, one registry entry, one guarded import.
- **Trust-fix framing:** most "roadmap features" were actually *declared-but-dead controls* (admin UI ships + persists behaviour nothing reads) — higher-value to make those real than to add net-new features.
- **Locked built-ins are IGNORED, not rejected, in `updateNode`** — NodeForm always submits `name`, so a throw would break valid edits (unlike the custom-field guard, which sees only changed fields via NodeInfoTab's pendingData).
- **Server-side data sort is in-memory** (materialise matching id+data, sort type-aware, fetch page) — safe for the module's bounded per-user data model, avoids raw-SQL casts throwing on non-numeric rows; opt-in (only when `dataSortField` is set), the common path is unchanged.
- **Escaping gotcha (record it):** a stray literal backtick inside a code *comment* in a `SERVICE_TEMPLATE` backtick string broke the string — `check:templates` only flags unescaped `${`, **not** backticks; `npm run type-check` (tsc on the template file) is what caught it. Always run type-check after template edits.

**Recorded where:** Docs — changelog v1.22.0 + v1.23.0; roadmap (Completed rows, Next-Priority + inert-renderer + conditional-colour + table-view entries, custom-detail promoted with its config-reconciliation decision, searchable-encryption line corrected); `cognitive.mdx` (chunk storage); `integration.mdx` + `registries-guide.mdx` (hook counts + chunk hooks); `module-starter/upstream-issues.mdx` (reconfigure gap). Memory — `chunk-storage-feature`, `trust-fix-v1-23-0` (host-validated), `module-starter-reconfigure-arc`. The roadmap-verification workflow run survives only in the transcript; its sized inventory's load-bearing outcomes are in the roadmap + the trust-fix memory.

**Open items for next session:** (1) **Pull + validate the module-starter reconfigure arc** — `npm run scaffold:check` → `npm run scaffold:update`, then `npm run build` + `npm run type-check` (the real compile check the module-starter repo can't run itself; all infra, so `src/templates/**` + `module.ts` stay untouched). (2) **Custom Detail Page Layouts** (next trust-fix — settle the overview config-path reconciliation first). (3) The larger verified backlog: Bulk Operations, Advanced Filtering + pagination, Public Views core (unblocked), Tree View, Node Merge, NodeSelector inline-create, typed hook contexts. (4) Optional: `builtInFieldConfig` computedTemplate auto-compute for built-ins.

---

## 2026-07-24 — `structure.nodes.bulkCreated` hook-name/payload doc fix + hook-table drift sweep (docs-only) — retroactive audit

**Verdict:** ℹ️ Superseded (work integrated, session now historical) — ✅ safe to archive
**Shipped this session:** A **docs-only** correctness fix to the Node Hooks table in `registries-guide.mdx`. The bulk-create action hook was documented as `structure.node.bulkCreated` with payload `{ userId, nodes, nodeType }`, but the service emits **`structure.nodes.bulkCreated`** (plural namespace) with **`{ userId, count, nodeTypes, nodeIds }`** — a developer copying the doc name into `actionRegistry.addActionAsync(...)` would register a listener that **never fires**. Fixed both the name and the payload to match the call site + the `registrations.ts` reference table. Commits: docs `8eae1c3` + parent pointer `4aa5d25`. **No module version bump** (docs-only, per the versioning rule). Both commits are **ancestors of `origin/main`** in their repos (module HEAD `1a4b359` = v1.21.1; docs origin ahead of `e037e43`). The fix **survives verbatim** in current HEAD (`registries-guide.mdx:738`) and was later propagated into `integration.mdx:734` by a subsequent session.

<Aside type="note" title="Older session — verify the chronology, not HEAD">
The bulkCreated fix was made when the module was at v1.13.1→v1.13.2 (2026-06-24); the repo has since moved far past (HEAD `1a4b359` = **v1.21.1**). Normal forward progress, **not loss**: this session's two commits are both ancestors of `origin/main` in the module and `docs/`, both working trees are clean and synced with `origin`, the fix is live in current HEAD, and `dist` (gitignored) stamps the current **1.21.1** lineage, not this session's. Nothing unfinished, unpushed, or orphaned. This entry also records the *archive-audit action itself* (2026-07-24), during which two residual doc/code drifts were closed (below).
</Aside>

**Unfinished / WIP:** None. The bulkCreated fix shipped and is integrated; the two loose ends this audit found were closed in the same pass (below).

**Loose ends & follow-ups:**
- **CLOSED in this audit — `structure.node.duplicated` wrong payload** (`registries-guide.mdx:735`). Documented as `{ userId, original, duplicate, options }` but emitted (`index.ts:2975`) as `{ userId, sourceNodeId, newNode, includeChildren }`. This session had flagged it via a background chip; a later session fixed `integration.mdx:730` but **missed the registries-guide row**, so it stayed a live copy-paste trap until this audit corrected it.
- **CLOSED in this audit — `structure.node.workflowStateChanged` missing keys** (`registries-guide.mdx:734`). Documented as `{ userId, node, oldState, newState }` but the call site (`index.ts:2661`) also passes `oldGroup`/`newGroup`; `integration.mdx:729` already had them. Added `oldGroup?, newGroup?`. Same class as the bulkCreated bug, surfaced by this audit's code-vs-docs reconcile sweep.
- **HANDED OFF (not fixed here) — three `structure.nodeTypeGroup.*` hooks undocumented.** `created` (`{ userId, nodeTypeGroup }`), `updated` (`{ userId, slug, changes }`), `deleted` (`{ userId, slug }`) are emitted (`index.ts:3333/3364/3393`) and listed in `registrations.ts`, but appear in **neither** `integration.mdx` nor `registries-guide.mdx` hook tables. This is additive doc work (needs a new "Node Type Group Hooks" section), so it was recorded + chipped rather than expanded into this docs-only session. Also: `integration.mdx`'s "**33 total**" hook count is stale (code emits **36** distinct names); `registries-guide.mdx` lists only a subset of connection/nodeType hooks (drops `connection.proposed`/`promoted`, `nodeType.proposed`/`proposalApproved`/`proposalRejected`) — those are covered in `integration.mdx`/`cognitive.mdx`, so not globally missing, but worth aligning.

**Decisions & ideas:**
- **Canonical name is the PLURAL `structure.nodes.bulkCreated` — the doc was the bug, not the code.** Rejected the tempting "align to the singular `structure.node.*` namespace" fix. The plural is **intentional**: this hook fires **once for the whole batch** (only on the `skipHooks` path), distinct from the per-node `structure.node.created`, and its payload reflects the aggregate (`count`, `nodeTypes`, `nodeIds` — no single `node`/`nodeType`). Two independent code sources (the `doActionAll` call site + the `registrations.ts` reference table) already agreed on the plural + payload; the doc was the lone outlier. Renaming the *code* to singular would be a **breaking change to a public event name**, would force rewriting the reference table, and would trigger a version bump — all downside. So the doc was aligned to the code.
- **General lesson (reinforced):** doc hook tables drift from the emitting code, and the failure mode is silent — a listener destructuring the documented-but-wrong keys just gets `undefined`. A cheap code-vs-docs reconcile sweep (grep every `doActionAll(` call site + every doc hook row, diff by name and payload keys) catches the whole class at once; it found the `duplicated` and `workflowStateChanged` drifts and the `nodeTypeGroup` omissions in one pass.

**Recorded where:** Docs — `registries-guide.mdx` (bulkCreated `:738`, duplicated `:735`, workflowStateChanged `:734`) and `integration.mdx:734` (bulkCreated, added by a later session); this archive-audit entry. Memory — none needed (no new durable convention). A follow-up chip was spawned for the undocumented `nodeTypeGroup.*` hooks + stale count. The reconcile-sweep Workflow run details survive only in the transcript; the load-bearing outcomes are in the docs + this entry.

**Open items for next session:** Document the three `structure.nodeTypeGroup.*` hooks in the developer hook tables (`integration.mdx` + a new section in `registries-guide.mdx`), refresh `integration.mdx`'s "33 total" → the true count (36), and optionally align `registries-guide.mdx`'s connection/nodeType hook subset with `integration.mdx`. All are additive documentation, not correctness bugs. (Chip spawned.)

---

## 2026-07-24 — Security feature + scaffold 1.12→1.14 + docs accuracy & health audits (Structure v1.13.0 → v1.13.3) — retroactive audit

**Verdict:** ℹ️ Superseded (work integrated, session now historical) — safe to archive
**Shipped this session:** The **chronologically-oldest work in the whole v1.13 → v1.21.1 arc** — earlier than the v1.14.0 connection-fields entry below (this session laid the foundation that entry completed). Four tracks + one WIP handoff:
- **(1) Security-feature integration (v1.13.0)** — declared Structure's security surface to the starter `securityRegistry`: manifest (6 `owned` dataCategories; `ownedModels: []` since the schema is `userId`-keyed, not `tenantId`) + 6 idempotent, subject-scoped account-deletion handlers (`deleteMany` per store), wired as a `security.registration.ts` side-effect import. No encrypted/hashed fields (no credential columns). Adversarially verified (0 findings). `9501d4f` (feat) + `cd706b1` (release) + docs `0e285e8`.
- **(2) Scaffold 1.12.0 → 1.12.1 → 1.14.0** — three `scaffold:update` merges; confirmed the v1.12.0 **module-owned templates** guarantee (`src/templates/**` + `module.ts` kept as-ours); removed the dead `my-feature` backend + frontend reference templates (`64b0dea`). **Reported three `scaffold:update` rough edges upstream** (self-update auto-push, re-run flag-drop → interactive hang, CLAUDE.md marker conflicts) — all **fixed in scaffold 1.13.0's CI-safe updater** (`-Yes` / `-PushSelfUpdate`), which landed via the 1.14.0 merge here. Shipped as v1.13.1. `0a07638`/`426a66f`/`511a84d` (merges) + `57d1ae0` (release).
- **(3) Docs accuracy audit — 64 verified fixes** across 9 structure doc pages: registry count 11→10 + the **phantom `connectionTypeRegistry`** removed (registries-guide renumbered), field-type counts, renamed/mis-shaped APIs, inert-renderer disclosures, scaffold-propagation refresh. docs `d7e3576`/`c335cd8`/`b9465e6`.
- **(4) Health audit — 23 fixes (v1.13.3)**, four dimensions, **0 critical / 0 security-moderate** (the `userId`-scoping + `requireAdmin` held): atomic `deleteNode` (+ descendant-connection cleanup), `userId` threaded through `calculatePath`/`updateDescendantPaths` + every caller, `dataFilters` validation + input bounds, `listNodes` sends the silently-dropped filter/workflow params, render-loop fix, `NodeSelector` resolve-by-id + debounce, memoised cards, error/retry states, `@@index([userId, path])`; `npm audit fix` → 0 vulns. `7ef0efd`/`a346028`/`e2fabd3` + docs `9d33b27`.
- **(WIP) Connection-fields step 1** — the admin Fields editor on the connection-type form (`a74ea01`, no version bump). Its create-collect + display steps and version bump were **handed to the `connection-fields-feature` memory and completed as v1.14.0** (the entry below). The user validated the v1.13.3 template changes in a live app ("installed perfectly ok").

Every commit from this session — the v1.13.0/v1.13.1/v1.13.3 releases, three scaffold merges, the 64-fix docs pass, and the `a74ea01` WIP — is an **ancestor of `origin/main`** in both the module and `docs/`; the v1.13.0–v1.13.3 changelog entries and this session's roadmap items (Searchable Encrypted Fields, inert-renderer implementation, deferred-perf refinements) are all still live in current docs.

<Aside type="note" title="Oldest work in the arc — verify the chronology, not HEAD">
At the top as the most recent audit *action* (2026-07-24), but this is the **oldest** work in the log — v1.13.0–v1.13.3 (shipped 2026-06-24/25), before even the v1.14.0 entry below. The repo has moved far past (HEAD `b12a4ac` = v1.21.1, scaffold 1.19.1). Normal forward progress, **not loss**: every commit is an ancestor of `origin/main` in both repos, both working trees are clean and synced with `origin`, the features are live in current HEAD code (`security.registration.ts`, the connection-fields foundation `ConnectionFieldInput`), and `dist` (gitignored) stamps the current **1.21.1**, not 1.13.3. Nothing unfinished, unpushed, or orphaned.
</Aside>

**Unfinished / WIP:** None outstanding. Connection-fields step 1 was WIP at session end but was completed and shipped as **v1.14.0** via the `connection-fields-feature` memory handoff (the entry below).

**Loose ends & follow-ups:** (all since closed)
- **Two deferred perf refinements** logged to the roadmap: `getTree` column `select` and raw data-filter-query pagination — correctly deferred (the naive fixes regress type-soundness / paginate the wrong pre-filter set). *`getTree` was later bounded with a `take` cap in v1.21.0; the `select` refinement stays deferred pending a lighter `NodeTreeItem.node` type — per the v1.21.1 entry.*
- **Three residual `scaffold:update` rough edges** reported upstream → **fixed in scaffold 1.13.0** (CI-safe updater).
- **Host-validation** flagged as the definitive check for template changes; the user confirmed the v1.13.3 install worked, and it was later covered by the veradai / use-foundry-ui runs (memory `host-app-validation`).

**Decisions & ideas:**
- **`bidirectional` default is `true` — reconciled with a concurrent code fix.** The accuracy audit flipped the doc to `false` (matching the old `?? false` service fallback); Kim concurrently fixed the *code* to `?? true` in `createConnectionType` — commits `1aa1a97` (fix) + `01209c0` (release **v1.13.2**), both ancestors of `origin/main` and live in current HEAD (`backend/index.ts` L4852). The subtlety that hid the bug: the service always passed an *explicit* `bidirectional`, so Prisma's `@default(true)` never fired and the `?? false` fallback *was* the effective default whenever the field was omitted from a connection-type JSON config; `true` is correct per that `@default(true)` + the admin form's `?? true` create toggle. Reverted the doc to `true` (`b9465e6`). Lesson: the audit correctly *found* every inconsistency but always recommended "doc-to-match-code" — sometimes the better fix is code-to-match-intent.
- **Combobox false-positive → verify shadcn imports against the docs catalogue, not the MCP.** The health audit (via a stale `Shadcn_UI` MCP snapshot) flagged `combobox` as a non-installable build-breaker; the user corrected it (`combobox` is a first-class shadcn component). **This is the origin of that lesson** (memory `host-app-validation`; later cemented as the `shadcn-components.mdx` catalogue — the v1.18.1 entry re-hit the same trap).
- **Searchable-encryption research** (deep, adversarially-verified, prompted by "encrypted but searchable within a user's space"): the honest spectrum — AES-GCM is opaque → **decrypt-in-memory** is the leak-free default for small per-user data → a **per-tenant blind index** buys equality-only on high-entropy fields → SSE for keyword → **reject OPE/ORE** (order → plaintext recovery on low-entropy columns). **Key blocker found: the starter's `encryptField({ tenantId })` param is IGNORED by the env adapter — every tenant currently shares one global key**, so any blind index built today is a single global tag space. Logged as the roadmap **Searchable Encrypted Fields** entry (Under Consideration). *The full crypto explainer survives only in the transcript; the conclusion + the `tenantId`-ignored blocker are in that roadmap entry + here.*
- **Reduced field subset for connection fields** (v1 scope: `id/type/label/description/required/defaultValue/options`, not full node-field parity — suits connection metadata). *Memory `connection-fields-feature`.*
- **Module build can't typecheck generated code** — templates are strings; only `npx structure update` + a starter build validates them. *Memory `validating-template-changes`.*
- **Use the app security feature for secrets** — used the starter's `hashSecret`/`encryptField` for the manifest, not hand-rolled crypto. *(later reinforced + recorded as memory `use-app-security-feature` in the v1.15.0 session.)*

**Recorded where:** Docs — changelog v1.13.0/v1.13.1/v1.13.2/v1.13.3 (all live); roadmap (Searchable Encrypted Fields, inert-renderer implementation, deferred-perf refinements — all still present); the 64-fix accuracy pass across the structure pages. Memory — `validating-template-changes`, `connection-fields-feature` (now SHIPPED v1.14.0), `use-app-security-feature`, and the combobox lesson in `host-app-validation` — all indexed in `MEMORY.md`. The adversarial-review Workflow run details (docs-audit, health-audit, searchable-encryption) survive only in the transcript; the load-bearing outcomes are in code + docs + this entry.

**Open items for next session:** None unique — fully integrated into v1.21.1; the live pick-up list is in the v1.21.1 (2026-07-23) entry and the roadmap.

---

## 2026-07-24 — Connection custom fields + public REST API (Structure v1.14.0 → v1.15.1) — retroactive audit

**Verdict:** ℹ️ Superseded (work integrated, session now historical) — safe to archive
**Shipped this session:** Structure **v1.14.0 → v1.15.1** — the **earliest work in the whole v1.14 → v1.21.1 arc**, the foundation every later pass built on. Three tracks: **(1) Connection custom fields (v1.14.0)** — wired the already-modelled `structure_connection_type.fields` end-to-end: admin Fields editor, collect-on-create in the Add Connection dialog (`ConnectionFieldInput`, required-gating, default seeding), and per-connection value display in an info popover. **(2) Public API-key-authed REST API (v1.15.0)** — new `structure_api_key` table (keys stored only as `hashSecret()` hashes via the security feature, declared `hashedSecrets` + deletion-cascade handler); a public router at `/api/v1/public/structure/:type` (`requiresAuth:false` + its own API-key middleware, opt-in per node type via `features.publicApi.enabled`, `GET`/`GET`-one/`POST`/`PATCH` — **no `DELETE`**, per-key `read`/`write` scopes); session-authed `/api-keys` management; an **API Keys** admin tab + a node-type **Public REST API** toggle; plus the `modelNames` registration fix and a scaffold merge to **1.15.0**. **(3) Auto-prime 429 fix (v1.15.1)** — `primeStructureNavigation()` no longer retries on rate-limit; the backoff is scoped to the `401`/`403` JWT-race it was built for. Commits `72004ca`·`6e14066`·`6f4f4fa` (v1.14.0) · `7cbd6e8`·`fe169d9`·`cc812c6`·`0a859b2` (v1.15.0 + scaffold) · `91cc93f`·`9085976` (v1.15.1) · `cf0ec82`; docs `7b687b6`·`21837e4`·`d66060d`·`abd6de7`. **All 10 module + 4 docs commits are ancestors of `origin/main`** (module HEAD `880f5e4` = v1.21.1; docs HEAD `313389c`).

<Aside type="note" title="Earliest work in the arc — verify the chronology, not HEAD">
Placed at the top as the most recent audit *action* (2026-07-24), but this is the **oldest** work in the log — v1.14.0–v1.15.1 (shipped 2026-06-25/26), chronologically *before* even the v1.15.3 pass below it. The repo has moved far past (HEAD = v1.21.1). Normal forward progress, **not loss**: every one of this session's commits is an ancestor of `origin/main` in both the module and `docs/`, both working trees are clean and even with `origin`, the features are all live in current HEAD code (`structure_api_key` model, the `/public/structure` router, `ConnectionFieldInput`, the `401`/`403` auto-prime guard), and `dist` (gitignored) stamps the current **1.21.1**, not 1.15.1. Nothing here is unfinished, unpushed, or orphaned.
</Aside>

**Unfinished / WIP:** None. Everything committed, pushed, and absorbed into the v1.15.3 → v1.21.1 lineage.

**Loose ends & follow-ups:** (flagged at the time; all since closed or moot)
- **QueryClient retries on `429` (the app-side amplifier)** — logged as a starter **Upstream Issue**; **since RESOLVED in starter v1.54.0** (that entry now sits in the page's *Completed* section). The module half shipped here as the v1.15.1 auto-prime guard.
- **KMS / `hashSecret` in `hardened` mode** — the user's dev app ran the `hardened` profile with field-encryption on but KMS unwired (*"encryptField/decryptField will throw on first use"*). I flagged that *if* `hashSecret` were keyed it could throw on API-key creation and asked to hear back. Per the docs `hashSecret` is plain SHA-256 (not keyed, not tier-gated), so it's unaffected — and the api-key feature is intact through v1.21.1, so it never bit. Env setup, not the module.
- **Definitive host-validation** (`npx structure update` + a starter build) — flagged as the real check for template changes; subsequently covered by the host-validation runs in the later v1.18.1 / v1.19–v1.21.1 sessions (veradai / use-foundry-ui).
- Minor housekeeping: a leftover git worktree `.claude/worktrees/sweet-gauss-78a5e6` was noted for removal.

**Decisions & ideas:**
- **Use the app security feature for secrets — not hand-rolled crypto.** I first proposed `node:crypto` SHA-256 for API keys; the user pushed back (*"why aren't we using the app security?"*). Switched to `hashSecret`/`verifySecret` from `../security/index.js` (a deterministic SHA-256, so direct hash-lookup still works) + declared `hashedSecrets`. *Recorded in memory `use-app-security-feature`.*
- **Public router at a SIBLING path `/public/structure`, not nested `/structure/public`** — so the session-authed `/structure` router can never prefix-shadow it. `requiresAuth:false` was flagged as the one unverified starter assumption; later confirmed supported (first-class `RouteConfig.requiresAuth`; the app boot log showed `publicRoutes` mounted cleanly).
- **Public-API scope (user's calls):** opt-in per node type (unknown/non-exposed → **404**, no existence leak); per-key `read` vs `read`+`write`; **no `DELETE`**.
- **Field-level validation is NOT server-enforced on public write** (same as the internal API) — the *honest* claim after an adversarial-review lens flagged the original "reuses all field/constraint validation" comment/doc as false; `data` JSON is size-bounded (100 KB) as cheap hardening.
- **GET-one strips nested `children`** — an adversarial-review **major**: `getNode` eagerly includes active children, which could leak instances of *non-exposed* child types under an exposed parent.
- **The 429 storm was the starter's global rate-limiter, not a runaway fetch.** Verified the `onSync → registerStructureNavigation` chain only *reads* the registry (no refetch) and the auto-prime is bounded (4 attempts). The real bug it surfaced: auto-prime (and the hook queries) *retried on 429*, deepening an exhausted limiter during dev testing. Fixed the module half; the query half is the app's `QueryClient` (the upstream issue above). Agreed with the starter-side chat: **keep the limiter, raise the dev ceiling — don't bypass** (the 429 is a useful signal).
- **System-default (`userId=null`) types with `publicApi.enabled` expose the endpoint for every account** (instances stay `userId`-scoped) — *documented* rather than gated, because default types are commonly `userId=null` and gating to user-owned-only would break the common setup.
- **Snyk skipped** — the user's free account can't auth the MCP and they're disconnecting it. *Memory `snyk-scan-skip`.*
- **Gotcha (record it):** an Edit `replace_all` of `data: z.record(...).optional()` over-matched into `createNodeSchema`/`updateNodeSchema`/`createConnectionSchema` (a common line) — caught by `grep` and reverted. Be wary of `replace_all` on common zod lines.

**Recorded where:** Docs — changelog (v1.14.0 / v1.15.0 / v1.15.1), roadmap *Completed* rows (custom connection fields; public REST API), `usage.mdx`, `configuration.mdx` (API-keys endpoints), the new `public-api.mdx`, and `starter-template/upstream-issues.mdx` (QueryClient-429, now *Completed*). Memory — `connection-fields-feature` (SHIPPED v1.14.0), `public-node-type-api` (SHIPPED v1.15.0), `use-app-security-feature`, `snyk-scan-skip` — all in `MEMORY.md`. Each verification pass this session used a 3-lens adversarial-review Workflow (caught the `useConnectionTypes` import-path blocker, the GET-one child leak, the false-validation claim, a duplicate-key React bug); those run details survive only in the transcript, but the load-bearing fixes are all in the code + this entry.

**Open items for next session:** None unique — fully integrated into v1.21.1; the live pick-up list is in the v1.21.1 (2026-07-23) entry and the roadmap. The two follow-ups this session raised (QueryClient-429; host-validation) are both since closed.

---

## 2026-07-24 — Structure v1.15.3 standards-alignment audit (retroactive; the foundation of the v1.18–v1.21.1 arc)

**Verdict:** ℹ️ Superseded (work integrated, session now historical) — safe to archive
**Shipped this session:** Structure **v1.15.3** — the *first* standards-alignment pass, on top of which every later alignment session was built. Four batches: **(A)** scaffold **1.17.0 → 1.18.0** (`scaffold:update --merge`; module contract v1.1.0 → v1.2.0; per-module DB migration runner `src/scaffold/migrations.ts` + `_module_migrations` table + `migrate-db` CLI; `migrations?` field left empty — opt-in; `esbuild`/`postgres` deps). **(B)** Fixed the `field-group` renderer emitting over-escaped `\${itemIdx+1}` / `\${field.label}` as literal JSX *text* (→ real JSX expressions), and added the missing `structure.nodeTypeGroup.created/updated/deleted` action hooks. **(C)** The §5 structure-only-styling sweep — ~218 forbidden classes removed from `frontend/pages.ts` + `components.ts` (typography scale, font weights, shadows, uppercase/tracking/italic), colour literals → semantic tokens, 3 `!important` Dialog/Sheet width overrides removed, ~50 emptied `className` stripped. **(D)** Four `@@index` on the `structure_node_type` template-FK columns + stray `@generated-by` removed. Commits `0bde5b9` (scaffold merge) + `96feadd` (batches B–D) + `67d0c37` (release v1.15.3) + `e0a8f2d` (docs pointer), plus docs `804797c`. All are **ancestors of `origin/main`** (HEAD `4e8abd3`, now v1.21.1), and `67d0c37` is an **ancestor of v1.18.0's `59cb6c0`** — this pass genuinely sits beneath the entire later arc.

<Aside type="note" title="Earliest work in the arc — verify the chronology, not HEAD">
Placed at the top as the most recent audit *action* (2026-07-24), but the work is the **oldest** alignment pass — v1.15.3, chronologically *before* the Type Hierarchy v1.18.0, v1.18.1, and v1.19–v1.21.1 entries below. The repo has moved far past it (HEAD `4e8abd3` = v1.21.1; scaffold now 1.19.1). That is normal forward progress, **not loss**: every commit from this session is an ancestor of `origin/main` in both the module and `docs/`, both working trees are clean and even with `origin`, and `dist` (gitignored) stamps the current **1.21.1**, not 1.15.3. Nothing here is unfinished, unpushed, or orphaned.
</Aside>

**Unfinished / WIP:** None. Everything committed, pushed, and absorbed into the v1.16 → v1.21.1 lineage. Both working trees (module + `docs/`) clean and even with `origin`. `dist` is gitignored and current (1.21.1, rebuilt after later bumps).

**Loose ends & follow-ups:** All since resolved by later sessions —
- **Deferred at the time:** the live host-app check (swept-JSX compile + `/admin/modules` row) was handed to the user because no clean, version-matched, non-WIP app was available — veradai was on the old 1.37.0 starter (no modules feature); use-foundry-ui (1.50.2) was mid multi-module `module.registration` migration (unsafe to mutate). *Subsequently covered* by the host-validation runs in the v1.18.1 and v1.19–v1.21.1 sessions (`tsc --noEmit -p tsconfig.json` in veradai / use-foundry-ui — see memory `host-app-validation`).
- The 3 `!important` → responsive-width rewrites (`sm:max-w-2xl`; `w-full sm:w-1/3 sm:max-w-[33vw]`) and the colour-token swaps (favourite-star / copied-check → `text-primary`, amber warning → `text-muted-foreground`) were flagged as needing eyeball QA; the §7 opacity + §8 composition sweeps in v1.18.1 continued this styling work (roadmap notes "narrow §7 opacity residue remains").

**Decisions & ideas:**
- **Skipped the ~90-header `// DESCRIPTION:` churn — it was a *non-gap*, not an omission.** A sub-agent flagged every generated file as missing the three-field header per LLM-rules §1, but the canonical module-starter templates themselves use **two-field** headers (`// FILE:` + `// PURPOSE:` — 53× PURPOSE vs 2× DESCRIPTION); three-field is a *core / hand-written-starter* convention (e.g. `user.routes.ts`). Structure already matched the module convention, so adding DESCRIPTION everywhere would have made it the lone diverging module. *Recorded in memory `module-header-convention` so no future audit re-raises it — and it is now cited by the later v1.18.1 audit's own decision list.*
- **`module.registration.ts` + `security.registration.ts` were already present** (shipped v1.15.2 / earlier) — Step 2 and the security manifest needed no work; confirmed via `update --dry-run`, which *would* add `module.registration.ts` to an app that lacks it (proving the runtime-registry wiring end-to-end).
- Minor calls shipped in v1.15.3: kept `module.ts` as-ours through the scaffold merge; regenerated `package-lock.json` via `--package-lock-only`; added the 4 template-FK indexes (low value but rule-consistent on a tiny table). The colour-token choices (star/check → `text-primary`) are design calls that later styling passes were free to revisit.

**Recorded where:** Roadmap — this session's items are all present and marked **✅ Resolved** with shipped versions + line refs (field-group `${…}` "Resolved (v1.15.3, reconfirmed 2026-07-19)"; node-type-group hooks "Resolved (v1.15.3)" L3124/L3155/L3184; styling sweep "Resolved" with a §7-residue note; scaffold "Resolved (v1.16.0)"). Memory — `alignment-v1-15-3` (this pass) and `module-header-convention` (the two-field-header finding), both live in `MEMORY.md` and referenced by the later audits.

**Open items for next session:** None unique to this session — its alignment is fully integrated into v1.21.1. The live pick-up list is in the v1.21.1 entry below and the roadmap (`inputMask`, `editableBy` wiring, Bulk Operations, Advanced Filtering, Public Views, Tree View, pagination controls).

---

## 2026-07-24 — Structure v1.18.1 standards-alignment audit (retroactive; work shipped 2026-07-19)

**Verdict:** ✅ Safe to archive
**Shipped this session:** Structure **v1.18.1** — a standards-alignment pass against the then-current LLM rules. Adopted scaffold **1.18.2 → 1.19.1** (`scaffold:update --merge --yes`; module-owned `src/templates/**` + `module.ts` preserved by the merge). §8 composition cleanup in `frontend/pages.ts`: 6 hand-rolled empty/error states → page-blocks `EmptyState`, admin 6-button tab bar → shadcn `Tabs`, 2 raw `<textarea>` → `Textarea`, connection-loading skeleton → `Skeleton`, detail sidebar nav → ghost `Button` (with `Card`/`cn`/`Link` imports cleaned up for `noUnusedLocals`). §7 opacity sweep: 19/22 modifiers → solid `bg-muted` / `border-border`. Three covering compound indexes: `structure_node [parentId, sortOrder]`, `structure_template [userId, sortOrder]`, `structure_api_key [userId, createdAt]`. Dropped the undocumented `version` field from `serviceRegistry.register`. Commits `aaf7dd7` (fixes) + `8c2a414` (release v1.18.1) + `fa96e08` (scaffold merge) + docs `57893f3`/`fb7ee3f` — all pushed.

<Aside type="note" title="This is the foundation of v1.21.1 — verify the chronology">
Placed at the top because it is the most recent audit *action* (2026-07-24), but the work shipped **2026-07-19** as v1.18.1 — *before* the v1.19.0→v1.21.1 arc in the entries below. Verified this is fully integrated, not orphaned: all four commits are **ancestors of the current v1.21.1 HEAD** (`73b74b3`) — the later sessions built v1.19–v1.21.1 on top of this pass and re-host-validated the changes. Both working trees (module + `docs/`) are clean and synced with `origin`; `dist` is gitignored and stamps `1.21.1` (rebuilt 14:23, after the 14:19 release commit). Nothing from this session is unfinished, unpushed, or at risk.
</Aside>

**Unfinished / WIP:** None. Everything committed, pushed, and absorbed into the v1.21.1 lineage.

**Loose ends & follow-ups:** (recorded at the time; most since resolved by v1.19–v1.21.1)
- **Deferred then:** `useResolvedTemplate` → TanStack Query (localized dynamic render-time ref resolution — judged justified); bare `<h2>/<h3>` → `SectionHeader` (LOW, already-semantic, restructure risk); offset → cursor pagination on the admin/review surfaces. *(Pagination controls are still tracked open in the v1.21.1 roadmap.)*
- **Kept as documented exceptions** (not violations): `bg-background/95` backdrop-blur sticky header; `bg-destructive/90` — the canonical shadcn destructive-variant string on `AlertDialogAction` (which has no `variant` prop, and `buttonVariants` was unconfirmed as a host export).
- Recommended a **test-install** to eyeball rendered output; subsequently covered by the v1.19–v1.21.1 host-validation runs in veradai.

**Decisions & ideas:**
- **The combobox false-positive that led to the shadcn catalogue.** An audit sub-agent's `Shadcn_UI` MCP snapshot was stale (46 components, no `combobox`), producing a false "build-breaking import" finding — `combobox` is a first-class installable shadcn component (`npx shadcn add combobox`). Lesson: verify `@/components/ui/*` imports against the docs' `starter-template/shadcn-components.mdx` catalogue, not the MCP. *(That catalogue page now exists.)*
- **Self-referential `@relation` on `structure_node` is not a DB-rule violation** — Prisma requires named relations for the hierarchy/connection self-joins and they touch no external model (self-contained; rule intent satisfied). Recorded so a future audit doesn't re-flag it.
- **The kept opacity idioms are design-layer, not bespoke effects** — solidifying the backdrop-blur header or the destructive-variant string degrades them; the module defers styling to the Style module rather than owning a design-layer utility.
- **Host-app type-check method:** `tsc --noEmit -p tsconfig.json` — *not* the app's `tsc -b --noEmit` script, which throws `TS6310` (referenced project may not disable emit) and never checks source. At the time, use-foundry-ui (structure v1.15.0) had **0** `features/structure` type errors — all errors were in `features/builder/**` + 2 starter components.

- **This audit caught a bug in its own prompt — and fixed it.** Auditing this now-older session, the check first misread a truncated `reflog` window as orphaned/lost work (a false "incident") before `git merge-base --is-ancestor` proved every commit is an ancestor of the current v1.21.1 HEAD. Root cause: the prompt implicitly assumed the audited session sits at HEAD. Fixed **in this session** by hardening the Archive Audit prompt + entry template on this page — the ancestor-of-`origin/main` test, a current-HEAD `dist` check, and an **ℹ️ Superseded** verdict — so future older-session audits skip the scare.

**Recorded where:** Roadmap — the "v1.18.x standards-alignment audit (2026-07-19)" entry (live; the later v1.19–v1.21 passes kept it as history). Memory — `alignment-v1-18-1` and `host-app-validation`. The **Archive Audit prompt + entry template on this page were hardened this session** (see the closing decision above) — that improvement is live in the prompt itself, not just recorded here.

**Open items for next session:** None unique to this session — its alignment is fully absorbed into v1.21.1; the live pick-up list is in the v1.21.1 entry below and the roadmap.

---

## 2026-07-24 — Type Hierarchy (Structure v1.18.0) — retroactive audit

**Verdict:** ✅ Safe to archive
**Shipped this session:** Structure **v1.18.0** — Type Hierarchy (is-a inheritance for node types). Commits `59cb6c0` (feat) + `cf0655f` (release), both dated 2026-07-16; docs `7ce01e7`. All pushed. `structure_node_type.superClasses` (multiple-inheritance DAG), `resolved` config on `/types`, subsumption-aware connection allowlists + the `'*'` wildcard fix, `includeSubtypes` on node listing, four new service methods (`getTypeAncestors`/`getTypeDescendants`/`isSubtypeOf`/`expandTypeSlugs`), admin UI (Type Inheritance picker + inherited-fields preview), import/export/proposals, delete guard, hierarchy-aware embedding selection. Ran the full playbook: 6-reader understanding → design brief + 3-critic critique (30 findings folded) → single-writer implement → 16-agent adversarial review (**10 confirmed findings, all fixed before push**) → gates green.

<Aside type="note" title="Out-of-order entry — read the dates">
This entry is at the top because it's the most recent audit *action* (run 2026-07-24), but the **work shipped 2026-07-16** as v1.18.0 — chronologically *before* the v1.19.0→v1.21.1 entry below it. The archive-audit page didn't exist when v1.18.0 shipped, so this is a retroactive entry. The module has since advanced to v1.21.1 (entry below) **on top of** this Type Hierarchy work, which is intact in history (`superClasses` still in the schema, 52 backend references) and battle-tested by that downstream work.
</Aside>

**Unfinished / WIP:** None in Structure. Working trees clean in the module and the `docs/` submodule, everything pushed (`main...origin/main` even). `dist/` is gitignored in both repos (never committed → not at risk from archiving); current on-disk `dist` matches HEAD (1.21.1). No coded-not-validated work — all gates passed (`check:templates`, `type-check`, `build`, a 96-file render-check, docs build with link check).

**Loose ends & follow-ups:** The two live items are **cross-repo, in `cognitive-ingest`, and were NOT fixed here** — only captured:
- **RSS event mismatch (real bug, unfixed).** `cognitive-ingest/src/templates/backend/services.ts` emits `my-feature.source.created/updated/deleted` via `actionRegistry.doAction` (`:251/:268/:280`), which *skips async subscribers*, while `rss.ts` subscribes to those exact hooks via `addActionAsync` (`:60/:72/:85`). So dynamic RSS polling start/stop on source changes likely never fires — polling only reconciles at app restart. Fix: emit via `doActionAll` (as `working-memory.ts:217` already does) or register the RSS listeners sync. Recorded in memory **and** a spawned task chip (`task_db0ca085`) — **but the chip is ephemeral (lost on app restart); the memory note is the durable record.**
- **Refactor idempotency note.** On host starter **≥ v1.54.0**, `addActionAsync` handlers on core `user.*`/`admin.*` hooks start firing (were silently skipped), so any such handlers the cog-ingest refactor registers must be idempotent; v1.54.0's new dev-mode warning flags exactly the `doAction`-skips-async mismatch above. Recorded in memory.

**Decisions & ideas:**
- **BFS linearisation, not C3.** Ancestor order is BFS (nearest-first, declaration order, dedupe-keep-first). Chosen because it's *total and never throws on read paths* — it runs on every `/types` response, so a hot path must not fail on an awkward diamond the way C3 would reject one. The cost (a generic parent can shadow a more specific one when both are declared directly) is bought back by **rejecting redundant parents at declaration time**. *This "why not C3" rationale lives only in the design brief/critique + this transcript, not in the public docs* (docs describe BFS behaviour, not the trade-off).
- **Only `fields` + `features` inherit** (deliberate v1 scope) — rules/workflow/context/navigation/templates/permissions/etc. explicitly excluded to keep the resolution contract small and predictable. *(In docs — "not inherited" table.)*
- **Redundancy rejected at declaration-time only.** Redundancy arising *later* (via edits to other types) is tolerated — otherwise the system would refuse to re-import its own exports, and an unrelated save would be blocked by a redundancy the admin didn't create. Surfaced by the adversarial review and fixed. *(In docs — validation section.)*
- **`'*'` wildcard fix** shipped as a deliberate, changelogged behaviour change (it was documented-but-dead and silently blocked every connection using it). *(In docs — changelog Aside.)*
- **`publicApi` deliberately NOT hierarchy-aware** — a subtype of a publicly-exposed type is not auto-exposed (security). *(In docs.)*
- **Concurrency cycle race accepted** for the solo-admin model — traversal is seen-set-guarded so it always terminates; a DB constraint isn't worth it. *(In docs.)*
- **Rejected/refuted (record so it isn't re-raised):** review finding "slug-keyed type graph drops a row when a shared and a user-scoped type share a slug" — **refuted**: the create paths prevent a slug from coexisting across scopes, so the collision can't arise. Not a bug.
- **Review fixes worth remembering:** removed a silent `MAX_TYPE_ANCESTORS=100` cap that truncated traversal and opened a cycle-detection hole (the seen-set already guarantees termination); made `resolveTypeInGraph` defensive against non-array `fields` JSON (was an app-wide `/types` 500 via the lenient import path); scoped the `deleteNodeType` reference scan to the delete's blast radius (unscoped for shared rows, user-scoped for user rows, since slugs are per-user).

**Recorded where:** Docs — new `type-hierarchy.mdx` (guide) + `configuration`, `json-reference`, `integration`, `changelog` (v1.18.0 entry w/ behaviour-change Aside), `roadmap`, and `cognitive-db/v2-rfc.md` (ontology-mapping row); all in docs `7ce01e7`. Memory — `project_v2-phase3-engine-retarget.md` (TH-shipped status + both cog-ingest follow-up notes) and the `MEMORY.md` index line. Session scratchpad (design brief, 6 understanding reports, 3 critiques, `render-check.mjs`) is **session-local and lost on archive** — the load-bearing conclusions from it are in docs/memory/this entry, but the full brief/critique detail survives only in the transcript.

**Open items for next session:**
- **Phase 3 wiring** (in `cognitive-ingest`) is the next track — unblocked by this release; `instanceOf` can now resolve to real subtype levels instead of `data.subtype` strings.
- **Fix the cog-ingest RSS `doAction`→`doActionAll` mismatch** (above) — do it before/during the refactor; don't rely on the ephemeral task chip.
- **Make cog-ingest async handlers on core hooks idempotent** for host starter ≥ v1.54.0.
- Deferred (not this session): normalized-name/alias index on `structure_node` (engine dedup stages 2/3); the **Node Merge** primitive; **Deep Perception** (engine track).

---

## 2026-07-23 — Meta audit → roadmap verification → module-owned field types

**Verdict:** ✅ Safe to archive
**Shipped this session:** Structure **v1.19.0 → v1.21.1** (all pushed). A four-dimension meta audit (171 verified findings) fixed across batches A–H; 74 latent host type-errors cleared; roadmap verified against the code and found wrong in *both* directions; then Table View, server-side field filtering, three field renderers, five latent bugs, module-owned field types, and removal of the built-in `media` type. Host-validated in veradai (starter v1.64.1) at every step — 0 structure `tsc` errors, backend and frontend.

**Unfinished / WIP:** None. All working trees clean (module + `docs/` submodule), everything pushed, veradai synced to v1.21.1 with the correct version label.

**Loose ends & follow-ups:**
- veradai was left **on** v1.21.1 (upgraded, not reverted) at the user's request — a change from earlier in the session where it was reverted to pristine after each validation.
- `dataSortField` / `dataSortType` are accepted and validated by the API but remain a **no-op in the service** (server-side JSON sort). Table View sorts client-side over the current page only.
- `NodeListPage` sends no `page` / `limit`, so the backend's default limit of 50 silently truncates — pagination controls are still needed.
- `renderCompact` (NodeColumnRegistration) is declared but unread.

**Decisions & ideas:**
- **Field types are module-owned; `media` is not built in.** The user's insight — "why not have modules register field types entirely?" — was correct: `nodeFieldTypeRegistry` already had the full contract and the renderer already delegated to it, so the capability was ~80% built. `media` was removed rather than kept as a disabled/URL-fallback built-in. Shipped as a **minor with a loud breaking note, not a major**, because nothing is in production and the deprecation cycle was explicitly waived.
- **The short-lived `nodeMediaUploadRegistry` (v1.20.0 only) was removed** in v1.21.0 as redundant — a full field-type registration covers upload UI + config + display in one mechanism instead of two.
- **Unknown-field safety was a prerequisite, not a nicety.** Once field types are module-owned, uninstalling a module orphans fields; the old code bound unknown types to a text input that overwrote arrays/objects with a string. Fixed to render read-only with the value preserved before making types module-owned.
- **`getTree` bounded, not `select`-narrowed.** Added a `take` cap (depth-ascending, so truncation can't orphan children); the heavier `select` refinement stays deferred because it needs a lighter `NodeTreeItem.node` type than the public `StructureNode`.

**Recorded where:** Roadmap (`Known Issues`, `Planned Improvements`, `Next Priority`, `Completed`) rewritten for accuracy both directions; full changelog entries v1.19.0 / v1.19.1 / v1.19.2 / v1.20.0 / v1.21.0 / v1.21.1; behaviour corrections across `fields`, `configuration`, `usage`, `integration`, `registries-guide`, `public-api`, `merge-fields`, `index`. Memory: `meta-audit-2026-07-20`, `roadmap-verification-2026-07-23` (+ their findings artefacts).

**Open items for next session:**
- **`builtInFieldConfig.{name,description,slug}` editableBy is unwired** — declared, admin-configurable, persisted, but nothing reads it. Flagged **Next Priority** on the roadmap. The Name/Description/Slug inputs render unconditionally and the backend update allowlist sets all three with no permission check.
- **`inputMask`** — the one field property still genuinely inert (no format-on-type). Build it or drop the property.
- **Bulk Operations UI** — no selection state, no bulk archive/delete/move **endpoints** (only bulk *create*); `useBulkActions` is dead code.
- **Advanced Filtering UI** — text/date/number range filters, saved presets, URL-param filter state; plus the pagination controls above.
- **Public Views** — `publishedSlug` generation, an unauthenticated `/p/:slug` route + read-only page, scheduled publishing. ("View Public" button was removed until this lands.)
- **Tree View** — `StructureTree` isn't rendered anywhere internally (no `tree` view option, `sidebarNav.style === 'tree'` unimplemented).
- **Connection-type toast labels** — node toasts now name the type; `useConnectionMutations` still says "Connection".
- **Gotcha to remember:** `npm run version:*` does **not** rebuild `dist` — always `npm run build` after a version bump, or the linked CLI ships new content under the old version label (hit three times this session).
