---
title: Roadmap
description: What is outstanding for the Structure module — the ranked Now/Next queue, the Later backlog, the open decisions, and the shipped record.
lastUpdated: 2026-08-20
sidebar:
  order: 11
---

import { Aside } from '@astrojs/starlight/components';

This page answers one question: **what is outstanding?** [Now / Next](#now--next) is the ranked, scheduled queue; [Later](#later) is real but unscheduled; [Decisions needed](#decisions-needed) are calls waiting on a person, not on work; [Parked](#parked--not-planned) is deliberately not on the queue; [Shipped](#shipped) is the release record; [Archive](#archive) holds closed audits, kept for provenance only. Anything not on this page is either shipped or abandoned.

Structure is the **admin-configurable architecture** an app is built from — the data model and organisation, not any one app's domain. What belongs here versus in Document Management or cog-ingest is set out under [Scope boundaries](#scope-boundaries) below, and argued in [Design Decisions](/modules/structure/decisions/).

<Aside type="note">
Every claim below was re-verified against `src/templates/**` on 2026-08-20. The four items that first led the queue have since shipped — formula traversal + list functions and built-in-field formulas (**v1.36.0**), typed action/filter hook contexts (**v1.36.1**), NodeSelector inline quick-create + a dialog variant (**v1.36.2**), and module-registered bulk actions in the list toolbar (**v1.36.3**) — all now under [Shipped](#shipped), alongside the cross-module `getActiveScope` retrieval seam (**v1.36.4**) added for cog-ingest. Where an item cites a file and line, that line is from the module templates at **v1.36.4**, not from a host app.
</Aside>

---

## Now / Next

**The ranked queue is clear.** Every item that survived the 2026-08-20 reconciliation of this page against the code has shipped: the integrity defect (v1.34.28), then formula traversal (v1.36.0), typed hook contexts (v1.36.1), NodeSelector quick-create (v1.36.2), bulk-action rendering (v1.36.3), the destructive-action confirmation unification (v1.36.5), a five-item sweep (v1.36.6–v1.36.7: merge-field admin gaps, sibling drag-reorder, the App Structure `PageHeader`, the `node-reference` `TypeListField` picker, the false public-API comment), and finally the `nodeFilterRegistry` list-filter wiring (v1.36.8) — which turned out **not** to need the backend param contract it looked like it would, since routing server-mode filters through the existing `DataFilter` engine covers it end-to-end.

Nothing is currently scheduled. The next work comes from [Later](#later) (real but unscheduled) or the open [Decisions](#decisions-needed). The ranking constraint still holds: cog-ingest is paused until Structure and AI Core reach a stopping point, then is built against whatever exists here — so the cross-module contract surface (typed hooks, `getActiveScope`, `getUnreadableTypes`) is the part most worth keeping stable.

---

## Scope boundaries

| Module | Owns |
|--------|------|
| **Structure** | Hierarchy/organisation, the field/type system, enforced scoping (spaces), server-side write-path validation, the audit-event contract. (**Node-record revisions are not Structure's** — dropped 2026-08-20 as likely document-management's; see [Parked](#parked--not-planned).) |
| **Document Management** | File upload/storage/dispatch; file chain-of-custody — read-only providers, SHA-256 `contentHash`, recent-access log, `resolveProvenance`, file versioning. |
| **cog-ingest** | Ingests data → runs the engine → **stores the output back into Structure** (via `serviceRegistry`, into `editableBy:'system'` fields). |

Tamper-evident hashing and chain-of-custody for **files** already live in document-management; Structure never hashes file bytes. The reasoning behind this split, and the settled parameters D1–D11 that follow from it, are in [Design Decisions](/modules/structure/decisions/).

---

## Later

Real, verified, and not scheduled. Nothing here is blocked on anything in [Now / Next](#now--next) except where noted.

### Admin UI

- **`NodeTypeForm` / `FieldSetEditor` decomposition.** One root state object drives ~28 sections across seven sub-tabs, and the field card is still a large inline `Collapsible` closing over a dozen locals, so every keystroke re-renders the whole form. Extract the field card into a `memo()`'d component and split the form state. *(The `mergeTokens` half of the original performance finding is already fixed — it is memoised on the fields, integrations and slug.)*
- **Admin list-row unification, and the non-functional field-row drag handle.** A `GripVertical` is drawn inside the field card's trigger with no drag sensors attached — a control that looks draggable and is not; a second decorative grip sits on the type list. Separately, every admin tab re-implements its own row markup (Groups, Connection Types, Field Templates, Functions). Either wire the handle to dnd-kit or remove it, and extract one shared admin list row.
- **Group the Structure Types list by navigation group.** The Types list is a flat sorted/DnD list — `groupSlug` appears in the Groups tab editor and the node-type form's Basic tab, never in the Types list render. Collapsible group sections mirroring the sidebar would make a long type list navigable.

### Features

- **Node templates.** Admin-defined per-type templates that pre-fill a new node's fields, with a picker in the create Sheet. No such concept exists today: `structure_template` is the **component** template table (card / listItem / detailHeader / overview), not a field-prefill template, and `NodeForm` offers no template picker.
- ~~**Node merge.** `mergeNode(fromId, intoId)`~~ — **shipped v1.36.13** (service primitive: re-point edges, re-parent children, backfill empty `data`, archive/soft-delete the loser, `structure.node.merged` hook). A **user-facing** merge (a UI affordance + an HTTP route + a two-node picker) is still unbuilt — the shipped method is serviceRegistry-only, for cog-ingest dedup.
- **Published nodes — public read-only views.** Half-built: `publishedSlug` is stored and writable and both the published/unpublished hooks fire, but nothing serves it — the route registration has no `/p/` entry and there is no `PublicNodePage` (the component code documents that absence as the reason the "View Public" menu item is not rendered). Scheduled publishing does not exist either.

### Lists, tree and bulk operations

- **Tree view — drag-to-reparent and inline child creation.** `TreeRow` renders chevron + icon + name only: no drop zones (the components file has no dnd-kit import at all) and no "+ child" affordance. `moveNode` already enforces hierarchy rules and the cross-space gate, so the backend half is done. Parked once already.
- **Bulk move.** No bulk-move surface exists; the bulk endpoints are archive, delete and restore, plus create for nodes and connections. Needs an endpoint and a target-parent picker in the selection toolbar, reusing the partial-success and per-row error shape the other three already return.

### Config promotion and data lifecycle

- **Node-instance import/export (CSV/JSON).** Zero case-insensitive matches for "csv" anywhere in `src/`. Import/export today is config-bundle only — both `/export` routes are admin-only node-**type** bundles. The only instance bulk surface is `bulkCreateNodes`, a JSON array of already-shaped inputs with no column mapping, no parsing and no per-row error UI. Needs a column-to-field mapping step, per-row validation through `validateNodeData` and `assertNodePermission`, an error report, and a list export. *(This one entry collapses four separate records.)*
- **Per-definition config versioning.** `exportConfigBundle` emits a single envelope-level `version: '1.0'` plus `exportedAt` for the whole bundle; no node type, connection type, field template, group or function carries a version of its own. So a promoted bundle cannot be reconciled item by item — the dry-run diff can say created/updated/deleted, never "target is newer" or "unchanged".
- **Retention / purge for soft-deleted rows.** No retention or purge surface exists. Soft delete sets `status='deleted'` and rows persist indefinitely; there is no scheduled or admin-triggered hard delete, and no retention config on the settings table or the node type. Mirror document-management's artefact-retention pattern. (Previously sequenced after node revisions; that dependency is gone now revisions are [parked](#parked--not-planned), so this stands on its own.)

### Performance

- **jsonb GIN + path `pattern_ops` indexing.** The path half already shipped: `rootNodeId` equality indexes replaced path-prefix scanning on the scoping hot paths, which is what actually mattered. Two halves remain — no GIN on `structure_node.data` (the filter builder still emits `"data"->>field` with a bound param, and the only GIN indexes in the migrations are the full-text functional ones, useless for `dataFilters`), and the `path` btree indexes have no `text_pattern_ops` variant, so `LIKE 'prefix%'` will not use them on a non-C-collation database. The migration file records a deliberate reason the functional-index route was deferred: settle the query shape first.
- **Boot-prime skip for anonymous visitors.** `primeStructureNavigation()` is called unconditionally at module scope and the catch retries on 401/403 while `attempt < 3`, so a logged-out visitor fires up to **four** doomed requests. *(This page previously said eight — that figure predates the retry cap.)* The query-cache seeding half is already done. Needs a reliable client-side session signal, or a host hook.

### Computed fields and search

- **Aggregation over children and connections.** No dedicated aggregate primitive exists yet, but the layer beneath it now does: [v1.36.0](/modules/structure/changelog/#v1360) shipped the traversal forms (`children`/`parents`/`scope`/`nodes`) and list functions (`map`/`filter`/`sum`/`count`/…) with a client-side I/O budget (`MAX_FETCHES`), so `sum(pluck(children(), 'amount'))` already aggregates. What remains is the ergonomic + connection half — a registered `sumChildren('field')`-style helper and aggregation over *connections* (the list functions walk hierarchy and reference fields, not connection edges) — the arithmetic layer on top of what landed.
- **Search and chunk deferrals, gated on measurement.** Five deliberate deferrals that stay closed until a consumer reports a problem: optional `halfvec` at 1024 dims (the embedding column stays full-precision `vector`); a compound edge index for graph fan-out (only the two single-endpoint indexes exist); `semanticSearchChunks` hydrating the parent node inline; a chunk-level staleness/hash column (delete-and-insert remains the re-embed path); and chunk recall under heavy filters (still 2× topK over-fetch plus `hnsw.ef_search` scaling).

### Cross-module DX

- **`createMockStructureService`.** Nothing exports an in-memory implementation of the `serviceRegistry` "structure" surface, so a consuming module cannot unit-test against Structure without a database. Cross-module DX over the module's exported surface — worth doing when a consumer next needs to unit-test against Structure without a database.
- **`contextConfig` on connection types.** `structure_connection_type` carries `contextHints` and `aiFields` but no `contextConfig`, which exists only on `structure_node_type`. Build it **only if wanted**: node-type `contextConfig` drives ancestor/connection/child-count assembly for `GET /nodes/:id/context`, and there is no connection-context endpoint for a connection-side equivalent to feed — so this may be a non-item.

### Presentation and polish

- **Custom detail-layout remainder.** Three unbuilt pieces: linking a node type's card/page to an externally-authored useFoundry component file (no `componentFile` or `componentPath` anywhere in `src/`); section grouping with conditional visibility on the Info tab (field-level `visibleWhen` / `requiredWhen` exist, but there is no *section* construct on the type config or in `NodeInfoTab`); and per-type custom CSS, which is itself [an open decision](#decisions-needed). The Components tab that would author these is parked behind `SHOW_COMPONENTS_TAB = false`.
- **Connection toast labels and silent mutations.** Node toasts are type-labelled, but connection mutations still emit the generic "Connection created" / "Connection removed", and favourite/reorder emit no success toast at all. Thread the connection type label through `useConnectionMutations`, and decide whether the silent favourite/reorder mutations are intentional — they probably are; an optimistic star does not need a toast.

### Breadth backlog

Three independent, unstarted items, each verified absent: **outbound webhooks** (zero hits across the templates); **saved full views** (only filter presets exist, in the settings blob, with no column or sort persistence); and **i18n labels** (the frontend templates carry seven `localeCompare` calls and no label layer — `grep -rni i18n` over `src/` returns zero). Kept as one list rather than three rows — none is urgent.

### Blocked on the starter

- **Searchable encrypted fields.** No blind-index or per-field encrypted flag exists; secret fields stay sealed at rest and un-filterable. This is correctly blocked on the **starter** shipping a per-tenant blind-index (HMAC) primitive, with the index key derived separately from the encryption key. The earlier prerequisite — per-tenant key derivation — shipped in starter v1.64.0. It is a dependency, not module work. If it ever unblocks, the honest-leakage constraints still apply: exact-match and keyword lookups only, high-entropy fields only, decrypt-in-memory as the default.

---

## Parked — not planned

Recorded so it stops being rediscovered. **These are not on the queue**, and an answer to "what's left?" should not include them. Each stays here until something concrete changes the case for it.

### Node revision history — likely belongs to document-management, not here

Per-record edit history: a revisions table, a prior-state snapshot on update, and a restore path. Long treated as scheduled work (it was [D8](/modules/structure/decisions/), and appeared in four places under three names), and **built in full on 2026-08-20 — then deliberately reverted before shipping**.

Why it was dropped:

- **Versioning is probably field/type-based and owned by document-management.** That module already owns file chain-of-custody, `contentHash` and file versioning. If record versioning turns out to be the same shape, Structure should consume it rather than run a second, parallel history.
- **The requirement is not understood yet.** What a diff of a JSON `data` blob looks like, whether restore should exist at all or only "show me what it was", and whether history is per-field or per-record are all open — and a storage shape chosen now would answer them by accident.
- **There is no demonstrated need.** Nothing has been lost; the apps are small and single-user. Soft-delete restore already covers a deleted record, and the action hooks already record *that* a change happened and who made it. The gap is only the prior *value*.
- **Backend-only history is invisible.** What was built was service methods and routes with no history view, no diff and no restore control, so none of it would have been usable without a second, larger piece of work.

**What would change the case:** cog-ingest writing into `editableBy: 'system'` fields in bulk and getting something wrong, with no way to see what a field said beforehand. If that lands, the useful feature is probably narrower than full history — *"what did the automated writer change here"* — which the existing action hooks may already carry without a revisions table at all.

---

## Decisions needed

Six open calls. Each is a **question**, not a task — none should be scheduled until it is answered.

### Per-instance node permissions — build an authority model, or stay owner-only?

Three former roadmap bullets (share nodes with specific users; owner/editor/viewer roles with inherit-from-parent; let ordinary users create and edit entries on shared types) all need the same missing primitive: a per-instance grant model. v1.34.27 deliberately shipped only the read half — shared/global **types** are read-shared, everything else stays owner-only — and there is no grant, role or share table among the ten models in `schema.ts`. Does the solo, `tenantId === userId` app model this module targets actually need per-instance authority, or is type-level sharing the permanent answer?

- **(a)** The permanent answer is type-level — mark all three bullets abandoned against D10 (userId-keyed tenancy) and D4 (no admin see-all mode). This page already parks the specific-user-sharing bullet that way; this extends the same call.
- **(b)** Build a per-instance grant table plus resolution in `assertNodePermission` — large, and it changes every read predicate.
- **(c)** Narrow the scope — allow writes on global types only (a `writable: true` flag beside `primaryScope.global`), unblocking the shared-reference-data case without a grant model.

### Offset vs cursor pagination — migrate, or record the deviation?

`listNodes` returns an offset shape (`total` / `page` / `totalPages`), which the list page consumes; `nextCursor` and `hasMore` appear nowhere in `src/`. The docs describe a cursor shape. There is no correctness impact either way, but the two disagree.

- **(a)** Migrate `listNodes` to a cursor shape and update the consumers — medium effort, and it breaks any external consumer of the current shape.
- **(b)** Record offset as the intentional design and correct the docs — near-zero effort.

  *Leaning:* **(b)**, unless a consumer has actually hit the deep-offset cost.

### Stage automation — is a scheduler inside Structure's scope?

The stage machine and transition enforcement shipped in v1.33.17, and `structure.node.stageChanged` fires. The three open bullets are field-value triggers, scheduled transitions, and an admin automation-rules UI. There is no scheduler anywhere in `src/`, and the scope boundaries above push time-based and cross-module behaviour out to other modules.

- **(a)** Declare it out of scope — Structure fires the hooks, a separate automation module owns triggers and scheduling. Consistent with "Structure is the parking space; other modules park their cars."
- **(b)** Build field-value triggers only — synchronous, in-process, no scheduler — and leave scheduled transitions out.
- **(c)** Build all three.

  *Leaning:* **(a)** or **(b)** — a scheduler is the part that clearly belongs elsewhere.

### Composition and accessibility residue — close it, or record permanent documented exceptions?

Three items have been deferred across two audits and keep reappearing: nine bare `h2`/`h3` headings in `pages.ts` that should be `SectionHeader`, a raw `table` element in the Registry Explorer, and a bare `h1` in `NodeDetailHeader` where a page-level header belongs. All three are verified present; all three have been explicitly deferred at least once as restructure risk.

- **(a)** Do the swaps in one pass — each is small in isolation, and the `h1` one also buys `NodeDetailHeader` page-level consistency.
- **(b)** Record all three as permanent documented exceptions, alongside the `bg-destructive/90` `AlertDialogAction` exception, and stop re-auditing them.

Either is fine. What is not fine is a fourth audit rediscovering them.

### In-place duplicate — should it respect `maxChildren`?

`duplicateNode` enforces the hierarchy rules only when `targetParentId` is supplied — the re-parenting case, which was the same defect `bulkCreateNodes` had (both fixed in v1.34.28). An **in-place** duplicate is currently left ungated: it copies a node beside itself, so `allowedParents`, `allowedChildren` and `maxDepth` are satisfied by construction, and the only rule it could breach is `maxChildren`, since the copy is one more child.

Enforcing it would start rejecting duplicates that succeed today, on a path that has never checked — a behaviour change rather than a defect fix, which is why it was left as a call for a person. The comment at the gate in `duplicateNode` points here, so this entry stays until it is answered.

- **(a)** Enforce `maxChildren` on in-place duplicates too — consistent with every other create path, at the cost of breaking flows that rely on duplicating past the limit.
- **(b)** Record the exemption as permanent and say so in the code comment — a duplicate is a user copying something they can already see, and `maxChildren` is a modelling guideline rather than an integrity rule.

*Leaning:* **(b)**, on the grounds that no other rule can be breached and the failure mode of (a) is silent breakage of an existing workflow.

### Per-node-type custom CSS — superseded, or a cheap complement?

The old page argued both sides: one bullet listed per-type custom CSS as planned work, while a note a few lines above called it moot under the useFoundry split, where an externally-authored component file carries its own styling. Verified: zero occurrences of `customCss` in `src/`, and `structure_node_type` has no `className` column.

- **(a)** Accept the supersession — strike custom CSS, and keep only the component-file link as the extension path.
- **(b)** Keep both — a cheap type-level `className` for tinting alongside the heavier component-file route.

*Leaning:* **(a)** — it removes an item rather than adding one, and this page already argued for it.

---

## Shipped

Features implemented and released. This table is the record; the [changelog](/modules/structure/changelog/) carries the per-release detail.

| Feature | Version | Description |
|---------|---------|-------------|
| Sidebar entry lists, node pinning, list grouping & node-detail sidebar sections | v1.37.0 | Wired three previously-dead admin config surfaces into real features. **`navigation.sidebarNav`** now lists a type's entries (favourites / recent / all / pinned, up to `maxEntries`) under its sidebar link — rendered async off the base nav, refreshed live on node changes, generation-guarded against concurrent-refresh races. A first-class per-node **pin** (`isPinned` column mirroring `isFavorite`: `togglePin` + `POST /nodes/:id/pin`, list filter, `structure.node.pinned` hook, detail-header toggle). **Per-user overrides** (`structure_settings.sidebarPrefs`, gated on `sidebarNav.userOverridable`) via a list-page popover. **Node-detail sidebar sections** (`useNodeSidebarSections`) rendered in a right-hand column honouring `integrations.sidebarSections`. **List grouping** (`navigation.listPage.groupBy`) by status / stage / a select field, with a type-editor control. Adds the `isPinned` column (run `structure update`). Adversarially reviewed (8 findings fixed). Host-tsc clean. |
| Cleanup & security audit — secret-exposure fixes + dead-code removal | v1.36.14 | Nine-finding cleanup pass: mask secret fields on the read-only node view (were plaintext for the owner); redact sealed secret envelopes on public-API list/create/update; gate `reorderNodes` on the `edit` permission; unify `visibleWhen` on the shared `isFieldVisible`; delete the unused `useStructureNavigation` hook (carried the fixed pre-v1.34.21 nav bug); trim `NodeTypeLabels` 18→6 keys. Host-tsc clean. |
| `mergeNode` + exact-name `listNodes` filter — node dedup for cognitive-db | v1.36.13 | `structureService.mergeNode(fromId, intoId, userId, options?)` (serviceRegistry-exposed; no HTTP route) folds a duplicate node into a canonical one in one transaction: re-points edges (drop self-loops, collapse duplicates, bidirectional-aware), re-parents children (name clashes suffixed, never dropped — the `(userId, parentId, name)` unique can't abort it), backfills the canonical's *empty* `data` fields (secrets re-sealed), then archives (or `retire:'delete'` soft-deletes + cascades) the duplicate. Same-owner/type/space, non-shared, non-deleted, non-ancestor endpoints; fires `structure.node.merged`. `NodeListQuery` also gains `name` (exact; `(userId, parentId, name)`-indexed with `parentId`) + `nameInsensitive` (LIKE-escaped so it can't wildcard-match), and `listNodes` honours `options.scopeRootId` for `isSystem` callers — the exact-name dedup confinement. The *archive*/*re-parent* halves of the same ask were already `archiveNode`/`moveNode`. Adversarially reviewed (9 findings fixed pre-release). Additive; host-tsc clean. |
| `getConnectionsForNodes` — batch edge read for cognitive-db | v1.36.12 | `structureService.getConnectionsForNodes(nodeIds, userId, { status? })` (serviceRegistry-exposed) returns every connection touching any node in `nodeIds` as from- or to-endpoint, in one query — `userId`-scoped, status-filtered (default `['active']`), `nodeIds: []` → `[]`, endpoints not hydrated, no `scopeRootId`. Loads all edges over a vertex set (up to ~5000) for the engine's structural-gap diagnostics without an N-query storm. Same `NodeConnection` shape as `getNodeConnections`; secrets unsealed per owner (defs cached per type). Additive; host-tsc clean. |
| `updateConnection` — connection field-update for cognitive-db | v1.36.9 | `structureService.updateConnection(connectionId, userId, { data?, metadata? })` (serviceRegistry-exposed + `PATCH /connections/:id`) replaces a connection's `data`/`metadata` JSON and returns the updated `NodeConnection`, so cognitive-db (v1.12.1) can strengthen a co-activation edge by raising `data.strength`. **Replace** semantics (caller does its own read-modify-write), `userId`-scoped, throws `NotFoundError` on a foreign id (no silent no-op), touches only `data`/`metadata`, and **re-seals secret fields** on write (unsealed reads would otherwise round-trip plaintext). Fires `structure.connection.updated`. Additive; host-tsc clean. |
| `nodeFilterRegistry` list filters wired end-to-end | v1.36.8 | The extension point for module-registered list-page filters — a fully-built `useNodeFilters` hook with **zero call sites** — is live. `NodeListPage` renders each registration's `component` in the filter bar and drives it via `useNodeFilters`; a **server** filter's new `toDataFilters(value): DataFilter[]` merges into the query's `dataFilters` (applied in the DB across all pages by the existing `buildDataFilterQuery` engine — 13 operators, parameterised `fieldId`, `.max(50)`), a **client** filter's `filterFn` refines the loaded page, and an active module filter disables drag-reorder. The server contract replaced the old `toQueryParams` (arbitrary `Record<string,string>` the backend couldn't apply) — a pre-launch clean break, zero consumers — so **no backend change was needed**. Host-tsc clean. |
| Now/Next queue sweep — five admin & list fixes | v1.36.6–v1.36.7 | Five ranked Now/Next items in one pass, plus two adversarial-review fixes (v1.36.7). **Merge-field admin gaps:** the merge-token picker surfaces inherited superClass + attached-template fields (merged with the type's live own fields, own winning on id collision), and the Card-display section gains **Title/Body template** inputs (`MergeFieldPicker`-backed, writing `navigation.listPage.card.titleTemplate` / `bodyTemplate`) — both consumed by `NodeCard` but previously JSON-only. **Sibling drag-reorder on node lists:** grid + list views gain drag-to-reorder wired to the existing `reorderNodes` (`POST /reorder`), gated to a manual-sort type **rendered in stored `sortOrder`** and showing one unfiltered, unpaginated page of a single parent's active siblings (the server assigns `sortOrder` by array position, so a filtered / paginated / differently-sorted set would write misleading positions). **PageHeader** added to `NodeTypesAdminPage`, and the **Registries tab** labelled read-only. **`node-reference` allowed-types** picker swapped from a raw comma-separated text box (two sites) to the reusable `TypeListField`, fed by `useNodeTypes()`. **Corrected a false public-API comment** that claimed per-field validation was UI-only (falsified since v1.33.31's server-side `validateNodeData`). Host-tsc clean; the drag-reorder `sortOrder`-gate hole and a stale-picker regression were caught by review and fixed pre-release. |
| One confirmation primitive for every destructive action | v1.36.5 | The admin UI carried three confirmation shapes — a plain `Dialog`, an `AlertDialog`, and a bare `onClick` — and two real gaps: the **API-key revoke fired on a single click with no confirmation** (irreversible for every integration holding the key), as did the built-in **bulk delete** and the **filter-preset** delete. All of it now routes through **one primitive**, `useConfirm()` (`components/common/ConfirmDialog.tsx`): an imperative `await confirm({ title, description, confirmLabel, destructive })` resolving `true` / `false`, backed by a single shadcn `AlertDialog`. Every destructive action — the node card and detail-header delete, settings-tab delete, built-in bulk delete, filter-preset delete, connection removal, and every admin delete (node types, connection types, groups, field templates, functions, template editor, API-key revoke) plus the "stop sharing" scope toggle and module bulk-action confirms — goes through it; the hand-rolled dialog blocks and their `useState` flags are removed. It is a **self-contained hook, not a provider** (Structure owns no app-root to mount above, and its pages are themselves confirm-consumers): each consumer calls `confirm()` and renders `{confirmDialog}` once. The resolver settles exactly once, so Radix's confirm-click plus its trailing `onOpenChange(false)` never double-resolves. Also corrected the settings-tab copy that wrongly called node deletion permanent — it is a **soft, cascading, restorable** delete. Adversarial review (completeness / primitive / call-sites) confirmed no defects; host-tsc clean |
| `getActiveScope` retrieval seam | v1.36.4 | `structureService.getActiveScope(userId, { scopeRootId? })` → `{ mode: 'allow' \| 'deny' \| 'none', scopeRootId? }`, exposing the active-space decision Structure's private `resolveActiveSpace` already computes so a `serviceRegistry` retrieval consumer (cog-ingest / ai-core) can match Structure's own read-scoping. Closes the one gap a security review of the retrieval arm flagged: an app enforcing spaces where the user has **no active space selected** — Structure's reads `deny` (return nothing) there, but the embeddings arm had no way to detect it and would surface the user's content across every space. `'allow'` → pass `scopeRootId` to the embeddings methods / `expandGraph`; `'none'` → don't confine. Same cross-type resolution the tree + keyword search use; shared/global types stay space-exempt; no `isSystem` (reports the *user's* confinement). Additive: one method + one exported `ActiveScope` type. See [Cognitive → read enforcement](/modules/structure/cognitive/#read-enforcement-for-retrieval-consumers) |
| Module-registered bulk actions in the list toolbar | v1.36.3 | The `nodeActionRegistry` `'bulk-action'` location — declared, admin-configurable, and readable via `useBulkActions` since it was introduced, but rendered by nothing (the same declared-but-dead trap the module has closed twice before; `useBulkActions` had one occurrence in the repo, its own definition) — now renders in the list page's **selection toolbar**, before the built-in Archive/Restore/Delete. Each button carries the action's `icon` (bundle-safe `IconRenderer`), `label`, `variant` (`default` → outline) and `order`, with the admin's per-action `label` / `order` / `enabled` overrides from the Integrations tab applied (`getBulkActions` was extended to honour label/order, not just enabled). Running one honours the contract: `bulkHandler(nodes, nodeType)` is called **once** with the selection when present, else the single-node `handler` **fans out per node** tolerating partial failure (summary toast); the selection is intersected with the **owner-filtered** `selectableNodes` (a shared-type action never touches another user's rows) and a declared per-node `isVisible` (nodes it excludes are skipped, though the button itself isn't hidden); a `confirm` block pops an `AlertDialog`; and the list refetches + clears afterwards. `detail-header` / `context-menu` remain the consumer's to render via `useNodeActions`. See [Registries → nodeActionRegistry](/modules/structure/registries-guide/#5-nodeactionregistry) |
| NodeSelector quick-create, `onNodeCreated`, dialog variant | v1.36.2 | The module's primary integration point stops making every consumer build its own creation flow. With `allowCreate`, picking `New {type}` now opens a **minimal name-only form inside the picker** — create-and-select without leaving it: on submit it creates the node (`createParentId`, previously declared-but-unused, sets the parent), auto-selects it, and fires the new **`onNodeCreated(node)`** callback. A quick-created node is name-only, so a type with required custom fields rejects server-side (toast shown, form kept open) — those delegate to a full form via `onCreateClick`, which is now **optional** and, when present, preserves the old delegate-and-close behaviour. A new **`variant="dialog"`** renders the same trigger + list + form inside a `Dialog` instead of a `Popover`, for use inside another dialog/sheet where a nested popover clips or fights the focus trap (the case that previously forced a hand-rolled picker); `dialogTitle` sets its heading. The trigger and panels were extracted so both variants share one implementation. Fully backward-compatible; host-tsc clean. See [Integration → NodeSelector](/modules/structure/integration/#using-nodeselector-to-reference-nodes) |
| Typed action/filter hook contexts | v1.36.1 | The cross-module hook contract, which cog-ingest is about to be written against, becomes typed. Every one of the ~50 action hooks the service fires through `actionRegistry.doActionAll` and the 4 filter hooks it applies through `filterRegistry.applyAll` now has a **named, exported payload interface** in `structure.types.ts` — `structure.node.created` → `NodeCreatedPayload`, and so on — collected in the `StructureActionPayloads` / `StructureFilterHooks` maps (indexable by hook name). A consumer types its handler against the interface and is checked at compile time instead of finding out at runtime: `addAction('structure.node.created', 'x', (p: NodeCreatedPayload) => …)`. The producer side is enforced too — the service fires through typed `fireAction()` / `applyFilter()` wrappers, so a payload that drifts from its interface fails the module's own build. Writing the interfaces surfaced **16 producer call sites** that had been passing a raw Prisma row where the contract is the domain type; each now carries the codebase's standard boundary cast. Zero runtime change (the wrappers forward identically); host-tsc clean both ends. See [Integration → Action hooks](/modules/structure/integration/#reacting-to-structure-events-action-hooks) |
| Formula traversal, list functions, formulas on built-in fields | v1.36.0 | The formula language becomes the engine for values derived across the graph. Four traversal keywords resolve related nodes into **node-views** — `children([type])`, `parents([type])`, `scope([type])` (the `rootNodeId` subtree), and `nodes(ids)` to hydrate a node-reference field like `nodes({parties})` — applied uniformly to structural *and* reference relationships. `map(list, expr)` and `filter(list, pred)` are higher-order special forms whose body is a per-item sub-expression (`{field}` = the item, `{_}` = the whole item; `filter` also takes a 3-arg field-name form), surrounded by the pure list functions `pluck`/`join`/`first`/`last`/`count`/`reverse`/`unique`/`includes`/`sortBy` (a new `list` category). The motivating "Schulz & Schulz & ICL" → **"Schulz B & Schulz J"** disambiguation is now expressible: `join(map(filter(children(), {nodeType} == 'person'), concat({surname}, ' ', upper(substr({first_name},0,1)))), ' & ')`. Built-in fields (`name`/`description`/`slug`) gained a `calcConfig` formula beside their `computedTemplate` (template wins if both set; resolved against `data` only, so no oscillation; computed on the form and stored). Everything stays client-computed ([D11](/modules/structure/decisions/)) and runs in both the edit and read views. Node-views are carried opaquely — no member access, every field read is `hasOwn`-guarded — the special forms are dispatched before the registry so they cannot be shadowed, and two new bounds (`MAX_LIST_LENGTH` 1000, `MAX_FETCHES` 200) join the step/depth caps, with `{ref->field}` derefs inside a map/filter body counted against the same fetch budget (an adversarial-review fix caught pre-ship). Full treatment in [Fields → formula](/modules/structure/fields/#formula) |
| Server-assigned reference numbers | v1.35.0 | An immutable, human-readable handle for every entry of a node type that opts in — `MAT-2026-0147` — configured by `referenceConfig` on `structure_node_type` (`enabled`, `prefix`, `separator`, `padding`, `includeYear`, `resetYearly`) and **off by default**, so a type that never enables it stores `NULL`. It exists because a derived name cannot carry identity: two unrelated matters both compute to "Schulz & Schulz", and adding a party silently rewrites a name something else has already cited — while `id` is an opaque cuid and `slug` is client-supplied with no de-dup helper. With a citable handle in place, the name is free to be computed. The number is allocated by one `INSERT … ON CONFLICT … DO UPDATE … RETURNING` against a dedicated `structure_reference_counters` table inside the node-create transaction, so two concurrent creates cannot receive the same number and a failed create burns none; the counter is deliberately not a column on the node type, because a high-water mark is runtime state and would otherwise travel through config export/import between environments. Nothing can change a reference afterwards — no create, update, public-API, bulk or metadata path — with one deliberate door: `POST /nodes/bulk` accepts an explicit `reference` from admin/system callers so a migration keeps its existing numbers, and an imported value pushes the counter past itself. Enabling references on a type that already holds entries backfills them in `createdAt, id` order and seeds the counter above the highest value assigned — the primary backfill path, since nothing is enabled yet when the migration runs on an upgrading host; enabling, disabling and re-enabling never renumber an entry that already has one. Full treatment in [Configuration](/modules/structure/configuration/) |
| Hierarchy enforcement on every node-creating path | v1.34.28 | `bulkCreateNodes` never called `enforceHierarchyRules`, so `POST /nodes/bulk` ignored `allowedChildren`, `allowedParents`, `maxChildren` and `maxDepth` — the path a bulk writer such as cog-ingest uses. It now enforces per row inside the existing try, so a failing row becomes that row's error and the batch continues, with per-type rules memoised alongside the field-def and permission caches. `duplicateNode` had the same hole on its re-parenting path and is gated there; an in-place duplicate is deliberately left ungated (see [Now / Next](#now--next)). `moveNode`'s `maxDepth` check now measures the deepest descendant of the moved subtree, but only for **deepening** moves — a sideways or shallower move cannot newly breach the limit, and measuring there would reject an already-deep subtree on the very operation that might be fixing it |
| Shared node types + owner-filtered bulk selection | v1.34.27 | A node type can be marked **Shared across all users** (`primaryScope.global`, carrying a contract docblock in the config type): its entries become readable by everyone, while writes are never relaxed — a user can only modify entries they own, at any tier, with no admin bypass — and **connections are never shared**. The type row itself must be app-level (`structure_node_type.userId === null`). Backend read-scoping runs through dedicated helpers (`isSharedTypeConfig`, `loadGlobalTypes`, `ownerScope`, `canHydrateEndpoint`); the admin UI handles mutual exclusion against the other scope modes. Because a shared list can legitimately show another user's entries, the same release confined bulk **selection** to owned rows — the checkboxes in every view, select-all, and the fan-out itself — with the toolbar hidden when nothing is selectable |
| Admin-UI polish round — positional ordering, two-way Groups, tabbed connection types, connection-type AI | v1.34.21–v1.34.26 | A shared `components/admin/OrderControls.tsx` (`OrderButtons` first/up/down/last, `PositionSelect` First / After &lt;name&gt; / Last) replaces every raw order input across App Structure — the stored `sortOrder` / `navigation.mainNav.order` numbers are computed, so existing config and JSON bundles round-trip. The Groups tab becomes a real two-way editor (add / remove types, per-row ordering inside the group's sidebar section) and the **Components** tab is hidden behind `SHOW_COMPONENTS_TAB = false` (parked, nothing removed — 7 tabs render). The connection-type editor becomes tabbed (Basic / Properties / Fields / Display, joined by the AI tab below), its From/To and field-template controls become the reusable `TypeListField` (extracted to `components/admin/TypeListField.tsx`), and an **AI** tab lands on connection types backed by new `contextHints` + `aiFields` columns on `structure_connection_type` — closing the Phase 2 connection half. Plus two live-use fixes: a type with no `mainNav` block is now in the sidebar unless explicitly switched off, and clearing an icon or colour persists (an empty string is translated to an explicit `NULL` by a new `clearable()` helper) |
| Performance pass on the generated output | v1.34.20 | 33 fixes from the 2026-08-18 eight-dimension audit, host-tsc validated. Highlights: `listNodes` keyword search moved onto the trigger-maintained GIN `searchVector` (`to_tsquery` prefix terms) with a self-healing fallback, replacing a full `jsonb_each_text` scan; field-filter inputs debounced with TanStack's abort signal forwarded through `listNodes`; the admin page registered `lazy()`; `StructureTree` virtualised with `@tanstack/react-virtual` over memo’d `TreeRow`s; the computed/formula hooks stop stringifying the form bag for fields that have no template; `getTree` narrowed onto a dedicated lightweight `NodeTreeNode` type; the `dataFilters` id pre-query capped at 5,000 rows. The lucide-bundle finding was rescoped to the starter's shared icon picker |
| Admin-UI clarity pass over App Structure | v1.34.3–v1.34.19 | A sustained pass to reduce the accumulated complexity of `NodeTypesAdminPage`. The shared field editor was extracted and renamed (`FieldSetEditor`) and is now used by node-type fields, connection-type fields and field templates; "Add Field" became a category-grouped type picker; the Fields tab became three consistent list sections; Field Templates became a full master-detail page; the merge-field picker became a two-layer grouped, searchable `Popover` + `Command`. A new **AI** tab (gated on AI Core via `useModuleInstalled`) holds AI Fields + AI Context, Sidebar Navigation moved into **Basic** and its duplicate control was removed, Primary Scope moved to Basic, and Basic/Properties were reworded in plain language. The Permissions section was reworked into a 3-across layout when the per-type **Read** tier landed beside it (its own row below). `rules.createFrom { sidebar, headerToolbar }` gave each type its own creation entry points — and fixed the pre-existing dead `?action=create` intent that had made the header quick-create a no-op. The **Hierarchy** tab was redesigned Children-first (Children editable and the source of truth, Parents auto-derived read-only, "Based on" collapsed into an accordion) on a new reusable `TypeListField`. The **Pages** tab pass then fixed a list-breaking `defaultSort` mismatch, wired the dead `listItemTemplateId` into the compact-list row, and completed per-widget overview config (enable / order / left-right column). Two adversarial reviews caught and fixed a data-loss bug, an AI-vs-regular field-id collision, a computed-field auto-persist regression, an infinite loop between mutually-referential computed built-ins, a stale-closure clobber, and a hard-load permission race before ship |
| Per-type Read permission + the retrieval (RAG) read-enforcement arm | v1.34.12–v1.34.13 | A `read` tier on `permissions` (taking it to five keys × three tiers), enforced across every read path: single reads 404 rather than leak existence, lists / tree / keyword search exclude blocked types, related entries are filtered, and the public API 404s them even when publicApi-enabled. The serviceRegistry-only embeddings + `expandGraph` arm takes an opt-in `excludeNodeTypes`, and a new `structureService.getUnreadableTypes(userId, { isAdmin, isSystem })` lets a trusted consumer resolve the caller's restricted slugs and pass them |
| Config dry-run/diff preview, the `auditEvents` contract, RAG `scopeRootId` | v1.34.2 | `POST /config/import` with `dryRun: true` computes the exact diff inside a rolled-back transaction (per-category created/updated/deleted/unchanged), surfaced by a **Preview** button in the admin dialog. The security manifest declares every lifecycle event the module fires, read via `securityRegistry.getAuditEvents()`. All six embeddings search methods take an opt-in `scopeRootId` — the Structure-side half of confining retrieval to the active space |
| Enforced scoping — server-side "spaces" | v1.34.0–v1.34.1 | Opt-in `primaryScope.isolation: 'filter' \| 'enforced'` plus a denormalised `rootNodeId` column (with `@@index([userId, rootNodeId])` / `([userId, rootNodeId, nodeType])`), giving fail-closed server-side confinement in two modes — *ambient* (list / tree / keyword search / getNode / neighbourhood / connections → the persisted `currentScopeNodeId`) and *anchored* (context / graph / `findSimilarNodes` → the seed node's own space, the AI-context leak fix). Connection carve-out, an audited out-of-space-move gate (`rules.allowCrossSpaceMove`, default off), `rootNodeId` on lifecycle payloads, and a header `ScopeSelector` |
| Atomic config-bundle import/export | v1.33.34–v1.33.35 | `GET /config/export` and `POST /config/import` cover all five config categories (node types, connection types, groups, field templates, functions) in one transaction, with a per-category created/updated/deleted summary and `replace`-mode pruning of every present category. Import is atomic — an invalid row rejects the whole bundle |
| Server-side node-type permission enforcement + `'system'` tier | v1.33.32 | The config-only `permissions` block is enforced in the service methods rather than the browser; the tier set extends to `'anyone' \| 'admin' \| 'system'`, where `'system'` means trusted in-process callers only (no user or admin can perform it via the UI or API) |
| Server-side field validation on the write path | v1.33.31 | One `validateNodeData` validator over the resolved field set (required / pattern / min-max / enum / type), run on create / update / bulk / public-API writes and throwing `BadRequestError` — so cog-ingest and every external integrator inherit it. Follow-ups: `requiredWhen` and field-group sub-field validation |
| Tree view, bulk operations, expand persistence, tree search + keyboard nav | v1.33.26–v1.33.30 | A fourth list-page view (`tree`) backed by a lazy `NodeTreeView` over `useNodeTree`; `POST /nodes/bulk-archive\|bulk-delete\|bulk-restore` partial-success endpoints (bounded to 500 ids, soft delete, summary hooks) with a multi-select UI — overlay checkboxes in grid/list, a leading table column, a selection toolbar with select-all and live count; per-node-type expanded ids persisted to `structure_settings.treeExpanded`; the detail-header and overview Component-template configs wired to their renderers; and a `StructureTree` refactor from recursion to a flat list with tree search (prune + auto-expand ancestors) and full `role="tree"` keyboard navigation |
| Saved filter presets + URL-persisted list state | v1.33.24–v1.33.25 | Search, status and field filters are read/written as `?q=` / `?status=` / `?filters=<json>` via `useSearchParams` (debounced, `replace: true`), so a filtered list survives refresh and back, and is shareable. Named per-node-type presets save the whole combination into the user's `structure_settings.filterPresets` blob — no new table — applied or deleted from a `Popover` in the filter bar |
| Type-aware list-page filters (text / number / date ranges) | v1.33.23 | Widens list-page field filters beyond `select`, driving the existing server-side `dataFilters` engine by field type: select → `equals` (dropdown), text/textarea/email/url → `contains` (search box), number → min–max range, date/datetime → from–to range (`between`/`gte`/`lte`). `DataFilter` gains an optional `valueType` (`'text' \| 'number' \| 'date'`) so `buildDataFilterQuery` casts comparison operands correctly — `::numeric` by default (backward-compatible), `::timestamptz` for dates. Admin "Filterable fields" selector widened to offer select/text/number/date fields. Host-tsc validated (0 structure errors). Deferred: saved presets, URL-persisted state, rendering custom `nodeFilterRegistry` components |
| Workflow → Stage rename + transition enforcement | v1.33.17 | The `workflow` vocabulary becomes `stage` throughout — `stageConfig` / `stages` / `defaultStage` / `structure.node.stageChanged`, with the DB column kept in place via `@map` (raw SQL aliases `"workflowState" AS "stage"`). The transitions an admin could always configure are now **enforced** on write, closing the gap where the stage machine was declared but never applied |
| Four-dimension meta audit remediation | v1.33.16 | 28 findings (1 critical, 13 moderate, 14 minor) from a standards / production / performance / security audit of v1.33.15, all fixed and host-validated. The critical: the public API's `GET /:type/:id` returned **decrypted secrets**, so a read-scoped API key could read stored credentials of any exposed node. Also fixed: `duplicateNode` persisting secret overrides in plaintext, `updateNode` leaking plaintext secrets to action-hook listeners, a bypassable formula string-length cap, `structure_function` missing from the security manifest, an empty-array data filter 500ing `listNodes`, `resolveTemplateAsync` corrupting merge output on `$` patterns, a `useCurrentScope` hydrate race, a phone-placeholder precedence bug, a dead Edit toggle on single-tab node types, a `workflowConfig: {}` form crash — and `NodeListPage` gained the pagination it had never had |
| Formula / calculation field type | v1.33.15 | An admin-authored `formula` field type across all three layers: a hand-rolled safe evaluator (`utils/formula.ts`), a module-extensible `formulaFunctionRegistry` (math / text / logic / similarity — the 11th registry, shipped from `utils/` rather than `config/`), and `structure_function` admin named-expressions on a new **Functions** tab. Node `semanticSimilarity` reads the embeddings. Values recompute live from the form bag |
| Always-on keyword search + Lakebase `lakebase_ann`; `textTemplate` dropped | v1.33.0 | Reworks the v1.32.0 lexical arm. Keyword search is now **always available** (no embeddings needed) via `structureService.searchNodes(userId, query, { topK?, nodeTypes?, status? })` — ranked with `websearch_to_tsquery` + `ts_rank_cd` (higher = better, not 0..1) over a trigger-maintained `structure_node.searchVector` (name + description + all string `data` values via `jsonb_to_tsvector`), served by a GIN index; `getCognitiveCapabilities().lexical.enabled` is now always true (was embeddings-gated). This is the ranked upgrade of the `listNodes` `search` substring filter. Drops the v1.32.0 `searchText` column and the `cognitiveIngest.textTemplate` knob (keyword search auto-indexes; `cognitiveIngest` is back to `{ enabled? }`), and removes `structureEmbeddings.lexicalSearchNodes` as superseded — `lexicalSearchChunks` / `hybridSearchNodes` / `hybridSearchChunks` stay on the embeddings tier. Adds feature-detected Neon Lakebase `lakebase_ann` indexes for the vector columns (pgvector-compatible `<=>`, storage-resident, 50–100× faster builds; HNSW fallback; `CREATE EXTENSION` fails cleanly without the preload libraries). `observedAt` unchanged. Host runs `prisma migrate dev` after `npx structure update`; the `searchVector` / `contentVector` triggers + GIN self-heal at runtime on first search |
| Lexical + hybrid search, graph expansion, bi-temporal `observedAt` | v1.32.0 | Storage-side cognitive retrieval, all additive. Keyword search (`lexicalSearchNodes` / `lexicalSearchChunks` over functional GIN indexes with `ts_rank_cd` scoring) and reciprocal-rank-fusion `hybridSearchNodes` / `hybridSearchChunks` (caller supplies query text **and** its embedding; fuses in SQL as `Σ weight / (rrfK + rank)`) added to the `structureEmbeddings` service, riding the `enableEmbeddings` opt-in. An `expandGraph` N-hop graph-expansion primitive on `structureService` — a single recursive CTE over connection-nodes, undirected, `userId`-scoped, returning each reached node's minimum hop distance — with `getCognitiveCapabilities` now reporting `lexical` and `graph` keys. A node-type `features.cognitiveIngest.textTemplate` resolving into the app-maintained `structure_node.searchText` column (the GIN index falls back to name + description when unset). Plus the ungated bi-temporal `structure_node.observedAt` (time-scoped WHERE filters only — never a ranking or decay signal). Structure owns storage only — it never calls an LLM/embedder; all raw SQL is parameterised. Migration `1.32.0__lexical-search.sql` adds the functional GIN indexes |
| Computed built-in fields populate | v1.23.3 | Closes the last `builtInFieldConfig` trust-fix item: `NodeForm` resolves name/description/slug `computedTemplate` via the (now-exported) `useResolvedTemplate` and writes the derived value into the read-only input; `updateNode` stops locking computed built-ins so the client-computed value persists (same model as custom computed fields, `system`/`admin` still enforced) |
| Conditional pgvector-datasource advisory | v1.23.2 | The embeddings heal reads the host schema and only advises the pgvector datasource declaration when it is actually missing (silent once declared), pointing at the starter's `enable-vectors` / "Enable vector database?" path rather than a manual edit. Sets up a future flip from advise→auto-enable once the starter ships a non-interactive `ensure-pgvector` |
| `onReconfigure` embeddings heal | v1.23.1 | Adopts the module-starter `reconfigure` command (scaffold v1.24.0) and adds an `onReconfigure` lifecycle hook: flipping `enableEmbeddings` off→on via `npx structure reconfigure` now runs the full pgvector heal (extension, P3009 failed-migration recovery, HNSW index) that `onUpdate` skips at a same version. Runtime `ensureInfrastructure` stays the backstop |
| Trust-fix release — declared controls made real | v1.23.0 | `builtInFieldConfig` editableBy enforced in `NodeForm` + server-side `updateNode`; custom-field `editableBy` applied in the create/edit form; `NodeChildrenTab` create buttons gated on `permissions.create`; server-side sort by custom (data) fields (`dataSortField`/`dataSortType`, type-aware) with `NodeTable` driving it; conditional-colour rules show option labels + warn on stale values; chained-select admin editor. Dropped the dead `renderCompact` and `inputMask` declarations |
| Chunk storage (`structure_chunk`) | v1.22.0 | Dedicated pgvector chunk table for sub-node retrieval spans, riding the `enableEmbeddings` opt-in (same `embeddingDimensions`). Chunk write/search added to the `structureEmbeddings` service (`replaceNodeChunks`, `setChunkEmbedding`, `listChunksNeedingEmbedding`, `getNodeChunks`, `deleteNodeChunks`, and `semanticSearchChunks` — which joins `structure_nodes` for source-node status/type filtering); a partial HNSW index (migration `1.22.0__chunk-storage.sql` + runtime `ensureInfrastructure` + install heal), a gated `structure.chunks` erasure handler, `chunks` on the capability probe, and two action hooks (`structure.node.chunksReplaced`, `structure.chunk.embeddingUpdated`) |
| Module-owned field types | v1.21.0 | Admin field-type picker driven by `nodeFieldTypeRegistry` instead of a hardcoded list; `configSchema` rendered into the field editor (stored on `NodeTypeField.config`); unknown/unavailable types render read-only with the value preserved instead of a data-corrupting text input. `media` removed as a built-in — a storage module registers it |
| Table view, field renderers, server-side filtering | v1.20.0 | `NodeTable` consuming the column registry (sortable headers, responsive scroll); `media` / `multiselect` / `chained-select` renderers implemented (they previously corrupted array values via a fallback text input) (the `nodeMediaUploadRegistry` named here was removed in v1.21.0, when `media` stopped being a built-in field type); list-page field filters moved to the server-side `dataFilters` engine; `conditionalColor` applied at render; cross-module DX hooks/utils; type-labelled toasts |
| Meta-audit remediation | v1.19.0–v1.19.2 | 171 verified findings: connection FK ownership + relation scoping (IDOR), five install blockers, deletion-handler guard, `NodeForm` reset, bulk-create semantics, secret fields encrypted at rest, server-side `editableBy`, key expiry, plus 74 latent host type errors cleared — generated code type-checks clean on Prisma 7.8 / React 19 |
| Type Hierarchy (is-a inheritance) | v1.18.0 | `superClasses` on node types — a multiple-inheritance is-a DAG with field/feature inheritance surfaced as a read-only `resolved` config on `/types` responses, subsumption-aware connection allowlists (plus the `'*'` wildcard fix), `includeSubtypes` node listing, four hierarchy service methods, a Hierarchy-tab parent picker with inherited-fields display, import/export/proposal support, a delete guard on referenced parents, and hierarchy-aware embedding selection |
| Context provider persistence | v1.17.0 | `contextHints` and `contextConfig` now persist — new columns on `structure_node_type`, written by create/update/import/proposal paths and read directly by `GET /nodes/:id/context` (previously accepted everywhere but silently dropped on write) |
| Cognitive foundations (cognitive-db v2 RFC Phase 1) | v1.16.0 | Opt-in pgvector embeddings with the `structureEmbeddings` semantic-search service and install heal, connection origin/status with a pending→promoted lifecycle, node-type proposal/approval API, `features.cognitiveIngest` flag, six new action hooks, and the first per-module DB migration (`init`/`update` now need a reachable database) |
| Public REST API (API-key authed) | v1.15.0 | External per-type REST API exposing node instances of opt-in types. GET/POST/PATCH (no DELETE), per-key read/write scopes, keys hashed via the security feature, managed in an API Keys admin tab. Base: `/api/v1/public/structure/:type` |
| Custom fields on connections | v1.14.0 | Admin-defined fields per connection type (text/textarea/number/select/multiselect/toggle/date), collected on the Add Connection dialog (with required-field gating and default seeding), and displayed in an info popover on each connection badge |
| Module-registered fields with admin Integrations tab | v1.11.28 | Modules inject fields onto node types via `structureRegistry.register({ fields })`. Admin controls on Integrations tab (enable/disable, label override, order) |
| Boolean field type, multi node-reference, field groups | v1.11.18 | `boolean` (Yes/No), `field-group` (repeater with sub-fields editor), `node-reference` with `multiple: true` |
| Node type groups — sidebar navigation grouping | v1.11.15 | New `structure_node_type_group` table. Groups organise node types under collapsible sidebar headers. Dedicated Groups admin tab. Navigation Group dropdown on the Pages tab *(it moved to the Basic tab in v1.34.10)* |
| Connection type registry | v1.10.0 | 10th registry. `structure_connection_type` table with constraints (fromTypes, toTypes, bidirectional, unique, maxConnections). CRUD API. Admin Connection Types tab |
| Stages | v1.10.0 | Optional per-node-type stage machine. `stage` on nodes, `stageConfig` on types with stages/groups/transitions/defaultStage. Admin UI with feature toggle + editor. `structure.node.stageChanged` hook. *(Renamed workflow→stage + transition enforcement added v1.33.17.)* |
| JSON import/export | v1.10.0 (atomic bundle v1.33.34) | Export/import the full config bundle — field templates, groups, node types, connection types, functions — via one atomic `/config/import` transaction with per-category created/updated/deleted results; merge/replace modes; file upload or paste |
| Metadata isolation | v1.10.0 | Atomic namespaced writes via `updateNodeMetadata(nodeId, userId, moduleId, data)` using Postgres `jsonb_set`. PATCH endpoint. Prevents cross-module corruption |
| Publish/unpublish hooks | v1.10.0 | `structure.node.published` and `structure.node.unpublished` action hooks fired on `isPublished` state change |
| Path-based queries | v1.10.0 | `pathPrefix` on NodeListQuery for hierarchical descendant scoping |
| Advanced JSON field filtering | v1.10.0 | 13 operators (equals, not_equals, contains, starts_with, gt/gte/lt/lte, between, in/not_in, is_empty/is_not_empty) on `data` JSON column via `dataFilters` |
| Bulk node creation API | v1.10.0 | POST `/nodes/bulk` for batch creation with per-node error collection and optional `skipHooks`. Originally described as "transaction safety"; v1.19.0 corrected it to genuine per-row atomicity (partial success) after the single interactive transaction was found to report phantom successes |
| Enhanced node duplication | v1.10.0 | `includeChildren`, `includeConnections`, `targetParentId`, `namePrefix`, `dataOverrides`. Subtree copy with ID remapping |
| Node context provider | v1.10.0 | GET `/nodes/:id/context` returns assembled context (node, type, ancestors, breadcrumb, childCounts, connectionCounts, connections). `contextHints` and `contextConfig` on NodeTypeConfig |
| Node create form as Sheet | v1.10.0 | Creation form opens as a right-side Sheet (1/3 width) with ScrollArea instead of a full-page form |
| Scope boundaries documented | v1.10.0 | Documented what Structure IS and IS NOT responsible for |
| Revert v1.9.7 `NodeTypeForm` Tabs rewrite | v1.9.8 | Returns the form to using shadcn `<Tabs>`. Rendering bug tracked as a Known Issue — the real fix needs diagnosis in the host app's `tabs.tsx` or surrounding layout, not a workaround in a module template |
| Retry the auto-prime | v1.9.6 | Auto-prime now retries with backoff (t+0, +1s, +2s, +3s) on failure. Critical for non-admin users — if the first fetch races with JWT loading, admins can fall back to opening Admin to force a sync, but non-admins have no way out of an empty sidebar. The retry window catches the JWT landing within ~6s |
| Restore auto-priming navigation | v1.9.5 | Reverts the v1.9.2 removal of the module-scope `listNodeTypes()` fetch. The sidebar now populates automatically on first load without requiring apps to wire in `<StructureInit />`. The component is kept as an optional helper for edge cases (feature imported before auth is ready) |
| `<StructureInit />` cold-load fix (superseded) | v1.9.4 | Added an optional component apps could mount in their authenticated layout. Superseded in v1.9.5 by restoring the auto-priming fetch, which is the zero-config default |
| Documentation accuracy audit | v1.9.3 | Rewrote `NodeTypeFeatures`/`NodeTypeRules`/`NodeTypeConfig`/`NodeTypeNavigation` interface blocks to match code; fixed `NodeSelector` example (`allowedTypes` prop, ID-string onChange); `dependencies: ['structure']` → `requiredModules`; `useNodeTree` signature, `useNodeMutations` description corrected; `NodeTypeRegistry` API table clarifies wrapper vs config returns; init example uses real system user ID; registry count unified (9 total, 8 content); template comment on `navigation.registration` refreshed; complete CLI reference + SQL notes added |
| Audit findings cleanup | v1.9.2 | Removed `console.*` from generated templates; removed module-import-time API call; added `useNodeTypeMutations` + `useConnectionMutations` so admin + connection CRUD use TanStack Query; update toast on `useNodeMutations.updateNode`; `log.info` on `reorderNodes`; renamed `mediaType: 'any'` → `'all'` |
| Scaffold v1.9.0 + TanStack Query migration | v1.9.1 | Data-fetching hooks migrated from manual `useState` to `useQuery`/`useMutation` with `createModuleQueryKeys('structure')`; scaffold bumped to v1.9.0 (hooks template, registrations split, styleRegistry support, template perf pass) |
| Action & filter hook firing, structured logging | v1.9.0 | 16 action hooks wired, 4 filter hooks applied, Pino structured logging, settings page, lifecycle hooks, scaffold v1.5.0 |
| Expanded registry system | v1.8.0 | 5 new registries (actions, field types, columns, filters, sidebar sections), customisable labels, Registries admin tab, getLabel() utility |
| Performance improvements | v1.7.6 | Toast notifications, batched reorderNodes/template lookups, search debouncing, info tab debounce, registry isEnabled filter, single-step createNode |
| Production readiness fixes | v1.7.5 | GET query param validation, requireAdmin on type/template endpoints, moveNode transaction, updateNode field allowlist, connection cleanup on delete, delete confirmation dialog |
| Zod validation, Structure hooks, audit fixes | v1.6.0 | Zod schemas for all POST/PATCH routes, Structure-specific filter/action hooks, console.log cleanup, VERSION sync, author metadata |
| Standards alignment | v1.5.4 | Migrate to settings-blocks, page-blocks, Zod validation, filter/action registrations |
| Scaffold update to v1.4.16 | v1.5.3 | Space-separated placeholders, docs submodule fixes |
| Card builder with live preview | v1.5.0 | Admin card configuration with CardPreview component |
| List page filtering | v1.5.0 | Admin-configured filter dropdowns for select fields |
| Detail page navigation styles | v1.5.0 | Tabs, sidebar, or none layouts |
| Full connections CRUD | v1.5.0 | Create, view, delete connections with NodeSelector |
| Registry reactivity fixes | v1.5.0 | Proper onSync subscriptions in hooks |
| Two-pane admin layout | v1.4.3 | Master-detail with drag-to-reorder |
| Visual badge picker | v1.4.10 | Click-to-select card field display |
| Container node support | v1.4.0 | Conditional container types |
| Template variable substitution | v1.3.0 | Dynamic template placeholders |
| Initial release | v1.0.0 | Full node system with registries |

---

## Archive

Closed audits and closed programmes of work, kept for provenance. **These are history, not plans.** Each entry records what the pass found and how it ended; anything that survived it is carried above in [Now / Next](#now--next) or [Later](#later), not here. The per-release detail lives in the [changelog](/modules/structure/changelog/).

### Audits

- **2026-08-19 — documentation accuracy audit, 258 findings across 20 pages.** Twenty per-page auditors, seventeen adversarial verifiers, eighteen hostile second-opinion re-checks: 55 ❌ incorrect, 94 ⚠️ outdated, 91 📝 incomplete, 18 💡 enhancement, 0 refuted. Systemic themes were wrong headline counts contradicting each other across pages, registry surface documented but unconsumed, enforced scoping undocumented, per-type permissions documented with the wrong shape, server-side validation actively denied, and the working-note pages having drifted furthest. **✅ Resolved 2026-08-19** via an 18 per-file fix pass with independent verification, which itself caught 62 defects the fixers introduced or left behind (64 corrections applied, 3 verifier findings rejected as themselves wrong). Docs-only, no version bump. Build clean: 187 pages, 0 orphans, 0 broken internal links.
- **2026-08-19 — the same audit's incomplete & enhancement half, 109 findings.** Pages that were *correct* but left a developer without something they needed: a half-complete hooks reference, partial registry method tables, no page listing the SQL migrations, no `utils/` reference, no public-API response or error shapes, and nine developer-workflow gaps (notably the missing "rebuild `dist/` after a version bump" step). **✅ Resolved 2026-08-19**, docs-only.
- **2026-08-18 — performance audit of the generated output, 34 confirmed findings.** Eight per-dimension agents (React rendering, list rendering, data fetching, backend queries, route handlers, bundle & registries, memory, hot-path utilities) with dedup and adversarial per-finding verification: 41 raw → 34 confirmed (6 critical, 22 moderate, 6 minor). **✅ 33 shipped in v1.34.20**, host-tsc validated. The lucide-bundle critical was **rescoped** — all eight icon sites moved onto the starter's shared icon picker, and the bundle weight itself is a [starter-template upstream issue](/starter-template/upstream-issues/). Two tails survive and are carried in [Later](#later): the `NodeTypeForm` render decomposition, and the boot-prime anonymous skip.
- **2026-08-07 — four-dimension meta audit, 28 findings.** ✅ Resolved in v1.33.16 and host-validated; see the [Shipped](#shipped) row for what it caught.
- **2026-08-07 — documentation accuracy audit, 50 findings across 14 pages.** Stage-rename stragglers, the formula feature under-documented, admin-UI drift in `usage.mdx`, connection-field shape errors. ✅ Resolved in a 12 per-page fix pass, docs-only, none refuted.
- **2026-08-01 — documentation accuracy audit, 54 findings across 12 pages.** Two systemic errors fixed everywhere: three pages carried three different action-hook counts, and `media` was still listed as a built-in field type after v1.21.0 removed it. The worst cluster documented an install-prompt onboarding wizard that does not exist. ✅ Resolved, docs-only. One code fix rode along: `src/module.ts` now declares all ten module tables, so uninstall strips the full set.
- **2026-07-23 — roadmap verification, five latent bugs.** Re-verifying every open roadmap item against the code surfaced five defects nobody had logged: `conditionalColor` was write-only, the "View Public" button dead-ended, three field types could corrupt data through a fallback text input, `StructureTree` chevrons never worked uncontrolled, and `getTree` was unbounded. ✅ Four fixed in v1.20.0; the fifth (the tree was not rendered anywhere) was answered by the v1.33.26 tree view.
- **2026-07-22 — four-dimension meta audit, 171 findings.** 312 raw candidates reduced by a three-lens refute-by-default review. Nine criticals: a cross-user connection read (IDOR), deletion handlers that could wipe entire tables, `bulkCreateNodes` reporting partial success from an aborted transaction, `NodeForm` never resetting between opens, and five install blockers that stopped generated code compiling on a current host. ✅ Resolved across v1.19.0–v1.19.2, verified by a real host install; v1.19.2 additionally cleared all 74 pre-existing latent type errors, so a fresh install type-checks at 0 errors on both ends.
- **2026-07-19 / v1.15.2 — standards-alignment and styling sweeps.** ✅ Resolved in v1.18.1: §8 composition cleanup, the §7 opacity sweep, compound indexes, and the typography/weight/colour-literal/shadow sweep (0 hits in every one of those categories today). **Correction:** this page long carried "§7 opacity residue (25)". A grep across the frontend templates now returns exactly **one** hit — `bg-destructive/90` in the canonical `AlertDialogAction` string, which the same entry records as a kept documented exception. The count was stale, not the exception.

### Closed programmes

- **Veradai-driven build sequence (2026-07-26).** Mapping one consuming app's requirements against the code produced a six-phase gap set. **All six phases shipped**: primary-scope selector + hierarchy-rule enforcement (v1.24.0), AI fields on node types (v1.30.0) and on connection types (v1.34.24), the generic `governance` / classification config (v1.25.0), field UX including `defaultCollapsed` and the repeater `visibleWhen` fix (v1.26.0), merge-field exposure via `getMergeFieldSchema` (v1.28.0), and shared field templates (v1.29.0) — plus the workflow → **stage** rename with real transition enforcement (v1.33.17), search over custom field data and bulk connection creation (v1.27.0). Two follow-ups from Phase 1 shipped as [v1.36.0](/modules/structure/changelog/#v1360) — formula traversal + list functions, and formulas on built-in fields.
- **Generic platform hardening (2026-08-09).** A nine-agent gap-analysis audit against generic data-platform benchmarks found the field/type *definition* layer already rich, and the real gaps in server-side **enforcement**. Shipped: server-side field validation (v1.33.31), node-type permission enforcement with a `'system'` tier (v1.33.32) and the `read` tier (v1.34.12–13), enforced scoping / "spaces" with `rootNodeId` (v1.34.0–1), the `auditEvents` contract and config dry-run/diff (v1.34.2), and atomic config-bundle export/import (v1.33.34–35). **Removed by decision:** computed-value server trust — a solo app whose only external write surface is the user's own API keys has no untrusted writer to spoof, and a faithful server recompute would duplicate the evaluator ([D11](/modules/structure/decisions/)). **Superseded:** the nullable-owner design for shared rows, and a separate `inputScope` key — v1.34.27 keys sharing on the *type* and keeps `structure_node.userId` non-nullable, while "who creates" is just the `permissions.create` tier. Remaining tails are carried above.
- **Trust-fix releases.** Two passes closed the same class of bug — admin controls that were declared, persisted, and read by nothing. v1.23.0 made `builtInFieldConfig.editableBy`, server-side data sort, conditional-colour labels and the chained-select editor real (with computed built-ins following in v1.23.3); v1.33.29 and v1.34.19 wired the component-template configs and the overview widget map that the admin editor had been persisting into the void. The class is not extinct: [Now / Next](#now--next) items 2 and 6 are the survivors (the third — module-registered bulk actions — shipped as v1.36.3).
- **Deferred items now closed.** The v1.13.3 health audit's two deferred performance refinements (`getTree` column select, data-filter query pagination) both landed properly in v1.34.20. The inert built-in field renderers were implemented across v1.20.0–v1.23.0, with `inputMask` **dropped** rather than built. Toast copy now names the node type. Server-side field filtering resolved in v1.20.0. Node-type-group CRUD gained its action hooks in v1.15.3. The dead scaffold reference templates were removed, and `src/templates/**` is module-owned so `scaffold:update` never merges into it. **Abandoned by design:** a `styleRegistry` contribution — theming belongs to a dedicated Style module, not Structure.
- **Scaffold adoption.** The per-module DB migration runner was adopted in v1.16.0 and the module now ships six migrations. This page previously carried "adopt scaffold v1.18.0" and "scaffold 1.18.2 → 1.19.1 available" as open items; the module has been on **scaffold v1.31.15** since 2026-08-16, thirteen minors past both.

---

## Related roadmaps

- [Module Catalogue](/modules/) — complete inventory of all modules: active, planned, and under consideration
- [Starter Template Roadmap](/starter-template/roadmap/) — planned features for the core template
- [Module Starter Roadmap](/module-starter/roadmap/) — planned features for the scaffolding tool
- [Structure Changelog](/modules/structure/changelog/) — detailed release notes per version
- [Design Decisions](/modules/structure/decisions/) — the standing calls (D1–D11) behind the items above

---

## Contributing ideas

1. **Check what is already here** — this page and the [module repository](https://github.com/ui-git-kim/structure)
2. **Open an issue** — describe the feature and its use cases
3. **Discuss** — engage with feedback to refine the proposal

<Aside type="tip">
When proposing features, consider how they integrate with the registry system. Features that other modules can extend through registries are preferred.
</Aside>
