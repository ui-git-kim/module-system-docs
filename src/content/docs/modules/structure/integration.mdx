---
title: Integration
description: How other modules depend on and integrate with the Structure module
sidebar:
  order: 5
---

import { Steps, Tabs, TabItem, Aside, Card, CardGrid } from '@astrojs/starlight/components';

Structure is the organisational backbone of every app built on the starter template. It defines the "things" users work with — Clients, Projects, Properties, Matters — and provides a universal system for organising, displaying, and navigating them. **Almost every module you build will integrate with Structure.**

This page is the definitive guide for module developers who need to extend, reference, or react to Structure nodes.

---

## How Modules Use Structure

There are three levels of integration, and most modules use all three:

### 1. Attach UI to Nodes (Frontend Registries)

Your module adds tabs, widgets, actions, columns, filters, sidebar sections, and fields to Structure's node pages. For example, a Tasks module adds a "Tasks" tab to every project's detail page, a task count badge on project cards, and a "Task Status" filter on list pages. This is done through Structure's **8 content registries** plus `structureRegistry` for field injection — you register components and field definitions, and Structure renders them in the right place.

<Aside type="note" title="11 registries total">
Structure exposes **11 module-specific registries**. 8 are content registries (`nodeTabRegistry`, `nodeWidgetRegistry`, `nodeCardExtensionRegistry`, `nodeActionRegistry`, `nodeColumnRegistry`, `nodeFilterRegistry`, `nodeSidebarSectionRegistry`, `nodeFieldTypeRegistry`). `nodeTypeRegistry` is a DB-synced configuration registry for node types (with a dedicated admin tab). `structureRegistry` handles module field injection, lifecycle hooks, and delete validation. The 11th is `formulaFunctionRegistry`, which ships from `utils/formula.ts` rather than `config/` — deep-import it from `@/features/structure/utils/formula` and call `.register({ name, fn, arity?, description?, category? })` to add named functions usable inside admin-authored `formula` fields. Connection types are not a code-based registry — they're managed through `structureService` methods and a dedicated admin tab (see below).
</Aside>

### 2. React to Node Events (Backend Action Hooks)

When nodes are created, updated, deleted, moved, archived, published, duplicated, or when stages change, Structure fires **50 action hooks** (47 always-on, plus 3 that only exist on embeddings-enabled installs). Your module listens to these events and reacts: clean up tasks when a project is deleted, generate an invoice when a client is archived, send a notification when a node is moved. This is the primary mechanism for **cross-module coordination**.

### 3. Transform Node Data (Backend Filter Hooks)

Your module can transform Structure data as it flows through the system — enrich node responses with billing status, modify list queries to exclude certain types, or validate node data before creation. This uses **4 filter hooks** that run in a pipeline.

<Aside type="caution" title="Action hooks are the key to module integration">
Without action hooks, modules are islands — they can display UI on nodes but can't react to what happens to those nodes. With action hooks, your module stays in sync: when a node is deleted, your module's related data is cleaned up. When a node is created, your module can set up defaults. This is what makes the module system truly composable.
</Aside>

---

## Integration Points at a Glance

<CardGrid>
  <Card title="React to Events" icon="rocket">
    Hook into 50 lifecycle events — run logic when nodes are created, updated, deleted, moved, archived, published, duplicated, or connected.
  </Card>
  <Card title="Add Tabs & Widgets" icon="list-format">
    Add tabs to node detail pages, widgets to the Overview tab, and sections to the sidebar.
  </Card>
  <Card title="Add Actions & Columns" icon="puzzle">
    Add action buttons to nodes, custom columns to table views, and filter controls to list pages.
  </Card>
  <Card title="Register Node Types" icon="document">
    Your module can define its own node types that appear in Structure's hierarchy and navigation.
  </Card>
</CardGrid>

| Integration | Frontend | Backend | Registry |
|-------------|----------|---------|----------|
| Add tabs to detail pages | ✅ | — | `nodeTabRegistry` |
| Add widgets to Overview tab | ✅ | — | `nodeWidgetRegistry` |
| Add badges/actions to cards | ✅ | — | `nodeCardExtensionRegistry` |
| Add action buttons to nodes | ✅ | — | `nodeActionRegistry` |
| Add columns to table views | ✅ | — | `nodeColumnRegistry` |
| Add filter controls to list pages | ✅ | — | `nodeFilterRegistry` |
| Add sidebar sections to detail pages | ✅ | — | `nodeSidebarSectionRegistry` |
| Register custom field types | ✅ | — | `nodeFieldTypeRegistry` |
| Register formula functions | ✅ | — | `formulaFunctionRegistry` (deep import from `utils/formula`) |
| Register fields on node types | ✅ | — | `structureRegistry.register({ fields })` |
| Register connection types | — | ✅ | `structureService` methods + admin tab (no registry) |
| Register new node types | ✅ | ✅ | `nodeTypeRegistry` + `structureService` |
| Write namespaced metadata | — | ✅ | `structureService.updateNodeMetadata()` |
| Reference nodes (NodeSelector) | ✅ | — | Component import |
| Customise UI labels per type | ✅ | — | `getLabel()` utility |
| Access Structure data at runtime | — | ✅ | `serviceRegistry.get('structure')` |
| Semantic search / embeddings (opt-in) | — | ✅ | `serviceRegistry.get('structureEmbeddings')` |
| Transform Structure data (filters) | — | ✅ | `filterRegistry` |
| React to Structure events (actions) | — | ✅ | `actionRegistry` |

---

## Declaring the Dependency

If your module depends on Structure, declare it with `depends` in your `module.ts` (scaffold v1.16.0+; the older `requiredModules` remains honoured):

```typescript
// src/module.ts
export const moduleDefinition: ModuleDefinition = {
  id: 'tasks',
  name: 'Tasks',
  version: '1.0.0',
  depends: { structure: '^1.0.0' },
  // ...
};
```

This ensures Structure is installed first and lets the scaffold validate the dependency at install time.

<Aside type="caution" title="Don't confuse the two `dependencies` fields">
The top-level `dependencies: string[]` field doesn't exist on `ModuleDefinition`. Use `depends` (module id → semver range; the deprecated `requiredModules` still works) for module-to-module dependencies. The `frontend.dependencies` and `backend.dependencies` arrays are for npm packages only.
</Aside>

---

## Frontend Integration

### Adding Tabs to Node Detail Pages

This is the most common integration. Your module adds a tab that appears on specific (or all) node types.

<Steps>

1. **Create your tab component**

    ```typescript
    // frontend/src/features/tasks/components/TasksTab.tsx
    import { useParams } from 'react-router';

    interface TasksTabProps {
      node: { id: string; nodeType: string };
    }

    export function TasksTab({ node }: TasksTabProps) {
      // Your tab content - fetch tasks for this node, display them, etc.
      return (
        <div>
          <h3>Tasks for {node.id}</h3>
          {/* Your task list, create button, etc. */}
        </div>
      );
    }
    ```

2. **Register the tab**

    ```typescript
    // frontend/src/features/tasks/registrations/structure-tabs.registration.ts
    import { nodeTabRegistry } from '@/features/structure';
    import { TasksTab } from '../components/TasksTab';

    nodeTabRegistry.register({
      id: 'tasks',
      moduleId: 'tasks',
      label: 'Tasks',
      icon: 'CheckSquare',
      nodeTypes: '*',           // Show on all node types, or ['project', 'client']
      order: 30,                // Sits between the built-in Children (20) and Connections (80) tabs
      component: TasksTab,
      // Optional: dynamic visibility
      // isVisible: (node, nodeTypeConfig) => nodeTypeConfig.features?.tasks !== false,
    });
    ```

3. **Import the registration in your feature index**

    ```typescript
    // frontend/src/features/tasks/index.ts

    // Side-effect imports (registrations) FIRST
    import './registrations/structure-tabs.registration';

    // Then public exports
    export { taskRoutes } from './routes.config';
    ```

</Steps>

<Aside type="note" title="Tab ordering">
Structure's built-in tabs use orders 0 (Overview), 10 (Info), 20 (Children), 80 (Connections) and 100 (Settings). Place a module tab at 30-70 to sit between Children and Connections, or above 100 to sit last — 30 is *not* "after the defaults". Use the [order value ranges](/getting-started/llm-rules/) from the LLM rules for consistency.
</Aside>

### Adding Widgets to the Overview Tab

Widgets appear on a node's Overview tab in a configurable layout.

```typescript
// frontend/src/features/activity/registrations/structure-widgets.registration.ts
import { nodeWidgetRegistry } from '@/features/structure';
import { RecentActivityWidget } from '../components/RecentActivityWidget';

nodeWidgetRegistry.register({
  id: 'recent-activity',
  moduleId: 'activity',
  title: 'Recent Activity',
  location: 'node-overview',
  nodeTypes: '*',
  order: 10,
  component: RecentActivityWidget,
});
```

Your widget component receives the same `{ node }` prop as tabs.

### Adding Badges or Actions to Node Cards

Extend how nodes appear in list/grid views.

```tsx
// frontend/src/features/tasks/registrations/structure-cards.registration.tsx
import { nodeCardExtensionRegistry } from '@/features/structure';
import { TaskCountBadge } from '../components/TaskCountBadge';

nodeCardExtensionRegistry.register({
  id: 'task-count',
  moduleId: 'tasks',
  position: 'badge',       // 'badge' | 'action' | 'header' | 'content' | 'footer'
  nodeTypes: '*',
  order: 10,
  render: (node) => <TaskCountBadge node={node} />,
});
```

### Adding Action Buttons to Nodes

Add buttons to node detail headers, context menus, or bulk action bars.

```typescript
// frontend/src/features/invoicing/registrations/structure-actions.registration.ts
import { nodeActionRegistry } from '@/features/structure';

nodeActionRegistry.register({
  id: 'generate-invoice',
  moduleId: 'invoicing',
  label: 'Generate Invoice',
  icon: 'Receipt',
  nodeTypes: ['client', 'project'],
  locations: ['detail-header'],
  variant: 'default',
  order: 30,
  handler: async (node) => {
    await invoicingService.generateForNode(node.id);
  },
  confirm: {
    title: 'Generate Invoice?',
    description: 'This will create a new invoice for all unbilled items.',
  },
});
```

### Adding Columns to Table Views

Register custom columns that appear in the table view on list pages.

```tsx
// frontend/src/features/tasks/registrations/structure-columns.registration.tsx
import { nodeColumnRegistry } from '@/features/structure';

nodeColumnRegistry.register({
  id: 'open-tasks',
  moduleId: 'tasks',
  label: 'Open Tasks',
  nodeTypes: ['project'],
  order: 40,
  width: '100px',
  alignment: 'center',
  sortable: true,
  render: (node) => <span>{node.metadata?.openTaskCount ?? 0}</span>,
});
```

### Adding Filter Controls to List Pages

Register filter UI controls for list pages. Filters can operate client-side or server-side.

```typescript
// frontend/src/features/tasks/registrations/structure-filters.registration.ts
import { nodeFilterRegistry } from '@/features/structure';
import { TaskStatusFilter } from '../components/TaskStatusFilter';

nodeFilterRegistry.register({
  id: 'task-status',
  moduleId: 'tasks',
  label: 'Task Status',
  icon: 'Filter',
  nodeTypes: ['project'],
  mode: 'client',
  order: 30,
  component: TaskStatusFilter,
  filterFn: (node, filterValue) => {
    if (!filterValue) return true;
    return node.metadata?.taskStatus === filterValue;
  },
});
```

### Adding Sidebar Sections

Add custom sections to the detail page sidebar (when using sidebar navigation style).

```typescript
// frontend/src/features/tasks/registrations/structure-sidebar.registration.ts
import { nodeSidebarSectionRegistry } from '@/features/structure';
import { TaskSummarySection } from '../components/TaskSummarySection';

nodeSidebarSectionRegistry.register({
  id: 'task-summary',
  moduleId: 'tasks',
  title: 'Task Summary',
  icon: 'CheckSquare',
  nodeTypes: ['project'],
  order: 30,
  collapsible: true,
  defaultCollapsed: false,
  component: TaskSummarySection,
});
```

### Registering Custom Field Types

Extend the field editor system with custom field types. Structure includes 22 built-in types; your module can add new ones. Field type ids must be unique — a registration whose id a built-in already uses is skipped with a console warning, so namespace your ids (e.g. `editor.rich-text`).

```typescript
// frontend/src/features/editor/registrations/structure-fields.registration.ts
import { nodeFieldTypeRegistry } from '@/features/structure';
import { RichTextEditor, RichTextDisplay } from '../components/RichTextEditor';

nodeFieldTypeRegistry.register({
  id: 'rich-text',
  moduleId: 'editor',
  label: 'Rich Text',
  category: 'text',
  icon: 'Type',
  order: 50,
  component: RichTextEditor,
  displayComponent: RichTextDisplay,
});
```

Once registered, the `NodeFieldRenderer` automatically picks up your custom field type. Admins can assign it to fields in the node type configuration.

### Using NodeSelector to Reference Nodes

If your module needs users to pick a Structure node (e.g., linking a task to a project), use the `NodeSelector` component:

```tsx
import { NodeSelector } from '@/features/structure';

function TaskForm() {
  const [selectedNodeId, setSelectedNodeId] = useState<string | null>(null);

  return (
    <NodeSelector
      value={selectedNodeId}
      onChange={setSelectedNodeId}
      allowedTypes={['project', 'client']}  // Optional: restrict to certain types
      placeholder="Select a project..."
    />
  );
}
```

`NodeSelector` is a searchable combobox (a shadcn `Popover` wrapping a `Command` list), restricted by `allowedTypes` and with optional "Create new" entries. It's the recommended way for any module to reference Structure nodes.

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `value` | `string \| null` | — | Selected node id (controlled) |
| `onChange` | `(value: string \| null) => void` | — | **Required.** Called with the new id, or `null` when cleared |
| `allowedTypes` | `string[]` | all types | Restrict the option list to these node type slugs |
| `excludeIds` | `string[]` | `[]` | Node ids to hide (e.g. the node being edited) |
| `rootOnly` | `boolean` | `false` | Offer root-level nodes only |
| `parentId` | `string` | — | Offer children of this node only (ignored when `rootOnly`) |
| `placeholder` | `string` | `'Select node...'` | Trigger text when nothing is selected |
| `showPath` | `boolean` | `false` | Show each node's ancestor path beneath its name |
| `showIcon` | `boolean` | `true` | Show the node type icon |
| `disabled` | `boolean` | `false` | Disable the trigger |
| `allowCreate` | `boolean` | `false` | Show a "Create new" group at the bottom of the list |
| `createTypes` | `string[]` | falls back to `allowedTypes` | Which types the create entries offer |
| `createParentId` | `string` | — | Accepted by the props interface but not yet used — `onCreateClick` owns creation today |
| `clearable` | `boolean` | `true` | Show a clear (✕) button when a node is selected |
| `searchable` | `boolean` | `true` | Show the search input inside the popover |
| `size` | `'sm' \| 'md' \| 'lg'` | `'md'` | Trigger button size |
| `className` | `string` | — | Extra classes on the trigger |
| `onCreateClick` | `(nodeType: string) => void` | — | Called with the chosen slug when a create entry is picked. **Required for `allowCreate` to render anything** — the selector doesn't create the node itself |

### Registering Fields on Node Types

Modules can inject their own fields onto node types without modifying the node type configuration directly. This is useful when your module needs to store data on nodes (e.g., a Billing module adding a "plan" field to all projects).

```typescript
// frontend/src/features/billing/registrations/structure.registration.ts
import { structureRegistry } from '@/features/structure';

structureRegistry.register({
  moduleId: 'billing',
  displayName: 'Billing',
  fields: [
    {
      id: 'billing.plan_id',           // namespace with module ID
      moduleId: 'billing',              // required on NodeFieldRegistration (register() also re-stamps it from the parent registration's moduleId)
      nodeTypes: ['project', 'client'], // or '*' for all types
      order: 90,                        // unified 0-100 scale; module fields always render after admin fields
      field: {
        type: 'select',
        label: 'Billing Plan',
        options: [
          { label: 'Free', value: 'free' },
          { label: 'Pro', value: 'pro' },
          { label: 'Enterprise', value: 'enterprise' },
        ],
      },
    },
  ],
});
```

Module-registered fields appear after admin-defined fields on the Info tab. Admins can control them on the **Integrations tab** of each node type — enabling/disabling, overriding the label, and changing display order. Field values are stored in the node's `data` JSON alongside admin-defined field values.

### Importing Structure Types

```typescript
// Import types for use in your module
import type { StructureNode, NodeTypeConfig } from '@/features/structure';

// Import hooks for reading Structure data
import { useNodes, useNode, useNodeTypes } from '@/features/structure';

// Import the typed client for Structure's REST surface — listNodes/getNode/
// createNode/updateNode/…/bulkArchiveNodes, listNodeTypes, connections,
// templates and settings, without importing Structure's internals
import { structureService } from '@/features/structure';
import type { StructureService } from '@/features/structure';

// Import registries for extension
import {
  nodeTabRegistry,
  nodeWidgetRegistry,
  nodeCardExtensionRegistry,
  nodeActionRegistry,
  nodeFieldTypeRegistry,
  nodeColumnRegistry,
  nodeFilterRegistry,
  nodeSidebarSectionRegistry,
  nodeTypeRegistry,
  structureRegistry,
} from '@/features/structure';

// Import label and value utilities for customised UI text
import { getLabel, getNodeFieldValue, resolveNodeColor, isExportSafe } from '@/features/structure';

// formulaFunctionRegistry is NOT re-exported from the barrel — deep import it
import { formulaFunctionRegistry } from '@/features/structure/utils/formula';
```

<Aside type="tip" title="structureService vs the hooks">
Use the `use*` hooks inside components — they carry TanStack caching and invalidation. Use `structureService` directly in event handlers, effects, and non-React code, and as the type for your `serviceRegistry.get('structure')` handle on the frontend.
</Aside>

### Invalidating Structure's Caches

Every Structure hook builds its TanStack Query key from `createModuleQueryKeys('structure')`, so **all** Structure cache entries live under the `['structure', …]` namespace. When your module mutates node data (through `structureService`, `updateNodeMetadata`, or your own endpoint), invalidate the keys Structure owns so its UI refetches:

```typescript
import { useQueryClient } from '@tanstack/react-query';

const queryClient = useQueryClient();

// Targeted — the node detail page and every list/grid/table query
queryClient.invalidateQueries({ queryKey: ['structure', 'node', nodeId] });
queryClient.invalidateQueries({ queryKey: ['structure', 'nodes'] });

// Blunt — everything Structure owns
queryClient.invalidateQueries({ queryKey: ['structure'] });
```

The sub-keys Structure uses are `nodes`, `node`, `node-path`, `node-children`, `node-connections`, `node-context`, `node-tree`, `node-types`, `node-type`, `node-type-groups`, `connection-types`, `connection-type`, `templates`, `field-templates`, `formula-functions`, `api-keys`, and `settings`.

---

## Frontend Registry API Reference

This section documents every configuration property for each frontend registry. Use this as a reference when writing registration code.

<Aside type="tip" title="Common patterns across all registries">
All registrations share these properties: `id` (unique identifier), `moduleId` (your module's ID), `nodeTypes` (`'*'` for all types or `string[]` of slugs), and `order` (display order, default 50). Structure's built-in items don't sit in one narrow band — built-in tabs run 0-100 and built-in field types 10-100 — so check the built-in orders for the registry you're extending rather than assuming 30+ lands last.
</Aside>

### nodeTabRegistry

Adds tabs to node detail pages.

```typescript
import { nodeTabRegistry } from '@/features/structure';
import type { NodeTabRegistration } from '@/features/structure';
```

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `id` | `string` | ✅ | Unique tab identifier |
| `moduleId` | `string` | ✅ | Your module's ID |
| `label` | `string` | ✅ | Tab display label |
| `icon` | `string` | — | Lucide icon name |
| `nodeTypes` | `string[] \| '*'` | ✅ | Which node types show this tab |
| `order` | `number` | — | Position in tab bar (default 50) |
| `component` | `ComponentType<NodeTabProps>` | ✅ | React component receiving `{ node, nodeType }` |
| `getBadge` | `(nodeId: string) => Promise<string \| number \| null>` | — | Async badge count/text shown on the tab |
| `isVisible` | `(node, nodeType) => boolean` | — | Dynamic visibility based on node state |

**Component props:** Your tab component receives `{ node: StructureNode, nodeType: NodeTypeConfig }`.

### nodeWidgetRegistry

Adds widgets to the Overview tab, dashboard, or node sidebar.

```typescript
import { nodeWidgetRegistry } from '@/features/structure';
import type { NodeWidgetRegistration } from '@/features/structure';
```

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `id` | `string` | ✅ | Unique widget identifier |
| `moduleId` | `string` | ✅ | Your module's ID |
| `title` | `string` | ✅ | Widget display title |
| `icon` | `string` | — | Lucide icon name |
| `location` | `'node-overview' \| 'dashboard' \| 'node-sidebar'` | ✅ | Where the widget renders |
| `nodeTypes` | `string[] \| '*'` | — | Which node types show this widget |
| `size` | `'1x1' \| '2x1' \| '1x2' \| '2x2' \| 'full'` | — | Widget size in the grid layout |
| `order` | `number` | — | Position in widget layout (default 50) |
| `component` | `ComponentType<NodeWidgetProps>` | ✅ | React component receiving `{ node?, nodeType?, size }` |
| `isVisible` | `(node, nodeType) => boolean` | — | Dynamic visibility based on node state |

### nodeCardExtensionRegistry

Extends node cards in grid and list views. Each extension renders in one of 5 positions on the card.

```typescript
import { nodeCardExtensionRegistry } from '@/features/structure';
import type { NodeCardExtension, CardExtensionPosition } from '@/features/structure';
```

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `id` | `string` | ✅ | Unique extension identifier |
| `moduleId` | `string` | ✅ | Your module's ID |
| `nodeTypes` | `string[] \| '*'` | ✅ | Which node types show this extension |
| `position` | `CardExtensionPosition` | ✅ | Where on the card to render (see table below) |
| `order` | `number` | — | Position order within the slot (default 50) |
| `render` | `(node: StructureNode) => ReactNode` | ✅ | Render function returning JSX |

**Card extension positions:**

| Position | shadcn Card Part | Description | Example Use |
|----------|-----------------|-------------|-------------|
| `badge` | `CardHeader` (top-right) | Small status indicators | Task count, status dot, priority flag |
| `action` | `CardHeader` (actions area) | Interactive buttons/icons | Quick-add, bookmark, share |
| `header` | Below `CardHeader` | Additional header content | Progress bar, subtitle, tags |
| `content` | `CardContent` | Main card body extension | Preview text, thumbnail, summary stats |
| `footer` | `CardFooter` | Bottom of card | Metadata, timestamps, assignees |

Admins can move extensions between positions via the admin panel.

### nodeActionRegistry

Adds action buttons to node detail headers, context menus, and bulk action bars.

```typescript
import { nodeActionRegistry } from '@/features/structure';
import type { NodeActionRegistration } from '@/features/structure';
```

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `id` | `string` | ✅ | Unique action identifier |
| `moduleId` | `string` | ✅ | Your module's ID |
| `label` | `string` | ✅ | Button/menu item label |
| `icon` | `string` | — | Lucide icon name |
| `nodeTypes` | `string[] \| '*'` | ✅ | Which node types show this action |
| `locations` | `NodeActionLocation[]` | ✅ | `'detail-header'`, `'context-menu'`, `'bulk-action'` |
| `order` | `number` | — | Position in action list (default 50) |
| `variant` | `'default' \| 'destructive' \| 'secondary'` | — | Button style variant |
| `handler` | `(node, nodeType) => Promise<void>` | ✅ | Single-node action handler |
| `bulkHandler` | `(nodes[], nodeType) => Promise<void>` | — | Bulk action handler for multiple nodes |
| `isVisible` | `(node, nodeType) => boolean` | — | Dynamic visibility |
| `confirm` | `{ title, description, confirmLabel?, variant? }` | — | Show confirmation dialog before executing |
| `shortcut` | `string` | — | Keyboard shortcut (e.g., `'mod+shift+i'`) |

### nodeColumnRegistry

Adds columns to table view on list pages.

```typescript
import { nodeColumnRegistry } from '@/features/structure';
import type { NodeColumnRegistration } from '@/features/structure';
```

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `id` | `string` | ✅ | Unique column identifier |
| `moduleId` | `string` | ✅ | Your module's ID |
| `label` | `string` | ✅ | Column header text |
| `nodeTypes` | `string[] \| '*'` | ✅ | Which node types show this column |
| `order` | `number` | — | Column position (default 50) |
| `render` | `(node, nodeType) => ReactNode` | ✅ | Cell render function |
| `width` | `string` | — | CSS width (`'120px'`, `'15%'`, `'auto'`) |
| `minWidth` | `string` | — | CSS min-width |
| `alignment` | `'left' \| 'center' \| 'right'` | — | Text alignment (default `'left'`) |
| `sortable` | `boolean` | — | Whether users can sort by this column |
| `getSortValue` | `(node) => string \| number` | — | Custom sort value extractor |
| `isVisible` | `(nodeType) => boolean` | — | Dynamic visibility |
| `needsPrefetch` | `boolean` | — | Whether data must be loaded before rendering |
| `prefetch` | `(nodeIds: string[]) => Promise<void>` | — | Batch data loader for multiple nodes |

<Aside type="tip" title="Prefetch for performance">
If your column displays data from your module's API (e.g., task count per node), use `needsPrefetch: true` and `prefetch` to batch-load data for all visible nodes in one request, rather than making per-row API calls.
</Aside>

### nodeFilterRegistry

Adds filter controls to list page filter bars. Supports client-side or server-side filtering.

```typescript
import { nodeFilterRegistry } from '@/features/structure';
import type { NodeFilterRegistration } from '@/features/structure';
```

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `id` | `string` | ✅ | Unique filter identifier |
| `moduleId` | `string` | ✅ | Your module's ID |
| `label` | `string` | ✅ | Filter label in the filter bar |
| `icon` | `string` | — | Lucide icon name |
| `nodeTypes` | `string[] \| '*'` | ✅ | Which node types show this filter |
| `order` | `number` | — | Position in filter bar (default 50) |
| `mode` | `'client' \| 'server'` | ✅ | Client-side filtering or server-side query params |
| `component` | `ComponentType<FilterRenderProps>` | ✅ | Filter UI component |
| `filterFn` | `(node, filterValue) => boolean` | — | Client-side filter function (required when `mode: 'client'`) |
| `toQueryParams` | `(filterValue) => Record<string, string>` | — | Convert filter value to API query params (required when `mode: 'server'`) |
| `isVisible` | `(nodeType) => boolean` | — | Dynamic visibility |

**Filter component props:** `{ value, onChange, nodeType, nodes? }`

### nodeSidebarSectionRegistry

Adds sections to the detail page sidebar (when using sidebar navigation style).

```typescript
import { nodeSidebarSectionRegistry } from '@/features/structure';
import type { NodeSidebarSectionRegistration } from '@/features/structure';
```

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `id` | `string` | ✅ | Unique section identifier |
| `moduleId` | `string` | ✅ | Your module's ID |
| `title` | `string` | ✅ | Section display title |
| `icon` | `string` | — | Lucide icon name |
| `nodeTypes` | `string[] \| '*'` | ✅ | Which node types show this section |
| `order` | `number` | — | Position in sidebar (default 50) |
| `component` | `ComponentType<{ node, nodeType }>` | ✅ | Section content component |
| `collapsible` | `boolean` | — | Whether section can collapse (default `true`) |
| `defaultCollapsed` | `boolean` | — | Start collapsed? (default `false`) |
| `isVisible` | `(node, nodeType) => boolean` | — | Dynamic visibility |

### nodeFieldTypeRegistry

Extends the field type system with custom field types beyond the 22 built-in types.

```typescript
import { nodeFieldTypeRegistry } from '@/features/structure';
import type { NodeFieldTypeRegistration } from '@/features/structure';
```

| Property | Type | Required | Description |
|----------|------|----------|-------------|
| `id` | `string` | ✅ | Field type identifier (e.g., `'rich-text'`, `'map-location'`) |
| `moduleId` | `string` | ✅ | Your module's ID |
| `label` | `string` | ✅ | Human-readable name shown in admin field picker |
| `icon` | `string` | — | Lucide icon for the field type picker |
| `description` | `string` | — | Help text in admin field type selector |
| `category` | `'text' \| 'media' \| 'reference' \| 'layout' \| 'custom'` | — | Field categorisation |
| `order` | `number` | — | Position in field type picker (default 50) |
| `component` | `ComponentType<FieldTypeRenderProps>` | ✅ | Form field component for editing |
| `displayComponent` | `ComponentType<FieldTypeDisplayProps>` | — | Read-only display for cards/columns |
| `validate` | `(value, fieldConfig) => string \| null` | — | Custom validation (return error string or null) |
| `transformBeforeSave` | `(value) => unknown` | — | Transform value before saving to database |
| `getDefaultValue` | `() => unknown` | — | Factory for default field value |
| `configSchema` | `NodeTypeField[]` | — | Additional config fields shown in admin when this type is selected |

**Component props:**
- Edit: `{ field: NodeTypeField, value: unknown, onChange: (value) => void, error?: string, disabled?: boolean }`
- Display: `{ field: NodeTypeField, value: unknown }`

### NodeTypeRegistry (Database-Synced)

Unlike the 8 content registries above (which are code-based), the `NodeTypeRegistry` is synced with the database and provides methods for managing node types at runtime.

<Aside type="caution" title="`get` vs `getConfig`">
The registry stores `NodeTypeRegistration` wrappers (which bundle the config with optional component overrides). Use `getConfig(slug)` / `getAllTypes()` when you want the bare `NodeTypeConfig`. The `get()` / `getAll()` methods return the wrapper.
</Aside>

| Method | Returns | Description |
|--------|---------|-------------|
| `register(registration)` | `void` | Register a node type registration (wraps `config`) |
| `unregister(slug)` | `void` | Remove a type from the registry |
| `update(config)` | `void` | Update the config for an existing slug |
| `get(slug)` | `NodeTypeRegistration \| undefined` | Get the wrapper (config + optional overrides) |
| `getConfig(slug)` | `NodeTypeConfig \| undefined` | Get just the config — the common case |
| `getAll()` | `NodeTypeRegistration[]` | All registrations (wrappers) |
| `getAllTypes()` | `NodeTypeConfig[]` | All configs, sorted by `sortOrder` |
| `getEnabledTypes()` | `NodeTypeConfig[]` | Enabled configs only, sorted by `sortOrder` |
| `getByModule(moduleId)` | `NodeTypeConfig[]` | Types owned by a given module |
| `getSlugs()` | `string[]` | All registered slugs |
| `has(slug)` | `boolean` | Check existence |
| `isLoadedFromDb()` | `boolean` | Whether `syncFromDatabase` has run |
| `syncFromDatabase(types)` | `void` | Sync registry state with database records; fires `onSync` |
| `onSync(callback)` | `() => void` | Subscribe to sync events — returns an unsubscribe function |

**Reactive hooks:**
- `useEnabledNodeTypes()` — returns enabled configs, re-renders on `onSync()`
- `useStructureNavItems()` — returns sidebar navigation items, re-renders on `onSync()`

---

## Backend Integration

### Reacting to Structure Events (Action Hooks)

<Aside type="tip" title="Start here">
Action hooks are the most important backend integration point. If your module only does one backend integration with Structure, it should be this one.
</Aside>

Action hooks fire after every Structure mutation. Your module registers listeners that run automatically when nodes are created, updated, deleted, moved, archived, restored, or connected. This is how modules stay in sync — and it's what makes the module system truly composable.

**Common use cases:**
- **Cleanup on delete** — archive or remove your module's data when the parent node is deleted
- **Setup on create** — create default records when a new node of a specific type is created
- **Sync on update** — update cached data, trigger recalculations, or send notifications
- **Audit trail** — log significant events for compliance or debugging

<Aside type="caution" title="Per-node cleanup ≠ account-deletion cleanup">
Node-delete action hooks (`structure.node.deleted`) only fire when an *individual node* is deleted — they do **not** fire on account deletion. To erase your module's data when a user's account is deleted, use your module's own generated `security.registration.ts`, which registers a deletion handler with the starter's `securityRegistry`. Structure ships its own `security.registration.ts` that erases all Structure rows on account deletion; your integrating module needs its own to do the same for its tables. See the [Security guide](/module-starter/security/) for details.
</Aside>

```typescript
// backend/src/features/tasks/action.registration.ts
import { actionRegistry } from '../../config/action.registry.js';

// Clean up tasks when a node is deleted
actionRegistry.addActionAsync(
  'structure.node.deleted',
  'tasks-cleanup-on-delete',
  async (context) => {
    const { userId, nodeId } = context;
    // Delete or archive tasks associated with this node
    await prisma.task.updateMany({
      where: { nodeId, userId },
      data: { status: 'archived' },
    });
  },
  20
);

// Set up default task list when a node is created
actionRegistry.addActionAsync(
  'structure.node.created',
  'tasks-setup-defaults',
  async (context) => {
    const { userId, node, nodeType } = context;
    if (nodeType === 'project') {
      // Create a default "To Do" task list for new projects
      await prisma.task_list.create({
        data: { nodeId: node.id, userId, name: 'To Do' },
      });
    }
  },
  20
);
```

**Available Structure action hooks (50 total — the three embeddings hooks only fire on embeddings-enabled installs):**

| Hook | Context | Fires when |
|------|---------|------------|
| `structure.node.created` | `{ userId, node, nodeType }` | A node is created |
| `structure.node.updated` | `{ userId, node, changes }` | A node is updated |
| `structure.node.deleted` | `{ userId, nodeId, nodeType }` | A node is deleted |
| `structure.node.moved` | `{ userId, node, oldParentId, newParentId, oldRootNodeId, newRootNodeId }` | A node moves parent (or space — compare `oldRootNodeId`/`newRootNodeId`) |
| `structure.node.archived` | `{ userId, node }` | A node is archived |
| `structure.node.restored` | `{ userId, node }` | A node is restored from archive |
| `structure.node.favorited` | `{ userId, node, isFavorite }` | Favourite is toggled |
| `structure.node.published` | `{ userId, node }` | A node's `isPublished` changes to true |
| `structure.node.unpublished` | `{ userId, node }` | A node's `isPublished` changes to false |
| `structure.node.stageChanged` | `{ userId, node, oldStage, newStage, oldGroup?, newGroup? }` | A node's stage changes |
| `structure.node.classificationChanged` | `{ userId, node, oldClassification, newClassification }` | A node's per-node classification override changes |
| `structure.node.duplicated` | `{ userId, sourceNodeId, newNode, includeChildren }` | A node is duplicated |
| `structure.node.reordered` | `{ userId, parentId, nodeIds }` | Nodes are reordered within a parent |
| `structure.node.metadataUpdated` | `{ userId, nodeId, moduleId }` | Namespaced metadata is written |
| `structure.node.embeddingUpdated` | `{ userId, nodeId, embeddingModel }` | A node's embedding is stored or cleared (`embeddingModel` is `null` on clear) (opt-in embeddings) |
| `structure.node.chunksReplaced` | `{ userId, nodeId, count }` | A node's chunk set is replaced (opt-in embeddings) |
| `structure.chunk.embeddingUpdated` | `{ userId, chunkId, embeddingModel }` | A chunk's embedding is stored (opt-in embeddings) |
| `structure.nodes.bulkCreated` | `{ userId, count, nodeTypes, nodeIds }` | Multiple nodes are created with `skipHooks: true` — **mutually exclusive** with the per-node `structure.node.created` hook. The default (`skipHooks: false`) fires `structure.node.created` once per node and does **not** fire this summary |
| `structure.nodes.bulkArchived` | `{ userId, nodeIds }` | Multiple nodes are archived in one bulk operation (each node's own `archived` hook also fires) |
| `structure.nodes.bulkDeleted` | `{ userId, nodeIds }` | Multiple nodes are soft-deleted in one bulk operation (each node's own `deleted` hook also fires) |
| `structure.nodes.bulkRestored` | `{ userId, nodeIds }` | Multiple nodes are restored in one bulk operation (each node's own `restored` hook also fires) |
| `structure.connection.created` | `{ userId, connection }` | A connection goes live — created with status `active`, or promoted from pending |
| `structure.connection.proposed` | `{ userId, connection }` | A connection is created with status `pending` (awaiting promotion) |
| `structure.connection.promoted` | `{ userId, connection }` | A pending connection is promoted to active (fires after `created`) |
| `structure.connection.deleted` | `{ userId, connectionId }` | A connection is deleted |
| `structure.connections.bulkCreated` | `{ userId, count, connectionIds }` | Multiple connections are created with `skipHooks: true` — **mutually exclusive** with the per-connection `structure.connection.created` / `.proposed` hooks, which the default (`skipHooks: false`) fires instead |
| `structure.nodeType.created` | `{ userId, nodeType }` | A node type goes live — created directly, or a proposal is approved |
| `structure.nodeType.proposed` | `{ userId, proposal }` | A node type proposal is created (awaiting admin approval) |
| `structure.nodeType.proposalApproved` | `{ userId, nodeType, scope }` | A proposal is approved (fires alongside `structure.nodeType.created`) |
| `structure.nodeType.proposalRejected` | `{ userId, slug, config, meta }` | A proposal is rejected — the row is deleted; the full proposed config rides on the payload |
| `structure.nodeType.updated` | `{ userId, slug, changes }` | A node type is updated |
| `structure.nodeType.deleted` | `{ userId, slug }` | A node type is deleted |
| `structure.nodeType.imported` | `{ userId, stats, mode }` | Node types are imported (merge or replace) via the node-types-only endpoint |
| `structure.config.imported` | `{ userId, result, mode }` | A full config bundle is imported atomically (`result` = per-category `{ created, updated, deleted }` for field templates, groups, node types, connection types, functions) |
| `structure.nodeTypeGroup.created` | `{ userId, nodeTypeGroup }` | A node type group is created |
| `structure.nodeTypeGroup.updated` | `{ userId, slug, changes }` | A node type group is updated |
| `structure.nodeTypeGroup.deleted` | `{ userId, slug }` | A node type group is deleted |
| `structure.connectionType.created` | `{ userId, connectionType }` | A connection type is created |
| `structure.connectionType.updated` | `{ userId, slug, changes }` | A connection type is updated |
| `structure.connectionType.deleted` | `{ userId, slug }` | A connection type is deleted |
| `structure.template.created` | `{ userId, template }` | A template is created |
| `structure.template.updated` | `{ userId, templateId, changes }` | A template is updated |
| `structure.template.deleted` | `{ userId, templateId }` | A template is deleted |
| `structure.fieldTemplate.created` | `{ userId, template }` | A field template is created |
| `structure.fieldTemplate.updated` | `{ userId, fieldTemplateId, changes }` | A field template is updated |
| `structure.fieldTemplate.deleted` | `{ userId, fieldTemplateId }` | A field template is deleted |
| `structure.function.created` | `{ userId, function }` | A formula function is created |
| `structure.function.updated` | `{ userId, functionId, changes }` | A formula function is updated |
| `structure.function.deleted` | `{ userId, functionId }` | A formula function is deleted |
| `structure.settings.updated` | `{ userId, settings }` | User settings are updated |

### Transforming Structure Data (Filter Hooks)

Register filters to modify Structure data as it flows through the system. Filters run in a pipeline — each receives the output of the previous one.

```typescript
// backend/src/features/billing/filter.registration.ts
import { filterRegistry } from '../../config/filter.registry.js';

// Enrich node responses with billing data
filterRegistry.register(
  'structure.node.response',
  'billing-enrich-node',
  (node, context) => {
    // Add billing status to node metadata.
    // NOTE: this runs on single-node detail reads only — list responses
    // are not passed through this filter.
    // (actual implementation would fetch from database)
    return {
      ...node,
      metadata: {
        ...node.metadata,
        hasBilling: true,
      },
    };
  },
  20
);

// Modify list queries to exclude certain types
filterRegistry.register(
  'structure.node.listQuery',
  'billing-filter-query',
  (query, context) => {
    // Example: modify the query before it executes
    return query;
  },
  20
);
```

**Available Structure filter hooks:**

| Hook | Receives | Purpose |
|------|----------|---------|
| `structure.node.beforeCreate` | Node input data | Transform data before a node is saved |
| `structure.node.response` | A single node object | Enrich node data before sending to client. Applied in `getNode` **only** — detail reads. List responses are *not* passed through this filter, so enrichment added here won't appear in grid/list/table views; use `structure.node.listQuery` to influence lists |
| `structure.node.listQuery` | Query parameters | Modify list query before execution |
| `structure.nodeType.response` | Node type object | Transform node type data before sending to client. Runs on both single (`getNodeType`) and list (`listNodeTypes`, per item) responses |

### Accessing Structure Data via Service Registry

Other modules access Structure's backend service through the `serviceRegistry` — never by importing directly from the Structure feature folder.

```typescript
// backend/src/features/tasks/tasks.service.ts
import { serviceRegistry } from '../../config/service.registry.js';

export const tasksService = {
  async getTasksForNode(nodeId: string, userId: string) {
    // Access Structure service at runtime
    const structureService = serviceRegistry.get('structure');

    // Verify the node exists and belongs to the user
    const node = await structureService.getNode(nodeId, userId);

    // Now fetch tasks for this node
    return prisma.task.findMany({
      where: { nodeId: node.id, userId },
    });
  },
};
```

<Aside type="caution" title="Never import across module boundaries">
Do NOT import directly from `../../features/structure/structure.service.js`. Always use `serviceRegistry.get('structure')` to access other modules' services. This maintains module isolation and allows the system to detect missing dependencies.
</Aside>

### Scoping and Read Permissions for Consuming Modules (v1.34.2+)

Structure's retrieval methods — the search, similarity, and graph primitives — are **system-tier**: they receive a `userId` but no role or request context, so they cannot work out which node types that user is allowed to read, or which space they're currently in. That makes it the *consumer's* job. If your module serves Structure retrieval results back to an end user (a RAG answer, an AI context bundle, a search page), you must resolve the restrictions yourself and pass them down.

Each node type carries a `permissions.read` tier of `'anyone' | 'admin' | 'system'`. Ask Structure which types the end user may **not** read:

```typescript
const structureService = serviceRegistry.get('structure');

// Pass the END USER'S flags — not your module's service identity
const excludeNodeTypes = await structureService.getUnreadableTypes(userId, {
  isAdmin: user.isAdmin,
  isSystem: false,
});
// Admin/system callers get []
```

Then pass that array as `excludeNodeTypes` on every retrieval call:

| Method | Service | Options that must carry the contract |
|--------|---------|--------------------------------------|
| `semanticSearchNodes` | `structureEmbeddings` | `excludeNodeTypes`, `scopeRootId` |
| `semanticSearchChunks` | `structureEmbeddings` | `excludeNodeTypes`, `scopeRootId` |
| `lexicalSearchChunks` | `structureEmbeddings` | `excludeNodeTypes`, `scopeRootId` |
| `hybridSearchNodes` | `structureEmbeddings` | `excludeNodeTypes`, `scopeRootId` |
| `hybridSearchChunks` | `structureEmbeddings` | `excludeNodeTypes`, `scopeRootId` |
| `findSimilarNodes` | `structureEmbeddings` | `excludeNodeTypes`, `scopeRootId` |
| `expandGraph` | `structure` | `excludeNodeTypes` (scope is anchored on the seeds automatically) |

`scopeRootId` is the second half of the contract. When the app uses **enforced spaces**, results must be confined to the active space: pass that space's root node id (`structure_node.rootNodeId`) as `scopeRootId`. Your module resolves and validates the active space; Structure only applies the filter, and rows with a null `rootNodeId` are excluded when it's set. `expandGraph` is the exception — it anchors traversal on the seed nodes' own space, so it needs no `scopeRootId`.

<Aside type="caution" title="Omitting these leaks data">
`excludeNodeTypes` and `scopeRootId` both default to "no filtering". A consumer that forgets them will happily surface admin-only node types, and content from spaces the user isn't in, inside its own results. Structure cannot catch this for you.
</Aside>

### Shared Node Types: Read Across Owners, Write Owner-Only (v1.34.27+)

An admin can flag a node type **Shared across all users** from the Scope section of its admin form — `primaryScope.global: true` on `NodeTypeConfig`. Any module that reads nodes, search results, or retrieval results has to account for this, even if your own module never sets the flag: on a shared type the rows Structure hands back are no longer guaranteed to be the caller's own. See [Configuration](/modules/structure/configuration/) for turning it on and [Usage](/modules/structure/usage/) for what an end user sees.

The whole contract is one sentence: **a shared type is read-shared and never write-shared.**

**Reads widen.** Every read path admits other users' rows of a shared type alongside the caller's own — `getNode`, `listNodes`, `getTree`, `getChildren`, `getDescendants`, the ancestor chain, `searchNodes`, every `structureEmbeddings` search and similarity method, `getNodeChunks`, `expandGraph`, and the hydrated `fromNode`/`toNode` on a connection. `excludeNodeTypes` still applies exactly as above — a shared type named there is dropped like any other. `scopeRootId` does **not** confine a shared type: it is exempt from enforced-space confinement, so it stays visible in every space and is a valid connection target from any space.

**Writes do not.** `updateNode`, `deleteNode`, `archiveNode`, `restoreNode`, `moveNode`, `duplicateNode`, favourite toggles and every bulk method keep their plain `userId` ownership predicate and throw `NotFoundError` for a row the caller doesn't own — at **any** permission tier, with **no admin bypass**. The per-type `permissions` tiers only narrow who may create and edit their own entries; they never widen who may change someone else's.

<Aside type="caution" title="Never assume you can write what you can read">
This is the one mistake to avoid. If your module reads a node and then writes it back, compare `node.userId` with the acting user first — otherwise your UI renders an Edit button that 404s, and your backend logic silently fails on rows it was never allowed to touch.
</Aside>

```typescript
const structureService = serviceRegistry.get('structure');

// On a shared type this may be ANOTHER user's row
const node = await structureService.getNode(nodeId, userId);
const isOwner = node.userId === userId;

if (!isOwner) {
  // Read-only: structureService.updateNode(nodeId, userId, ...) throws NotFoundError
}
```

`userId` is on the `StructureNode` wire type, so frontend registrations (tabs, actions, sidebar sections, card extensions) make the same comparison against the session user. Structure's own detail header and Settings tab do exactly that — every write control is hidden and a read-only marker takes its place for a non-owner.

**A non-owner's row arrives sanitised.** Structure strips the creator's private material before the row leaves the service, on every surface — detail, list, tree, search, children, descendants, connection endpoints, and semantic/lexical/hybrid results:

| Field | What a non-owner receives |
|-------|---------------------------|
| `data` | Secret-typed field values are **removed** — the keys are absent, not blanked. Probe with `'fieldId' in node.data`, never for `''` |
| `embeddingText` | `null` |
| `settings`, `metadata` | `{}` |
| `isFavorite` | `false` — favourites are the creator's, and the Favourites filter returns owned rows only |

Sanitisation happens inside the service, *before* the `structure.node.response` filter runs — so anything your filter hook adds back onto a node is **not** sanitised. Gate the enrichment on ownership if it carries per-user data.

**Edges are never shared.** Sharing applies to nodes, not to connections: a connection row is itself a node of the reserved `connection` type, which can never be flagged shared. `getNodeConnections`, `listConnections` and `expandGraph`'s edge join all keep their plain `userId` predicate, so two users looking at the same shared node see different edge sets — each sees only their own, and may create, promote, restore and delete only their own. The one deliberate asymmetry: deleting or restoring a shared node sweeps the connections referencing it **across owners**, so a delete can't leave another user holding a live edge to a dead endpoint.

**Chunks and embeddings are restricted.** This is what an ingest or RAG module most needs to know:

| Method | Behaviour on a shared type |
|--------|----------------------------|
| `listNodesNeedingEmbedding` | Owner-scoped, never widened — a shared node is embedded once, on its owner's ingest pass, and a reader's pass never picks it up |
| `setNodeEmbedding`, `clearNodeEmbedding`, `replaceNodeChunks`, `setChunkEmbedding`, `deleteNodeChunks` | Owner-scoped writes — they don't match a node the caller doesn't own |
| `getNodeChunks`, `lexicalSearchChunks`, `semanticSearchChunks`, `hybridSearchChunks` | Widen on the **source node's** type. A non-owner's chunk has `metadata` blanked, and the chunk is **omitted entirely** when the source type declares any secret field — chunk `content` is prose, and a secret can sit inside it. Fails closed when the type or its field definitions can't be resolved |

So a shared type that declares secret fields contributes node-level retrieval results but no chunk-level ones, and `embeddingText` is never available for another user's row. Build context bundles from what comes back, not from what you expect to come back.

<Aside type="note" title="Guards and known limits">
Structure **refuses** to un-share a type while any other user holds a live node of it (`BadRequestError` naming the slug and the count), counts a shared type's nodes across owners in `deleteNodeType`, and **fails** a replace-mode `importNodeTypes` rather than sweeping a shared type away under other users' rows. Surface those errors to the operator instead of retrying — each names what has to be migrated first. Two limits worth knowing: `dataFilters` run against the raw `data` column *before* sanitisation, so an equality probe on a secret field matches the sealed envelope's shape rather than plaintext (legacy values written before the field was marked secret are the real residual, until the next write seals them); and the shared-slug list is cached for 15s per process — bounded safe in both directions, because newly sharing is purely additive and un-sharing is refused while other users hold nodes.
</Aside>

### Connection Lifecycle Methods (v1.16.0+)

Connections carry provenance (`origin`) and lifecycle (`status`). Your module can create a connection as a `pending` proposal — it stays invisible to normal queries until it's promoted to active. All methods are available via `serviceRegistry.get('structure')`:

| Method | Description |
|--------|-------------|
| `createConnection(userId, fromNodeId, toNodeId, connectionType, data?, metadata?, options?)` | `options` is `{ origin?, status?, skipHooks? }`. `origin` is a lowercase-kebab provenance tag stamped into `data.origin` (default `'explicit'`; other well-known values: `'discovered'`, `'active'`). `status` is `'active'` (default, goes live immediately) or `'pending'` (fires `structure.connection.proposed` instead of `created`). `skipHooks: true` suppresses that per-connection hook — it's how bulk creation trades the per-row fan-out for the single `structure.connections.bulkCreated` summary |
| `promoteConnection(id, userId)` | Promotes a pending connection to active. Re-validates that both endpoints are still active and re-checks the connection type's `isUnique`/`maxConnections` rules against the active set (`BadRequest` with machine-readable prefixes `duplicate-active-connection:`, `max-connections-reached:`, `endpoint-not-active:`). Fires `structure.connection.created` then `structure.connection.promoted` |
| `getNodeConnections(nodeId, userId, options?)` | `options` is `{ status?: string[], origin?: string }`. Returns **active connections only by default** (previously all statuses) — pass `status: ['active', 'pending']` to include proposals |
| `listConnections(userId, query?, options?)` | Paginated listing across all nodes: `query` is `{ status?, origin?, connectionType?, page?, limit? }` → `{ connections, total, page, limit, totalPages }`. `options` is `{ scopeRootId?, isSystem?, isAdmin? }` — pass `scopeRootId` to confine the listing to one space |

The `NodeConnection` wire type now includes `status` and `origin` (legacy rows report `origin: 'explicit'`). Note that `origin` is a reserved connection field id — connection types can't define a custom field with that id.

### Node Type Proposals (v1.16.0+)

Instead of creating node types directly, your module can propose them for admin approval. A pending proposal reserves its slug but stays disabled and invisible to every other node-type query until approved:

| Method | Description |
|--------|-------------|
| `proposeNodeType(userId, config, { proposedBy, reason?, sourceContext? })` | Creates a pending, disabled node type from a standard `NodeTypeConfig`; `proposedBy` (your module id) becomes the `moduleId` fallback. Fires `structure.nodeType.proposed` |
| `listNodeTypeProposals(userId)` | Returns pending proposals, oldest first |
| `approveNodeTypeProposal(slug, userId, { scope? })` | Makes the proposal a live, enabled type. `scope: 'user'` (default) keeps it owned by the user; `'system'` promotes it to a shared system type. Fires `structure.nodeType.proposalApproved` and `structure.nodeType.created` — the type goes live at approval |
| `rejectNodeTypeProposal(slug, userId)` | Deletes the proposal row (freeing the slug). Fires `structure.nodeType.proposalRejected` with the full proposed config — listen for it if your module should remember not to re-propose |

### Cognitive Integration (v1.16.0+)

Structure ships opt-in cognitive foundations: pgvector node embeddings with a semantic search service, keyword (lexical) and hybrid search, and a `features.cognitiveIngest` flag on node types. Because embeddings are an install-time opt-in, probe availability with `structureService.getCognitiveCapabilities()` — it returns `{ embeddings: { enabled, dimensions }, chunks: { enabled }, lexical: { enabled }, graph: { enabled } }` without hard imports. `lexical.enabled` is always `true` — node keyword search (`structureService.searchNodes`) is always-on and needs no embeddings; only the *chunk*-keyword and hybrid methods additionally require the embeddings opt-in. `graph.enabled` is likewise always `true` because connection-nodes are core:

```typescript
const structureService = serviceRegistry.get('structure');
const caps = await structureService.getCognitiveCapabilities();

if (caps.embeddings.enabled) {
  // Generated only on embeddings-enabled installs — a separate service
  const embeddings = serviceRegistry.get('structureEmbeddings');
  const results = await embeddings.semanticSearchNodes(userId, queryEmbedding, { topK: 20 });
}
```

Node keyword search is always-on and lives on the **main** service: `structureService.searchNodes(userId, query, options?)` filters candidates with Postgres full-text `websearch_to_tsquery` and ranks them with Lakebase BM25 when the BM25 index is present, otherwise `ts_rank_cd` — the `score` is a rank (higher is better, not a `0..1` similarity), and no embeddings opt-in is required.

The `structureEmbeddings` service (registered only when the app opted into embeddings) provides `setNodeEmbedding`, `clearNodeEmbedding`, `listNodesNeedingEmbedding`, `semanticSearchNodes`, `semanticSearchChunks`, and `findSimilarNodes`, plus (v1.32.0+) the chunk-keyword method `lexicalSearchChunks` and the hybrid methods `hybridSearchNodes` and `hybridSearchChunks`. It also carries the chunk write/read path an ingest module needs — `replaceNodeChunks`, `setChunkEmbedding`, `listChunksNeedingEmbedding`, `getNodeChunks`, `deleteNodeChunks` — and the pairwise `nodeSimilarity`. `lexicalSearchChunks` ranks the same way as `searchNodes` (BM25, else `ts_rank_cd`); the hybrid methods fuse the keyword and semantic arms with reciprocal rank fusion in SQL, and the caller supplies **both** the query text and its embedding. Structure owns storage only — it never calls an LLM or embedder, so callers always supply the vectors and query text. It is an in-process API only — there are no HTTP routes for embeddings or search. See the [Cognitive guide](/modules/structure/cognitive/) for full signatures, the `features.cognitiveIngest` flag, and install/opt-in details.

Separately, `structureService.expandGraph(userId, seedIds, options?)` is a graph-expansion primitive available to every consumer (it rides `graph.enabled`, which is always `true`). It returns the N-hop neighbourhood over connection-nodes as `{ nodeId, hopDistance }[]` — undirected, userId-scoped, and `origin`/`connectionType`-filterable, reporting each reached node's minimum hop distance. It is strictly a query primitive (orchestration, reranking, and budgets live upstream), and graph distance is a purely structural relevance signal — never temporal.

### Type Hierarchy Methods (v1.18.0+)

Node types can declare `superClasses` — parent type slugs forming an *is-a* DAG with field/feature inheritance (e.g. `court-order` is-a `legal-document`). Four traversal methods are available via `serviceRegistry.get('structure')`:

| Method | Description |
|--------|-------------|
| `getTypeAncestors(slug, userId)` | Returns ancestor slugs nearest-first (declared parents in declaration order, then theirs; deduped) |
| `getTypeDescendants(slug, userId)` | Returns all transitive subtype slugs (self not included) |
| `isSubtypeOf(subSlug, superSlug, userId)` | `true` when `subSlug` *is* `superSlug` or a transitive subtype (reflexive) |
| `expandTypeSlugs(slugs, userId)` | Expands a slug list to self + all transitive subtypes, deduped — the helper behind `listNodes`' `includeSubtypes` option |

`getNodeType` / `listNodeTypes` responses now also carry the raw `superClasses` and a read-only `resolved` key (`{ fields, features, ancestors }`) with inherited fields and features merged in — see the [Type Hierarchy guide](/modules/structure/type-hierarchy/) for inheritance semantics, subsumption in connection allowlists, and `includeSubtypes`.

### Registering Node Types from Your Module

If your module introduces its own node type (e.g., a "Task Board" type), seed it during backend initialisation — your module's init function, not per-user request hooks. Two rules make the seed correct:

1. **Seed via `importNodeTypes`, not `createNodeType`.** `createNodeType(userId, config)` pins the new row to the `userId` you pass, making the type invisible to every other user. `importNodeTypes` is the only service path that creates **shared rows** (`userId: null`) that every user can see.
2. **Guard with an existence check.** The guard is load-bearing: merge-mode import *updates* existing slugs, so an unguarded import would re-clobber admin customisations on every boot.

The actor string passed to both calls is only a visibility filter and audit label — any value sees shared rows, and nothing from it is written onto the row. A module-scoped label like `'tasks-init'` keeps the audit trail readable.

```typescript
// backend/src/features/tasks/tasks.init.ts
import { serviceRegistry } from '../../config/service.registry.js';

const TASK_BOARD_TYPE = {
  slug: 'task-board',
  name: 'Task Board',
  namePlural: 'Task Boards',
  icon: 'Kanban',
  moduleId: 'tasks',
  rules: {
    isRoot: false,
    allowedChildren: [],
  },
  features: {
    favorites: true,
    archiving: true,
  },
  fields: [],
  navigation: {
    mainNav: { enabled: false },
    pageNav: {
      style: 'tabs',
      builtInTabs: {
        overview: false,
        info: true,
        settings: true,
      },
      defaultTab: 'info',
    },
    listPage: { defaultView: 'grid' },
  },
  isSystem: true,
  isEnabled: true,
  sortOrder: 40,
};

export async function initTasks(): Promise<void> {
  const structureService = serviceRegistry.get('structure');

  // Existence check — load-bearing. Merge-mode import UPDATES existing
  // slugs, so re-running it unguarded would clobber admin customisations
  // on every boot.
  const existingTypes = await structureService.listNodeTypes('tasks-init', true);
  if (existingTypes.some(t => t.slug === 'task-board')) return;

  // importNodeTypes is the only service path that creates shared
  // (userId: null) rows visible to every user.
  await structureService.importNodeTypes('tasks-init', { nodeTypes: [TASK_BOARD_TYPE] }, 'merge');
}
```

<Aside type="caution" title="Don't seed shared types with createNodeType">
`createNodeType(userId, config)` writes the `userId` straight onto the row with **no** user-existence check — module schemas carry no foreign key to the user table, so nothing rejects a made-up actor string. Passing a module label creates a row owned by nobody and visible to nobody. It's the right call for user-initiated type creation — never for module seeding.
</Aside>

<Aside type="note" title="Two senses of shared">
A `userId: null` type row is a shared **configuration** — every user sees the same type definition, but each still has their own private entries of it. That's different from the **Shared across all users** flag (`primaryScope.global`), which makes the *entries* readable across owners. Seeding with `importNodeTypes` gives you the app-level row that flag requires. A seed payload that leaves `primaryScope.global` unset never shares the entries, and it should stay that way — leave the flag to the admin. (A payload that *does* set it is a full sharing switch, guards and all.) See [Shared Node Types](#shared-node-types-read-across-owners-write-owner-only-v13427) above for what it changes for consumers.
</Aside>

<Aside type="note" title="Declare containment on the container type">
Containment belongs on the *container* type's `rules.allowedChildren` — that's the admin UI's source of truth, and each type's parent list is derived from the other types' `allowedChildren`. `rules.allowedParents` is a legacy restriction: it's still enforced server-side, but the Hierarchy tab renders it read-only with only a **Clear** button. Seeding it makes your type's placement uneditable by admins, so leave it out and have the parent type list your slug instead.
</Aside>

For a live example, the Cog Ingest module seeds its shared `working-memory` node type at backend boot with exactly this guard-then-import pattern — see the [Cog Ingest integration guide](/modules/cog-ingest/integration/).

<Aside type="note" title="moduleId matters">
Always set `moduleId` to your module's ID when creating node types. This lets the system know which module owns the type, and enables cleanup on module uninstall. Default node types seeded by Structure itself set `moduleId: 'structure'`.
</Aside>

### Namespaced Metadata Writes

When your module needs to store small amounts of data on a node without defining custom fields, use the metadata isolation API. This performs an atomic `jsonb_set` operation scoped to your module's namespace, preventing cross-module data corruption.

```typescript
// backend/src/features/billing/billing.service.ts
import { serviceRegistry } from '../../config/service.registry.js';

const structureService = serviceRegistry.get('structure');

// Write billing data to node.metadata.billing namespace
await structureService.updateNodeMetadata(
  nodeId,
  userId,
  'billing',  // moduleId — becomes the namespace key
  { planId: 'pro', trialEndsAt: '2025-06-01' }
);
// Result: node.metadata = { billing: { planId: 'pro', trialEndsAt: '2025-06-01' }, ...otherModules }
```

The API endpoint is `PATCH /api/v1/structure/nodes/:id/metadata` with body `{ moduleId, data }`. Each module's writes are isolated — updating the `billing` namespace never touches the `tasks` namespace. Fires the `structure.node.metadataUpdated` action hook.

On a [shared type](#shared-node-types-read-across-owners-write-owner-only-v13427), the metadata of another user's entry is blanked to `{}` on read and cannot be written, so namespace state you attach this way is always per-owner.

<Aside type="tip" title="Metadata vs custom fields">
Use **custom fields** (defined in node type config or via module field registration) for data that users see and edit. Use **metadata** for internal module state that drives logic but isn't directly user-facing — like sync status, cached counts, or external IDs.
</Aside>

---

## Storing Node References in Your Schema

If your module's database tables need to reference Structure nodes, use a plain `nodeId` string field — not a Prisma `@relation`:

```prisma
model task {
  id        String   @id @default(cuid())
  userId    String
  nodeId    String              // References structure_node.id
  title     String
  status    String   @default("todo")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([userId])
  @@index([nodeId])             // Index for fast lookups by node
  @@index([userId, nodeId])     // Composite for user+node queries
}
```

<Aside type="caution" title="No cross-module @relation">
Module schemas must be self-contained. Use plain string fields with `@@index` for foreign keys to other modules' tables. Never use `@relation` across module boundaries — this would create tight coupling and break independent installation.
</Aside>

---

## Complete Integration Example

Here's how a hypothetical "Tasks" module would integrate with Structure end-to-end:

<Tabs>
  <TabItem label="Frontend">

```
src/features/tasks/
├── index.ts                                    # Side-effect imports first
├── components/
│   ├── TasksTab.tsx                            # Tab content for node detail pages
│   ├── TaskCountBadge.tsx                      # Badge on node cards
│   ├── TaskSummaryWidget.tsx                   # Widget for Overview tab
│   ├── TaskSummarySection.tsx                  # Sidebar section
│   └── TaskStatusFilter.tsx                    # Filter control for list pages
├── registrations/
│   ├── structure-tabs.registration.ts          # Register TasksTab
│   ├── structure-widgets.registration.ts       # Register TaskSummaryWidget
│   ├── structure-cards.registration.tsx        # Register TaskCountBadge (JSX)
│   ├── structure-actions.registration.ts       # Register action buttons
│   ├── structure-columns.registration.tsx      # Register table columns (JSX)
│   ├── structure-filters.registration.ts       # Register filter controls
│   └── structure-sidebar.registration.ts       # Register sidebar sections
└── ...
```

A registration file whose `render` function returns JSX must be a `.tsx` file — JSX in a `.ts` file won't compile. Import specifiers stay extensionless either way.

**index.ts:**
```typescript
// Side-effect imports (registrations) FIRST
import './registrations/structure-tabs.registration';
import './registrations/structure-widgets.registration';
import './registrations/structure-cards.registration';
import './registrations/structure-actions.registration';
import './registrations/structure-columns.registration';
import './registrations/structure-filters.registration';
import './registrations/structure-sidebar.registration';

// Then public exports
export { taskRoutes } from './routes.config';
```

  </TabItem>
  <TabItem label="Backend">

```
src/features/tasks/
├── index.ts
├── tasks.routes.ts
├── tasks.service.ts
├── tasks.types.ts
├── tasks.init.ts                               # Register task-board node type
├── route.registration.ts
├── service.registration.ts
├── filter.registration.ts                      # Optional: transform Structure data
└── action.registration.ts                      # React to Structure events
```

**action.registration.ts:**
```typescript
import { actionRegistry } from '../../config/action.registry.js';
import { tasksService } from './tasks.service.js';

// Clean up when nodes are deleted
actionRegistry.addActionAsync(
  'structure.node.deleted',
  'tasks-cleanup',
  async (context) => {
    await tasksService.archiveTasksForNode(context.nodeId, context.userId);
  },
  20
);
```

  </TabItem>
</Tabs>

---

## Admin Control of Module Integrations

Admins can control module-provided content per node type in two places:

1. **Integrations tab** in the node type form (App Structure › Structure Types › Edit Type › Integrations) — control tabs, widgets, card extensions, actions, columns, filters, sidebar sections, and module-registered fields per type
2. **Registries tab** on the App Structure admin page — browse all registered items across the 8 content registries (actions, columns, filters, sidebar sections, field types, tabs, widgets, card extensions)

Your module's registered items can be:

- **Enabled/disabled** per node type
- **Renamed** — admins can override the label you provide
- **Reordered** — admins can change the display order
- **Slot reassigned** (card extensions only) — admins can move an extension between card positions

Your module doesn't need to do anything special to support this. The registries automatically check the `integrations` config on each node type before returning results. If an admin disables your action for a particular node type, `getActionsForNode()` simply won't include it.

<Aside type="tip">
Module integrations are discovered at render time from the registries. As modules are installed or removed, the admin UI updates automatically to show available integrations. The Registries tab provides a central view of everything that's been registered across all modules.
</Aside>

---

## Integration Checklist

Use this checklist when building a module that integrates with Structure:

- [ ] **Dependency declared** — `depends: { structure: '^1.0.0' }` in `module.ts`
- [ ] **Frontend registrations** — Tabs, widgets, actions, columns, filters, sidebar sections, and/or card extensions registered via side-effect imports
- [ ] **Backend service access** — Use `serviceRegistry.get('structure')`, never direct imports
- [ ] **Event hooks** — Action registrations for cleanup on node delete, setup on node create
- [ ] **Schema isolation** — Plain `nodeId` string field with `@@index`, no `@relation`
- [ ] **Node types** — If registering types, set `moduleId` to your module ID
- [ ] **Order values** — Chosen against the built-in orders for that registry, not a blanket 30+ (built-in tabs run 0-100, so 30-70 sits between Children and Connections and 100+ sits last)
- [ ] **Labels** — Use `getLabel(nodeType, key)` for user-facing text instead of hardcoding type names

---

## Next Steps

- [Registries Guide](/modules/structure/registries-guide/) — Comprehensive reference for all 11 registries, module fields, and hooks
- [Cognitive](/modules/structure/cognitive/) — Embeddings, semantic search, connection promotion, and node type proposals
- [JSON Import & Export](/modules/structure/json-reference/) — Import and export Structure configurations
- [Usage](/modules/structure/usage/) — Admin page, components, and hooks reference
- [Configuration](/modules/structure/configuration/) — Database schema and API endpoints
- [Developer Workflow](/modules/structure/developer-workflow/) — Setup, building, and testing
