---
title: Roadmap
description: Planned features, known issues, and completed work for the Style module
---

import { Aside } from '@astrojs/starlight/components';

This page tracks what's coming, what's broken, and what's shipped for the Style module. It was bootstrapped during the v1.12.0 standards-alignment pass that brought the module up to the scaffold v1.9.0 baseline.

<Aside type="note">
Items here reflect current plans and may change as the starter template evolves.
</Aside>

---

## In Progress

Items actively being worked on.

*(nothing in flight — the v1.12 alignment pass just landed)*

---

## Planned Improvements

Enhancements with committed direction.

### Full TanStack Query migration of legacy hooks

**Priority:** Medium

`useProjectTheme` and `useThemePresets` still manage their own loading/error/dirty state with `useState` + `useEffect`. The v1.12 alignment added a new `useStyleQueries` file with the correct pattern, but the legacy hooks haven't been rewritten against it yet — they still call the service directly. Consumers can migrate piecemeal; the two should eventually be fully replaced by composition of the query hooks with pure UI state for dirty tracking.

### Bootstrap cache for styleRegistry tokens

**Priority:** Medium

`style.registration.ts` fetches the active app theme asynchronously on module load. Until the fetch resolves, the page renders with starter defaults, which causes a first-paint flash to the admin-chosen theme. A `localStorage` cache keyed by app id would let us apply the last-known tokens synchronously at module load, then reconcile once the network response arrives. The starter's style-system docs flag this same gap for the framework itself.

### Conditional template generation for install modes

**Priority:** Low

The module exposes an `installMode` prompt (admin vs. full) but only the barrel file's side-effect imports are conditionally emitted. The `structure/` and `components/structure/` templates are always generated. When a user picks **Admin only**, those files sit unused. Either add `when:` gating on individual templates (pending scaffold support), or split the module into `style` and `style-builder`.

### Flesh out the module docs

**Priority:** Low

The sister Structure and Builder modules each have Configuration, Usage, Integration, Developer Workflow, and Changelog pages. Style currently ships only Overview and Roadmap. Filling these in would bring it to parity.

---

## Known Issues

Technical debt and minor issues to be addressed.

| Issue | Status | Notes |
|-------|--------|-------|
| **Legacy hooks bypass TanStack Query cache** | Low priority | `useProjectTheme` and `useThemePresets` use local state; the cache hit is available through `useStyleQueries` but consumers that use the legacy hooks re-fetch on every mount |
| **First-paint flash on app theme load** | Low priority | Registry tokens apply once async fetch resolves; tracked above under Planned Improvements |
| **Unused files when install mode is admin-only** | Low priority | Structure and property-panel component files generate but are never imported in admin mode |

---

## Completed

Features that have shipped.

| Feature | Version | Description |
|---------|---------|-------------|
| Standards alignment | v1.12.0 | Scaffold → 1.9.0, `styleRegistry` integration, TanStack Query hooks template, module.ts cleanup, docs page |
| Palette generator removal | v1.11.0 | Colour palette generator extracted to a separate module; primary/secondary/accent now pull from brand colours |
| Initial release | v1.0.0 | Admin theme editor, visual className designer, project theme tabs, version history |
