---
title: Roadmap
description: Planned features, known issues, and completed work for the Style module
---

import { Aside } from '@astrojs/starlight/components';

This page tracks what's coming, what's broken, and what's shipped for the Style module.

<Aside type="note">
Items here reflect current plans and may change as the starter template evolves.
</Aside>

---

## In Progress

Items actively being worked on.

*(nothing in flight)*

---

## Planned Improvements

Enhancements with committed direction.

### @theme inline block alignment with shadcn

**Priority:** High

The module's generated `global.css` has the right tokens in `:root` and `.dark`, but the `@theme inline` block structure differs slightly from shadcn's `buildInstructions` output. Shadcn puts `--font-sans`/`--font-heading`/`--font-mono` first, then colour mappings, then multiplicative radius derivations (`calc(var(--radius) * 0.6)` rather than `calc(var(--radius) - 4px)`). Full alignment needed for byte-perfect diff against `shadcn init`.

### Full TanStack Query migration of legacy hooks

**Priority:** Medium

`useProjectTheme` and `useThemePresets` still manage their own loading/error/dirty state. The `useStyleQueries` hooks shipped in v1.11.5 provide the right pattern; legacy hooks should migrate.

### Bootstrap cache for styleRegistry tokens

**Priority:** Medium

`style.registration.ts` fetches the active app theme asynchronously on load. Until the fetch resolves, the page renders with starter defaults. A `localStorage` cache keyed by app id would let us apply last-known tokens synchronously at module load.

### Catalogue browser UI in admin theme settings

**Priority:** Medium

Catalogue entries currently toggle through the starter's generic `styleOverrides` UI. A Style-module-owned browser in the admin theme settings page would group entries by primitive, show live previews, and surface the CSS for audit.

### Per-slot typography overrides

**Priority:** Low

The typography preset is a global choice. Future work: let admins override individual `--typography-*` tokens (e.g. keep Default but bump `--typography-page-title-size`).

### More catalogue entries

**Priority:** Low

Current catalogue covers tabs, buttons, cards, sidebar-rounded, sidebar-accent-bold, admin-typography. Expand to cover dialog/sheet/alert-dialog, table row density, form field sizing, etc.

### Conditional template generation for install modes

**Priority:** Low

`installMode: admin` still emits structure/property-panel component files that are never imported. Needs `when:` gating on individual templates (pending scaffold feature).

### Flesh out the module docs

**Priority:** Low

Sister Structure and Builder modules each have Configuration, Usage, Integration, Developer Workflow, and Changelog pages. Style currently ships Overview, Design philosophy, and Roadmap.

---

## Known Issues

Technical debt and minor issues to be addressed.

| Issue | Status | Notes |
|-------|--------|-------|
| **`@theme inline` block differs from shadcn** | High priority | Tokens are correct; the surrounding structure (font vars at top, multiplicative radius derivations) isn't yet an exact match. Tracked above under Planned Improvements. |
| **Legacy hooks bypass TanStack Query cache** | Low priority | `useProjectTheme` / `useThemePresets` use local state; cache available via `useStyleQueries` but legacy consumers re-fetch on mount |
| **First-paint flash on app theme load** | Low priority | Registry tokens apply once async fetch resolves |
| **Unused files when install mode is admin-only** | Low priority | Structure / property-panel component files generate but never import |

---

## Completed

Features that have shipped.

| Feature | Version | Description |
|---------|---------|-------------|
| Shadcn preset alignment | v1.13.0 | `resolveTheme` matches shadcn `buildRegistryTheme` byte-for-byte (menuAccent='bold' mutates --accent not --sidebar-accent); preset codec is a line-for-line port of `shadcn/preset/preset.ts` with V1+V2 support; runtime menu colour binding (via `.cn-menu-target` / `data-menu-translucent`) for dropdowns; sidebar-accent extension as a catalogue entry; radius RADII values match shadcn (0.45/0.625/0.875 rem) |
| Terminology clarity | v1.13.0 | Admin settings panel now separates Menu (dropdowns) from Sidebar (our extension). Design doc explains the distinction |
| Style catalogue + typography pass | v1.12.0 | Opt-in `styleRegistry` entries targeting data-slot attributes (tabs, buttons, cards, sidebar), typography preset system, semantic status tokens including `info` |
| Standards alignment | v1.11.5 | Scaffold → 1.9.0, initial `styleRegistry` integration, TanStack Query hooks, module.ts cleanup, docs |
| Palette generator removal | v1.11.0 | Colour palette generator extracted to a separate module |
| Initial release | v1.0.0 | Admin theme editor, visual className designer, project theme tabs, version history |
