---
title: Archive Audit
description: Per-session archive-readiness log — unfinished work, loose ends, decisions, and ideas captured before a session is archived
sidebar:
  order: 7
---

import { Aside } from '@astrojs/starlight/components';

A running log of **archive-readiness audits**. Before a working session on the Cog Ingest module is archived, it is audited for anything that would be lost — unfinished work, loose ends, decisions, and ideas — and the findings are recorded here so a fresh session can pick them straight up.

<Aside type="note" title="Why this page exists">
Session transcripts get archived and are then awkward to search. This page is the durable, reviewable home for "what was in that session" — decisions and their reasoning, ideas raised in passing, work left mid-flight, and anything the code and the rest of these docs don't already capture. Newest entry first. Nothing here is deleted; when an item is actually done, it's ticked and left in place for provenance.
</Aside>

## How to use this page

At the end of a session you want to archive, run the archive-readiness prompt (below). It audits the session and **appends a new entry at the top** of the log, using the entry template. Then it builds and pushes the docs. Review the entry on the live site; archive the session; start the next one by reading the open items here.

### The prompt

> **Archive-readiness check.** Audit this session end-to-end for anything that would be lost if I archive it: (1) unfinished work — started-not-finished, committed-not-pushed, coded-not-validated, or left WIP (check `git status` and recent commits in every repo touched, including the `docs/` submodule — **and if this is an older session the repo has since moved past, expect HEAD, the version number, and the working tree to look _newer_ than this session's output, and its commits to sit deep in history, absent from a short `git log`/reflog window; that is normal forward progress, not loss. Confirm the work survived by identifying this session's own commits from the transcript and checking each is an ancestor of `origin/main` (`git merge-base --is-ancestor <sha> origin/main`) — reachable from origin means preserved even though HEAD is far ahead. Real loss is only a commit that is on no branch _and_ absent from `origin`; do not infer loss from "HEAD isn't at my version" or "the working tree differs from what I edited"**); (2) loose ends — TODOs, "later" items, deferred fixes, promised follow-ups, checks I said I'd run; (3) decisions & ideas — any decision, trade-off, rejected approach and *why*, or idea raised in conversation not already written in code or docs; (4) undocumented reality — anything we changed or learned the docs don't reflect. Then **append a new entry to the top of the Archive Audit log** (`docs/src/content/docs/modules/cog-ingest/archive-audit.mdx`) using the entry template already on that page. Build the docs, push the docs submodule **and** the submodule pointer. Verify before ticking anything "done" (if a version was bumped, confirm `dist` was rebuilt after the bump; for an older session, confirm `dist` — if gitignored — stamps the **current HEAD** version, not the session's). Finish by telling me plainly whether it's safe to archive.

### Entry template

```mdx
## YYYY-MM-DD — <session focus>

**Verdict:** ✅ Safe to archive  ·  ⚠️ Needs input first  ·  ℹ️ Superseded (work integrated, session now historical)
**Shipped this session:** <version(s), key commits, what went live>

**Unfinished / WIP:** <or "none">
**Loose ends & follow-ups:** <deferred fixes, promised checks, TODOs — or "none">
**Decisions & ideas:** <decisions + why, rejected approaches, ideas raised — or "none">
**Recorded where:** <docs pages / memory files updated, so it's findable>
**Open items for next session:** <the pick-up list — or "none">
```

---

## 2026-09-08 (session 7) — DM upload-location registry: cog-ingest declares a default per-source upload folder

**Verdict:** ✅ Safe to archive — built, gate-clean, host-tsc-verified against veradai's **real** DM barrel type, committed + pushed, fully synced (HEAD == remote). This completes the DM `nodeUploadLocationRegistry` follow-up that sessions 5 & 6 flagged "for a bit later". **Not yet Kim-`npx`-validated** in the running veradai app.

**Shipped this session:** **v1.21.95** — cog-ingest contributes a default upload folder for each ingest source to Document Management's new module-owned `nodeUploadLocationRegistry` (its server-side analog of Structure's node-tab registry). A file uploaded to an ingest-source node now defaults into a top-level folder named after that source (e.g. `Court Documents/`) instead of a flat default; an admin's own mapping always overrides it.
- Parent `cognitive-ingest/main` **5c4035d..59663e4**: `c26b4a9` feat → `993a733` chore(release) v1.21.95 → `59663e4` docs pointer. Docs submodule `origin/main` **0dd72b3** (changelog v1.21.95 + integration.mdx). All four commits confirmed ancestors of their remotes; `dist` was rebuilt after the bump and stamped 1.21.95 at the time (dist is gitignored); engine unchanged (vendored 1.12.17).
- **⚠️ Concurrency note (read the chronology, not HEAD).** A *parallel* session was running while this audit ran and, right after my push, committed its own unrelated work on top of mine: `737070a feat(canvas): cleaner Model-purpose labels` → `a5fbe81 chore(release): v1.21.96` (+ docs `dabe9e1`). It correctly stepped past my already-pushed 1.21.95 to **1.21.96**, so there is no version clash. Consequence: **HEAD and `dist` are now at 1.21.96, not 1.21.95** — that is that other session's release, not a regression, and this session's 1.21.95 commits remain ancestors of `origin/main` (verified, preserved). The parallel session's own WIP (the `pipeline-flow.ts` label tweak I'd earlier noted as "pre-existing" — it was actually *their* live work) is now committed; only the docs `drafts/*` remain uncommitted, and those are not this session's.
- **New backend template** `document-upload-locations.registration.ts` (`DOCUMENT_UPLOAD_LOCATIONS_REGISTRATION` in `registrations.ts`), wired into the barrel side-effect imports (`routes.ts` `INDEX_TEMPLATE`), re-exported from backend `index.ts`, and mapped in `module.ts`. Reached with the guarded `await import('../document-management/index.js')` idiom (variable specifier) → silent no-op when DM is absent, load-order-immune (cog-ingest sorts before document-management alphabetically). One registration per ingest-source slug (`nodeType === source.slug`), folder = the source's display name (flat), seeded from `prisma.cog_ingest_source` at boot (fire-and-forget, guarded) and kept current via the module's `source.created / .updated / .deleted` action hooks (register on create/update, unregister by source id on delete).

**Unfinished / WIP:** none for this feature — it is complete and pushed. Two **pre-existing** working-tree changes were deliberately left untouched (they are NOT from this session): `src/templates/frontend/pipeline-flow.ts` (a small extraction-purpose label tweak) and the docs submodule's `drafts/*.md`. They remain uncommitted.

**Loose ends & follow-ups:**
1. **veradai host validation pending** — the emitted registration is host-tsc-verified against veradai's real DM types (see below), but the new `document-upload-locations.registration.ts` has not yet been compiled inside the running veradai backend. It becomes part of the host-tsc gate on Kim's next `npx cognitive-ingest update`.
2. **⚠️ Reality worth knowing — cog-ingest's OWN uploads bypass this registry.** Every cog-ingest upload path (the ingest form, auto-ingest, `uploadAndQueueIngest`, the by-reference/multi-file flow) passes `folder: 'Ingested'` **explicitly** to DM's `uploadToDefault`, and an explicit `opts.folder` has the HIGHEST precedence (`opts.folder > node-type template > uploadPath > …`). So the new per-source folders apply **only** to files uploaded to a node *without* an explicit folder — i.e. through DM's own UI, or any consumer passing `nodeId`. Net effect today: cog-ingest's ingest-form / auto-ingest uploads still land in `Ingested/`; a DM-UI upload to an ingest-source node lands in `<Source Name>/`. If Kim wants cog-ingest's own uploads foldered per-source too, the change is to pass `nodeId` (not the fixed `folder`) so DM resolves the registered template — a deferred design choice, and it needs the node to exist at upload time (node creation is currently opt-in and happens *during* the pipeline, not before the upload).
3. The pre-existing `pipeline-flow.ts` label tweak + docs `drafts/*` are uncommitted — someone should commit or discard them (not mine to decide).

**Decisions & ideas:**
- **Kim's UX call (two rounds).** Scope = the **source** node types — **rejected** the split-derived containers (`document` / `conversation` / `email-thread`) and **rejected** a blanket `'*'` catch-all (too presumptuous — it would set an app-wide default for *every* node type, not just cog-ingest's). Folder = the source **name**, **flat, with NO `{nodeName}`** subfolder — Kim's reasoning: "nodeType === sourceName; a per-node folder is almost a folder-per-upload" (each node ≈ one uploaded doc).
- **Mechanism = per-source dynamic registration** (boot seed + live sync via the source hooks). Chosen over a static catch-all `'*'` (which can only expand `{nodeType}` — the raw slug — never the friendly source name Kim wanted) and over a boot-seed-only variant (which would drift until an app restart whenever a source is added or renamed).
- **Decoupling note.** The emitted file uses a *structural* registry interface plus an *untyped* variable-specifier `await import()`, so it stays degrade-safe and never hard-imports DM's types. A consequence: the emitted file does **not** itself statically cross-check its entry shape against DM — the host-tsc harness assertion (which imports veradai's real `RegisteredNodeUploadLocation`) is what proves the contract.
- **Solo-business caveat.** DM's registry is process-wide with no user dimension; the boot seed queries all sources without a user filter. Correct under `tenantId === userId` (one tenant per deployment); noted in the file's header comment.
- **Verification method** (reusable): a new backend host-tsc harness `scratchpad/gen-hostcheck-uploadloc.cjs` — sibling to the frontend `gen-hostcheck.cjs` — compiles the REAL emitted template against faithful typed stubs of the three host imports AND a `dm-contract.check.ts` that statically imports veradai's real `RegisteredNodeUploadLocation` (that types file imports only `node:stream`, so no transitive blow-up) and replays the exact `register()` / `unregister()` shapes. Needs `types:['node']` + `typeRoots` → cog's `node_modules/@types` (npx tsc from a scratchpad dir won't find node types otherwise). Result: **0 errors** = emitted body compiles AND the entry shapes satisfy the real DM type. Plus all module gates green (check:templates / type-check / conformance / parity / build).

**Recorded where:** changelog (v1.21.95), `integration.mdx` (a Document Management upload-folders paragraph under "Hooks Cog Ingest Listens To"), memory `project_dm-auto-ingest-v1-19.md` (a top-of-file 2026-09-08 section — this is the cog-ingest↔DM integration memory) + `MEMORY.md` index. Reusable harness at `scratchpad/gen-hostcheck-uploadloc.cjs`.

**Open items for next session:** (a) Kim `npx update`s veradai to v1.21.95, runs the tsc gates → confirm the new `document-upload-locations.registration.ts` compiles in the host, then upload a file to an ingest-source node via DM to see it land in `<Source Name>/`. (b) Decide whether cog-ingest's own ingest-form / auto-ingest uploads should also use the per-source folders (pass `nodeId` instead of the fixed `Ingested/`) — see loose end 2. (c) Carried over from prior sessions: AI Op Binding Slices 2 & 3, and Slice 3b (multer removal, gated on Kim's real-PDF E2E).

---

## 2026-09-08 (session 6) — AI Op Binding, Slice 1: the extraction-level AI pass now runs

**Verdict:** ✅ Safe to archive — Slice 1 is built, gate-clean, host-tsc-verified against veradai's real deps, adversarially reviewed (3 confirmed findings all fixed + re-verified), committed + pushed. **Not yet Kim-`npx`-validated** (the `services.ts` wiring is the host-tsc gate on Kim's next `npx cognitive-ingest update`, as usual for that file). The AI passes only *fire* once Kim maps a model to the `extraction` purpose in the AI Core admin (she's choosing one — leaning Gemini); everything is seeded + degrade-safe until then.

**Shipped this session:** **v1.21.94** — AI Op Binding (Slice 1). The canvas AI Op nodes stop being config surface and start executing via one reusable binding.
- **`ai.ts`:** extended the `AiCoreServiceLike` seam (`variables?` on `run`; `ensurePurpose`; `ensurePromptVariable` + an `AiPromptVariableSpec` type); generalised `seedLanguageOperation` (explicit `purpose` param + declared `promptVariables`); added `ensureModulePurposes` (seeds the novel `extraction` / `reasoning` **language** purposes create-if-absent); added the extraction op — `EXTRACTION_OPERATION` (`cog-ingest/extract-content`), `EXTRACTION_INSTRUCTION_VARIABLE`, per-purpose lazy op seeding (`extractionOpId` / `extractionOpsSeeded`), a research-grounded no-hallucination objective carrying the `{cog_ingest_extraction_instruction}` token, `EXTRACTION_SCHEMA` `{recoveredText, notes}`, `ensureExtractionOperation()` (boot entry) + `runExtraction()`.
- **`services.ts`:** imported `runExtraction`; wired the extraction pass into the **acquire stage** (Thorough+ → recover + append, guarded/degrade-safe); boot-seeds `ensureExtractionOperation()`.
- **`types.ts` (backend + frontend):** tolerant `purpose` field on `AcquisitionExtractionConfig` (relaxed string, not an enum — save-loose/read-strict).
- **`pipeline-flow.ts`:** a **Model purpose** selector (`Extraction`/`Reasoning`/`Analysis`, default `Extraction`) + the instruction on the extraction AI Op node, stored on `acquisition.extraction.purpose`.
- **`module.ts`:** `ai-core` dep `>=1.15.0` → **`>=1.52.0`**.
- **Fixes from the adversarial review (all 3 confirmed findings, verified fixed):** (HIGH) content identity is now hashed over the **deterministic** extraction (a new `PipelineContext.deterministicContent` snapshot taken in acquire before the append), so a non-deterministic AI recovery pass can't defeat `skipUnchanged` / cross-source dedup on identical source material — the AI-augmented content still flows downstream via `normalisedContent`; and the extraction config folds into `computeStageVersion` so a level/purpose/instruction change re-runs the pass rather than reusing a stale skip. (MEDIUM) the acquire gate defaults an unset extraction block to `'thorough'` (matching the schema + canvas default) so legacy/untouched sources run the pass the UI shows as default, not silently as Fast. (LOW) documented that `Thorough`/`Verified`/`Verified+` all run a single gap-fill pass today (the per-pass instruction fields + compare semantics are the config surface for Slice 3).

**Unfinished / WIP:** none for Slice 1. The binding is deliberately staged: Slice 2 (migrate the other seven AI ops onto the purpose/prompt-variable binding + give each AI Op node the purpose selector) and Slice 3 (per-level compare — `Verified`/`Verified+` as separate reads compared in the check-extraction stage; the compare-action select on the check-extraction node) are not started.

**Loose ends & follow-ups:** (1) **Kim maps the `extraction` purpose → a model** (Gemini) in the AI Core admin so the pass actually fires when tested; without it the op seeds + refuses at run and the ingest degrades to deterministic-only. (2) `services.ts` host-tsc is Kim's `npx` gate (the acquire wiring + identity-stage hash-basis + `computeStageVersion` change) — the emitted `ai.ts` + `pipeline.ts` + the cross-file call/usage contract were verified locally against veradai's real `cognitive-db` / `@xyflow/react`, but the large `services.ts` is validated on Kim's update. (3) A **DM `nodeUploadLocationRegistry`** follow-up was raised (DM now publishes a module-owned registry so modules declare default upload folders per node type via a guarded `await import()`) — Kim flagged it "for a bit later"; tracked as a spawned task, separate from this slice. (4) Slice 3b (delete the dormant multer `/ingest` path) still gated on Kim's real-PDF E2E test. (5) v1.21.87–.94 remain not-yet-`npx`-validated.

**Decisions & ideas:** Model binding is **per-purpose** in AI Core (an op binds a purpose at seed time; `run` takes no purpose arg), so "run under a different purpose" = a distinct op record → the extraction op seeds one variant per bound purpose (default boot-seeded, others lazy). The instruction is a **prompt variable** (`{token}` declared via `ensurePromptVariable`, value in the run `variables` map) so the objective stays admin-editable and nothing is baked at seed time. The extraction op is **text-only** for Slice 1 (gap-fill / repair of the deterministic extraction); image/vision extraction is a separate op deferred to the per-format slice. The extraction purpose (`extraction`) + `reasoning` seed with no default model and refuse at run until mapped — degrade-safe by design so it ships now and lights up when Kim maps a model. Chose **not** to regress Kim's shipped 4-level dial for the LOW finding (config-surface-ahead-of-runtime is his established pattern) — documented instead.

**Recorded where:** changelog (v1.21.94), pipeline.mdx ("AI ops with variables" — the binding mechanism + purposes), roadmap.mdx (a "Shipped v1.21.94" Aside + the deferred slices), and memory `project_admin-ui-visualisation.md` (the 🔵 AI OP BINDING section) + `MEMORY.md`.

**Open items for next session:** (a) Kim maps the `extraction` model + tests a Thorough ingest E2E (upload/scrape a PDF → recovered text appended). (b) **Slice 2** — migrate the seven existing ops (runVision / runParseQa / runPopulateFields / runFieldQuality / runFindEntities / runTriage / runScreenshotMessages) onto the purpose/prompt-variable binding + add the purpose selector to each AI Op node. (c) **Slice 3** — per-level compare. (d) Slice 3b (multer removal) once the PDF E2E passes. (e) the DM upload-location-registry follow-up when Kim wants it.

**Follow-on same session — AI Op node prompt block (design + v1.21.96):** Kim wants the node's bare "Instruction" box to become a proper prompt editor over the AI Core op — Langflow-style: an editable prompt **template** pre-filled with the default extraction prompt, a field per declared `{variable}`, and a live preview. Design decision (via AskUserQuestion): **Hybrid** — one *shared* prompt per purpose (the AI Core op's objective), editable from the node, with per-source **variable values**; the system-prompt structure is AI Core's (the objective slot), so no toggle on the node. **Blocker found:** AI Core's *consumer* facade is create-only (`ensure*`) — no consumer method to READ an op's prompt/variables or UPDATE its objective (those exist only on AI Core's admin HTTP routes + `recordsService`), and `getOperationInfo` returns no segments. So edit-from-node needs AI Core to expose `getOperationPrompt` (read objective + declared variables), a consumer objective **write**, and `previewOperation` on the facade. **Ask note written:** `~/Downloads/cogingest-ask-ai-core-consumer-prompt-editor.md` (Kim will hand it to ai-core). Until those land, the prompt is the op's objective (shipped default, editable in the AI Core admin) + the node's per-source instruction variable. **Shipped v1.21.96:** dropped the parenthetical hints from the Model-purpose labels (`Extraction`/`Reasoning`/`Analysis`); the full prompt-block build is parked behind the AI Core ask. (v1.21.95 was the concurrent DM upload-folders session — see the session-7 entry above.) **⏭️ Next-session note:** build the node prompt block once ai-core ships the consumer read/update/preview methods. **✅ RESOLVED + SHIPPED v1.21.98:** AI Core instead shipped an **embeddable `OperationPromptEditor` component** + a starter **component registry** (slot `ai-core.operation-prompt-editor`), which is cleaner than the consumer-facade methods (the ask note is now moot). cog-ingest embeds it via the starter's `<EmbeddedSlot>` in a Sheet behind the node's "Edit prompt" button — decoupled, degrade-safe, admin-gated. Backend change: the extraction op seeds its objective as the reserved `op.<id>.objective` segment (what the editor reads/edits) for every selectable purpose. A 2nd adversarial review found + fixed a frontend op-id clamp gap and made all selectable purposes boot-seed so the editor is never blank. Slice 2 (migrate the other 7 ops onto the binding + editor) is the next AI-Op step.

---

## 2026-09-08 (session 5, continued — handover) — DM offline-upload contract + the AI Op Binding design (the mid-flight capstone)

**Verdict:** ✅ Safe to archive — everything shipped this session (**v1.21.87 → v1.21.93**) is committed + pushed (every session commit confirmed an ancestor of `cognitive-ingest/main`; `dist` rebuilt, stamps 1.21.93; docs `origin/main` **4729407**). The **AI Op Binding** is DESIGN-ONLY (no code written) — fully specced + AI Core substrate-verified + recorded in memory, so nothing is lost. **⏭️ Next session = build the AI Op Binding Slice 1** — a ready-to-paste handover prompt is in the session chat.
**Shipped this session:** v1.21.87 → v1.21.93 (full per-release SHAs in the session-5 entry below). Highlight: **v1.21.92 = DM offline-upload contract** — the queued ingest pull now prefers `getDownloadStream(documentId)` (reconcile-proof for offline-staged uploads); 2-lens adversarial review found no defects in the change.

**Unfinished / WIP:** The **AI Op Binding** (Kim: "get the AI Op Node working, used everywhere") — DESIGN LOCKED + **AI Core v1.52.0 substrate VERIFIED** (has `ensurePurpose` / `ensurePromptVariable` / run-`variables`) + exact API captured, but **NO code written**. Full turnkey spec = the 🔵 section at the TOP of memory `project_admin-ui-visualisation.md`. Slice 1 = `ai.ts` seam ext + generalise `seedLanguageOperation(purpose)` + the `extraction` op + boot-seed the `extraction`/`reasoning` purposes + wire into the acquire stage (Thorough+ gap-fill, degrade-safe) + frontend node purpose-selector/instruction + bump `ai-core` dep `>=1.52.0`. Slice 2 = migrate the 7 existing ops onto purposes. Slice 3 = per-level compare (Thorough=gap-fill, Verified/+=separate read(s) compared).
**Loose ends & follow-ups:** (1) **Slice 3b** (delete the dormant multer `/ingest` path) — GATED on Kim's real-PDF E2E test. (2) **DM v1.16.0 now carries `documentId` in the `documents.document.uploaded` event** (docs ed05e94) → the auto-ingest offline lane closes with NO cog-ingest change once veradai runs DM v1.16.0 (optionally bump cog-ingest's DM `depends` `>=1.16.0`); the `~/Downloads/cogingest-ask-dm-carry-documentId.md` ask is now likely satisfied. (3) v1.21.87–.93 not yet Kim-`npx`-validated. (4) Ingest-form 413/403/400 surfaced per-file but not `code`-decoded (minor). (5) Upload-form live STAGE PANEL was scoped (create job at enqueue + return id + a job `stage` field + frontend polling) but DEFERRED when Kim pivoted to the AI Op work.
**Decisions & ideas:** AI Op binding — purposes grouped by model need (`extraction`/`vision`/`reasoning`/`analysis`); model binding is PURPOSE-level only in AI Core (per-op model control ⇒ a dedicated purpose via `ensurePurpose`); each AI Op node config carries a purpose SELECTOR that AUTO-defaults by the node's goal but is overridable (Kim). Kim leans **Gemini for `extraction`**. Build degrade-safe (seeds now; fires once a model is mapped to the purpose). AI Core changes are on the table (write an ask note like the DM one). Kim's research doc (`~/OneDrive - Illumination of Life Photography/research.docx`) = prompt-design reference (schema-first, OCR-robust, no-hallucination) + confirms the extraction-LEVEL (raw-text depth) vs "extract keys"/`populate` (structured fields) split.
**Recorded where:** memory `project_admin-ui-visualisation.md` (🔵 AI OP BINDING section = the turnkey build spec) + `MEMORY.md` index (flags the current work arc); changelog + the session-5 entry below for the shipped work.
**Open items for next session:** (a) **BUILD the AI Op Binding Slice 1** — the ready-to-paste handover prompt is in the session chat above. (b) Slice 3b once Kim confirms the PDF E2E works. (c) Optionally bump cog-ingest's DM `depends` `>=1.16.0`. (d) Kim maps the `extraction` purpose → a model (Gemini) in the AI Core admin so the AI passes actually run when tested.

---

## 2026-09-07 (session 5) — Slice 3a: retire the standalone Ingest page (fold stats/jobs into the admin Ingest tab; RSS triage → its own admin tab)

**Verdict:** ✅ Safe to archive — Slice 3a is committed + pushed and verified via the junction host-tsc harness (emitted admin + job-detail pages clean under `noUnusedLocals`) plus a 2-lens adversarial review (clean). **Not yet Kim-`npx`-validated.** Slice 3b (removing the multer upload path) is a deliberate **gated hold**, not lost work.
**Shipped this session:** **v1.21.87 → v1.21.90** (parent `cognitive-ingest/main` — remote is named `cognitive-ingest`, not `origin`: .87 = feat **35c015a** + release **f4992b1**; .88 = fix **c1b0d52** + release **b5ab466**; .89 = feat **520a4c7** + release **b2c815d**; .90 = fix **353405b** + feat **530df47** + release **a489da0**; .91 = fix **859ead4** + release **80beb33**; .92 = fix **d4712d0** + release **b21ad72**; .93 = feat **7dd9f90** + release **fee6de0**; `dist` gitignored + rebuilt after each bump, stamps 1.21.93; HEAD == remote after push). Engine unchanged (vendored 1.12.17).

- **Slice 3a — the standalone user Ingest page (`MyFeaturePage` / `PAGE_TEMPLATE`) is retired.** Kim's direction: the page is no longer needed — ingest moves into each node type's create-new flow via the shared `IngestForm`. Deleted the page + its `/my-feature` route, main-nav "Ingest" entry, barrel export, and `module.ts` template mapping. Relocated what it hosted: **RSS entry triage** (`RssFeedsPanel`/`RssEntryList`/`RssEntryRow` + 7 hooks) → a **new RSS admin tab**; **stats + Recent Jobs** → the **admin Ingest tab**. Job-detail back-arrow → `navigate(-1)`, its breadcrumb crumb made pathless (was `/my-feature`).
- **Kept intentionally (for 3b):** the backend multer `/ingest` route, `uploadAndQueueIngest`, and the frontend `uploadAndIngest` service method — so a lingering orphaned `MyFeaturePage.tsx` in a host still type-checks until `npx update` prunes it (the scaffold moves orphaned templates to backup, gated on approval).
- **v1.21.88 — canvas theme fix (Kim, same session):** the per-source pipeline flow canvas set React Flow `colorMode="system"` (followed the OS `prefers-color-scheme`, so it could mismatch the app's light/dark). Now a `useAppColorMode` hook reads the app's resolved theme (the `dark` class the starter toggles on `<html>`, observed via `MutationObserver` — mirrors the header `ThemeToggle`) and drives `colorMode`, updating live on toggle. Verified via the junction host-tsc harness extended to the emitted canvas (real `@xyflow/react`; fidelity-proven — an invalid `colorMode` literal is rejected as `ColorMode`).
- **v1.21.89 — admin Ingest sub-tabs (Kim, same session):** the admin Ingest tab split into **Form / Stats / RSS** sub-tabs and now fills the width (dropped the `max-w-2xl` column). RSS feed triage moved from a top-level tab into the Ingest RSS sub-tab (top-level tabs are now Ingest · Engine · Producers · Sources; Kim may move RSS to the Sources section later).
- **✅ v1.21.90 — Text mode on upload-only Court Rules RESOLVED (real root cause):** the form's `MODE_METHODS.text` was `['paste','api','in_app_capture']`. `in_app_capture` was folded into the Acquire node's "In-app input" (`paste`) switch in **v1.21.80** and is no longer toggled on the canvas — so Court Rules carried a **stuck legacy `in_app_capture: true`** (or `api`) the admin could not turn off, and the form still counted it as a Text method. Kim confirmed **In-app input + Scrape had been OFF for days**, which ruled out the autosave-debounce theory. **Fix:** `MODE_METHODS` aligned 1:1 to the Acquire switches — `text: ['paste']`, `url: ['url']`, `upload: ['file_upload']`. (The earlier observation that the SourceConfigPanel autosave is debounced 800ms and cancelled on unmount — `clearTimeout` in the effect cleanup — still stands as a **separate** robustness follow-up worth a flush-on-unmount, but it was NOT this bug.) ⚠️ LESSON: when a config taxonomy is consolidated (a method/toggle merged/removed), sweep every place that still reads the retired id — here the form's `MODE_METHODS` kept counting `in_app_capture` after the canvas dropped its toggle.
- **v1.21.90 — "Reset flow" button removed from the pipeline canvas (Kim):** it reverted the drawn stage order/positions and cleared `config.flow` on a single click — a real risk to a carefully drawn pipeline. Removed the button + its `resetFlow` handler + the now-unused `RotateCcw` import (host-tsc clean).
- **✅ v1.21.91 — source-config autosave flush-on-unmount (the follow-up noted above, done; Kim: "yeah do that"):** the SourceConfigPanel autosave debounces 800ms and cleared its timer on unmount, so an edit made just before leaving the panel (switching config tabs, closing the source) was dropped. Added an unmount-only effect that flushes a still-pending save; both the debounced save and the flush read the latest form+config+mutation through refs (no stale closure, debounce deps unchanged). **VERIFIED via the host-tsc harness EXTENDED to compile the REAL emitted `SourceConfigDialog.tsx` (211KB, all tabs) + the real canvas against veradai's `@xyflow/react` + `noUnusedLocals`** — the reusable `scratchpad/gen-hostcheck.cjs` now stubs sheet/tabs/separator/combobox + the extra source/mapping hooks + a `../services/my-feature.service` (dynamic-import) stub + ~19 more `../types`. (Fidelity proven: an unstubbed dep surfaced as a real TS2307 before the stub was added.)
- **✅ v1.21.92 — DM offline-upload contract: pull uploaded bytes by durable documentId (Kim: "make sure the upload component works with the offline contract"; DM note = `~/Downloads/dmofflineuploadnoteforcogingest.md`, DM v1.15.3):** when a user's default is an OFFLINE Local Connect folder, DM stages bytes on the shared default and on reconnect RECONCILES (moves to local, repoints the doc IN PLACE, DELETES staged bytes) → the staged `{providerId,remotePath}` 404s. cog-ingest's QUEUED pull can race that. FIX = `ingestDocumentReference` prefers `dmService.getDownloadStream(ref.documentId, userId)` (DM resolves it to the CURRENT location via `getDocument` — reconcile-proof) when a documentId is present, else the old path pull. Added optional `getDownloadStream?` to the DM seam. The **upload component** (IngestForm → `/ingest/by-reference`) always has a documentId — `queueIngestByReference` calls `ensureReference` SYNCHRONOUSLY while the staged path is valid. **Checklist status:** ①Content-Length = DM's `useUploadToDefault` (File body) ✅; ②staged-not-permanent = ensureReference captures early ✅; ③delayed-pull-404 = fixed ✅; ④413/403/400 = surfaced per-file (not `code`-decoded — minor); ⑤event documentId=null = the AUTO-INGEST lane (separate). **⚠️ CONFIRMED gap (2-lens review, verified real but LOW/pre-existing/degrade-safe):** the auto-ingest event lane (`documents.document.uploaded` listener) still pulls by path — DM emits `documentId:null` on the bare upload event even for staged uploads, so `ingestDocumentReference` takes the else/path branch → 404 after reconcile (records `download-failed`, job not lost). Narrow (needs offline default + shared staging + a broadly-configured auto-ingest source + reconcile-beats-job race). SYMMETRIC FIX if Kim wants: have the auto-ingest listener call `dm.ensureReference(userId,providerId,remotePath)` in the awaited fan-out to capture the durable id before enqueue — but that adds a DB round-trip to the upload hot path (§5 says keep the callback O(1)), so it's a trade-off = Kim's call. VERIFIED: gates + build + 2-lens review (no findings against the change; P2002 reconcile-delete edge = no-action, old+new fail identically+gracefully). ⚠️ backend services.ts = Kim-`npx` host-tsc gate (change mirrors the host-validated `let/try{assign}catch{return}/for-await` pattern).
- **✅ v1.21.93 — clearer "Thorough" extraction-level label** ("Thorough — + AI" → "Thorough — deterministic + AI check"; label only). Kim confirmed DM upload works well now → back to the pipeline.
- **ℹ️ Extraction level — WIRING STATUS (Kim asked "is it wired up?"):** the level is a **config surface + recorded seam only** — `acquire` reads `sourceConfig.acquisition.extraction.level` and records it on the job's `contentMetadata.extractionLevel` (services.ts ~759-767), but **Fast/Thorough/Verified/Verified+ produce IDENTICAL deterministic output today**; the differentiating AI passes (`EXTRACTION_AI_PASSES` fast 0 / thorough 1 / verified 1 / verified_plus 2) are **DEFERRED to the AI-Core plumbing step** (Kim's own sequencing: non-AI nodes first, then plumb AI Core). So deterministic extraction runs; the AI check/compare does not yet.
- **⏭️ OPEN FEATURE — upload-form STAGE PANEL (Kim asked "do we have that... uploading then extracting raw data"):** NO. The IngestForm shows per-file UPLOAD status only (Ready→Uploading→Queued/Failed) and stops at Queued. Blockers to live pipeline-stage progress: (1) `/ingest/by-reference` returns `{queued:true}` with **no jobId**; (2) the `cog_ingest_job` is created **during** background processing (in `ingestDocumentReference→ingest()`), not at enqueue; (3) the job tracks only a **coarse status** (queued/processing/completed/…), **no per-stage field**. To build it: create the job at enqueue in `queueIngestByReference` (status queued, title from ref) → return its id from the route; thread it as `existingJob` into `ingestDocumentReference`→`ingest()`; add a `stage` field + have the `runPipeline` executor update it per stage (acquire="extracting raw data", detect="detecting shape", …); frontend polls `getJobById(id)` per file + renders the stage progression. This is the docs' "pipeline-progress screen" (not yet shipped). Core-path + async-lifecycle change → confirm scope + Kim-npx-gate. AWAITING KIM's go on scope.

**Unfinished / WIP:** Slice 3b — remove the multer `/ingest` route + its file branch + the sync base64 fallback + `uploadAndQueueIngest` (backend) + `uploadAndIngest` (frontend); keep the plain-JSON `POST /ingest` (text/url) and `/ingest/by-reference` (DM-first). Deliberately held until Kim validates 3a.
**Loose ends & follow-ups:** (1) **Node-creation E2E still broken** — Kim's real-PDF test *uploaded* fine but the pipeline created **no Structure node**. Most likely cause: node creation is **opt-in** (the `create-node` stage returns early unless `postIngest.createNode.enabled`; the only other node-producing path is `split.mode !== 'off'`), so the test source likely enabled neither. Confirm the job status (completed / failed / quarantined) + the source's Create-Node / Split config before treating it as a code bug. (2) Transcript cue-stripping (`cleanTranscript`) currently lives only on the multer path — decide whether to move it into the pipeline as part of 3b. (3) `usage.mdx` / admin docs carry older pre-reshape drift (8-tab source dialog, old admin tab names) beyond this slice — a separate docs-accuracy pass.
**Decisions & ideas:** Kim, in order: upload transport works but node creation doesn't ("we have to work on here in cog-ingest") → still greenlit Slice 3. "We don't need the ingest page anymore at all" → delete it (ingest → node create-new). Stats + jobs → admin Ingest tab. RSS triage → its **own** admin surface (a new RSS tab, explicitly **not** folded into Ingest). Split Slice 3 into **3a (frontend page retirement)** + **3b (backend multer removal)** to sidestep the orphan-timing risk and honour the don't-stack cadence.
**Recorded where:** changelog (v1.21.87), roadmap ("Consolidating the ingest UI (v1.21.87+)"), usage ("Ingesting content"), this entry; memory `project_admin-ui-visualisation`.
**Open items for next session:** (a) Kim `npx update`s veradai to v1.21.87, runs the tsc gates, and confirms the orphaned `MyFeaturePage.tsx` was pruned → then **Slice 3b** (multer removal). (b) Diagnose the node-creation E2E (opt-in create-node / split config, or a real defect). (c) The design-agreed deferred work: accepted-format enforcement, Enrich reshape + execution wiring, flow-membership "skip = don't run", AI-Core plumbing of the AI-Op nodes.

---

## 2026-09-04 (session 4) — The DM-first upload rework: latent ingest-brick fix, extension labels + drag-drop box, and multi-file upload delegated to Document Management

**Verdict:** ✅ Safe to archive — all work is committed + pushed **and** host-validated; the one open item (Kim's real-PDF end-to-end test, which gates slice 3) is a deliberate hold, not lost work.
**Shipped this session:** **v1.21.81 → v1.21.86** (6 releases), all committed + pushed (parent `cognitive-ingest/main` **f3ae542** — the parent remote is named `cognitive-ingest`, not `origin`; docs `origin/main` **2e2b2d0**; `dist` gitignored + rebuilt after every bump, stamps 1.21.86; HEAD == remote on both, nothing unpushed). Engine unchanged (vendored 1.12.17). **✅ v1.21.74–.86 ALL HOST-VALIDATED in veradai 2026-09-04** (Kim updated the starter + ran `npx cognitive-ingest update`→.86; the veradai tsc gates were run READ-only): backend `tsc --noEmit` = **0 errors in `features/cog-ingest`** (the only 2 backend errors are document-management's own pre-existing ones — `adapters/index.ts:49 TS2352` + `document-management.service.ts:2675 TS2322`), frontend = **0 total** (both tsconfigs). This confirms the new DM hard-dependency and the frontend's `@/features/document-management` hook imports (`useUploadToDefault` / `useAppSettings`) resolve + type-check against the real DM v1.11.0.

- **v1.21.81 — canvas brick-fix (cast):** the .80 `'tags'` control's `setAcceptedFormat` appended `{ format /* string */, enabled }` but `FormatConfig.format` is the `ContentFormat` union → a bad union array (host `TS2322` at `SourcePipelineFlow.tsx:225`). Fixed with a `format as ContentFormat` cast + a type-only import. **Lesson (recorded):** the esbuild seed-test / gen-check STRIP types — any `ControlDef`/type/schema change to a canvas or emitted file MUST run a real tsc harness, not only the esbuild harness.
- **v1.21.82 — the real brick:** the .81 cast only masked the compile symptom; the deeper bug was that the Upload node offered accepted-type VALUES that aren't `ContentFormat` members (`word`/`csv`/`plain_text`/…). Those store on `input.acceptedFormats[].format`, strict-parsed at EVERY ingest → adding such a tag threw and **bricked every ingest for that source**. Fixed three ways: corrected the option values to real `ContentFormat`s; made the `acceptedFormats` read TOLERANT (a `z.preprocess` that drops elements with an invalid `.format`, coerces non-array→`[]`, and repairs strict siblings — `enabled`/`settings`); enforcement left DEFERRED (a 3-lens review found the naive "enforce" bypassed by the DM-delegation path + retroactively blocking existing sources' recording uploads — it needs its own opt-in slice firing at the upload entry).
- **v1.21.83 — non-AI upload refinements:** removed the text-family (plain/markdown/code/html) from the Upload node's accepted types (those are pasted in-app, not uploaded files); accepted types render as pill badges on the node + set the file-picker `accept` in the ingest form; `preserveLayout` default false→true (reflects Docling's real runtime default); the acquire stage now READS + records `acquisition.extraction.level` on the content metadata (a non-AI seam — Fast/Thorough/Verified are deterministically identical today; the level's AI differentiator is deferred to AI Core).
- **v1.21.84 — drag-and-drop box + extension labels:** the Upload surface is now a dashed drop-zone (upload-icon + heading + Browse) matching Kim's three reference screenshots; line 2 shows accepted EXTENSIONS ("PDF, PNG, JPG … accepted"), line 3 the count + size; and labels are EXTENSIONS ("PDF", "DOC, DOCX", "PNG, JPG, GIF, WEBP") not category words like "image"/"word" — on BOTH the canvas node and the ingest component.
- **v1.21.85 — DM-first backend (slice 1):** new `POST /ingest/by-reference` + `queueIngestByReference` (validate source → `dm.ensureReference` → enqueue `my-feature.ingest-file`; 202; degrade-safe); audio/video UN-SKIPPED in `ingestDocumentReference` (classify → `recording` → the acquire Docling-ASR path); `MAX_AUTO_INGEST_BYTES` 25→50MB via `Math.max(50MB, DOC_MAX_UPLOAD_SIZE env)` so cog-ingest's guard never shadows DM's real cap.
- **v1.21.86 — DM-first multi-file upload (slice 2):** the ingest form's Upload became a multi-file drop-zone (input `multiple` + drag) → a per-file list where each file loops `useUploadToDefault({folder:'Ingested'})` → `POST /ingest/by-reference` INDEPENDENTLY (one file → one DM upload → one job → one node), each row with its own status; size + count come from `useAppSettings()`. A 2-lens review found + fixed 4 issues: index-based status desync → a stable per-file id + the list frozen while uploading; the auto-ingest path downloading+failing media → a `pre.type==='recording' && !doclingConfigured()` skip BEFORE download; the byte-cap shadow (folded into .85's `Math.max`); a dead `uploadError` state removed. The old multer path is KEPT dormant as a safety net this release.
- **Architecture locked this session (Kim):** "uploading = Document Management; extraction tools = ingest." DM owns upload transport + storage + the 50MB cap; cog-ingest owns all extraction. cog-ingest now declares DM a HARD dependency in `module.ts` (`'document-management': '>=1.11.0'`) — "the majority of cog-ingest is documents being uploaded into the graph db." The DM-first flow (Flow B) was chosen over a DM-`requestIngest` flow (Flow A, rejected for a double-event hazard).

**Unfinished / WIP:** none stranded — every release built, verified (gates + gen-check + real-dep tsc harnesses + the reviews), committed, pushed, AND host-validated. `dist` stamps 1.21.86. Docs tree carries other sessions' `drafts/*.md` (deliberately left).

**Loose ends & follow-ups:** (1) **⏭️ SLICE 3 — GATED on Kim's real-PDF E2E test.** Once Kim confirms a real PDF upload works end-to-end through the DM-first path (drop → each row Uploading→Queued → job processes → node created → raw file linked), delete cog-ingest's multer `/ingest` route + its file branch + the sync base64 fallback + the 25MB `MAX_UPLOAD_MB` + `uploadAndQueueIngest`; KEEP the plain-JSON `POST /ingest` for text/url. Do NOT start slice 3 before that confirmation — the multer path is the current safety net. (2) **ASR sidecar** — audio/video upload now routes to Docling-ASR, but that needs Kim's Docling ASR sidecar image (env: `DOCLING_SERVE_ARTIFACTS_PATH=/models`, `…LOAD_MODELS_AT_BOOT=false`, `…DEFAULT_PICTURE_DESCRIPTION_PRESET=smolvlm`); without it the recording path fails loud by design (it does NOT silently skip). Speaker diarization ("split for speakers") is NOT in Docling ASR — that's a future WhisperX slice. (3) **Accepted-format ENFORCEMENT** is still deferred (needs an opt-in + to fire at the upload entry + per-file fine-format detection). (4) The extraction LEVELS + COMPARE still don't RUN (config-surface only) — that's the AI-Core plumbing stage, deliberately after the non-AI nodes. (5) **Enrich** reshape (config + reposition-after-checks + execution) and flow-membership / Next-step / node-merge — design-agreed, not built. (6) Still on Kim in the document-management module: the earlier `uploadToDefault` shared-provider `opts.folder` fix (unchanged from the prior entry) — a narrow double-ingest edge on shared providers.

**Decisions & ideas:** (a) **DM-first (Flow B)** over DM-`requestIngest` (Flow A) — Flow B reuses cog-ingest's existing DM primitives and needs nothing new from DM beyond exposing the upload limit + re-exporting two hooks (both already present in DM v1.11.0). (b) **Multi-file = a UX convenience over N independent ingests** — each file is its own DM upload and its own pipeline pass; one failure doesn't sink the rest; the pipeline stays one-item-per-ingest. (c) **File-COUNT limit is cog-ingest's UX cap, not DM's** (DM only ever sees N single-file uploads). (d) **Docling = the deterministic layer** (text-layer / OCR / Whisper-ASR / SmolVLM picture-description baseline); **AI Core = every interpretive AI pass** (the extraction levels' AI read + the rich visual/design description) — this split is now in the docs (`pipeline.mdx`). (e) **SmolVLM (256M) is correct for Docling picture-description, not Granite** — quality/design descriptions live in AI Core, so Granite in Docling would be redundant. (f) The "brick, then a cast masked it" episode → the reusable rule that esbuild harnesses never type-check; a real-dep tsc harness is mandatory for canvas/emitted-file type changes.

**Recorded where:** docs changelog (v1.21.81–.86), `pipeline.mdx` (the deterministic-Docling vs AI-Core layer split — an Aside + an expanded "Who owns what"), and this entry. Memory: `project_admin-ui-visualisation.md` — the top-of-file pick-up marker refreshed to v1.21.86 + the slice-3 gate, plus the full per-release SESSION-3 detail (the DM-first flow, the brick fix + tolerant read, the extraction-level model, the Docling/AI-Core split, the reusable junction-based tsc harnesses); `MEMORY.md` index refreshed.

**Open items for next session:** the ready-to-paste handover prompt is in the session chat (the assistant message just above this audit). Top item = **Kim's real-PDF E2E test is the gate**; once she confirms it works, do **slice 3** (delete the dormant multer path). Nothing is blocked on code — only on that manual confirmation.

---

## 2026-09-03 (session 3) — The pipeline node-walk: check-extraction front stage, Detect-before-Preprocess reorder, and the Acquire/Upload reshape (extraction levels + AI-prompt nodes + accepted-type tags)

**Verdict:** ✅ Safe to archive — all work is committed + pushed; the one open item (host-validating .74–.80) is a documented next-session check, not lost work.
**Shipped this session:** **v1.21.76 → v1.21.80** (5 releases), all committed + pushed (parent `cognitive-ingest/main` **ff54137** — remote is named `cognitive-ingest`, not `origin`; docs `origin/main` **4cc1691**; `dist` gitignored + rebuilt after every bump, stamps 1.21.80; HEAD == remote on both, nothing unpushed). Engine unchanged (vendored 1.12.17). **⚠️ v1.21.76–.80 NOT host-tsc-validated by me** — I ran the veradai gates for .74/.75 early this session (0 in `features/cog-ingest`) but never re-ran them for the .76+ work; Kim is running the emit (her .80 screenshots show the new canvas).

- **v1.21.76 — check-extraction front stage + Detect-before-Preprocess reorder:** a new observe-only `check-extraction` stage right after acquire (a quick deterministic "does the extracted text match the source?" for extracted/scraped formats; records a verdict on the job, never halts). DEFAULT_FLOW reordered to 19 stages. Both constraint mirrors updated byte-identically (27 pairs). **Stale-flow graceful degrade** — a reorder invalidates a source's saved `config.flow` edges (a dragged-node source's old default chain becomes a now-illegal reversal): `resolveFlowOrder` drops reversal edges instead of rejecting the whole flow, and `seedEdgePairs` drops them + degrades a non-custom stale drawing to the clean default (no "cannot run" banner). 3-lens review: type-fidelity + behaviour clean; the one flagged stale-flow regression fixed before release.
- **v1.21.77 — Preprocess→"Enrich" + API/In-app nodes + drop save-indicator.**
- **v1.21.78 — left-aligned the admin Ingest form** (dropped `mx-auto`).
- **v1.21.79 — extraction-level dial + AI-prompt nodes:** replaced the Upload node's confusing per-format toggles (standard/OCR, image OCR + vision) with ONE **Extraction level** dropdown (Fast / Thorough default / Verified / Verified+) whose AI passes surface as their own connected **AI-prompt satellite nodes** (0/1/1/2 by level, each editable). "Capture visuals"→"Visual Description"; More-settings dropped; old image-vision satellites removed; orphaned per-format helpers cleaned (host-tsc TS6133 risk — re-grep = 0).
- **v1.21.80 — accepted-file-type tags + input reorg:** a new `'tags'` ControlDef kind (chips + add-dropdown) for "Accepted file types" on Upload; merged "Direct input" + "In-app capture" into one "In-app input"; put the Extraction level on the URL node too.

**Unfinished / WIP:** none stranded — every release built, verified (gates + gen-check + byte-identical mirror-parity + resolver runtime + a `seed-test` harness that bundles + loads the *real* emitted canvas), committed, pushed. `dist` stamps 1.21.80.

**Loose ends & follow-ups:** (1) **⚠️⚠️ HOST-VALIDATE v1.21.74–.80** — the #1 next action (veradai backend + frontend tsc gates, grep `features/cog-ingest`, expect 0). (2) The whole Acquire config is CONFIG-SURFACE — **Slice 3 = runtime wiring** (extraction levels actually run det + N AIs + compare = the `check-extraction` stage's real job [observe-only today]; enforce `input.acceptedFormats` server-side [not enforced]; Scrape+Feed share URL config; Audio/Voice own settings). (3) **Enrich** is only relabelled — its clean/enrich config + reposition-after-the-checks + execution wiring pending. (4) **Flow-membership "skip=don't run" + Next-step dropdown/handle + node-merge** (Detect+Record, Split+Record-split → 1 node) — design-agreed, not built. (5) **AI-Core plumbing** of all AI Op nodes (Kim's note: after the non-AI nodes). (6) Preprocess step-execution still unwired (predates this session). (7) Still on Kim (document-management): the `uploadToDefault` shared-provider folder fix from the prior entry, unchanged.

**Decisions & ideas:** (a) **Two checks** — a QUICK front extraction check ("does it match", observe-only, shipped) + the THOROUGH parse-QA staying later; don't collapse them. (b) **Enrich runs AFTER the checks** (Kim: "don't change the input before checking" — skip/dedup/parse-QA must see the original); Enrich makes a cleaned COPY the ENGINE reads, the original stays canonical for checks/split/provenance; the deterministic clean stays early (Identity). This overturned an earlier "merge clean+enrich into one early stage" idea. (c) **Extraction levels** Fast/Thorough/Verified/Verified+ — Thorough = deterministic WITH AI backup/gap-fill; "Verified" (compare det vs AI) IS the check-extraction stage's real job. (d) **Semantic meaning for images lives in the AI extraction pass** (a promptable node — no separate mechanism; an image's pass reads meaning, a document's reads text). (e) **"standard vs OCR" is jargon** the admin shouldn't face; the level auto-picks the method. (f) **Vision-analysis ≠ Capture-visuals** (one extracts from an image, the other describes a URL/PDF page's appearance) — folded: vision→the level's AI pass, visuals→"Visual Description". (g) **A persistent-looking UI bug after `npx update` may be a stale Vite HMR bundle** — the banner Kim saw was verified-correct on disk + by runtime test; a hard-refresh clears it. (h) **AI Op nodes → AI Core** as set prompts with `{{variables}}` (pure extracts) or full AI operations (complex) — Kim's note, do after the non-AI nodes.

**Recorded where:** docs changelog (v1.21.76–.80), pipeline.mdx (Check-extraction marked shipped) + roadmap.mdx ("Near-term — reshaping the pipeline front"), and this entry. Memory: `project_admin-ui-visualisation.md` — a top-of-file "STATE AT SESSION END" pick-up marker + the full per-release "SESSION 2" detail (the reorder + stale-flow rule, the extraction-level model + the semantic-meaning answer, the AI-Core plumbing note, the reusable harnesses); `MEMORY.md` index refreshed.

**Open items for next session:** the ready-to-paste handover prompt is in the session chat (the assistant message just above this audit). Top item = **host-validate v1.21.74–.80**, then ask Kim: (a) keep walking non-AI nodes past Acquire, (b) start the runtime wiring (Slice 3), or (c) other.

---

## 2026-09-03 (session 2) — Per-row handles, config tolerance, the ingest component + admin Ingest tab, entry-point, and Upload→Document-Management delegation

**Verdict:** ✅ Safe to archive
**Shipped this session:** **v1.21.71 → v1.21.75** (5 releases), all committed + pushed (parent `cognitive-ingest/main` **6622aed** — note the parent remote is named `cognitive-ingest`, not `origin`; docs `origin/main` **3e08cdd**; `dist` gitignored + rebuilt after every bump, stamps 1.21.75). **v1.21.71–73 host-validated** (Kim ran `npx update`; veradai backend + frontend tsc = 0 errors in `features/cog-ingest`, only document-management's 2 pre-existing backend errors). **v1.21.74 + v1.21.75 NOT yet host-validated** (pending Kim's next `npx update`). Engine unchanged (vendored 1.12.17).

- **v1.21.71 — per-row handles + Preprocess step nodes:** satellite connections now land on the exact control ROW they serve (Langflow-style per-row target handles + `useUpdateNodeInternals`); the Preprocess stage went config-in-node with each `preprocessing.steps` entry as its own connected AI Op node. 3-lens review → 4 fixed (incl. an empty-step-name that broke ingest — same save-loose/read-strict class as the next release).
- **v1.21.72 — config-tolerance sweep:** source config is saved loosely but strict-parsed at ingest, so any out-of-range/empty editor value silently broke every ingest for that source. Swept the whole schema: bounded numbers now clamp toward their bound at read (no default-poisoning); `min(1)` dropped on editor-seeded identity strings; long text capped at the write site; a `safeParseSourceConfig` backstop turns any residual failure into a clear, logged `SourceConfigError`; the RSS source-updated handler (which stopped polling before an unguarded parse) is guarded. Runtime-proven against real zod 4 (42/42) + 3-lens review (2 fixed).
- **v1.21.73 — shared IngestForm + admin Ingest tab:** a new exported `IngestForm` (Text / URL-with-visuals-toggle / Upload, driven by the source's enabled input methods) hosted in a new admin **Ingest** tab (a source dropdown + the form). `ContextFieldInput` moved into the shared file. Emitted component type-checked against real deps.
- **v1.21.74 — entry-point + Ingest-tab landing:** a per-source **entry-point** (Admin / Users / Admin+users) on the Acquire node — placement only; **Structure owns the permission** (the admin Ingest tab clamps the effective entry-point to admin when the source's target node type is create-restricted, read from `getNodeType(...).permissions.create` — contract confirmed by reading veradai/structure). Admin page now lands on the Ingest tab.
- **v1.21.75 — Upload→Document-Management delegation + compact canvas:** a PDF/image/office upload is handed to Document Management for storage + ingested in the background (fixes the browser freeze) and the raw file is linked to the created Structure node; text/audio/video/transcript keep the direct path; degrades to today's direct path when DM is absent or errors. Plus a compact canvas layout (main pipeline top row, settings nodes just below the node they serve, no default overlap) with drag de-overlap. 3-lens review → 2 HIGH fixed (audio/video/transcript were being silently skipped; a masked enqueue failure).

**Unfinished / WIP:** none stranded — every release built, verified, committed, pushed. `dist` stamps 1.21.75 (rebuilt after the final bump). Docs tree carries other sessions' `drafts/*.md` (deliberately left).

**Loose ends & follow-ups:** (1) **Host-validate v1.21.74 + v1.21.75** (Kim's next `npx update` → run the veradai tsc gates). (2) **Preprocess EXECUTION wiring** — the step nodes are a config surface only; nothing runs `preprocessing.steps` yet (preprocessorsRegistry has no entries). (3) Refactor the user ingest page (`PAGE_TEMPLATE`/MyFeaturePage, still 8 tabs) onto the shared IngestForm + add a user-facing entry-point (`user`/`both`) filter. (4) Package-level public export of IngestForm for other modules. (5) The pipeline-progress screen. (6) Lower: chunk-level long-doc dedup, HITL Confirm/Clarify re-route, conversation-finder follow-ups. To FIRE the pipeline AI ops end-to-end: wire AI Core `analysis`+`vision` purposes to models + enable per-source config.

**Decisions & ideas:** (a) **Entry-point = placement, not permission** (Kim's refinement): cog-ingest places the ingest surface; Structure's node-type `permissions.create` decides who can actually create, so a create-admin-only source auto-resolves to admin-only. (b) **Upload delegates to DM on the BACKEND via the service registry** (`getOptional('document-management')`), not by importing DM's frontend component — the service registry is the correct optional cross-module seam, so an app without DM degrades cleanly. (c) **Only heavy binary types (PDF/image/office) delegate**; text/audio/video/transcript keep the direct path so nothing is silently skipped or ingested differently. (d) **Compact canvas** — Kim: "nodes way too far apart, settings just below the main nodes, no overlapping"; done via a derived layered layout + drag de-overlap. (e) The DM double-ingest guard uses an `Ingested/` folder skip — correct on owned providers.

**⚠️ ONE OUTSTANDING CROSS-MODULE ASK — ON KIM (document-management module):** `uploadToDefault` currently **ignores `opts.folder` on the shared/app-default provider** (veradai `document-management.service.ts` ~717–723; only the owned-provider branch reads it). So a shared-provider upload's stored path lacks the `Ingested/` segment and cog-ingest's double-ingest guard can't flag it — a double-ingest is then possible only if a catch-all `autoIngest.documentManagement.enabled` source is also configured (rare). **Fix:** make `uploadToDefault` honour `opts.folder` on the shared provider too (e.g. `u/<userId>/<folder>/…`). Closes the edge; owned providers already work. (Not logged on an upstream-issues page — DM has no such page; recorded here + in memory + relayed to Kim in chat.)

**Recorded where:** docs changelog (v1.21.71–75), roadmap + pipeline.mdx (editor steps + shipped items), and this entry. Memory: `project_admin-ui-visualisation.md` (the full arc — per-release detail, the verified Structure + Document-Management contracts, the entry-point model, the Upload→DM design + the DM ask, the compact-layout detail, and a top-of-file "STATE AT PAUSE" pick-up marker); `MEMORY.md` index refreshed.

**Open items for next session:** the ready-to-paste handover prompt is in the session chat (the assistant message just above this audit). Top items = **host-validate v1.21.74/75**, then **walk each pipeline node stage and refine** (Kim's stated plan), then preprocess execution wiring + the user-page/IngestForm refactor. The one thing needing Kim's action in another module: the Document-Management `uploadToDefault` folder fix above.

---

## 2026-09-03 — Acquisition satellites, AI Op nodes, edges fixed, config wired into the processors

**Verdict:** ✅ Safe to archive
**Shipped this session:** **v1.21.60 → v1.21.70** (11 releases; v1.21.66 came from a spawned side-session), all pushed (`cognitive-ingest/main` `f5e72e1`, docs `0b62010`; dist rebuilt after every bump). **ALL host-validated 2026-09-03** — veradai on 1.21.70, frontend tsc **0 errors**, backend **0 in `features/cog-ingest`** (only document-management's 2 pre-existing). Engine unchanged (vendored 1.12.17).

- **v1.21.60–61 — drawn flows:** the PHASE_LABEL host-tsc fix, then editor increment 2 — user-drawn connections + branching persisted as `config.flow`, executed via `resolveFlowOrder` (topo sort of all 18 stages under the drawn edges, a 26-pair hard-constraint table enforced underneath, byte-identical frontend mirror refusing impossible edges live). 3-lens review → 3 findings (missing detect/skip-cost constraints, malformed-flow crash) fixed + regression-tested.
- **v1.21.62–64 — the acquisition reshape (Kim-directed after a "circles" correction + AskUserQuestion):** acquire organised by HOW content arrives — four switches (Upload / Direct input / Scrape / Feed-RSS, mapped 1:1 to `input.methods`), each enabled source materialising as its own satellite node wired into Acquire; tight flat source list; **auto-save** (no Save button/header); all blurb removed.
- **v1.21.65–68 — visuals + the AI-Op model:** `acquisition.visual` (No / User selects / Always) + Kim's **raw-to-text principle** (acquire extracts faithful text; anything AI is an explicit **AI Op node** — read-image off Upload, describe-visuals, judge-the-parse off the check-parse stage, custom prompts as their own nodes); main flow laid along the bottom; feed node re-pointed at the live polling config (and a spawned session removed the old tab's dead RSS section, v1.21.66).
- **v1.21.69 — the invisible-edges root cause:** inline `hsl(var(--token))` strokes resolve INVALID against the host theme's full-colour tokens → the canvas edges never painted (since v1.21.57). Now on React Flow's own theme-aware colours, with Langflow-style handle dots. **Repo rule: never `hsl(var())` in inline styles.**
- **v1.21.70 — the nodes steer the real ingest:** per-source acquisition threaded into the processors — PDF text-extraction routing (standard/ocr/auto, Docling-aware, never silent), the visual mode genuinely gating the URL **and PDF** vision passes (the v1.21.65 note claimed URLs worked — the flag was derived but never consumed; owned + fixed), AI-Op focus steering the vision instructions, scrape selectors, image vision-off with hash-collapse protection. 2-lens adversarial review → **4 confirmed defaulted-value traps, all fixed** (legacy `standard` killing Docling, pageRendering prefault overriding globals ×2, empty-content dedup collapse) → the **zod-default-poisoning rule** now recorded in memory.

**Unfinished / WIP:** none stranded — every release built, verified, committed, pushed, host-validated. Docs tree carries other sessions' `drafts/*.md` (deliberately left).

**Loose ends & follow-ups:** (1) **Per-row handles** — Kim's ask: edges must land at the ROW they relate to (Acquire's Upload row ← Upload node; Upload's Vision-analysis row ← read-image op) — implementation notes in memory (RF multi-handle + `useUpdateNodeInternals` gotcha). (2) **Preprocess nodes** — `preprocessing.steps` is a stored-but-unwired seam; each step should become a connected node per the AI-Op pattern, execution wiring to follow. (3) The shared 3-mode ingest component (Text/URL/Upload — makes Direct input + User-selects real for users). (4) Remaining tabs (extraction/prompts/mappings/context) into nodes + retire tabs; Producers as subflows (AI Op nodes fold into producers; AI-Op configs connect to app/source variables — Kim flagged, deferred). (5) Residual: sources whose old-tab edits persisted a side-effect `standard` PDF method skip Docling until the Upload node is touched once (logged, changelog-noted). (6) Audio per-source toggles stored-not-wired (Docling handles recordings); URL `preserveFormatting`/`full_page` selection stored-not-wired (zod-default poisoning risk documented).

**Decisions & ideas:** (a) Acquisition sources as **connected nodes**, multiple per source; Upload's auto-detect holds per-format handling (AskUserQuestion-confirmed). (b) **Raw-to-text at acquire; AI as explicit op nodes** — Kim's principle, now structural. (c) Parse-quality stays a main-flow stage (it checks every arrival path); its judge config is an AI Op node. (d) The visuals Focus likely becomes a **producer** when that layer lands. (e) Session-process: mid-flight claims must be wiring-verified — the v1.21.65 misclaim is the counter-example; the wiring audit that followed is the pattern.

**Recorded where:** docs changelog (v1.21.60–70), roadmap + pipeline.mdx (drawn flows + editor steps marked shipped), and this entry. Memory: `project_admin-ui-visualisation.md` (the full arc — locked directions, the handover asks with implementation notes, the zod-default-poisoning + hsl(var()) rules, the cfgcheck/bkcheck/flow-runtime verify recipes); `MEMORY.md` index refreshed.

**Open items for next session:** the ready-to-paste handover prompt is in the session chat (the assistant message just before this audit). Top items = **per-row handles** + **start the Preprocess nodes**, then the shared ingest component; full detail + implementation notes in memory.

---

## 2026-09-01 — DAG collapse, admin-editor direction locked, visual pipeline editor (config-in-node)

**Verdict:** ✅ Safe to archive
**Shipped this session:** **v1.21.56 → v1.21.59** (4 releases), all pushed (`cognitive-ingest/main` `f31b88e`, docs `cd998ff`; `dist` gitignored + rebuilt after each bump, stamps 1.21.59). **v1.21.56 host-validated 2026-09-01** (Kim ran `npx cognitive-ingest update`; veradai backend + frontend `npm run type-check` = **0 errors in `features/cog-ingest`** — the only 2 backend errors are document-management's pre-existing ones); **v1.21.57 host-validated** (veradai frontend tsc = 0 errors); **v1.21.58/59 NOT yet host-validated** (pending Kim's next `npx update` — first item next session). Engine unchanged (vendored 1.12.17).
- **v1.21.56 — THE DAG COLLAPSE:** `ingest()` went from ~10 separate `runPipeline([stage])` passes to ONE `runPipeline(DEFAULT_FLOW)` over 18 stages. The former inline glue moved into stages (`identity`, `check-parse`, `scope`, `finalize`, `split-persist`); `gate` + `quarantine` persist their terminal job and `halt`; `finalize` deliberately stays mid-flow (before create-node) so the postIngestWarning is never clobbered. **Byte-identical** — verified by gates + gen-check + a **runtime harness (27/27)** exercising skip/quarantine/gate-fallthrough/normal/split/opt-ins against the real emitted `runPipeline`/`DEFAULT_FLOW`, type-fidelity cross-checked against the real engine `IngestInput`/`IngestResult`, and a **3-lens adversarial review (zero findings)**. Host-validated.
- **Admin-editor direction — LOCKED after a full review:** Kim (she/her — noted + saved to memory) said we'd been circling and asked me to review ALL docs + memory then ask questions. A 4-agent review workflow → synthesis → AskUserQuestion produced the locked direction: admin = **3 layer tabs Engine · Producers · Sources**; a **per-source React Flow canvas** of the 18 stages in the Sources tab; **flow coexists with the old 10 config tabs, retire each as its node-equivalent finalises** (gradual replace, so nothing's lost).
- **Visual pipeline editor — three cuts:** **v1.21.57** the flow editor (React Flow `@xyflow/react` v12 added as a frontend dep; per-source canvas, opt-in toggles inline, click-to-config; host-validated). **v1.21.58** restyled the nodes to the **React Flow UI-kit schema-node card** look Kim linked (`BaseNode`+`BaseHandle` vendored inline, adapted to shadcn tokens). **v1.21.59 config-in-node** — each stage's config is now EDITABLE inside its node (switch/select/number written to the source config): gate / dedup-quarantine (3 switches) / retrieve / parse-QA / populate / create-node / split / find-connect. A `ControlDef` union + `patch*` helpers drive it; complex surfaces keep a "More settings" tab link. Verified: gates + gen-check (46/0) + the emitted component **tsc'd clean against the real `@xyflow/react` 12.11.5** with typed shadcn stubs + a faithful `SourceConfigJson`.

**Unfinished / WIP:** none stranded — every release built + verified + committed + pushed. Working tree clean except ` m docs` (other sessions' `drafts/*.md` — deliberately left). The ONE open verification: **v1.21.58/59 not yet host-validated** (needs Kim's `npx update`, then run the veradai frontend tsc gate — expect 0, given the RF-component type-check already passed against the real library).

**Loose ends & follow-ups:** (1) **Editor increment 2** — user-drawn connections + branching, persisted as a per-source flow the executor runs. Nodes are currently STATIC (fixed left→right layout, not draggable/connectable). This is the meatier backend piece (persist a flow graph + `runPipeline(sourceFlow ?? DEFAULT_FLOW)` + DAG/branch support in the linear runPipeline + validate stage deps). File map in memory. (2) **Increment 3** — pull the remaining complex config (acquire/extraction/prompts/mappings/context) into nodes + retire each tab. (3) **Increment 4** — Producers as React Flow subflows (needs a producers store). (4) Still need to FIRE the pipeline ops: wire AI Core `analysis`+`vision` purposes to models + enable per-source config. (5) Lower: chunk-level long-doc dedup, conversation-finder participant-overlap + auto-create-nodes, HITL Confirm/Clarify re-route.

**Decisions & ideas:** (a) **Do the DAG collapse now** (Kim confirmed via AskUserQuestion) — it was the session's opening task; I recommended it, verified byte-identical, shipped. (b) **`finalize` stays mid-flow, not literally last** — the handover said "move COMPLETED to the end", but that would let the completion write clobber create-node's postIngestWarning; realised as "COMPLETED is the last *terminal* write, post-ingest stages patch after it, same as pre-collapse" → one runPipeline call AND byte-identical both satisfied. (c) **Stop circling → full review → ask** — Kim's explicit process correction after I pivoted repeatedly (React Flow ↔ hand-built) on partial context; the lesson (full review + crisp questions before building) is in memory. (d) **React Flow confirmed** + Kim prefers known libraries over hand-rolling (I'd briefly pivoted to hand-built after she said "we don't have to use React Flow" — she clarified she'd *rather* use the known lib). (e) **Adopt the React Flow UI kit / Langflow node style** (Kim linked the Database Schema Node + ~17 Langflow templates + Dify/Flowise) — vendored the kit's `BaseNode`/`BaseHandle`, swapped its 2 colour-literals to shadcn tokens (kept `font-semibold` on the node title). (f) **Producers = React Flow subflows** (Kim linked reactflow.dev sub-flows) — increment 4. (g) The old **"Pipeline Trace" observability artifact** was explicitly not the direction (config surface, not observability) — parked.

**Recorded where:** docs changelog (v1.21.56–59), roadmap + `pipeline.mdx` (DAG collapse + editor v1 marked shipped), and this entry. Memory: `project_admin-ui-visualisation.md` (the DAG-collapse shipped detail + reusable runtime-harness pattern; the LOCKED editor direction; the refined Langflow/Dify direction + 4-increment roadmap + increment-2 file map + the RF-component type-check recipe); `user_kim.md` (new — she/her + role); `MEMORY.md` index refreshed (+ the user note).

**Open items for next session:** the ready-to-paste handover prompt is in the session chat (the assistant message just before this audit). Top items = **host-validate v1.21.58/59** (Kim's `npx update` → veradai frontend tsc), then **editor increment 2** (connect + branch + persisted flow + executor-run; file map in memory), then increments 3–4; alongside, wire AI Core purposes + enable config so the ops fire.

---

## 2026-09-01 — Retrieve surfacing, backend capability sprint (7 releases), config data-loss fix, DAG-collapse scoped

**Verdict:** ✅ Safe to archive
**Shipped this session:** **v1.21.47 → v1.21.55** (9 releases; session spanned 2026-08-29 → 09-01), all pushed (`cognitive-ingest/main` `f17fbd0`, docs `8d45ba5`). `dist` gitignored + rebuilt after each bump (stamps 1.21.55). **All host-validated 2026-09-01** — Kim ran `npx cognitive-ingest update`; veradai backend + frontend `npm run type-check` = **0 errors in `features/cog-ingest`** (the only 2 backend tsc errors are document-management's pre-existing ones, not ours). Engine unchanged (vendored 1.12.17).
- **v1.21.47 — Retrieve → Suggestions inbox surfacing:** the observe-only retrieve matches now surface as a `possible_duplicate` inbox item (post-ingest `surfaceRetrieveMatches` listener, ≥90% match, resolves node names, upsert-keep-status) + a job-detail card. Fixed the flagged gotcha first — `refreshSuggestions`' stale-drop scoped to the five engine insight types so an ingest-time item isn't wrongly dropped. 3-lens adversarial review: zero findings.
- **Backend capability sprint (Kim: "smash out everything in the backend, refine once the admin UI lets me see it"):** **v1.21.48 populate** — description-driven AI field-fill (each Structure field's description is the prompt, via live `getNodeType` defs; deterministic-first; values coerced to the field's type — a review-caught bug where a boolean stored the truthy string `"false"`). **v1.21.49 field-quality judge** — a second model grades filled values (faithful + grounded), flags the shaky ones. **v1.21.50 find-connect** (the second connection parse) — finds other node types the content describes and match/create/connects them; 3-lens review, zero findings. **v1.21.51 AI triage** — an async-producer seam in the split stage + AI segmentation of freeform content; 3-lens review incl. a split-regression lens, zero findings. **v1.21.52 vision screenshot reader** — a structured vision op reading chat screenshots into messages. **v1.21.53 populate inherited fields** — reads the resolved (own + inherited) field set. **v1.21.54 conversation finder (suggest-only, Kim's design choice)** — surfaces `related_conversation` links by reusing the retrieve matches in the 0.8–0.9 band for conversation-shaped content; never auto-creates/merges Conversation nodes.
- **v1.21.55 — config round-trip data-loss FIX:** `parseSourceConfig` rebuilt the source form from a whitelist that omitted `contentHandling` / `processingMode` / `autoIngest`, so editing a source and saving **silently wiped** those blocks (dedup / QA / quarantine / chunking / processing-mode / DM auto-ingest) — the backend replaces the whole config column on update. All three now round-trip. Added the two toggles the config audit flagged: **Skip unchanged content** (`skipUnchanged`) and **Find similar existing content (semantic)** (`dedup.semantic` — the switch the possible-duplicate + related-conversation features needed and previously had no UI for).

**Unfinished / WIP:** none stranded — every release built + verified (type-check / check:templates / conformance / parity / build / esbuild gen-check 46-0), engine/Structure/AI-facing slices adversarially reviewed (populate, find-connect, AI-triage — one low finding fixed; the rest reused already-reviewed patterns), and **all host-validated in veradai**. All committed + pushed. Working tree clean except ` m docs` (other sessions' `drafts/*.md` — deliberately left).

**Loose ends & follow-ups:** (1) **The DAG collapse** — Kim asked to do it this session; it was **fully scoped, not started, and left pending his go-ahead** (see below). (2) The new ops need AI Core `analysis` + `vision` purposes wired to models + per-source config enabled to actually fire (nothing fires until Kim turns them on). (3) Lower-priority: chunk-level long-doc dedup (a 2nd retrieve pass with per-chunk vectors — verify the embedding read first); the conversation finder's participant-overlap matching + the auto-create-Conversation-nodes version; the HITL Confirm/Clarify re-route (UI-gated). (4) 2 pre-existing `document-management` host-tsc errors — the DM team's, not ours; flag but don't fix cross-repo.

**Decisions & ideas:** (a) **Sequencing** — Kim steered from "config editor / DAG collapse" to "smash out the backend first, refine once the admin UI lets me see it"; the capability sprint followed. (b) **Populate driven by live Structure field definitions**, not a duplicate config-side schema — verified `getNodeType().fields`/`.resolved.fields` carry descriptions + `updateNode` merges `data`. (c) **Conversation finder = suggest, not auto-create** (Kim's call via AskUserQuestion) — the safe v1 can't pollute the graph; auto-creating Conversation nodes is a follow-up. (d) **DAG collapse timing** — I recommended it as its own fresh session because it's the big all-or-nothing "collapse-proper" (not the small remainder Kim expected), high-blast-radius on the core path, zero visible change; left with him. (e) Built a **"Pipeline Trace" observability artifact** (a mock, synthetic data) — Kim clarified it "isn't what I mean" (he needs the *config* editor, not observability), so it's parked, not a deliverable. (f) A full **admin config-surface carry-over inventory** was produced (the "must-not-lose" spec for the future React Flow reshape) — captured in memory.

**Recorded where:** docs changelog (v1.21.47–55), roadmap + `pipeline.mdx` (stages marked shipped), and this entry. Memory: `project_admin-ui-visualisation.md` (the whole sprint + the fully-scoped DAG-collapse plan with exact `ingest()` line offsets + the config-bug + admin inventory) and `project_content-driven-splitting.md` (v1.21.47 detail); `MEMORY.md` index refreshed.

**Open items for next session:** the ready-to-paste handover prompt is in the session chat (the assistant message just before this audit). Top item = **the DAG collapse** (confirm Kim's go first — it's all-or-nothing; plan + line offsets in `project_admin-ui-visualisation.md`), then the React Flow admin config editor; alongside, help Kim wire AI Core purposes + enable config so the new ops fire.

---

## 2026-08-28 — Split Person realignment, DAG collapse (10 stages), runtime test, retrieve #3 first slice

**Verdict:** ✅ Safe to archive
**Shipped this session:** **v1.21.39 → v1.21.46** (8 releases), all pushed (`cognitive-ingest/main` `ef9dc3b`, docs `29510a0`). `dist` gitignored + rebuilt after each bump (stamps 1.21.46). Engine unchanged (vendored 1.12.17).
- **v1.21.39 — Split Person realignment** (resolved last session's top loose end): the splitter now records raw `from`/`to` **handles** as unit fields and no longer creates or links Person nodes; `linkParticipants` defaults **off**; person resolution moves to the engine + the future retrieve stage. Verified incl. strict host-tsc on the emitted code.
- **v1.21.40–45 — the DAG collapse** (6 inline blocks migrated to executor stages, each byte-for-byte behaviour-unchanged): the change-detection **gate** (early-return → `ctx.gate.prior` outcome the ingest reads back), the engine **cog-db** pass (input assembled inline as an annotated const, call in the stage), **create-node** (the ~130-line post-ingest block extracted verbatim into a helper), the **quarantine/dedup** gate, **preprocess**, and **persist-detection**. The whole front of the pipeline (acquire + preprocess + detect + persist-detection) now runs as **one** `runPipeline([...])` pass. **Ten stages** through the executor.
- **Runtime test (no version):** built + ran a behavioural test of the emitted pipeline executor — **9/9** (multi-stage order + ctx threading, `halt` short-circuit, unknown-stage skip, error-propagate vs swallow). The DAG machinery is validated at runtime, not just by types.
- **v1.21.46 — Feature #3 retrieve/match, first slice:** an **observe-only** `retrieve` stage that runs BEFORE the engine — embeds the content, semantic-searches the existing chunk store for similar existing nodes, and records the matches on the job (new `cog_ingest_job.retrieve` column). Opt-in (`contentHandling.dedup.semantic`); does not re-route the ingest; entity/person resolution stays the engine's job. **Eleven stages** through the executor. Adversarially reviewed (host-tsc lens clean); its findings applied (made opt-in; bounded + documented the head-biased embedding).

**Unfinished / WIP:** none stranded — every one of the 8 releases built + verified (type-check / check:templates / conformance / parity / build / esbuild gen-check 45-0), and the core-path slices adversarially reviewed (gate, cog-db, create-node, retrieve — cog-db + create-node reviews returned **zero** findings) or line-by-line self-diffed (quarantine, preprocess, persist-detection). All committed + pushed.

**Loose ends & follow-ups:**
- **⚠️ v1.21.46 (retrieve) NOT yet host-validated** — Kim's next `npx cognitive-ingest update` in veradai is the definitive host-tsc check for the retrieve stage's inline glue. (v1.21.45 and earlier were host-validated between slices.)
- **#3 retrieve follow-ups (the pick-up list):** (1) **surface the matches to the Suggestions inbox** as a "possible duplicate — match / iteration-of / new?" item — ⚠️ `refreshSuggestions` reconciles the inbox against the *engine's* `runFullCheck` insights and would DROP a non-engine `possible_duplicate` row as stale, so scope its drop-stale to engine types first; (2) a **job-detail UI card** for the matches (mirror detection's v1.21.34 card); (3) **chunk-level long-doc dedup** — a 2nd retrieve pass after chunk-embed using the per-chunk vectors (the faithful fix for the documented head-bias); (4) the **confirmation → re-route** (recognise a re-uploaded doc as an edition).
- **Low finding left open (retrieve):** `resolveStructureReadScope` is resolved **twice** per ingest (the retrieve stage + cog-db's `ingestScope`) — harmless double read; could resolve once + stash on ctx.
- **DAG collapse-PROPER not done (deliberately deferred):** the literal single `runPipeline(DEFAULT_FLOW)` needs re-introducing `halt` to gate/quarantine (removed in v1.21.40 for the separate-pass reuse model; correct again in a fresh-ctx single flow), threading the concurrent **parse-QA** via a `ctx.qaPromise`, and reconciling the **FINALIZE** block (afterIngest + QA-await + provenance + the COMPLETED update — big terminal bookkeeping that arguably stays in the ingest). Its own careful session; mostly architectural gain.
- **#4 populate** and the visual node-flow editor / shared 3-tab ingest component remain unbuilt (unchanged from before).

**Decisions & ideas:**
- **Kim chose "keep grinding the DAG collapse"** over pivoting to feature #3 (via AskUserQuestion) — so the gate/quarantine/preprocess/persist-detection migrations happened before #3.
- **The clean incremental DAG slices are DONE; the remainder is the collapse-proper (hard) — an honest assessment given to Kim:** either invest in that architectural refactor, or call the DAG migration "functionally done" (the node-flow editor can already drive the 11 registered stages) and move on. Kim then said "test, then move on" → I tested + started #3.
- **#3 retrieve design (Kim's steer, verbatim):** *"the engine does the entities/persons. i think like observe, but think closer before ai."* → observe-only + positioned BEFORE cog-db + entity/person resolution left to the engine's `retrieveToConnect`, NOT the retrieve stage.
- **Retrieve head-bias limitation (from the adversarial review, accepted + documented):** a single query vector can only represent a long document's opening, so the observe-only first slice is a *coarse head-based signal* — reliable for short/medium content, a first approximation for long composites; chunk-level dedup is the faithful follow-up. The embed input was bounded to an explicit 8000-char slice to make the truncation intentional rather than provider-dependent.
- **Migration patterns proven + reusable** (all in the memory file): gate = lookup-only + outcome the ingest reads back; cog-db = annotated-const engine input inline + thin call stage; create-node = verbatim block → module helper with destructured params; a stage must NOT set `ctx.halt` while the context is reused across separate passes (halt only for the eventual single flow).

**Recorded where:** `docs/…/cog-ingest/changelog.mdx` (v1.21.39–46) · `pipeline.mdx` (build-order: 11 stages + retrieve first-slice note) · `roadmap.mdx` (unchanged this session) · memory `project_content-driven-splitting.md` (full arc — every slice, the collapse-proper assessment, the #3 design + follow-ups + the refreshSuggestions gotcha) + `MEMORY.md` index.

**Open items for next session:** the ready-to-paste **handover prompt is in the chat** for this session (above this audit run) — it lists the pre-flight, the priority-ordered pick-up (A: #3 retrieve follow-ups starting with the inbox-surfacing + the `refreshSuggestions` gotcha; B: #4 populate; C: the DAG collapse-proper), and the gotchas. First real-world step: `npx cognitive-ingest update` to 1.21.46, then set `contentHandling.dedup.semantic = true` on a source (with Structure embeddings on) to see the retrieve matches recorded.

---

## 2026-08-28 — Split fan-out shipped, pipeline design locked in the docs, DAG-migration started

**Verdict:** ✅ Safe to archive
**Shipped this session:** **v1.21.36 → v1.21.38** (3 releases), all pushed (`cognitive-ingest/main` `8dfa7fc`, docs `b2cc4ce`). `dist` gitignored + rebuilt after each bump (module used via `npm link`; stamps 1.21.38). Engine unchanged (vendored 1.12.17).
- **v1.21.36 — Producer framework + split fan-out** (the big one): `producers.ts` (3 pure deterministic parsers — `structured-sections` / `chat-export-text` / `email-thread` — validated against the real rules PDF, iMessage export, and email fixtures via a prototype→emitted-code round-trip) + `split-stage.ts` (the Structure fan-out: container + one child node per unit via `createNode({parentId})`, with `extracted_from` / `sent_by` / `received_by` / `followed_by` edges, node-type resolution declared→auto→propose→fallback, fully degrade-safe) + `SplitConfigSchema` + `cog_ingest_job.split` column + a Split/Outputs UI section in the source dialog's Preprocessing tab + a split-result card on the job page. **Adversarially reviewed** (4-lens workflow → 1 medium + 3 low findings, all fixed + re-verified). Escaping done via the author-real-TS → machine-escape → byte-identical round-trip method + a strict-tsc harness against host-shaped stubs.
- **v1.21.37 — chunk+embed as a stage** (DAG step 1, first + lowest-risk block, non-fatal so it can't break an ingest).
- **v1.21.38 — acquire as a stage** (the content-type switch → clean text/vision via Docling; propagates errors like the inline version). **Four stages now run through the executor: acquire · detect · split · chunk-embed.**
- **Design work (no version):** re-read the master "Anything In, Graph Out" artifact in full; moved the master design into the docs as a new **Ingestion Pipeline** page (`pipeline.mdx`), reconciled + refined it over several review passes with a second session. Ran a **provider-research workflow** (18 tools: Docling / Unstructured / MinerU / LlamaParse / Tensorlake / Extend / Unstract / Elicit / scite / …) and folded the patterns in.

**Unfinished / WIP:** none stranded — every increment built + verified (type-check / check:templates / conformance / parity / build / esbuild gen-check 51-0; adversarial host-tsc review on the split slice), committed and pushed. Step 1 (DAG-migration) is **mid-arc** (4 of ~9 stages migrated) but each migrated stage is byte-for-byte behaviour-unchanged and self-contained.

**Loose ends & follow-ups:**
- **⚠️ Split Person-creation realignment: AGREED but NOT YET CODED.** The pipeline design (and `pipeline.mdx`) now say the splitter must *only record raw `from`/`to` handles*, with person matching done by `retrieve` + the engine + the inbox — because Person is the app's curated "single source of truth" and auto-creating duplicates would pollute it. But the **shipped v1.21.36 `split-stage.ts` still creates/links Person nodes** (`linkParticipants` defaults **true**). Next session must: default `linkParticipants` OFF + stop creating Person in split (record handles as fields), and build the `retrieve` stage as the real home for resolution. This is a **docs-ahead-of-code** gap, not a bug (it's opt-in behind `split.mode`, degrade-safe).
- **Remaining step-1 DAG migrations:** the change-detection **gate** (trickiest — its early `return` must become a `ctx.halt` the ingest reads back, since stages return void), the **engine (cog-db) pass**, **create-node** (post-ingest), and **link** — ending at `ingest()` = build context → `runPipeline(DEFAULT_FLOW)`.
- **`acquire` stage not strict-tsc'd** (budget) — it mirrors the proven inline switch exactly, but the definitive host-tsc check happens on Kim's next `npx cognitive-ingest update` in veradai.
- **Acquisition gotcha (court rules):** the legislation URL `…/latest/text` plain-fetch returns the **table of contents only** (rule titles, no bodies — JS-loaded); full text needs the **downloadable PDF via Docling** or a **browser-rendered** URL fetch. The browser-rendered URL acquisition is an unbuilt follow-up.
- **Not built yet:** the `retrieve` / `organise` / `populate` / find-other-node-types / conversation-finder / AI-triage stages; the visual (React Flow) source-level node-flow editor; the top-level Producers config; the full shared 3-tab ingest component + pipeline-progress screen (only the Split config section landed).
- **Court-rules extraction test** still pending on Kim's side (`npx update` + AI Core purpose defaults + Structure embeddings + active scope).

**Decisions & ideas:** (all captured in `pipeline.mdx` + `project_content-driven-splitting.md`)
- **Master design moved from the artifact into the docs** (`pipeline.mdx`) at Kim's request — the docs are now the single reference.
- **The core rule changed:** deterministic-first, *then AI to enrich / clean / verify / confirm* — AI is **not** just a fallback (important for long docs → clean searchable vectors).
- **Two human feedback stages:** *Confirm* (after retrieve — "match / iteration / new?") and *Clarify* (during analyse — the AI hands questions back to the human). Feedback is generic; the per-app wording (a Person match vs a design-iteration) is configuration.
- **Deterministic populate runs before AI organise** (gives the AI context). **Node-flow editor = mainly source-level** (producers are the reusable blocks). **No backward-compat** needed. **RSS = an input mode but admin-only for now → source config.**
- **Entity matching is the engine's job** (`retrieve-to-connect`) + the suggestions inbox, not the splitter (→ the realignment loose end above).
- **Producers carry standard shapes** (node type + known fields). **Second connection parse** finds *other* node types (e.g. "behaviours") and connects them — AI *and* a retrieve, one pass per type. **Cross-medium conversation-finder** joins a text thread + an email thread on the same subject into one conversation.
- **Two detects** (input-kind before extraction; content-shape after) and **two QA passes** (parse, then fields). Provider patterns adopted: one clean "middle representation" (Docling), cheapest-first / escalate-only-when-hard, **field-description-as-prompt**, a **second-model semantic judge** over self-reported confidence, **source-grounding** on every field/edge, semantic dedup = detect-auto / resolve-manual / never-auto-delete, and a correction feedback loop.
- **DAG-migration strategy:** migrate the current fixed pipeline into stages incrementally, **lowest-risk block first**, each byte-identical + verified (rather than one big risky rewrite).

**Recorded where:** `docs/…/cog-ingest/pipeline.mdx` (new master design page) · `changelog.mdx` (v1.21.36–38) · `roadmap.mdx` (build-order + shipped markers) · `configuration.mdx` (Split config) · memory `project_content-driven-splitting.md` (full arc, verified veradai contracts, escaping method, step-1 plan) + `MEMORY.md` index.

**Sensitivity note:** the name "Tim Collier" (Kim's ex-husband) was scrubbed from the docs *and* the private memory this session → replaced with a generic placeholder ("John Citizen"). Test documents are family-law **evidence** — read locally only, never put message/email content in artifacts or chat.

**Open items for next session:** (1) **code the split Person-creation realignment** (linkParticipants OFF + record handles only); (2) finish step-1 DAG migration — the **gate** (early-return → halt) next, then engine / create-node / link; (3) then the `retrieve` + `populate` stages, and the source-level node-flow editor. A ready-to-paste handover prompt was produced in the session chat.

---

## 2026-08-27 — Suggestions persistence, Ontology growth, then the content-driven-splitting arc (detection → executor)

**Verdict:** ✅ Safe to archive
**Shipped this session:** **v1.21.31 → v1.21.35** (5 releases), all pushed (`cognitive-ingest/main` `f502e80`, docs `e7d1a1e`). `dist` rebuilt after the final bump (mtime confirms, stamps 1.21.35). No engine re-vendor (stayed 1.12.17).
- **v1.21.31 — Durable Suggestions inbox** (feature 1, persistence): `cog_ingest_suggestion` table + reconciled `refreshSuggestions` (honours dismiss/resolve, drops stale insights, dedupKey) + debounced post-ingest trigger + `POST /suggestions/refresh · /:id/dismiss · /:id/resolve` + per-row Dismiss/Resolve + erasure wiring.
- **v1.21.32 — Ontology growth** (feature 4, both entity + connection types): entity-type proposals reuse Structure's **native** node-type queue (`proposeNodeType`/`approve`/`reject`); relationship types (with `relationClass`) get a new `cog_ingest_ontology_proposal` store; auto-add on `auto` sources, review queue on `propose`; unified `/my-feature/ontology` page. Adversarial host-tsc review: **no defects**.
- **v1.21.33 — Content-shape detection** (observe-only): `my-feature.detection.ts` `detectContentShape` — 6 shapes (email-thread / chat-log / chat-screenshot / structured-document / notes-list / document) from cheap deterministic signals, form-agnostic, `needsTriage` seam. Validated on real files (rules PDF → structured, an iMessage export → chat, .eml → email, notes → notes).
- **v1.21.34 — Surface detection** on `cog_ingest_job.detection` + a job-detail card (visible while testing).
- **v1.21.35 — Stage-DAG executor**: `my-feature.pipeline.ts` (`registerStage` / `runPipeline(flow, ctx)` with halt); the detect step now runs *through* it as the `detect` stage (zero behaviour change); `DEFAULT_FLOW` documents `acquire→detect→retrieve→split→organise→populate→cog-db→link`.

**Unfinished / WIP:** none — every increment built + verified (type-check / check:templates / conformance / parity + esbuild gen-check, adversarial host-tsc review on the ontology slice, runtime esbuild verification of the emitted detector + executor), committed and pushed. The **content-driven-splitting feature is mid-arc** (detection + executor shipped; producers + frontend still to build) — but each shipped increment is self-contained and safe.

**Loose ends & follow-ups:**
- **Producer framework not built** — the `split` stage + `split-email` / `split-chat` / `split-structured` producers + the fan-out (container + child nodes, `extracted_from` provenance, Person edges) that finally turns "one ingest → many nodes" into real behaviour. This is the primary next task.
- **Frontend config not built** — Kim explicitly needs it to test: the shared 3-tab ingest component (Text / URL / Upload-via-DM, cog-ingest-exported with props) + a pipeline-progress screen. Collapse the current 8-tab `PAGE_TEMPLATE`.
- **Later stages not built** — `retrieve` (dedup + resolve-to-existing, before AI), `organise` (AI), `populate` (deterministic → AI field + connection population), `cog-db` (per-node extraction), the React Flow node-flow admin, the top-level Producers config.
- **Read-lane features 3 & 2 still not started** — Related/duplicates (`findSimilarNodes`/`findPotentialDuplicates`), Graph analytics (centrality/community/paths). Parked while the splitting arc runs.
- **Kim's court-rules extraction test** still pending (from prior sessions): `npx cognitive-ingest update` in veradai + 3 AI Core purpose defaults + Structure embeddings + an active scope.

**Decisions & ideas:** (the content-driven-splitting architecture — all consolidated in the **"Anything In, Graph Out"** artifact + `project_content-driven-splitting.md`)
- **Config = 3 layers:** Engine (exists) / **Producers** (top-level = the node types) / **Sources** (per content-kind = the fields, context + AI-op variables). Producers make node types + generic fields; **children inherit the container type's fields** via Structure hierarchy. Detection routes.
- **Two levels of output:** producer-generic (base type + own fields) + source-specific population profile. **Structure owns field/connection _definitions_; cog-ingest owns _population_** (which field, filled how). `context`/`insights` are **per-source** (not universal — "insight" must be precisely scoped for legal). **AI analysis is a required pipeline stage** (deterministic-first, AI where it must); connections populated too (deterministic where target known, AI-discovery otherwise).
- **Pipeline = a configurable node-flow** (React Flow editor) over a **backend stage-DAG executor** — Kim's call. Hybrid chosen: build the executor, run the current fixed sequence through it to validate, before the visual editor / new stages. **Retrieve stage before AI** (skip already-ingested = token savings; resolve who/what to existing nodes — a text sender → an existing Person).
- **Open producer pipeline, not a rigid tree:** undeclared child types resolve **declared → default → propose** (reuse the shipped ontology-growth queue) — never dropped. Entity/concept/connection come from cog-db extraction **per evidence node**.
- **Evidence hierarchy + chain-of-custody:** `Source Document → Evidence → Conversation → Message` as node types; **every derived node carries an `extracted_from` edge back to the raw artifact** (+ offset/bbox) so it's verifiable and the lawyer can be handed the original. Generalises to the brain-assistant as `Source → Segment → Item`.
- **Shared ingest component** lives in cog-ingest (exported), `submitLabel` + target as **props** — LOCKED. **Detection is form-agnostic** (runs on acquired text) so PDF / URL (legislation.gov.au, AustLII) / screenshot / paste classify identically.
- **Two small open decisions:** (1) source-output = a Structure shared field-template (definitions) + a cog-ingest population profile (fill) — confirm; (2) how far to take the config-driven stage-DAG (React Flow + executor) vs a fixed pipeline, and when.
- **⚠️ Evidence sensitivity:** the test documents (an iMessage export, a legal .eml, chat screenshots incl. one involving a child) are **family-law evidence** — read locally only; **no message/email content in any artifact or chat** (structural/format analysis only). Working files saved to `~/Downloads/fcfcoa-ingest/` and `~/Documents/Family Law/iPhone/parse/`.

**Recorded where:** changelog v1.21.31–35 + the roadmap **Content-driven splitting** section (+ Ontology-growth + Suggestions updates); memory `project_content-driven-splitting.md` (the full arc — THE primary file) + `project_readlane-features-build.md` + `MEMORY.md`; three private artifacts — *One Rule, One Node* (rules ingest plan), *One Thread, Many Nodes* (messages parse plan), *Anything In, Graph Out* (the master architecture; URL in the memory file).

**Open items for next session:** a ready-to-paste **handover prompt** was produced in the session chat (Phase 2 of the handover) — grab it before archiving. Pick-up = the "What Needs Doing" list: producer framework (`split` stage + fan-out) then the frontend ingest component, both plugging into the shipped stage-DAG executor.

---

## 2026-08-26 — Retrieve-to-connect activation, engine 1.12.6→1.12.17 pickup, scaffold + security fix, connection-type seeding, feature flags, Suggestions Inbox (feature 1)

**Verdict:** ✅ Safe to archive
**Shipped this session:** **v1.21.22 → v1.21.30** (9 releases), all pushed (`cognitive-ingest/main` `33d49ba`, docs `7aab8e3` before this audit entry). Scaffold updated 1.31.15 → **1.31.17**. Engine re-vendored 1.12.4 → **1.12.17**; `dist` rebuilt after the final bump (mtime confirms).
- **v1.21.22 — Retrieve-to-connect activation** (engine 1.12.6): shared `resolveStructureReadScope` → ingest pushes `scopeRootId` + `unreadableTypes` on the engine `ingest()` call; adapter forwards `scopeRootId`/`excludeNodeTypes`; `retrieveToConnect` default-on; `cog_content_archive` table provisioned.
- **v1.21.23 — Scaffold 1.31.17 + C9 security fix**: the account-erasure deletion handler could run an unscoped `deleteMany` (undefined userId = every tenant) — now throws when the subject id is absent. Surfaced by the scaffold's new conformance rule.
- **v1.21.24 / .26 — Re-vendor engine 1.12.10 then 1.12.16** (additive, verified no host change): read/intelligence lane flipped to Structure storage; new exports `COGNITIVE_DB_CONNECTION_TYPES`, `GraphExpandOptions`/`GraphExpansionResult`.
- **v1.21.25 — Wire `expandGraph` + `getConnectionsForNodes`** adapter reads (Structure now exposes them); destructive lifecycle methods left unwired.
- **v1.21.27 — Connection-type seeding** (D3): per-user seed of the 8 engine connection types from the engine export, with `relationClass`; idempotent, degrade-safe, awaited before edge writes.
- **v1.21.28 — Enable `expectationPass` / `structuralGaps` / `sequentialAdjacency`** (D1).
- **v1.21.29 / .30 — Suggestions Inbox (feature 1, end-to-end)**: `GET /suggestions` backend (runs the engine's `runFullCheck` proactive pass, transient) + a user-facing Suggestions page (nav + `/my-feature/suggestions`) rendering the five insight types.

**Unfinished / WIP:** none — every increment built + verified (type-check / check:templates / conformance / parity + esbuild gen-check + adversarial host-tsc reviews on all engine-facing code), committed and pushed. `contract:check` was removed by scaffold 1.31.17 (no longer in the recipe).

**Loose ends & follow-ups:**
- **Suggestions persistence + dismiss** — the inbox is transient (recomputed on demand). A durable inbox needs a `cog_ingest_suggestion` table (+ dedup + dismiss/resolve + erasure wiring + a post-ingest refresh). Open sub-decision, flagged to Kim.
- **Features 4 / 3 / 2 not started** — Ontology growth (auto-add + review queue, user-facing), Related/duplicates, Graph analytics. Build order 4→3→2; the module's backend+frontend slice conventions are now proven and captured in memory.
- **Court-rules extraction test** is still Kim's action: `npx cognitive-ingest update` to v1.21.30 in veradai + 3 AI Core purpose defaults (analysis/vision/embedding) + enable Structure embeddings + **set an active scope** (retrieve-to-connect skips without one, by design).
- **Cross-repo engine asks still open** (from the engine's handoff "still NOT closeable"): Structure `getConnection(id)`; ontology hierarchy (superClasses + relationClass) surfaced in `listNodeTypes`/`listConnectionTypes` config API; a scope/parent filter on `StructureNodeQuery` for exact-name dedup.
- **`condensation` note sent to the engine session** (`docs/drafts/condensation-data-minimisation-note.md`) — asking them to update their condensation docs to the parked/data-minimisation framing.

**Decisions & ideas:**
- **Condensation PARKED, reframed as data minimisation** (Kim): the future goal is *shrink the bulky, regenerable data — raw text, chunk text, vectors — never the entity/relationship graph*. The staleness-based "archive unused" job is dropped (it's decay in disguise; violates never-decay). Recorded in the roadmap (Long-Term / Ideas) + a note for the engine.
- **4-feature build scoped from Kim's decisions** (via AskUserQuestion): D1 flags = enable all three; D2 read lane = Proactive suggestions + Graph analytics + Related/duplicates (skip Metrics); D4 ontology growth = auto-add **and** review queue, **user-facing from the start**; D5 `deleteConnection` = leave unwired (no consumer). `archiveNode`/`moveNode`/`mergeNode` stay **parked** (the data-minimisation levers — Structure now has them, but building is the parked design work).
- **Suggestions: transient first, persistence later** — the render UI is the same either way; dismiss layers on. Chose the smaller slice to validate the read-lane pattern (reused by features 2/3).
- **`cog_node` retirement — NOT ready yet** (Kim asked): the direction is right and there's no data to migrate, but the engine still hard-binds `cog_node` in condensation (`summariseOldContent` does `SELECT * FROM cog_nodes`), and **`cog_working_memory_events` is NOT retireable** (it's used in Structure mode — a correction to the roadmap's Phase-5 "3 tables" framing). Gate on the engine confirming all `cog_node` hard-binds are gone/flag-gated.
- **`relationClass` is now persistable** on Structure connection types (Structure added the field) — this half-unblocks the deferred relationClass persistence; it now needs only a proposal-approval path (= feature 4).
- **Host-tsc-invisible traps caught this session:** `GraphExpandOptions`/`GraphExpansionResult` were NOT root-exported at 1.12.10 (fixed via inline mirrors; the engine later added the export at 1.12.11); the engine's connection-type slugs use underscores (`related_to`) but the Structure *service* `createConnectionType` enforces no slug regex, so they seed fine and match the edge strings.

**Recorded where:** changelog + roadmap (per version, incl. the Engine v1.12.10 host-activation-plan section + the data-minimisation entry); memory `project_readlane-features-build.md` (the 4-feature plan + conventions + engine proactive API) + `project_retrieve-to-connect-activation.md` (the 1.12.6→1.12.17 arc) + `MEMORY.md`; engine handoff note `docs/drafts/condensation-data-minimisation-note.md`.

**Open items for next session:** A ready-to-paste **handover prompt** was produced in the session chat (Phase 2). Pick-up = continue the 4-feature build: decide **Suggestions persistence + dismiss** (or defer), then build **feature 4 (ontology growth)** — auto-add proposed relationship types on `'auto'` sources (persist to Structure connection types **with `relationClass`** — field now exists) + a review-queue store + approve/reject + a user-facing surface. Then feature 3 (related/duplicates: `findSimilarNodes` / `findPotentialDuplicates`), then feature 2 (graph analytics: `analyzeCentrality` / `detectCommunities` / path-finding). Note: the docs working tree carries **uncommitted engine/Structure-session draft files** (`drafts/engine-cog-ingest-handoff.md`, `drafts/structure-*.md`, a modified `retrieve-to-connect-cog-ingest-wiring.md`) — **not this session's work**; left untouched.

---

## 2026-08-21 — Structure write-path (B2/B3), AI Core alignment (A4 + model-purpose), working-memory consolidation (C2), Structure-native storage default

**Verdict:** ✅ Safe to archive
**Shipped this session:** v1.21.16 → **v1.21.21**, all pushed (`cognitive-ingest/main` `c5a7a11`, docs `5a742ae`). Scaffold updated 1.31.9 → 1.31.15. Engine vendored 1.12.4; dist rebuilt after the final bump.
- **v1.21.16 — B2/B3 Structure write-path hardening:** bulk `errors[]` logging (structure-adapter); post-ingest `createNode` fail-loud + a non-fatal `result.postIngestWarning` on the job + a non-destructive advisory on the job-detail page; stale working-memory hierarchy comment corrected.
- **v1.21.17 — A4:** seed the engine's op catalogue from its `cognitive-db/operations` package export (v1.12.0); adapter routes by `role` + `marker`.
- **v1.21.18 — cognitive-db v1.12.2 pickup:** `updateConnection` adapter method (the C2 lockstep).
- **v1.21.19 — C2 working-memory consolidation wired:** `retrieve()` records co-retrieval events via `trackEvent`; a `working-memory.session.ended` listener runs user-wide `consolidate`. Both Structure-mode gated.
- **v1.21.20 — model-purpose migration:** op seeding binds a semantic AI Core purpose (analysis / vision / embedding) instead of `{ kind }`; seeding is now failure-tolerant (seeds up front, refuses at run time until the admin wires a purpose default).
- **v1.21.21 — Structure-native storage is now the DEFAULT** (was opt-in); `COG_ENGINE_STORAGE=cog_node` is the escape hatch.

**Unfinished / WIP:** none — every increment built, verified (check:templates / type-check / build / conformance / contract / parity + esbuild gen-check + adversarial reviews), committed and pushed; dist mtime confirms it was rebuilt after the v1.21.21 bump.

**Loose ends & follow-ups:**
- **Court-rules extraction test** is Kim's next action: `npx cognitive-ingest update` to v1.21.21 in veradai + set 3 AI Core purpose defaults (analysis / vision / embedding) + enable Structure embeddings.
- Deferred by design: helpfulness feedback + `'used'` events for consolidation; typing the `co-activated` edge; scoping the engine's adapter graph-searches (B1 follow-up — now a hard prerequisite for retrieve-to-connect).
- Engine's C1 (Suggestions-Inbox) + C3 (condensation) landed engine-side (1.12.3 / 1.12.4) but are not consumed by cog-ingest yet.

**Decisions & ideas:**
- Storage-default flip verified safe because the engine's Structure lane is now substantially complete (ingest routes structure-native; 21 flag call-sites). Pre-launch clean break — no `cog_node` data migration.
- Consolidation uses **user-wide `consolidate`** (not `consolidateSession`): `retrieve()` keys co-retrieval events independently of the working-memory session node, so session-scoped would match nothing.
- Three knowledge-graph articles (Vijayan; Burgess Semantic-Spacetime / intent) discussed → **retrieve-to-connect** design brief written (`docs/drafts/retrieve-to-connect-brief.md`), plus the admin-config-fields idea (app-specific extraction guidance flows as config-driven **context**, not a hardcoded prompt).

**Recorded where:** changelog + roadmap (per version); memory `project_ai-core-prompt-model-break.md` (sessions 4–6) + `MEMORY.md`; design briefs in `docs/drafts/` (`cog-ingest-wm-consolidation-brief.md`, `retrieve-to-connect-brief.md`).

**Open items for next session:** The **engine session left a wiring handoff** — `docs/drafts/retrieve-to-connect-cog-ingest-wiring.md` (preserved this session) — engine is now **v1.12.6** with retrieve-to-connect support. ⚠️ That note is written from veradai's *stale* view and lists items already shipped in the module (A4 seeding, adapter role/marker routing, `updateConnection`) — do **not** redo them. Reconciled real work list: (0) re-vendor engine → **1.12.6**; (1) **retrieve-to-connect activation** — push `scopeRootId` + `unreadableTypes` on the engine `ingest()` call, forward `scopeRootId` / `excludeNodeTypes` through the adapter's `semanticSearchNodes` / `findSimilarNodes`, enable `features.retrieveToConnect`; (2) `relationClass` persistence on connection types; (3) seed proactive connection types (`qualifies` / `may_answer` / `answers`); (4) C3 `cog_content_archive` + configure the `summarise` op; (5) §7 per-source extraction-guidance field. A ready-to-paste handover prompt was produced in the session chat.
