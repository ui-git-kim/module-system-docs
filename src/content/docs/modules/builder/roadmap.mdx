---
title: Roadmap
description: Planned features, improvements, and known issues for the Block Builder module
sidebar:
  order: 6
---

import { Aside, Card, CardGrid } from '@astrojs/starlight/components';

This page tracks planned work, known issues, and completed milestones for the Block Builder module.

<Aside type="note">
This roadmap reflects current plans and may change based on feedback and requirements from other modules.
</Aside>

---

## Known Issues

Issues discovered but not yet resolved.

| Issue | Severity | Where | Workaround |
|-------|----------|-------|------------|
| Registry service component mappings are incomplete | Low | `registry.service.ts` — `getImportForType()` only maps common shadcn components | Extensible but needs more component mappings for full code generation coverage |
| Tree panel is a placeholder | Low | `TreePanel.tsx` — needs a full tree view with expand/collapse and drag reorder | Use the canvas panel for tree navigation |
| No undo/redo support | Medium | Builder canvas — no history stack for block tree changes | Save frequently; use browser back if needed |
| Icon picker limited to Lucide | Low | `IconPicker.tsx` — only supports Lucide icons in the picker UI, despite configurable icon library for code output | Use string input for non-Lucide icon names |
| Data hooks bypass TanStack Query | Medium | All `use*` data hooks (`useProjectBlocks`, `useBlocksLibrary`, `useNativeComponents`, `useNativeComponentsAdmin`) use imperative `useState`/`useEffect` instead of `useQuery`/`useMutation` — no cache sharing, deduplication, or scoped invalidation | Works functionally but diverges from the starter template's [data-fetching conventions](/starter-template/data-fetching/) |
| No styleRegistry contribution | Low | Module does not register with `styleRegistry` despite controlling iframe preview CSS and pristine shadcn component selection | No runtime impact — CSS is injected directly via iframe postMessage and scaffold shadcn sync |

### From the v1.12.1 health audit

A full standards/production/performance/security audit of the generated templates was run at v1.12.1. **All 16 critical findings and most moderate findings were remediated in [v1.13.0](/modules/builder/changelog/)** — route authorisation, block ownership and `isSystem` authority, generated-code escaping, preview origin/sandbox hardening, the PropertyPanel/DnD/widget crashes, the empty-component registry generator, fresh-install seeding and the `pickers`/env declarations, the `securityRegistry` deletion handler, backend validation, and fetch timeouts.

The items still open are tracked below.

| Issue | Severity | Where | Notes |
|-------|----------|-------|-------|
| Registry handle needs server-side uniqueness/reservation | High | Handle uniqueness and format are only pre-checked client-side before being persisted via the generic preferences path | Backend resolver still trusts `preferences.handle`; needs a reserved-name denylist and a server-side uniqueness check |
| Public `/r` route registered with empty `versions: []` | Medium | `route.registration.ts` — undocumented mounting for the unversioned public routes | Left unchanged (working endpoint); verify against `mountAll` behaviour before altering |
| Forbidden Tailwind classes in generated feature code | Medium | ~127 occurrences of typography-scale, font-weight, colour-literal and `!important` classes outside `components/ui/*` | Cosmetic; migrate to semantic tokens (see Code Health below) |
| Block-list endpoint over-fetches | Medium | `getBlocks` selects full `data` trees + `generatedCode` for list views | Add a `select` projection once list vs. editor needs are separated |
| Schema uniqueness ineffective on nullable columns | Medium | Compound unique constraints include nullable columns (NULLs are distinct in Postgres) | Needs a partial index and a migration for existing installs |
| Duplicate drag id across palette and child-add | Medium | `PaletteItem` and `ChildElementItem` both register `palette-\${type}` | Needs a distinct id plus a matching change to the drag-end/overlay parsing |

<Aside type="caution">
The full audit report (151 findings) is tracked privately. This roadmap records the remediation surface, not exploitation detail.
</Aside>

---

## Planned Improvements

### Short-term (Next Version)

**Migrate Data Hooks to TanStack Query**
- Replace imperative `useState`/`useEffect` fetch patterns with `useQuery`/`useMutation`
- Namespace all query keys with `['builder', ...]` prefix per starter convention
- Enable scoped invalidation (`queryClient.invalidateQueries({ queryKey: ['builder'] })`)
- Remove hand-rolled loading/error state where TanStack Query already provides it
- *Context:* Scaffold v1.9.0 ships a data-hooks template aligned with the starter's `QueryClient`. Apply the same pattern to `useProjectBlocks`, `useBlocksLibrary`, `useNativeComponents`, `useNativeComponentsAdmin`. Keep optimistic updates where UX demands them via `setQueryData`.

**Undo/Redo Stack**
- Add undo/redo support for block tree changes
- Keyboard shortcuts: Ctrl+Z / Ctrl+Shift+Z
- History panel showing recent operations
- *Context:* The `useBuilder` hook manages block state — add a history array alongside `block` state. Each `setBlock` call pushes to history. Limit to ~50 entries.

**Tree Panel Implementation**
- Full hierarchical tree view with expand/collapse
- Drag-and-drop reorder within the tree
- Right-click context menu (delete, duplicate, wrap)
- Visual indicators for containers vs. elements
- *Context:* `TreePanel.tsx` exists as a placeholder. Use a recursive tree component with `@dnd-kit/sortable` for reorder.

**Broader Structure Hook Integration**
- Listen to `structure.node.updated` to handle project renames (update display references)
- Listen to `structure.node.archived` to cascade archive state to blocks
- *Context:* Currently only listens to `structure.node.deleted` for cleanup and `structure.node.response` for metadata enrichment. The structure module fires 16+ action hooks that the builder could react to.

### Medium-term (Upcoming)

**Component Import/Export**
- Export blocks as standalone shadcn registry items
- Import from external shadcn registries
- Version tracking for imported components
- Conflict resolution for name collisions
- *Context:* The registry JSON format is already defined in `registry.service.ts`. Needs a frontend UI for browsing external registries and an import resolution flow.

**Builder Logic Module**
- Separate module (`builder-logic`) for adding interactivity
- Event bindings (onClick, onChange, etc.)
- State management integration
- Data source connections
- Conditional rendering rules
- *Context:* This will be a new module that depends on `builder`. It should extend the property panel with a "Logic" tab (order 20) and the code generation registry with event handler imports and prop transforms. Store event bindings alongside block data.

**Enhanced Code Generation**
- Complete import mappings for all shadcn components
- Support for custom component libraries
- Code formatting with Prettier
- Multiple output formats (React, Vue in future)
- *Context:* Currently `registry.service.ts` has a limited `shadcnComponents` map. Expand it to cover all components in the `native_component` table dynamically.

**Multi-Library Icon Picker**
- Extend `IconPicker` to support all configured icon libraries (Tabler, Phosphor, Remix, Hugeicons)
- Load icon sets dynamically based on `shadcn:icon-library` config
- *Context:* Currently the picker only renders Lucide icons. The icon library config affects code output but not the picker UI.

### Long-term / Ideas

**Builder Style Module**
- Separate module (`builder-style`) for visual styling
- Tailwind class picker with live preview
- Theme generator for consistent design tokens
- Style presets and templates
- Responsive style overrides per breakpoint
- *Context:* Should register a "Style" tab (order 30) in the property panel tab registry and use `transformProps` in the code generation registry to apply className modifications.

**Multi-Block Canvas**
- Work with multiple blocks simultaneously on a split canvas
- Cross-block drag-and-drop
- Collection-level editing with entry point management
- Shared component references across blocks

**AI-Assisted Building**
- Natural language to component tree conversion
- Property suggestions based on context and common patterns
- Layout recommendations based on content type
- Accessibility improvement suggestions

**Collaborative Editing**
- Real-time multi-user editing with cursor presence
- Conflict resolution for simultaneous edits
- Change attribution and activity feed

**Concurrent radix + base shadcn styles**
- Support the radix and base style libraries at the same time rather than a single global `shadcn:builder-style`
- Style-scoped pristine folders and per-project (or per-block) style selection
- *Context:* the `shadcn:component:{style}:{name}` cache is already style-keyed; the single-style constraint lives in the sync target folder and the global style config.

### Code Health (from the v1.12.1 audit)

Lower-severity cleanups surfaced by the health audit, grouped for a maintenance pass:

- **Dead code:** remove unused template files (`frontend/types.ts`, `frontend/routes.ts`) and the unexported `ADMIN_PAGE_COMPONENT_TEMPLATE`; drop unused imports/vars and DEV `console.*` in the generated CanvasPage.
- **Conventions:** fix import ordering (external → `@/` → relative) across generated files; replace forbidden Tailwind classes with semantic tokens; refresh `package.json` description/keywords off the scaffold placeholder.
- **Config declarations:** declare `envVariables` in `module.ts` for `VITE_REGISTRY_DOMAIN`; export `CONFIG_KEYS`; remove dead `native_component` indexes; drop published sourcemaps from the npm package.
- **Performance passes:** `React.memo` on canvas recursion components, memoise context values, add `staleTime` to the near-static native-components query, lazy-load heavy registration imports, and avoid bundling the full lucide icon set.
- **Registry hardening (defence in depth):** reserved-handle denylist, timing-safe token comparison, enumeration-safe 404s, batched component-import upserts, and request timeouts on shadcn proxy fetches.

---

## Completed

Features that have been implemented and released.

| Feature | Version | Description |
|---------|---------|-------------|
| Health-audit remediation | v1.13.0 | Route authorisation, block ownership, generated-code escaping, preview origin/sandbox, crash/TS-build fixes, registry generator + fresh-install seeding, `securityRegistry` deletion handler, backend validation |
| Perf hot-path fixes | v1.12.0 | Structural-sharing tree mutations; eliminate Chrome-blocking hot paths |
| Extensible iframe preview CSS | v1.11.11 | `builder.canvas.globalCss` filter hook, `foundry:global-css` postMessage protocol |
| Extensible components.json | v1.11.10 | `builder.registry.componentsJson` filter hook for ProjectRegistryTab |
| CRUD action hooks | v1.11.8 | 8 lifecycle hooks for blocks, categories, and collections |
| Structured logging | v1.11.8 | Backend `createModuleLogger()` migration, console cleanup |
| Standards alignment audit | v1.11.8 | module.ts fixes, personal-info registration, modelNames |
| Positional drop intents | v1.11.1 | Visual before/after/inside indicators when dragging |
| Icon library support | v1.11.x | Configurable icon library with IconPlaceholder resolution |
| Property panel tab registry | v1.10.0 | Companion modules register extra tabs |
| Code generation registry | v1.10.0 | Three-hook pipeline for import/body/prop transforms |
| Iframe preview with Sucrase | v1.8.0–v1.10.x | Runtime JSX compilation, dot-grid background, responsive viewports |
| Pristine shadcn isolation | v1.9.0 | Sync components from official registry, configurable style |
| Shadcn registry proxy | v1.7.7 | Backend proxy with caching for shadcn component source |
| Bare canvas layout | v1.7.5 | Full-screen builder at `/builder/canvas` |
| Header toolbar integration | v1.7.x | Project selector, undo/redo, import, clear, save buttons |
| Config-driven code gen | v1.6.0–v1.6.7 | codePlacement, codeAlias, codeFormat — no hardcoded checks |
| Remove shared options | v1.6.0 | Shared options system fully removed |
| ComponentPanel scroll fix | v1.5.76 | Replaced Sidebar component with plain divs |
| Structure lifecycle hooks | v1.5.59 | Node deletion cleanup, metadata enrichment, card badge |
| Compliance audit | v1.5.57–v1.5.58 | Registry patterns, module isolation, schema design verified |
| Block collections | v1.5.0 | Multi-block compositions with entry block, global/system scopes |
| Structure integration | v1.5.0 | Project blocks tab, widget, registry tab, Blocks Library page |
| Admin components page | v1.4.0 | Full CRUD for native component definitions |
| Dashboard and toolbar | v1.3.0 | Dashboard widget, header toolbar save button, sidebar auto-collapse |
| Initial release | v1.0.0 | Core canvas, panels, DnD, code generation, save/load |
