---
title: Private Registries
description: Per-project shadcn registries — the slug namespace, public/private visibility, encrypted token lifecycle, and the install flow
sidebar:
  order: 2.5
---

import { Aside, Tabs, TabItem, Steps } from '@astrojs/starlight/components';

Every Block Builder project ships its **own shadcn registry**. A designer builds blocks in the canvas; an AI agent or developer then installs them with the standard shadcn CLI — turning the builder into a **design → install pipeline**. A registry can be **public** (anyone with the URL can install) or **private** (gated by a per-project bearer token).

This page is the single reference for the registry surface. Per-page tables in [Usage](/modules/builder/usage/) and [Integration](/modules/builder/integration/) link back here.

<Aside type="note">
Shipped across v1.31.10–v1.31.13 (see the [changelog](/modules/builder/changelog/)). No schema migration is required — the token and visibility live in the project node's existing `data` JSON, and the slug lock in the node type's `builtInFieldConfig`. Hosts adopt it with `npx builder update` and a restart.
</Aside>

## The namespace is the project slug

The install namespace is the **project's `node.slug`**, keyed per project:

```bash
npx shadcn add @my-project/hero-section
```

Here `@my-project` maps (via the consumer's `components.json`) to the project whose slug is `my-project`. Earlier builds keyed the namespace on the user **handle** (`@myhandle`), which collided when one user had several projects — installing from two of them registered the same alias twice. Since v1.31.11 the namespace is per-project, so no collision.

<Aside type="caution">
The project slug is your **permanent** registry namespace. The projects node type seeds (and idempotently retrofits) `builtInFieldConfig.slug.editableBy: 'system'`, so `structure` refuses slug changes in `updateNode` and renders the slug field read-only in the node editor. Renaming a live project's slug would orphan every app that has already run `npx shadcn add @old-slug/…`.
</Aside>

The **URL** is separate from the namespace and carries both the user (as a subdomain, or a `?handle=` query param) and the slug (as a path):

```
https://{handle}.usefoundryui.app/r/{project-slug}/{block-name}.json
```

## Visibility: public vs private

A project's registry is **public** by default. Toggle it on the project's **Registry** tab (an *Access* card with a public↔private `Switch`); the state is stored as `data.visibility` (`'public'` | `'private'`) on the project node.

- **Public** — `GET /r/…` serves block JSON to anyone. Responses are cacheable (`Cache-Control: public, max-age=300`).
- **Private** — every `/r` request must present a valid bearer token or it gets `401`. Responses are `Cache-Control: private, no-store` so a shared/CDN cache can't replay a token-gated response to an anonymous client.

## The token is encrypted at rest

When you make a registry private you mint a token. The token is:

- **Generated server-side** — a 256-bit `base64url` value (`crypto.randomBytes(32)`), so it drops into an `Authorization: Bearer` header and a `.env` value without escaping.
- **Encrypted at rest** — sealed with `encryptField(token, { tenantId: ownerUserId })` (the same AES-256-GCM envelope `structure` uses for its `secret` fields) and stored as `{ __sealed: '…' }` on `data.registryToken`. It is **never** stored in plaintext.
- **Encrypted-recoverable, not hashed** — so the owner can reveal and re-copy it. On `/r`, the route decrypts it with the resolved owner as `tenantId` (plain `decryptField`, no audit write on the hot path) and does a constant-time compare; a missing/undecryptable stored token, or a wrong/absent presented token, is a uniform `401`.

<Aside type="note">
The token is deliberately **not** a declared `structure` `secret` field, so it never appears in the generic node editor — the builder owns its whole lifecycle. A legacy plaintext `registrytoken` written by an earlier build is still honoured on read, but re-minting replaces it with the sealed form.
</Aside>

### Token lifecycle

All four actions live on the **Registry** tab and are ownership-guarded on the backend:

| Action | Route | Notes |
|--------|-------|-------|
| **Generate / Rotate** | `POST /api/v1/builder/projects/:nodeId/registry/token` | Returns the plaintext **once** (`no-store`). Rotating immediately invalidates the previous token. |
| **Reveal** | `GET /api/v1/builder/projects/:nodeId/registry/token` | Decrypts and returns the plaintext so the owner can re-copy it (`no-store`). |
| **Revoke** | `DELETE /api/v1/builder/projects/:nodeId/registry/token` | Removes the token. |

## Installing blocks

<Tabs>
  <TabItem label="Public registry">
    Add the registry to your consumer app's `components.json`, keyed on the project slug:

    ```json
    {
      "registries": {
        "@my-project": "https://myhandle.usefoundryui.app/r/my-project/{name}.json"
      }
    }
    ```

    Then install by name:

    ```bash
    npx shadcn@latest add @my-project/hero-section
    ```
  </TabItem>
  <TabItem label="Private registry">
    A private registry needs the bearer header, so the registry entry is the **object form**:

    ```json
    {
      "registries": {
        "@my-project": {
          "url": "https://myhandle.usefoundryui.app/r/my-project/{name}.json",
          "headers": { "Authorization": "Bearer ${FOUNDRY_REGISTRY_TOKEN}" }
        }
      }
    }
    ```

    Add the token to your consumer app's `.env.local`:

    ```bash
    FOUNDRY_REGISTRY_TOKEN=<the token you minted in the Registry tab>
    ```

    Then install as usual — the CLI reads the token from the environment:

    ```bash
    npx shadcn@latest add @my-project/hero-section
    ```
  </TabItem>
  <TabItem label="Direct URL">
    Install a single block without configuring `components.json`:

    ```bash
    npx shadcn@latest add https://myhandle.usefoundryui.app/r/my-project/hero-section.json
    ```

    <Aside type="caution">
    The direct-URL form has nowhere to attach a bearer header, so it only works for **public** registries. For a private registry, use the `components.json` object form above.
    </Aside>
  </TabItem>
</Tabs>

The **Registry** tab renders these snippets for you (with the current namespace and visibility baked in) plus copy buttons and the list of available blocks.

## How `/r` resolves a request

<Steps>

1. **User** — the handle comes from the request subdomain (`handle.usefoundryui.app`) or a `?handle=` query param (useful on localhost / hosts without wildcard subdomains). It resolves to a user through the indexed `registry_handle` table (with a lazy legacy fallback that self-heals). Unknown handle → `404 Not found`.

2. **Project** — resolved by `{ userId, slug: projectSlug, nodeType: <configured projectNodeType> }`. The node type defaults to the seeded `projects` slug but is admin-settable via the `projectNodeType` builder config. Unknown slug → the same uniform `404` (so the endpoint can't be used to enumerate which slugs exist).

3. **Visibility gate** — if `data.visibility === 'private'`, the presented bearer token is decrypted-and-compared against the stored one; failure → `401`.

4. **Serve** — `GET /r/:projectSlug/:blockName.json` returns the block's `registry-item.json`; `GET /r/:projectSlug` returns the project's block listing.

</Steps>

<Aside type="note">
A private project currently answers `401` (not `404`) for a missing/invalid token — the shadcn auth-contract signal a CLI expects — so a private project's *existence* is observable to someone who already knows the (public) handle. Only the slug leaks, never the blocks or token. Tightening this to a uniform `404`, plus `403` (valid token, no access) and `429` (rate-limit) semantics, is tracked on the [roadmap](/modules/builder/roadmap/#private-project-registry--cli-install).
</Aside>

## Backend surface

**Public registry routes** (`registry.routes.ts`, mounted at `/r`, `requiresAuth: false`):

| Method | Path | Description |
|--------|------|-------------|
| `GET` | `/r/:projectSlug` | List blocks in a project |
| `GET` | `/r/:projectSlug/:blockName.json` | A block's `registry-item.json` |

**Authenticated token/visibility routes** (`builder.routes.ts`, mounted at `/api/v1/builder`, `requiresAuth: true`, ownership-guarded):

| Method | Path | Description |
|--------|------|-------------|
| `POST` | `/projects/:nodeId/registry/token` | Mint / rotate — plaintext once |
| `GET` | `/projects/:nodeId/registry/token` | Reveal the token |
| `DELETE` | `/projects/:nodeId/registry/token` | Revoke the token |
| `PATCH` | `/projects/:nodeId/registry/visibility` | Set `{ visibility: 'public' \| 'private' }` |

**Service methods** (`builderService`):

| Method | Purpose |
|--------|---------|
| `mintRegistryToken(userId, projectId)` | Generate + seal + store; returns plaintext once (`null` if not owner) |
| `revealRegistryToken(userId, projectId)` | Decrypt + return the stored token |
| `revokeRegistryToken(userId, projectId)` | Remove the token |
| `setRegistryVisibility(userId, projectId, visibility)` | Set `data.visibility` |
| `decryptStoredRegistryToken(projectData, ownerUserId)` | Pure decrypt used by the `/r` hot path |
| `userOwnsProject(userId, projectId)` | Ownership guard shared by the block/registry routes |

<Aside type="note">
All three write methods (`mint`/`revoke`/`setRegistryVisibility`) use a single atomic `$executeRaw` `jsonb` update on the current row value (v1.31.13), so a concurrent registry mutation can't clobber another's key.
</Aside>

**Action hooks** fired after registry writes (companion modules can listen via `actionRegistry`):

- `builder.registryToken.minted` — `{ userId, projectId }`
- `builder.registryToken.revoked` — `{ userId, projectId }`
- `builder.registryVisibility.changed` — `{ userId, projectId, visibility }`

## The `components.json` snippet is extensible

The Registry tab builds its `components.json` snippet through the `builder.registry.componentsJson` [filter](/modules/builder/integration/#filters), so companion modules (e.g. the style module) can enrich it with `$schema`, `style`, `tailwind`, and `aliases`. The filter context is `{ handle, slug, registryUrl, nodeId }` — `slug` is the per-project namespace source.
