---
title: Changelog
description: Version history and release notes for the Block Builder module
sidebar:
  order: 5
---

import { Aside } from '@astrojs/starlight/components';

All notable changes to the Block Builder module are documented here.

---

## v1.5.76 — February 2026

**Current release.**

- Fix: Replace Sidebar component with plain divs for ComponentPanel scroll issues
- Fix: Properly constrain ComponentPanel scroll within nested layout
- Fix: Use SidebarContent native scrolling instead of ScrollArea

## v1.5.59

- Structure lifecycle hooks for backend integration
- Backend filter: `structure.node.response` enriches nodes with `metadata.builder.blockCount`
- Backend actions: `structure.node.deleted` cleanup, archived/restored handlers
- Frontend: `nodeCardExtensionRegistry` badge showing block count on project cards

## v1.5.58

- Audit cleanup: removed hardcoded domain URLs
- Standardised error state types to `string`
- Cleaned up `console.log` and TODO comments
- Tightened `any` types in renderer

## v1.5.57

- Full compliance audit against system documentation
- Registry patterns, module isolation, schema design, and file headers all verified
- Known issues documented in roadmap

## v1.5.5

- Fixed `block.tree` bug in registry service (should be `block.data`)
- Populated `registrations` array in module definition
- Cleaned up legacy navigation config

## v1.5.0

- Block collections: multi-block compositions with entry block designation
- Structure module integration: project blocks tab, widget, and registry tab via node registries
- Blocks Library page with grid/list views, scope filtering, and search
- Block duplicate and edit-in-place with changelog tracking
- System blocks (admin-created templates available to all users)
- Global blocks available across all projects

## v1.4.0

- Admin Components page: full CRUD for native component definitions
- Native component import endpoints (bulk array and JSON format)
- Shared options system for reusable property dropdowns

## v1.3.0

- Dashboard widget for quick access to builder
- Header toolbar "Save Block" button integration
- Auto-collapse sidebar when in builder mode

## v1.0.0

- Initial release
- Drag-and-drop canvas with `@dnd-kit/core` and `@dnd-kit/sortable`
- Component palette with containers, elements, and primitives
- Property panel with generic field types
- Block tree state management
- Code preview and generation
- Live preview with responsive device switching
- Save/load blocks via API with changelog history
- Database-driven native component registry
- Block categories for organisation
