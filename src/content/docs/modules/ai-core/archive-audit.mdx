---
title: Archive Audit
description: Per-session archive-readiness log — unfinished work, loose ends, decisions, and ideas captured before a session is archived
sidebar:
  order: 16
---

import { Aside } from '@astrojs/starlight/components';

A running log of **archive-readiness audits**. Before a working session on the AI Core module is archived, it is audited for anything that would be lost — unfinished work, loose ends, decisions, and ideas — and the findings are recorded here so a fresh session can pick them straight up.

<Aside type="note" title="Why this page exists">
Session transcripts get archived and are then awkward to search. This page is the durable, reviewable home for "what was in that session" — decisions and their reasoning, ideas raised in passing, work left mid-flight, and anything the code and the rest of these docs don't already capture. Newest entry first. Nothing here is deleted; when an item is actually done, it's ticked and left in place for provenance.
</Aside>

## How to use this page

At the end of a session you want to archive, run the archive-readiness prompt (below). It audits the session and **appends a new entry at the top** of the log, using the entry template. Then it builds and pushes the docs. Review the entry on the live site; archive the session; start the next one by reading the open items here.

### The prompt

> **Archive-readiness check.** Audit this session end-to-end for anything that would be lost if I archive it: (1) unfinished work — started-not-finished, committed-not-pushed, coded-not-validated, or left WIP (check `git status` and recent commits in every repo touched, including the `docs/` submodule — **and if this is an older session the repo has since moved past, expect HEAD, the version number, and the working tree to look _newer_ than this session's output, and its commits to sit deep in history, absent from a short `git log`/reflog window; that is normal forward progress, not loss. Confirm the work survived by identifying this session's own commits from the transcript and checking each is an ancestor of `origin/main` (`git merge-base --is-ancestor <sha> origin/main`) — reachable from origin means preserved even though HEAD is far ahead. Real loss is only a commit that is on no branch _and_ absent from `origin`; do not infer loss from "HEAD isn't at my version" or "the working tree differs from what I edited"**); (2) loose ends — TODOs, "later" items, deferred fixes, promised follow-ups, checks I said I'd run; (3) decisions & ideas — any decision, trade-off, rejected approach and *why*, or idea raised in conversation not already written in code or docs; (4) undocumented reality — anything we changed or learned the docs don't reflect. Then **append a new entry to the top of the Archive Audit log** (`docs/src/content/docs/modules/ai-core/archive-audit.mdx`) using the entry template already on that page. Build the docs, push the docs submodule **and** the submodule pointer. Verify before ticking anything "done" (if a version was bumped, confirm `dist` was rebuilt after the bump; for an older session, confirm `dist` — if gitignored — stamps the **current HEAD** version, not the session's). Finish by telling me plainly whether it's safe to archive.

### Entry template

```mdx
## YYYY-MM-DD — <session focus>

**Verdict:** ✅ Safe to archive  ·  ⚠️ Needs input first  ·  ℹ️ Superseded (work integrated, session now historical)
**Shipped this session:** <version(s), key commits, what went live>

**Unfinished / WIP:** <or "none">
**Loose ends & follow-ups:** <deferred fixes, promised checks, TODOs — or "none">
**Decisions & ideas:** <decisions + why, rejected approaches, ideas raised — or "none">
**Recorded where:** <docs pages / memory files updated, so it's findable>
**Open items for next session:** <the pick-up list — or "none">
```

---

## 2026-08-24 — Prompt structure: per-block headings/tags, objective split, lists (v1.50.0)

**Verdict:** ✅ Safe to archive.
**Shipped this session:** **v1.50.0** — the prompt-structure redesign Kim designed live (per-block heading/tag; Objective→Objective/Goals/Tasks blocks; approaches/disciplines as lists; per-item wrapping like a persona in `<role>`; a "Must remember" heading for the restated-important block; Build/Preview moved beside the Prompt heading and kept visible in both faces; System Prompt operations drop the Input block). Schema: three additive nullable columns — `ai_operation.sectionWrapping` (JSON, per-slot `{heading?,tag?}`) + `heading`+`tag` on `ai_approach` and `ai_discipline` — plus two new slot-type *values* (`goals`, `tasks`) stored in `ai_prompt_segment.slotType` (no table). Assembler: `renderSlot` (sectionWrapping override → framework fallback; key-rules "Must remember" default), `renderLabelled`, `renderListItem`, `numberedListContent`, list rendering in approach/discipline builders, and `effectiveOrder` now **weaves** undeclared canonical slots into position (was append-at-end). Objective is stored as three private per-op segments split by kind (records.ts) and recombined on load (frontend). config-io round-trips all three columns + goals/tasks slot types, no config-version bump (additive). Module: release commit `610c821` (v1.50.0) + the feature commit for the 13 template files; **`dist` rebuilt after the bump** (v1.50.0, `sectionWrapping`/objective-split confirmed baked in). Docs pushed (changelog, data-model, json-reference, prompt-assembly, configuration, roadmap Completed, this entry).

**Unfinished / WIP:** none. The change is committed, pushed, and verified.
**Loose ends & follow-ups:** (1) the **formal adversarial-review workflow did not run** — all 6 subagents hit the account weekly limit (resets Aug 26). The review was done **inline by the main loop** instead across the same six dimensions; it found + fixed one real bug (goals/tasks ordering). A belt-and-braces workflow re-run after Aug 26 is optional, not required. (2) A **per-framework in-place editor** is still the open framework-editing follow-up (per-block wrapping reduces its urgency). (3) Pre-existing roadmap items untouched (vestigial columns; the seeded guardrail `itemsPath` mismatch).
**Decisions & ideas:** (1) per-block heading/tag lives **on the operation** (`sectionWrapping`), overriding the framework, per Kim's "on operation block" call — deliberately per-operation (not per-model), trading model-adaptation for direct control; the framework stays the fallback. (2) Objective became **three separate palette blocks** (Kim's choice over one mixed list). (3) per-item heading/tag lives **on the approach/discipline record** (reusable, travels everywhere) not per-use. (4) key-rules heading defaults to **"Must remember"** in the assembler (no UI needed to satisfy Kim's ask). (5) **No config-version bump** — the new fields are additive+optional on existing entities, so old exports still import and a re-export carries the new fields. (6) `effectiveOrder` weaving was added because every seeded framework lists the full slot set explicitly, so a newly-added slot would otherwise orphan at the tail.
**Recorded where:** `changelog.mdx` (v1.50.0), `data-model.mdx` (columns + slot values), `json-reference.mdx` (Approach/Discipline `heading`+`tag`, Operation `sectionWrapping`, segment `goals`/`tasks`), `prompt-assembly.mdx` (per-block heading/tag subsection, list rendering, weaving), `configuration.mdx` (Operations composer row), `roadmap.mdx` (Completed entry). Memory: topic file `ai-core-block-builder-v124.md` + `MEMORY.md` index updated.
**Verification:** backend materialised **linked-check** clean (0 errors); frontend **type-consistency check** clean in changed files (residuals are known react-stub harness artifacts) + all 3 tabs esbuild-parse; repo gates green (build/type-check/check:templates/conformance/parity); a unit-test of `effectiveOrder` confirmed goals/tasks land right after objective on all seeded frameworks; docs build clean (0 broken links).
**Open items for next session:** a ready-to-paste **handover prompt** was produced in chat this session (Phase 2 of the handover) — grab it before archiving. **Kim's action:** run `npx ai-core update` in veradai to pull v1.50.0 (it carries the additive schema migration for the three new columns; goals/tasks are slot-type values, no separate migration). Then the operation editor shows per-block Heading/Tag fields, the Objective/Goals/Tasks blocks, and the moved Build/Preview.

## 2026-08-23 (evening, cont.) — Import operations unbound, bind later (v1.49.2)

**Verdict:** ✅ Safe to archive.
**Shipped this session:** **v1.49.2** — an operation may now be **unbound while disabled**. Prompted by a live veradai issue: Kim's `default-system-prompt` disciplines failed `guardrail-scorer-missing` validation because their model-scored guardrails named scorer operations that did not exist — and those scorer ops had been **skipped** on config import for having *no model binding in the config*. Root cause diagnosed read-only against veradai's DB (7 scorer ops missing; all 7 skipped on import). Fix (Kim's choice — "add it later"): `upsertOperation` now permits an unbound operation only while disabled (`willBeEnabled = input.enabled ?? existingBinding?.enabled ?? true`; enabling an unbound op is refused; explicit clear-to-unbound allowed only while disabled); `config-io` `importOperations` creates a no-binding operation **disabled + named** (`createdUnbound`) instead of skipping; the admin editor opens an unbound op in "by purpose" mode and the list enable-toggle is held (disabled + title hint) until bound. The invariant preserved is "every **enabled** operation binds exactly one model/purpose"; the run resolver still refuses an unbound op (`operation-has-no-model-binding`) as a backstop, and `validateDisciplineSet` still treats an unbound (disabled) scorer as missing — so a discipline can't pass prematurely.

**Unfinished / WIP:** none.
**Loose ends & follow-ups:** Kim to add a scorer model purpose in veradai, re-import (the 7 scorers land disabled), bind + enable each, then the default-system-prompt validation passes. Pre-existing roadmap items unchanged.
**Decisions & ideas:** chose the "import unbound → create disabled, bind later" path over requiring a binding in the config (Kim's call). Rejected relaxing the invariant for *enabled* ops (an enabled unbound op can't run). No schema change (modelId + modelPurposeId already nullable) → **patch** (v1.49.2).
**Recorded where:** `changelog.mdx` (v1.49.2), `json-reference.mdx` ("Operations bind a model on import" aside updated — unbound imports disabled, not skipped). Verified: all repo gates + materialised linked-check clean; a focused 3-dimension adversarial review (invariant soundness / import path / downstream safety) found **0** issues; frontend OperationsTab esbuild-parses.
**Open items for next session:** none. veradai still needs `npx ai-core update` to reach v1.49.2 (no migration in .1 or .2; the v1.49.0 `ai_prompt_variable` migration + seed-39 ride along).

---

## 2026-08-23 (evening) — Config-io round-trip for the purpose & variable catalogues (v1.49.1)

**Verdict:** ✅ Safe to archive.
**Shipped this session:** **v1.49.1** — the two open config-io follow-ups, closed together. `config-io.service.ts` gained export + import + delete-absent passes for the `ai_model_purpose` / `ai_model_purpose_default` catalogue (a `modelPurposes` entity) and the `ai_prompt_variable` catalogue (a `promptVariables` entity); config format bumped **5.0 → 6.0** (a clean-break version gate). Purposes travel **model-agnostically** — the default model rides as a portable `providerId` + `modelId` pair, resolved best-effort on create (named-as-gap when absent), left untouched on update (the admin owns the local mapping); `kind` is immutable (a named skip on change). Prompt variables travel verbatim; the replace-mode delete guard scans the four substitution sources (segment content + approach/discipline/guardrail promptText) so a variable a `{token}` still uses is a named skip. Also added the missing `guardrailAttachments` / `contexts` entries to the import validation schema (they had been accepted only by pass-through) and a `guardrailAttachments` export count. Frontend: `AiCoreConfigDto` + ConfigTab copy/placeholder updated (no logic). Module repo: release `412bd73` (v1.49.1) after the feature commit; **`dist` rebuilt after the bump** (config version 6.0 confirmed baked in). Docs pushed.

**Unfinished / WIP:** none.
**Loose ends & follow-ups:** none new. Pre-existing roadmap items untouched — drop the vestigial `ai_operation` / `ai_guardrail` columns (blocked by immutable seeds); let Context-builder retrieval blocks bind an embedding purpose; the seeded `guardrail-source-verification` `itemsPath` mismatch.
**Decisions & ideas:** (1) **Patch, not minor** — a new feature with no schema change (all three tables already existed) is a patch per the LLM rules; the config-format break is internal to config-io, not a module-level breaking change. (2) **Purpose default model = portable ref, applied best-effort on create, preserved on update** — chosen to mirror BOTH the retrieval-embedding-model portability rule AND the operations-pass "admin's binding is never overwritten by config" rule. (3) **A replace-mode usage guard for prompt variables** — added (scanning the four text sources) because a variable is referenced only textually, so ordering can't protect it; a dangling `{token}` would leak literally into the prompt, exactly the silent-dangle the marker guard prevents. (4) Fixed the pre-existing **v5.0 drift** on the json-reference page (guardrails still shown with `disciplineId`, no `guardrailAttachments`) since the page is now stamped 6.0.
**Recorded where:** `json-reference.mdx` (major update — version 6.0; new Prompt variable / Model purpose / Guardrail attachment entities; standalone-guardrail fix; dependency + delete order; what-the-config-does-not-carry), `changelog.mdx` (v1.49.1), `roadmap.mdx` (both items retired to done), `data-model.mdx` (config version 6.0 + round-trip note). Verified via a five-dimension adversarial review (8 agents: 5 finders + 3 verify) — one low-severity finding (a partial-merge purpose-description clobber) fixed + re-verified; two of the five dimensions (portability, replace-delete) came back clean. All repo gates green (build/type-check/check:templates/conformance/parity) plus a materialised **linked-check** (real record-service signatures; the three touched Prisma models typed precisely) clean, and the docs build clean (0 broken links).
**Open items for next session:** none pressing. **Kim's action:** veradai is on v1.48.0 — run `npx ai-core update` to reach **v1.49.1** (it carries the v1.49.0 `ai_prompt_variable` migration + seed-39; v1.49.1 itself adds no migration). A config exported before v1.49.1 (version ≤ 5.0) is refused by the 6.0 importer — re-export after updating.

---

## 2026-08-23 (pm) — Prompt variables (`{token}` substitution in any segment) (v1.49.0)

**Verdict:** ✅ Safe to archive.
**Shipped this session:** **v1.49.0** — a generic prompt-variable system Kim designed live (AnythingLLM's system-prompt-variables as the spark, generalised to *any segment* and wired to real sources). New **`ai_prompt_variable`** catalogue (name/description/injectionRisk/onMissing+fallback/source/sourceRef); substitution in the assembler (`substitutePromptVariables`, single-brace, **declared-tokens-only** so a JSON format segment is untouched); wired into `buildPrompt` over every authored segment (segment content + approach/discipline/**guardrail** promptText), with a resolved-value map (built-in `{date}`/`{time}` from `now`; `{communication_style}` from `formatCommunicationStyle(preferences)`; everything else from `RunOptions.variables`, a caller value never overriding a built-in). Per-variable **injectionRisk** = sanitise (`sanitiseRiskyContent`) + a self-describing per-call nonce fence; per-variable **onMissing** = fallback|skip; skipped/fallback/fenced tokens named in run feedback + preview notes. `ensurePromptVariable` for seeding; seed-39 seeds the built-ins. **Optional field source**: a `PromptVariableFieldSource` service (interface + `/admin/variable-field-sources` + `/admin/variable-fields`, resolved via `serviceRegistry.getOptional('promptVariableFields')` — never importing Structure) + a searchable field picker in the new **Variables** admin tab (registered in admin-page + module.ts + index barrel; also added `ai_discipline_guardrail` + `ai_prompt_variable` to `schema.modelNames`, the former missing since v1.48.0). Module repo feat `4229617` on release `4acd950`; docs pushed. `dist` rebuilt after the bump. Backend AND frontend type-checked **clean** (0 ai-core errors) against veradai's real Prisma client (schema patched with the model via the Edit tool) and React/TanStack; veradai byte-restored (dir + the now-dirty schema, both via cp — Kim had updated veradai to v1.48.0 mid-session, so the schema is his install and must not be git-checkout'd).

**Unfinished / WIP:** none.

**Loose ends & follow-ups:**
- **Deferred (roadmap Short-Term):** config-io does not yet carry the `ai_prompt_variable` catalogue (add a `promptVariables` entity + bump the config version — additive, standalone).
- The **field search** only lights up once a host registers a `promptVariableFields` provider; a reference adapter (mapping Structure's `getMergeFieldSchema`/`MergeFieldToken`) is documented in `integration.mdx`. Nothing to wire in AI Core.
- Earlier-arc items still open (unchanged): drop the vestigial `ai_operation` context columns + `ai_guardrail.disciplineId`; the seeded `guardrail-source-verification` itemsPath; the model-purposes config-io catalogue.
- **Docs gap (Kim asked at handover):** the `assertion-rules` guardrail (v1.48.0) is documented as a **config shape** in `disciplines-and-guardrails.mdx` (the `rules` model, conditions, a worked JSON example, feedback naming) — but there is no step-by-step **admin-UI walkthrough** (Disciplines → Guardrails → "Custom rules on the output" → build rules with the rule-builder). A short `usage.mdx` walkthrough would round it out. Not blocking; the mechanics are covered.

**Decisions & ideas:**
- **The Structure boundary (Kim's "structure already has merge fields so?").** AI Core stays a generic `{token}` substitutor and NEVER resolves Structure fields (or imports Structure) — the consumer resolves its merge fields and passes the values in `variables`; single-brace syntax matches so a resolved value drops straight in. The field *search* is an authoring aid via an optional host-registered provider; resolution stays consumer-side (AI Core has no node at run time). Preserves `depends: []`.
- **"injectionRisk", not "trust" (Kim's correction).** Per-variable `injectionRisk` (AI Core's existing term) — dropped the trusted/untrusted framing; a short value is plain, a long Structure field can be flagged and gets sanitise + fence.
- **Per-variable onMissing = fallback|skip (Kim's choice)** — admin-configured, not a global "report". The honest report still rides along (feedback/preview) but is subordinate.
- **App-registered check functions considered + NOT built** (a prior-turn tangent) — even KB resolution goes via Structure, not the consumer, so the generic engine lives in AI Core.
- **Rule form always emits allOf/anyOf** — n/a here; that was v1.48.0.
- **Adversarial review (5 dims→verify, 12 agents): 4 of 7 confirmed, all fixed.** (medium) `variableReport` computed but never consumed → now folded into run feedback (`prompt-variable-unresolved/-fallback/-fenced`) + preview notes; (medium) a `{token}` in a guardrail's promptText was NOT substituted (built before `applyVariables`) → now substituted; (low) version over-bumped on any `sourceRef` presence → compare by value; (low) `pickField` re-armed injectionRisk on a re-pick, discarding the admin's OFF → only suggest on the first bind. Refuted/dropped: 3 (a ReDoS claim, etc. — not confirmed).

**Recorded where:** this entry; `changelog` (v1.49.0), `data-model` (28 tables, the `ai_prompt_variable` table + counts), `integration` (the Prompt Variables section + the field-source registration + reference adapter), `roadmap` (Completed + the config-io Short-Term item); memory `ai-core-block-builder-v124.md` + `MEMORY.md`.

**Open items for next session:** none pressing. config-io for variables, the assertion-rules admin-UI walkthrough, and the deferred earlier-arc items if wanted. **A full handover prompt for the next session is in the session chat** (from the session-handover skill). Gotchas re-confirmed this session: (a) **the backtick trap struck again** — a literal backtick around a word in a template-file COMMENT (`` `vars` `` in assembler.ts) closed the outer template string; `check:templates` misses it, only the build/tsc catches it (checked BUILD_EXIT, caught immediately). (b) Patch veradai's `schema.prisma` with the **Edit tool**, never a shell `node -e` (em-dash mangling — hit last session). (c) When re-verifying the frontend after a fix, materialise the WHOLE changed set, not just the one edited file — a lone tab file referencing new hooks/types/services throws phantom errors otherwise.

---

## 2026-08-23 — Assertion-rules + reusable (many-to-many) guardrails (v1.48.0)

**Verdict:** ✅ Safe to archive.
**Shipped this session:** **v1.48.0** — two guardrail capabilities Kim asked for. (1) **`assertion-rules`** — a generic deterministic built-in whose test is admin-authored: a `rules` array over the operation output, each rule `{ forEach?, when?, clause | anyOf | allOf }` with conditions (`exists`, `one-of`, `matches`, `gte`/`lte`/`between`, `length-between`, `unique`, `is-type`, `appears-in-source`, …); failing/misconfigured rules name themselves, no model call (`run/run.ts` executor + dispatch). (2) **Reusable guardrails** — `ai_guardrail` became standalone (`disciplineId` nullable + DEPRECATED) and attaches to many disciplines through a new **`ai_discipline_guardrail`** join; admin creates a guardrail on its own and attaches/detaches from the discipline editor, and the Guardrails list shows which disciplines each measures. seed-38 folds existing `disciplineId` into the join (idempotent, fresh-install-safe). config-io **v5.0** (guardrails standalone + a `guardrailAttachments` entity). Touched: `schema.ts`, backend `run`/`resolver`/`disciplines`/`controller`/`routes`/`validation`/`config-io`/`service`/`types`, `migrations`, frontend `types`/`hooks`/`services`/`tab-disciplines`. Module repo feat `e909d69` on release `336500e`; docs pushed. `dist` rebuilt after the bump and re-confirmed to carry the review fixes. Backend and frontend both type-checked **clean** (0 ai-core errors) against veradai's real Prisma client (schema patched with the join model, `prisma generate`) and React/TanStack toolchain; veradai byte-restored after each check (schema via `git checkout`, files via a `cp` backup — never checkout on the dirty ai-core files).

**Unfinished / WIP:** none.

**Loose ends & follow-ups:**
- Roadmap "drop the vestigial columns" item now also lists **`ai_guardrail.disciplineId`** (blocked by immutable seed-16, same schema-before-seed constraint as the context columns).
- Deferred, by design (told to Kim in the plan): the **`resolves-in-store`** assertion operator (a Structure retrieval inside a guardrail — Kim's own "retrieve step for the guardrail" idea) and a **coverage-join** operator. Neither is app code; both slot into the assertion engine later.
- The seeded **`guardrail-source-verification`** still ships `itemsPath: "items"` (the long-standing "extraction guardrail measures nothing" roadmap item) — untouched this session; assertion-rules now gives a general alternative but the specific seed is unchanged.
- config-io does not carry the model-purposes catalogue (pre-existing roadmap item, untouched).

**Decisions & ideas:**
- **Why the check isn't an app-registered function.** Kim asked "why would the check functions be in the app?" — answer: they wouldn't, except a check needing app-specific data ai-core can't see. All of Kim's example disciplines (provenance/register/confidence tags, unsourced-marker, consequential-step-gate, citation formats) are structural, so the generic engine lives **in ai-core**; even the one external case (KB resolution) goes through **Structure**, not the consumer. App-registered check functions were considered and **not built** (no real case yet).
- **Cardinality = many-to-many** (Kim chose "reusable across many disciplines" over "standalone, attach to one"). `ResolvedCheck.disciplineId` was dropped (never read in execution); the resolver runs the deduped set; a guardrail's promptText surfaces under every attaching discipline.
- **Additive schema** (mirrors v1.47.0): kept `disciplineId` nullable+vestigial rather than dropping it, to avoid the schema-before-seed data-strand trap.
- **Rule form model:** the editor always emits `allOf`/`anyOf` (never a bare `clause`), with an all/any toggle — simpler UI, full backend expressiveness (one level of grouping; nested groups not offered).
- **Adversarial review workflow (5 dims → verify) confirmed 5 of 7**, all fixed. **Refuted (correctly):** a seed-38 re-run resurrecting deleted attachments (Prisma runs a migration once), and a replace-mode strand as a "bug" (it is the documented deliberate replace-mode behaviour). Fixes: empty `allOf/anyOf` → misconfigured; malformed/null clause → clean false via a `clauseObject` guard (not a throw); `unique` keys type-tagged; `detachGuardrail` warns only when a row was actually removed; the *only-when* guard writes on `hasWhen` alone so a blank field (= the item itself) is not dropped.

**Recorded where:** this entry; `changelog` (v1.48.0), `data-model` (27 tables, the two guardrail tables, config-io v5.0), `disciplines-and-guardrails` (assertion-rules section + reusable-guardrail model + the ensure/attach example), `roadmap` (Completed + the vestigial-column item); memory `ai-core-block-builder-v124.md` + `MEMORY.md`.

**Open items for next session:** none pressing. The deferred `resolves-in-store` / coverage-join operators and the seeded-`source-verification` itemsPath fix are there if wanted. Process gotcha re-learned this session: **patching veradai's `schema.prisma` from a bash `node -e` string with an em-dash in the anchor silently fails to match** (the em-dash gets mangled in transit) → Prisma generates without the new model → a wave of false "table does not exist" tsc errors. Use the **Edit tool** for the schema patch (as the memory already says for perl), not a shell heredoc/`node -e`.

---

## 2026-08-22 (pm/2) — Operations Owner/Name split + veradai confirmed healthy (v1.47.3)

**Verdict:** ✅ Safe to archive.
**Shipped this session:** **v1.47.3** — the **Operations editor gained the same Owner (optional) + Name split** as the Context editor. `OperationForm.id` became `owner` + `name`, composed to `owner/name` on create (validated client-side against the shared namespaced-kebab rule, with a suggested slug on a bad id) and split back apart on edit; the id stays **immutable** once created (consumers call the job by it) and the operations list still shows the full `owner/name`. Frontend template-only — the backend already accepted `owner/name` operation ids. Also **merged the scaffold to v1.31.17** (infra-only: `scaffold/`, `scripts/check-conformance`+`check-parity`, `init`/`update`; no change to module-owned templates or `module.ts`). Module repo feat `64ad52f` on top of release `3a45eb3` (v1.47.3) and scaffold merge `8303fa3`; docs pushed. `dist` rebuilt after the bump. The generated `OperationsTab.tsx` type-checked **clean** against veradai's whole frontend project (`tsc -p tsconfig.json --noEmit`, 0 errors) with the change materialised in, then the host file was restored byte-for-byte (identical SHA).

**Unfinished / WIP:** none. Module tree clean after the docs-pointer commit; docs submodule pushed.

**Loose ends & follow-ups:**
- **✅ RESOLVED — veradai is healthy again.** Last session's frontend/backend mismatch is gone: Kim ran `npx ai-core update`, and both `.module-state.json` and `.module-manifests.json` report **1.47.2** (the git tree shows the update landed — modified validation/tabs/service/types plus the `20260822102226_update_ai_core` migration). Confirmed the live namespaced-id contract accepts `veradai/what-veradai-is` for a **context** *and* an **approach**, and `veradai/answer-from-context` for an **operation** id, via `safeParse` against veradai's real generated `ai-core.validation.ts` (junk ids like `"What Veradai Is"` correctly rejected).
- The live "create `veradai/what-veradai-is` in the Context tab" remains a UI click for Kim in the running admin — the wire contract is proven, so I did not write to veradai's live database to demonstrate it.
- Roadmap items untouched this session (still open): drop the vestigial `ai_operation` context columns (needs seeds 27/28 retired first); the extraction guardrail measures nothing (`itemsPath`/`valueKey` mismatch); config-io does not carry the model-purposes catalogue.

**Decisions & ideas:**
- **Owner is optional; a bare `name` stays a valid id**, so existing un-namespaced operations (`extract-entities`) are unaffected and load with an empty Owner field.
- **Immutable on edit, no rename path** — both fields are `disabled` on edit and `updateOperation` sends `editingId`, never the composed value; there is no way to rewrite a live consumer's job name through the editor.
- **No workflow/agent review for this change** — it is a UI mirror of an already-shipped, verified pattern and it passed the real host compiler; re-reading a ~40-line diff with subagents would have added noise, not confidence. The proof was the host `tsc`, not a re-read.
- **Verification technique refined** — rather than `git checkout` to restore veradai's file (last session's mistake, which had wiped uncommitted host work), the file was backed up with `cp`, overwritten, `tsc`'d, then restored from the copy; the before/after SHA matched, so the host tree was never git-touched.

**Recorded where:** this entry; `changelog` (v1.47.3 entry, `lastUpdated` → 2026-08-22); memory `ai-core-block-builder-v124.md` (SESSION PICK-UP block) + `MEMORY.md` index. Roadmap left unchanged (no open roadmap item covered this UI-consistency patch; the Completed section stays at milestone granularity).

**Open items for next session:** none pressing. Optional roadmap items above if wanted. Process gotchas still stand: (a) the Bash shell cwd **persists** across a compound command — `pwd`/cd back to the module root before `version:*` / `build` / the parent docs-pointer commit; (b) never `git checkout` to "restore" host files without first checking the host's `git status` — use a `cp` backup or `git stash`.

---

## 2026-08-22 (pm) — context becomes its own table, then namespaced ids (v1.46.2–v1.47.2)

**Verdict:** ✅ Safe to archive — **but veradai (the live host) needs one command to become usable again; see Loose ends.**
**Shipped this session:** **v1.46.2** — the "add context = 400" fix (the Context builder id must be kebab-case, but the editor only checked non-empty and was the one admin tab showing the bare axios message instead of the server's reason; added `apiErrorMessage` + client-side id validation). **v1.47.0** — **context is now its own `ai_context` table**, carrying a **kind** that places it in the prompt: a **foundation** context is the app's trusted authored framing (Text + Shared-segment blocks only), rendered plain at the **top** (system-prompt region); a **background** context is reference/retrieved material folded into the mid-prompt context slot (the existing retrieval/budget engine). An operation binds up to one of each via `ai_operation.foundationContextId` / `backgroundContextId`. Schema is **additive** (the old `builderRole`/`contextBlocks`/`contextOperationId` columns are kept, unused, because immutable seeds 27/28/32 reference them on a fresh install); a `1.47.0__seed-37` migration moves existing builders in as *background* contexts, repoints the operations and removes the builder rows. New `/admin/contexts` endpoints + `records.service` CRUD; `resolver`/`run` load contexts from the new table (foundation prepended to instructions, background assembled into input); config-io v4.0 carries contexts as their own entity. **v1.47.1** — context ids may be namespaced `owner/name`; the Context editor gained an **Owner (optional) + Name** pair that composes into the id. **v1.47.2** — made the namespace **systemic**: every admin-created record id/key (approaches, segments, markers, input-types, tools, purposes, flows, eval cases — not just contexts/operations) now accepts the optional `owner/name`. Module repo `main = f4b070d`; docs submodule `main = 5f78214`. All pushed; `dist` rebuilt after each bump (dist mtime newer than package.json). Generated code type-checked **clean** against veradai's real React/TanStack (frontend) and a Prisma client regenerated from the new schema (backend); an adversarial logic review ran and its 8 confirmed findings were all fixed.

**Unfinished / WIP:** none. Both working trees clean; 0 unpushed commits in the module repo and the docs submodule; every session commit is on `origin/main`.

**Loose ends & follow-ups:**
- **⚠️ veradai is temporarily broken and Kim must fix it — my mistake.** Kim had run `npx ai-core update` (v1.47.0) so veradai's ai-core files were modified-uncommitted; while type-checking against veradai I `git checkout`-reverted its **frontend** back to v1.46.1, leaving veradai with a **v1.47.0 backend + v1.46.1 frontend mismatch** (its admin won't behave until they match). **Fix: run `npx ai-core update` in `C:\Users\kimbe\ReactApps\veradai`** — it pulls v1.47.0/.1/.2 in one go, re-syncs the frontend, and adds **no** new DB migration (.1/.2 are template-only). Kim has been told, in plain language, in chat.
- **Offered, awaiting Kim's yes:** give the **Operations** editor the same Owner/Name two-field split as the Context editor (the operation id field already accepts a typed slash — this is UI consistency only).
- No promised check left unrun — build/type-check/validation/review all completed this session.

**Decisions & ideas:**
- **"context" (ai-core) = "background info/data"** in the 10-step prompt guides — the same concept, confirmed with Kim; the Context builder is the tool that fills that section.
- **Two context kinds, named `foundation` + `background`** (Kim renamed "primary" → "foundation"). Foundation lands at the top of the prompt; background in the context slot. Kim chose to **fold this into the same change** rather than a follow-up.
- **A foundation context holds authored blocks only** (Text + Shared segment) — my implementation decision, flagged to Kim, so it renders as plain trusted framing without fencing/budget machinery. Reversible later if Kim wants retrievals in a foundation.
- **Communication style stays an approach, not a context** — it is *supplied* by the app (merge-field-like), not retrieved; it would only become a "communicate context" if ai-core had to fetch it. "Who the AI is" stays the `identity-anchor` **approach**; a foundation context stacks on top of it.
- **Namespaced ids made systemic** (v1.47.2) rather than per-tab — Kim hit the "id can't have a slash" wall three times, so the fix widened the shared id rule for every record instead of patching one editor.
- **Rejected (adversarial review):** the claim that migrating a builder dropped a load-bearing `contextMarker` — refuted (it was not load-bearing in the old engine). **Left as pre-existing (not a regression):** caller memory/session dropped-unnamed when a bound background context has no memory/session block (already a roadmap item).
- **Kept the vestigial `ai_operation` context columns** rather than dropping them — the schema step runs before the seed step, so a drop-from-schema would strand data and a drop-in-seed would drift; a Short-Term roadmap item now tracks removing them in a later coordinated release.

**Recorded where:** memory `ai-core-block-builder-v124.md` (rewritten "SESSION PICK-UP (2026-08-22 pm)" block with the full build shape, the two process mistakes, the verification technique, and an OPEN/NEXT list) + `MEMORY.md` index; docs updated across `changelog`, `data-model`, `architecture`, `configuration`, `glossary`, `index`, `integration`, `json-reference`, `prompt-assembly`, `prompt-model`, `service-api`, `usage`, and `roadmap` (new Short-Term "drop the vestigial context columns" item).

**Open items for next session:** (1) confirm veradai is healthy after Kim's `npx ai-core update`; (2) if Kim says yes, mirror the Owner/Name split into the Operations editor (`tab-operations.ts` — `OperationForm.id` → owner+name, compose on save, immutable on edit, following `splitContextId`/the owner+name pattern in `tab-context.ts`); (3) roadmap items — drop the vestigial context columns (needs seeds 27/28 retired), the extraction guardrail measuring nothing, config-io not carrying the model-purposes catalogue. **Two process gotchas to carry forward:** (a) the Bash shell cwd **persists** across a compound command — always `pwd`/cd back to the module root before `version:patch`/`build`/the parent docs-pointer commit; (b) **never `git checkout` to "restore" host files without first checking the host's `git status`** — if they were already dirty (a live `npx ... update`), you will wipe the host's real work; use a temp copy or `git stash` instead. The full handover prompt is in the session chat.

## 2026-08-22 — model purposes → models.dev catalogue → context-builder fixes (v1.45.0–v1.46.1)

**Verdict:** ✅ Safe to archive
**Shipped this session:** **v1.45.0** model purposes (an operation binds a semantic *purpose* — analysis/chat/titles/summary/vision/embedding/rerank — resolved to a model at run time, or a pinned model; two new tables `ai_model_purpose` + `ai_model_purpose_default`; embedding purposes carry per-width defaults; the op's `requiredDimensions` is authoritative on emit) · **v1.45.1** Models admin tab split into sub-tabs (Models / Prompt frameworks / Model purposes) · **v1.46.0** full models.dev catalogue + pricing (`ai_model.catalogue` read-only + `ai_model.pricing` USD/1M, fetched & hand-editable; Models table gained Context/Cost/Modalities/Capabilities/Cutoff columns + a catalogue detail view) · **v1.46.1** Context builders bind **no model** (fixed a "Create context builder" 400) + a **"None (unmarked)"** marker option on authored text/segment context blocks. Module repo `main = 3890f0d`; docs submodule `main = 84be6de`. All pushed; `dist` rebuilt after each version bump. **Milestone: the module is now INSTALLED and RUNNING LIVE in the veradai host** — the long-standing "never installed in a real host" gate is resolved; Kim is using the admin and surfacing issues through real use (the v1.46.1 400 was found that way).

**Unfinished / WIP:** none. Both working trees clean; 0 unpushed commits in the module repo and the docs submodule; each session commit is on `origin/main`.

**Loose ends & follow-ups:** none pending on our side. Kim must run `npx ai-core update` in veradai to pull v1.46.1 (the context-builder fix) — a host action, not a module task.

**Decisions & ideas:**
- **Naming = "purpose"** (not type/role/kind — `kind` stays reserved for the technical modality). Kim-confirmed; the old deleted-v1.1.0 "purpose keys" were a different (job-identity) layer, so the word was free.
- **Embedding purposes use per-width defaults** (Kim's pick over a single default); an op's declared vector width is authoritative on emit so a wrong-width vector can't silently corrupt a store.
- **Shipped operations stay PINNED** (no retro-bind to purposes) — avoids coupling the built-in judges to a general "analysis" purpose.
- **Cost is USD per 1M tokens** (one consistent unit so models compare); fetched from models.dev **and** hand-editable so Voyage (not on models.dev) can be priced by hand; a refresh never clobbers a hand-entered price for an un-listed model.
- **Context builders bind no model** (not an inert placeholder) — matches Kim: "context doesn't have models unless it's retrieving." Its retrievals each name their own embedding model.
- **"None (unmarked)"** is offered only on authored (text/segment) blocks; retrieval/memory/session stay marked so ingested content is still injection-fenced.
- **DROPPED (Kim's call — do not pursue):** a model-rankings integration (models.dev is a spec catalogue only — no rankings; would be a separate source such as Artificial Analysis / LMArena) and the model-picker "enable-on-pick" idea.
- **Provider API keys are env-only** — `reconfigure`/`update` only re-prompt a *newly-declared* env var, so there is no CLI path to add a key after install (edit the host backend `.env`). Logged as an upstream scaffold enhancement.

**Recorded where:** memory `ai-core-block-builder-v124.md` (dense frontmatter + a "SESSION PICK-UP (2026-08-22)" block at the top with the host-validation tooling and gotchas) and the `MEMORY.md` index; docs updated across `changelog`, `data-model`, `configuration`, `providers-and-adapters`, `prompt-model`, `registering-and-seeding`, `service-api`, `glossary`, `hooks-and-events`, `roadmap`; upstream issue in `module-starter/upstream-issues.mdx`.

**Open items for next session:** no pressing task — respond to Kim's next request (live-use feedback). Roadmap items if wanted: config-io does not yet export/import the model-purposes **catalogue** (an op bound to a custom purpose skips+named on import into a host lacking it; built-ins always exist); the extraction guardrail measures nothing (itemsPath/valueKey mismatch). **Gotcha to carry forward:** a literal backtick in a template-file **comment** breaks the build and `check:templates` misses it (only `tsc`/`tsup` catch it) — hit 3× this session; always check the build exit code and never chain a harness after a silenced build. The full handover prompt is in the session chat.
