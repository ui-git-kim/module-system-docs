---
title: Roadmap
description: Planned features, known issues, and future direction for AI Core
lastUpdated: 2026-08-18
sidebar:
  order: 15
---

import { Aside } from '@astrojs/starlight/components';

This page outlines planned features, known issues, and improvements for the AI Core module. Items are grouped by priority.

<Aside type="note" title="First live call made — v1.7.0">
The redesign was implemented as v1.1.0 (2026-07-23) and has been extended through v1.23.0. It installs into a real app, type-checks clean, its admin page renders and serves records, and — as of v1.7.0 (2026-08-01) — `run()`, `embed()` and `rerank()` have all executed end to end against live providers (Anthropic Claude Opus 4.6, Voyage). Remaining and unbuilt work is tracked on this page.
</Aside>

<Aside type="note">
This roadmap reflects current plans and may change based on feedback and requirements from other modules. Each item includes enough context for a future development session to pick it up without additional explanation.
</Aside>

---

## Known Issues

The six issues recorded here from the July 2026 alignment audit all lived in the **legacy template surface**, and all were **resolved by deletion** — v1.1.0 replaced that surface wholesale (see the [Changelog](/modules/ai-core/changelog/)). These are the current known issues.

### Documentation Accuracy — August 2026 audit

**Resolved:** 2026-08-09 — every reference page below was corrected in the documentation pass (per-page content + MDX verified; the Starlight site build has not been re-run). The record of what was wrong is kept here.

A full docs-vs-code audit (every `/modules/ai-core/` reference page cross-checked against `src/templates/` and `src/module.ts` at v1.23.0) found the **reference pages have drifted behind three shipped reworks** — **v1.22.0 Prompt Model v2** (discipline `tier`/`authority` hierarchy removed; `ai_guardrail_check` → `ai_guardrail`; everything-is-a-segment) and **v1.23.0** (presets removed; Approaches tab; per-operation approaches/disciplines slots). The [Changelog](/modules/ai-core/changelog/) and this Roadmap are current; the reference pages are not. The v1.23.0 "Doc-body sweep pending" note named only the preset drift on seven pages — the audit found the drift is wider and spans more pages.

**❌ Incorrect (describes removed features as present, or breaks copy-paste):**

- **Presets described as current** on `architecture`, `data-model`, `json-reference`, `configuration`, `glossary`, `service-api`, `usage`, `integration`, `registering-and-seeding`, `hooks-and-events` — the dead `ai_preset` table, the dead `ai_operation.presetId` column, a Presets admin tab, a `preset-missing` refusal reason, an `ai-core.preset.updated` hook, and a `presets`/`presetName` config-JSON surface the importer now silently ignores (a data-loss trap in `json-reference`). Removed in v1.23.0 (`resolver.ts` comment "presets were removed"; zero `ai_preset`/`presetId` matches in `src/`).
- **Discipline `tier` / `authority` hierarchy described as current** on `architecture`, `usage`, `configuration`, `glossary`, `service-api`, `developer-workflow` — the six-field discipline envelope, the "Tiers"/"Authority is structural" sections, the `hard`-rule-with-null-`check` enablement refusal. Removed in v1.22.0 (`schema.ts`: "No tier, no authority hierarchy — those were removed"). Surviving invariant: `validateDisciplineSet` refuses `unknown-discipline` / `discipline-without-guardrail` / `guardrail-scorer-missing`.
- **Dead / wrong API surface:** `ensureSegment` documented with a `type` field + `voice|approach|…` enum — real field is `slotType`, values `approaches|disciplines|objective|context|examples|format|edge-cases`, so the shown example will not compile (`service-api`, `registering-and-seeding`); `ensureOperation` documented with `presetId` / `disciplineIds` (neither exists on the spec); provider-adapter factories named `createAnthropic`/`createOpenAI`/… on `providers-and-adapters` (real exports: `createAnthropicAdapter` / `createOpenAiAdapter` / `createGoogleAdapter` / `createVoyageAdapter`); `ai_guardrail_check` listed as a current table on `architecture`; guardrail CRUD hook documented with `entity: 'check'` (real value `'guardrail'`) on `hooks-and-events`; `index` "The Shape" says an operation "selects disciplines and guardrails" (an operation never references a guardrail).

**⚠️ Outdated:**

- **Every reference page's self-version stamp is behind v1.23.0** — `index`/`providers-and-adapters`/`registering-and-seeding`/`hooks-and-events`/`developer-workflow` say v1.15.0; `architecture`/`configuration`/`usage`/`service-api`/`integration` say v1.18.0; `data-model`/`prompt-model`/`disciplines-and-guardrails`/`prompt-assembly` say v1.22.0.
- **Wrong table counts:** `data-model` ("twenty tables" / "Config tables (16)" / "Eleven of the sixteen … seeded"), `architecture` ("16 … tables" / "sixteenth table"), `developer-workflow` ("sixteen `ai_` tables"). Actual: **19** tables (15 config, 10 carrying `seeded`).
- **Stale vocabulary / omitted fields:** `voice`/`method`/`schema-instruction` slot types on `usage`/`architecture`/`glossary`; `classifier`/`grounding` guardrail kinds on `glossary` (real: `deterministic`/`model-scored`); `Feedback.groundedness` omitted on `service-api`/`glossary`/`integration`; embedding coverage still described as always-full on `providers-and-adapters` (v1.22.4 made it measured via `maxInputTokens`).

All ❌ and ⚠️ items above were corrected in the 2026-08-09 documentation pass. The 📝 completeness gaps were filled in the same pass — see [Planned Improvements → Documentation completeness follow-ups](#documentation-completeness-follow-ups-august-2026-audit).

### Performance Audit — August 2026

A full performance audit of the **generated** code (v1.22.2, every template) ran a parallel finder pass with adversarial verification: 53 candidates, **46 confirmed, 7 refuted**. The three "unbounded list render" scares were refuted — the usage ledger, review queue and eval-results lists are all already capped server-side (50 / 200 / 1000 rows via `clampLimit` + zod), so the frontend never receives the whole table (a *functional* "load older" gap remains, tracked under Review-Queue UI Polish, not a perf bug). The one critical and the confirmed moderate items are recorded here; the best-practice remainder is under [Performance — best-practice cleanups](#performance--best-practice-cleanups-august-2026-audit).

<Aside type="tip" title="Mostly resolved in v1.22.3">
Shipped in **v1.22.3** (behaviour-preserving; generated-code type-correctness still to be confirmed by a live install): the critical `getCurrentUsage` DB aggregate; the run/resolve round-trip collapse (two `Promise.all` waves, guardrail-row reuse, fire-and-forget review write); all the growable-table ordering and FK indexes; the eval bounded-concurrency pool; the admin query `staleTime` / `refetchOnWindowFocus`; the `guardrailsFor` / `providerName` per-render maps; and the config-io / controller cold-path parallelisations. **Still open:** the config-io `restoreSeeded` double-write (a broad rewrite of the import-provenance path — see below), and the frontend interactive refactors (operations-editor state extraction, and list pagination / virtualisation) — deferred until they can be exercised with the admin app running, which a module repo cannot do. The zod-validator memo shipped in v1.22.5.
</Aside>

#### `getCurrentUsage` scans the whole ledger (billing callback)

**Severity:** High (Critical) — the only finding that scales badly
**Where:** `ledger.service.ts` → `getCurrentUsage()` (the billing callback, design §15)

`getCurrentUsage` issues an unbounded `findMany` over every `ai_usage_ledger` row for the tenant (or since a date) and sums tokens in JS. The `where` is well indexed; the cost is transferring and iterating an ever-growing row set on every call — O(rows). This is the function handed to billing's `requireLimit`, so it runs on/near the hot path, and its latency and memory grow with the tenant's accumulated usage. The code comment already flags the DB aggregate as "a later optimisation". **Fix:** aggregate in the database — a raw-SQL `SUM((usage->>'inputTokens')::bigint) … GROUP BY operationId` (no schema change; the usage JSON keys are `inputTokens`/`outputTokens`), or add numeric `inputTokens`/`outputTokens` columns populated in `record()` and use `prisma.aggregate` / `groupBy`.

#### Resolve/assemble issues ~7 serialised DB round trips per run

**Severity:** Moderate
**Where:** `run/run.ts` (≈986, 1065, 1513/2498), `run/resolver.ts` (266), `records.service.ts` `getSegmentCompositionForOperation` (1149)

Every `aiCore.run()` resolves the operation graph as a deep chain of independent, sequentially-awaited Prisma calls: approaches/disciplines/guardrails (986) run one after another though none feeds the next; markers/framing/framework (1065) are three more serial round trips though they depend only on `input.segments` and could hoist above the segment load; `getSegmentCompositionForOperation` awaits `ownLinks` then the independent `systemOps` (1149); and the resolver reloads the guardrail table a second time (266) after `validateDisciplineSet` already fetched the same rows. The review-queue write (1513) is awaited before returning though the response never uses it. Each is a few ms on a same-region DB but tens of ms on a remote/pooled Postgres, on 100% of runs. **Fix:** collapse the independent reads into two `Promise.all` waves, reuse the guardrail rows validation already loaded, and make the review-item write fire-and-forget with a retained `.catch`.

#### Growable tables miss an ordering index

**Severity:** Moderate
**Where:** `schema.ts` — `ai_usage_ledger`, `ai_eval_result` (and, lower, `ai_review_item`)

The admin usage view (`listUsage`) and eval-results view sort the fastest-growing tables by `createdAt` with no supporting index. `ai_usage_ledger` is indexed on `(tenantId)`, `(tenantId,createdAt)`, `(operationId)` — none serves a global or `operationId`-only `ORDER BY createdAt`, so an unfiltered/`operationId` call becomes a seq-scan + top-N heapsort over the whole ledger; `ai_eval_result` (indexed on `(runId)`, `(operationId)` only) full-scans + sorts when called with no filter. Output is capped (≤200 / ≤1000) but the scan/sort work is O(table). **Fix:** add `@@index([operationId, createdAt])` and `@@index([createdAt])` to `ai_usage_ledger`, `@@index([operationId, createdAt])` (and/or `([createdAt])`) to `ai_eval_result`, and `@@index([status, createdAt])` to `ai_review_item`; add explicit `select`s so the JSON columns aren't shipped to list views.

#### config-io import doubles write round trips

**Severity:** Moderate (admin cold path, scales with config size)
**Where:** `config-io.service.ts` → `restoreSeeded` (244), used by every import pass

Every imported record is written twice — the upsert, then a separate `update` just to stamp `seeded` provenance — run sequentially in per-item loops, so a full-config import does ~2× the write round trips it needs. **Fix:** fold the provenance flag into the upsert payload (extend the records/disciplines upsert inputs to accept `seeded`), preserving the count-before-stamp reporting by deriving counts from the upsert result.

#### Eval harness runs golden cases strictly sequentially

**Severity:** Moderate (offline admin path)
**Where:** `eval/eval.ts` → `runEval()` (224)

Each golden case awaits a real model/retrieval call one after another, and each judge metric within a case awaits a further model call, all sequential — wall-clock ≈ the sum of every case's and every metric's latency. 50 cases × 2 judge metrics ≈ 150 sequential model calls (minutes). **Fix:** drive the cases through a bounded concurrency pool (e.g. 4–8 in flight) so wall-clock ≈ total work ÷ pool size while respecting provider rate limits.

#### Admin tabs refetch on every revisit (no `staleTime`)

**Severity:** Moderate (network waste, not UI jank)
**Where:** `hooks/useAiCore.ts` (all read hooks), `pages/AiCoreAdminPage.tsx` (74)

No query hook sets `staleTime` or `refetchOnWindowFocus`, and Radix `TabsContent` unmounts inactive tabs, so every tab revisit remounts and background-refetches that tab's whole query set — including the effectively-immutable terms-options catalogue. **Fix:** set a sensible `staleTime` (30–60 s, longer for terms-options) and `refetchOnWindowFocus: false` on the reference-data reads; keep the volatile reads (usage, review-items, eval-results) fresh with a short `staleTime`. (Depends partly on the host app's root QueryClient defaults, which the module can't see — so set it per hook.)

#### Operations editor re-renders the whole table per keystroke; list unpaginated

**Severity:** Moderate (bites once operations grow — they carry a `userId`, so user-authored ops accumulate)
**Where:** `pages/tabs/OperationsTab.tsx` (form state 780, table 944)

The operation editor's `form` state lives at the tab root, and the operations `<Table>` (unmemoised inline rows) renders unconditionally, so every keystroke in the editor reconciles every row. The list is also rendered in full with no pagination/virtualisation. **Fix:** extract the editor into a dialog component holding local state (the in-file `TestRunDialog` already shows the pattern), and paginate or virtualise the rows.

### ~~First Live Provider Call Not Yet Made~~ (made in v1.7.0)

**Resolved:** 2026-08-01 (v1.7.0)

`aiCore.run()`, `embed()` and `rerank()` have now executed end to end against live providers (Anthropic Claude Opus 4.6, Voyage) in a real installed app — prompt assembly → adapter → schema validation → deterministic checks → feedback → ledger write, every path leaving a ledger row. As predicted, the first call surfaced exactly one patch: an operation with no segments, disciplines or schema assembles an empty system prompt, and the adapter was sending it as an empty system content block (`system: text content blocks must be non-empty`). The adapter now omits the system field when instructions are empty; structured-output and embed calls were never affected. See the [Changelog](/modules/ai-core/changelog/).

The `sensitive`-floor terms gate held exactly as designed: under a sensitive floor a first-party Anthropic call refuses (`no-model-under-acceptable-terms`) until zero-retention terms are attested. For verification that means either a real ZDR attestation, or a temporary `normal` floor for the pipeline test alone — the gate itself is sound.


### ~~Knowledge Cutoff Must Be Admin-Entered~~ (resolved in v1.35.0)

**Severity:** Low
**Where:** model records / capability refresh

The providers' own REST listings never report a knowledge cutoff, so the cutoff — which feeds the date-context segment, the model's anchor for the edge of its own knowledge — had to be hand-entered. As of v1.35.0 the *Refresh model data* action overlays the open models.dev catalogue, which records cutoffs for most current models, and writes them onto the model records (context windows too — from the providers' listings where reported, Anthropic and Google, else from models.dev). The field stays admin-editable, and a model the catalogue doesn't know keeps whatever the admin entered.

### No CI Lint Yet for Seed Discipline/Guardrail Integrity

**Severity:** Low
**Where:** `src/templates/migrations/`

Write-time and resolution-time enforcement are live (see [Usage](/modules/ai-core/usage/)), but the designed CI lint — the one that reports orphaned guardrails and unreferenced rules in the *seed data* before it ships — has not been built.

### ~~Embedding Coverage Is Reported as Full~~ (resolved in v1.22.4)

**Resolved:** 2026-08-09 (v1.22.4)

The embed adapter used to report every input as fully covered (`covered === total`), because the AI SDK exposes no partial-embedding signal. The fix followed the design's own prescription — a per-model input ceiling **recorded on the model record** (`ai_model.maxInputTokens`, admin-editable, seeded with the providers' published limits). The adapter turns it into a coarse character budget (~4 chars/token), so a value longer than its model's ceiling is now reported as partially covered and the run engine's `partial-embedding-coverage` feedback fires. It stays honest-but-coarse by design: the record holds the real token ceiling, the ×4 estimate lives in the adapter, and a model with no recorded ceiling reports full coverage rather than an invented limit. See the [Changelog](/modules/ai-core/changelog/).

### Pre-v1.6.0 Installs Ignore the Embedding Width Requirement

**Severity:** Low (deployment, not a current-code defect)
**Where:** a host whose generated `records.service.ts` predates v1.6.0

The v1.6.0 width filter — `ensureOperation` selects an embedding model of exactly the required `dimensions` or refuses — only exists in generated code from v1.6.0 onward. A host installed earlier binds embedding operations to the oldest enabled embedding model, ignoring `dimensions`, so a consumer requiring 1024 can silently bind a 1536-width model; the run-time dimension gate then (correctly) refuses on first embed. Fix: `npx ai-core update` to v1.6.0+. Surfaced verifying cog-ingest's embed operations against a real app running mixed-version generated files, alongside the v1.7.1 async dimension-gate fix (the gate had never actually fired live until Structure was registered).

### ~~Frontend Prop Shapes Verified Against Stubs~~ (resolved in v1.1.2)

The first real install confirmed exactly this class: `ErrorAlert`'s real prop is `error`, and the query-keys helper takes scalar parts. Both fixed in v1.1.2–v1.1.3; the host app's frontend now type-checks the feature clean against the real starter components.


## Planned Improvements

### Short-Term

#### ~~Install Verification~~ (complete — v1.7.0)

Done. Installed into a real starter app, migrations and seeds applied, admin page exercised, and `run()` / `embed()` / `rerank()` all run live against real providers (v1.7.0). The frontend compiles against the real starter components. This gate is closed.

#### Documentation completeness follow-ups (August 2026 audit)

**Done — 2026-08-09.** The 📝 correct-but-incomplete gaps the [documentation-accuracy audit](#documentation-accuracy--august-2026-audit) surfaced were filled in the same documentation pass:

- `configuration` omits the **Approaches** tab and the **`maxInputTokens`** model field, and calls the Prompts tab "Prompts" though the UI labels it **"Segments"**.
- `service-api` / `registering-and-seeding` spec tables omit real fields — `ensureOperation`'s `systemWide` / `cache` / `evalConfig`, `ensureSegment`'s `contentSource` / `injectionRisk` / `marker` / `approaches` / `disciplines`, and `EnsureModelRequirements.multimodal`.
- `prompt-model` / `disciplines-and-guardrails` don't mention the v1.23.0 **per-operation approaches/disciplines slots** (private per-op segments under the reserved `op.<id>.approaches` / `.disciplines` key), including the second `validateDisciplineSet` write-time call site in `upsertOperation`.
- `hooks-and-events` omits the **`ai-core.approach.updated`** hook and mis-describes the admin review-item event variant (it carries only `{ id, status }`).
- `developer-workflow` Registries row omits **`routeRegistry`**, lists `settingsPagesRegistry` / `preferencesRegistry` (not wired by any template), and the check-command list omits **`npm run conformance:check`**.
- `architecture` config-tables card omits `ai_approach` and the three composition tables (`ai_operation_segment`, `ai_segment_approach`, `ai_segment_discipline`).

#### Admin consistency pass — done (v1.33.0–v1.35.0), one follow-up

Every admin browse list is now the same fitting, searchable table (fixed layout, truncated cells, per-row **Edit**) and every New/Edit opens a full-page in-tab editor: v1.33.0 brought Operations / Context / Approaches tables and slimmed + moved Segments; v1.33.1 pinned the header **New** button top-right; v1.34.0 rebuilt Disciplines / Markers / Tools (Disciplines as **Disciplines | Guardrails** sub-tabs, guardrails gaining a real Edit) and converted the Approaches dialog to full-page; **v1.35.0** brought Segments (type filter + create/delete) and Models (provider logo cards with click-to-filter, a full-page model editor, per-model frameworks, live model data via provider listings + models.dev) into the pattern, declared the operation **input contract**, and restricted the operation Context block to Context builders. The **frameworks → Models move shipped in v1.35.0**. Still open:

- **Guardrail `config` authoring.** The admin guardrail editor writes id / discipline / kind / rubric but not `config` — a deterministic guardrail authored in the admin carries no `config.builtin`, so at run time it reports `guardrail-check-unimplemented` instead of measuring anything (pre-existing, surfaced by the v1.34.0 adversarial review). Needs a small config surface — a builtin picker + threshold, or a validated JSON field like the tool editor's.
- **Framework editing.** The frameworks list (now on the Models tab) is still read-only — the backend CRUD exists (`upsertFramework` route + unconsumed frontend service/hook); an editor arrives with a later version.

### Prompt Model Evolution — settled 24 July 2026

The prompt taxonomy was reviewed end to end against authoritative prompt-engineering guidance, the old Wysli app's prompt system, and AI Core's own code, and **settled** — see the [Prompt Model](/modules/ai-core/prompt-model/) page for the full shape. The two-layer Frame/Substance split, per-model frameworks and the discipline↔guardrail pairing all ship today; the pieces below are the agreed evolution, in priority order.

#### ~~Prompt Model v2 — approaches · disciplines · segments · guardrails~~ (shipped — v1.22.0)

**Done.** The prompt machinery was *simplified by removing concepts*. Everything in a prompt is now a **segment** (`ai_prompt_segment`, one `slotType` of `approaches | disciplines | objective | context | examples | format | edge-cases`); an operation composes ordered segments through `ai_operation_segment`, and a **`systemWide`** operation's segments fold into every run — skipped on an internal scorer run, so a judge prompt stays clean. The two "how" kinds became their own records related *into* segments: an **approach** (`ai_approach`, an *unmeasured* how — expertise, persona, reasoning, tone) and a **discipline** (`ai_discipline`, a *measured* how whose id is its own key, owning one-or-more guardrails). A segment's members are objects on join rows (`ai_segment_approach` / `ai_segment_discipline`), each carrying `sortOrder` and **`important`** — restatement lives on the *membership*, not the record, and an important member is re-emitted at the prompt tail under a seeded `key-rules` marker (recency). **`enabled` was removed** from segments, approaches, disciplines and guardrails — membership in a segment's list *is* the switch (it stays on models, operations, markers and flows). `ai_guardrail_check` was renamed **`ai_guardrail`** (its own table, `disciplineId` FK, `kind` of `deterministic | model-scored`); a model-scored guardrail names an ordinary **scorer operation** in its config — there is no first-class "judge" any more (the old groundedness / correctness judges are ordinary operations). The discipline **tier / authority hierarchy** and the `role` (system/user) segment type were **removed** — the system/user split is derived at assembly from `systemWide` plus the slots. `validateDisciplineSet` enforces at **write time** (a disciplines-member change) and again at **resolution time** (every call refuses loudly), with violation kinds `unknown-discipline`, `discipline-without-guardrail`, `guardrail-scorer-missing`. The declarative config JSON moved to **v2.0** as a clean break (v2-only reader; merge = structure parity — update and report, never delete; replace = also deletes non-seeded omitted rows behind named delete-guards; seeded provenance round-trips; operations stay model-agnostic — requirements, not a model). See the [Changelog](/modules/ai-core/changelog/).

<Aside type="caution" title="Upgrading a populated v1 install is a clean break">
The reshaped tables (segments, disciplines, guardrails, operations) are **reset to the v2 built-ins** by the `onPrepareDatabase` hook — custom, non-seeded rows in those tables are **not** preserved, so export them via the Config tab first. Markers, eval cases and the model / provider / flow / ledger layer keep their shape (presets were dropped in v1.23.0). Requires scaffold ≥ 1.31.14 (it self-heals a failed module migration and regenerates the client). A future data-preserving migration path — export the custom rows and re-import them after the reset — is on the backlog below.
</Aside>

**Still open (deferred — all low, narrow; the resolver still refuses at run time):** a few config-io **replace-mode** edge cases — a scorer operation or an eval case left orphaned by a *self-inconsistent hand-authored* replace, and create-path seeded-provenance under a transient DB failure. None can produce a silently-wrong prompt: `validateDisciplineSet` re-checks the resolved discipline set on every call and refuses loudly.

#### ~~Guardrail-check execution~~ (shipped — v1.12.0)

**Done.** The rule-and-check pairing now EXECUTES. **Source verification** (a deterministic builtin, no model call) verifies every extracted item verbatim against the source and, where an independent denominator exists, whether everything the source contains was returned — saying so plainly where no count exists. A real **groundedness scorer** (a `grounding` check) runs a nested judge that decomposes the output into claims and produces a measured support score, which **abstention now compares against instead of the model's self-reported confidence**; where only self-report is available, the run *names* that provenance. Opt-in per operation through a discipline; a batteries-included judge is seeded, bound to a model by requirements. This subsumed **Groundedness Scorers** (below). See the [Changelog](/modules/ai-core/changelog/). Follow-ups: an admin editor for custom checks, and an `ensureCheck` consumer seed for a custom source-verification path.

#### ~~Markers — generalising framing~~ (shipped — v1.15.0)

A small admin-managed catalogue of `{ id, meaning, renderingHint }` records (`ai_marker`) that replaces the single `framing` segment. Content is wrapped in a marker; the meanings of the markers in use are stated on the system side; the framework picks the wrap syntax per model. One mechanism now covers external content, examples, sources and conversation history, and it is consumer-extensible (`ensureMarker` — chat adds a `conversation-history` marker fed by its own memory). Markers apply to content items only, never the operation's instructions or the task. The built-ins seed on install; managed in a Markers admin tab. **Superseded in v1.18.0:** markers are now purely *semantic* and the `external` marker and its non-overridable floor were removed — the security axis is a separate per-item `injectionRisk` flag on the content, and the framing prose (preamble + injection-risk notice) is configurable seeded records. See the [Changelog](/modules/ai-core/changelog/).

#### ~~Marker hardening — sanitise + unclosable spans~~ (shipped — v1.19.0)

Content flagged `injectionRisk` is **sanitised** (invisible/format injection-vector characters stripped — ASCII control, bidi override/isolate, zero-width, word joiner, interlinear, and the Unicode Tags block; directional marks and variation selectors kept) and, when it carries a marker, fenced with an **unclosable per-call nonce tag** so pasted content cannot close its own span. Named via `injection-content-sanitised` / `injection-risk-unmarked`. Still open: **datamarking / spotlighting** (interleaving a marker between words) — the strongest but noisiest defence, deferred until its task-quality cost can be measured on the eval harness; and an **injection-risk classifier** (the declared `classifier` check-kind) to auto-suggest the `injectionRisk` flag from content, defence-in-depth on top of the admin/app setting it.

#### Preferences — per-user

Per-user terminology (preferred / avoided terms) plus style and detail, registered through the starter's `personalInfoFieldsRegistry` (saved to `User.preferences`, no schema change) and injected selectively — only the terms that match the input. AI Core registers the fields and owns the assembly; the app owns the stored values, so AI Core holds no user data.

#### input-data / context split, and contextual-time

Two smaller refinements. Distinguish **input-data** — the new data the user brings this call — from **context** — what the app already knows — on a *new vs known* line (trust is carried by the item's marker, not a separate type). And grow the date anchor into **contextual-time** by adding *time since the last message*, so a reply five days after the last one does not read as though it were the same day.

**Contextual-time — shipped v1.23.1.** The date anchor is computed per call from *now* plus the model's knowledge cutoff and rendered into the date-context segment; contextual-time adds the *elapsed gap since the previous message*. `RunOptions.lastMessageAt` carries the previous message's moment and the date segment states how much time has passed (e.g. *"the previous message … was about five days before now"*). AI Core stays **conversation-stateless** — it holds no message history (conversations belong to the app or the chat module) — so the caller supplies the timestamp on the call; AI Core uses it only when valid and at or before now, phrases the gap, and never persists it. **Still open on this item:** the **input-data / context split** (the *new vs known* line) above — now folded into the shaping done by [Context Assembly — the context engine](#context-assembly--the-context-engine), where new-vs-known is one of the provenance/marker distinctions the engine carries. This pairs naturally with the [Tools](#tools) work — a model that can both see the real elapsed time and look things up is the difference between a stateless completion and an assistant.

### Consumer requirements — cog-ingest (Stage-1 live + Stage-2)

cog-ingest **seeds and calls** these operations; AI Core must **resolve + run** them. These are requirements (model *kinds*), never provider names — cog-ingest names jobs only, and AI Core owns the SDK/gateway (Vercel AI SDK now, possibly TanStack AI later).

**Stage-1 — make what already shipped actually run (done, v1.7.0–v1.7.1):**

- ~~**Live `embed`** at the app's vector width~~ **(shipped — v1.7.0/v1.7.1).** cog-ingest's `cog-ingest/embed-document` and `cog-ingest/embed-query` (kind `embedding`, `dimensions` = Structure's `getCognitiveCapabilities().embeddings.dimensions`) resolve and run live; the width-selection and dimension gate are verified against Voyage with Structure present.
- ~~**Live `run`** for language and vision ops~~ **(shipped — v1.7.0/v1.7.1).** `cog-ingest/contextualise-chunk` and the vision ops (`cog-ingest/analyse-image`, `-url-screenshot`, `-pdf-page`) produce output end to end.

**Stage-2 — new capabilities (mostly shipped):**

- ~~**Prompt caching**~~ **(shipped — v1.9.0).** `contextualise-chunk` can be fed the full source document per chunk affordably; the repeated document is cached across its chunks (contextual-retrieval shape), Anthropic-only.
- ~~**Multimodal / visual embedding op**~~ **(shipped — v1.11.0).** `aiCore.embedMultimodal` embeds images and text into one vector space (voyage-multimodal class), so screenshots/figures are retrievable by visual similarity.
- **Rich transcription op** — a transcription kind returning speakers + sentiment + **tone/prosody** + timestamps (Docling's built-in Whisper ASR is text-only). **Still open** — deferred until a recording use case is real; optional if Docling ASR suffices, required to capture tone.
- ~~**Parse-QA judge op** + **per-source summary op**~~ **(addressed — v1.10.0).** Both are consumer-seeded language operations needing no new AI Core capability; the `deterministic` dial (v1.10.0) supplied the temperature-0 determinism the judge required.
- ~~**Callable `rerank`**~~ **(shipped — v1.7.0).** rerank is a callable op (`aiCore.rerank`); its dial now drives reranking within the retrieval pipeline (v1.13.0), so cog-ingest's `retrieve()` can reorder and cut top-k.

**Dimensions gate:** the existing `embedding-dimension-mismatch` refusal already enforces width; cog-ingest passes the required width at seed time (converges on `getCognitiveCapabilities().embeddings.dimensions` under the Structure-storage retarget).

Cross-reference: cog-ingest [Stage 2 roadmap](/modules/cog-ingest/roadmap/).

### Medium-Term

#### ~~Retrieval (design §16)~~ (shipped — v1.13.0)

Semantic retrieval is now an operation — `aiCore.retrieve()`. If AI is doing the retrieving, AI Core owns the mechanism: it uses a model, it can be wrong, it reports what it found **and the shape of what it missed**. The grounding dials (breadth, rerank, abstention) that were recorded but inert now drive an embed → Structure chunk search → optional rerank/graph-expansion pipeline, degrading to named gaps rather than failing. AI Core retrieves; Structure stores. See [Integration → Retrieval](/modules/ai-core/integration/#retrieval).

Still open here: node-level retrieval (chunk-level ships first), a store-surfaced topK ceiling so the breadth clamp is cap-agnostic rather than coupled to Structure's current cap, and lexical/hybrid search once Structure exposes it.

#### ~~Evaluation harness~~ (shipped — v1.14.0)

A generic, portable harness that measures an operation's quality offline over a golden set of cases — configured per operation (`ai_operation.evalConfig`) and run with `aiCore.runEval()`. Deterministic metrics (`exact-match`, `context-precision`, `context-recall`) and judge metrics (`faithfulness` reusing the groundedness judge, `answer-correctness` via a seeded correctness judge). It runs the real pipeline and reports per-metric scores **and how many cases could not be evaluated** — a partial eval is never a clean pass; cases are synthetic fixtures and results carry no content. Managed in a new Evals admin tab; consumers seed cases with `ensureEvalCase`. Still open: a domain golden set (the planted "prepaid funeral" surfacing test, the density benchmark) lives in the consuming app, and gating promotions (chunk strategy, rerank on/off) is a consumer workflow over the scores.

#### ~~Groundedness Scorers~~ (shipped — v1.12.0)

**Done** as part of guardrail-check execution (above). A `grounding` check runs a nested judge (claim decomposition + entailment) and produces a measured score in `[0,1]` that the abstention threshold compares against and that lands in the new `feedback.groundedness` — an input to feedback, never a certificate the output is true. Pluggable per operation via the check's `config`. The provider-native-citation and dedicated-grounding-service implementations remain open as alternative scorer types.

#### Tools

**Provider-native web search wired — v1.25.0; web fetch added — v1.25.4.** The records foundation (v1.24.0: the `ai_tool` catalogue with a seeded `web-search` tool, the `ai_operation_tool` per-operation opt-in, and the `egress` flag) is now wired end to end: the resolver loads an operation's enabled tools and applies the egress→sensitivity gate (`no-tool-under-acceptable-terms`); the adapter translates them into the AI SDK's own tools and bounds the search-and-read loop with a fixed step cap; the run reports which tools ran and the sources a search found (by reference — the ledger stores tool name + call count only, in a new `ai_usage_ledger.toolCalls` column; found URLs are returned to the caller, not persisted); and the operation editor's **Tools** block plus a new **Tools** admin tab manage the opt-in and the catalogue. A **second built-in tool, `web-fetch`** (v1.25.4), rides the same machinery — the model reads a specific URL rather than searching for one — added as one new seeded `ai_tool` (its own seed file so existing installs pick it up) plus per-provider adapter builders (Anthropic `webFetch_20250910`, Google `urlContext`; OpenAI hosts no URL-fetch tool, so it skips with an audit line). It carries `egress: true`, so the sensitivity gate covers it exactly as it does search. Anthropic is the verified path; OpenAI and Google are wired but unverified. **Still deferred:** config-io round-trip for the tool opt-in, provider config passthrough for OpenAI/Google, function (app-registered) tools, and post-cutoff auto-abstention (deliberately not built — "info was needed but no tool" cannot be detected without fabricating a signal, which the honesty rule forbids).

The run engine is **single-shot** today — the generate request carries instructions / input / parameters / schema / attachments / cache, and the model cannot call out mid-generation (see [Providers & Adapters](/modules/ai-core/providers-and-adapters/)). **Tools** add that: the model may invoke a named capability — **web search first** — and the run continues with the result. It stays inside every existing principle.

- **Web search first.** The one most AI apps need — answer a question about something past the model's training cutoff by looking it up. Ship the **provider-native** search tool (Anthropic / OpenAI / Google each host one) before any general framework: least surface, the model decides when to search.
- **A tool is a record, opted into per operation.** A new `ai_tool` catalogue (seeded, admin-editable) plus an `ai_operation_tool` join — an operation declares which tools it may use, resolved like disciplines and dials. The consumer surface is unchanged: still `aiCore.run('answer-question', …)`, never a tool name in code. Two kinds: **provider-native** (provider-hosted, e.g. `web_search`) and **function** (an app-declared tool whose *declaration* is a seeded record and whose *execute* handler is **registered as code** — the same seed-data / register-behaviour split flows already use).
- **The SDK stays in the adapter.** The generate request gains `tools?` + `toolChoice?`; the result gains a normalised tool-call / step trace. The adapter translates the resolved tool set into the AI SDK's `tools` and runs the agentic loop (`stopWhen`), bounded by a **step cap expressed as a dial** — "dials promise work, not speed": each extra step is another model call. Nothing about tools leaks past the adapter layer.
- **The sensitivity gate extends to tool egress — the crux.** A web search sends the query (and possibly context) to a third party under *that tool's* terms. So a tool carries its own recorded data-handling terms, and the resolver's sensitivity gate covers them: a `sensitive` operation may use only tools whose zero-retention terms are attested, else it **refuses loudly** — a new `no-tool-under-acceptable-terms` reason mirroring `no-model-under-acceptable-terms`. Because the gate is opt-in per operation, a `normal` operation searches freely and a `sensitive` one refuses until the tool's terms are recorded. No new gating concept — the same gate, one more axis.
- **Transparency, ledger, abstention — the rules unchanged, applied.** The run reports which tools ran and (for search) what it found and the shape of what it missed, exactly as `retrieve()` does; the ledger records tool calls **by reference** (tool name, call count — never the query or the results content); and if a question needs post-cutoff information the operation cannot get (tool absent or gated off), it **abstains and names the gap** rather than answering from stale training data. A tool the model wanted but was denied is reported, never silent.
- **Phasing.** (1) ✅ **shipped (v1.25.0)** — provider-native web search behind the per-operation opt-in + egress/terms gate; ✅ **URL fetch (v1.25.4)** — the second provider-native tool on the same machinery; (2) the general function-tool framework (app-registered handlers); (3) a small built-in catalogue (code execution and the like) as demand appears. Structured-output-plus-tools and streaming remain open — some providers can't combine schema output with tools (the adapter passes both and reports any provider rejection rather than pre-suppressing), and the module is non-streaming today.

A fuller design sketch — the exact request/result shapes, the `ai_tool` / `ai_operation_tool` records, and how the gate / ledger / feedback extend — was worked through alongside this entry.

#### Operations editor — block builder

Redesigning the Operations admin (the current editor is a long confusing form, and approaches/disciplines appear twice). Target: a **drag-blocks-into-the-prompt** builder (`@dnd-kit`) where each block is a slot and you pick an existing record/segment of that type or author a new one inline. Blocks: **System Prompt** (select a system-prompt operation — 1 exists → auto/greyed, >1 → first-created is the default, changeable; absent when the op itself is a system prompt), **Input** (the data; was Task), **Objective** (a list of goal / objective / task), **Approaches** (Response Style [was voice], Audience, Environment, Language, persona, expertise, reasoning, tone), **Disciplines**, **Context** (fed by the future context engine), **Examples**, **Response Format** (was Format), **Edge-cases**, **Tools**. The standalone "Prompt segments" multi-select goes away — shared reuse lives *inside* each block. **Shipped:** System-Prompt-as-selected-operation (v1.24.1); block-builder **v1** (palette + click-to-add cards, v1.24.4) and **v2** (System Prompt + Input as locked canvas blocks, provider→model cascade, approaches/disciplines Add-picker with an *important* toggle, v1.24.5); and the **three v1.24.5 refinements** (v1.24.6) — the **User ID** field removed (operations still save `userId: null`), a **Model** heading with **Provider + Model inline** (was stacked), and the **typography inversion** done *structurally*: the section headings (Model, Prompt, Dials, Prompt caching) are now the `SectionHeader` primitive — the big text — over the smaller block/card titles, matching every other admin tab, with no `text-*` size classes (LLM-rules constraint). *(If the block titles still read large once rendered in the host, the next structural lever is a lower heading level for the card titles.)* **Tools run-wiring shipped (v1.25.0)** — provider-native web search opted in per operation, the egress→sensitivity gate, by-reference reporting, and a Tools admin tab (see [Tools](#tools)). **Inline block authoring shipped (v1.25.1)** — create a new shared segment (a "what" slot) or an approach from inside its block and it is added to the operation; a key that already exists is refused rather than overwritten; disciplines stay in the Disciplines tab (a discipline is refused until it owns a guardrail). Also the first UI path to create a shared segment — the Segments tab stays edit-only. **Objective shipped (v1.25.2)** — the Objective block is a goal/objective/task list editor stored as a private per-op objective segment (composes like any objective, no schema change); the same release fixed a bug where per-op **approaches/disciplines** were silently dropped by the admin route (`operationCreateSchema` stripped them — only config-import carried them). **Provider display names shipped (v1.25.3)** — the Model cascade shows friendly provider names instead of id slugs. **URL-fetch tool shipped (v1.25.4)** — a second built-in tool (`web-fetch`) on the same machinery as web search: one new seeded `ai_tool` plus per-provider adapter builders (Anthropic `webFetch_20250910`, Google `urlContext`; OpenAI hosts none, so it skips with an audit line), `egress: true` so the sensitivity gate covers it; it appears in the Tools tab and the editor's Tools block automatically (see [Tools](#tools)). **Drag (`@dnd-kit`) de-scoped** — the ↑↓ block reorder is sufficient; drag is dropped from the plan. **Behaviour (dials) rework — in progress.** The editor's "dials" are being reworked for *admin clarity* (the admin may not be an engineer): model parameters only on operations that have a model, retrieval dials moving onto the future Retrieve/Context builders, plain non-technical labels (no temperature/topK/rerank jargon), and the ordered settings as **multi-stop labelled sliders** mapped per model record. *Shipped R1 (v1.26.0):* the boolean `deterministic` dial became a three-level **Consistency** dial (Same / Balanced / Varied). *Shipped R2 (v1.26.1):* the section is now **Behaviour** — the ordered settings (Effort / Response length / Consistency / Double-check) are **multi-stop labelled sliders** with plain wording, grouped *How it responds* / *Look-up*, and the whole section is hidden for System Prompt operations. (The finer "retrieval dials move onto the Retrieve/Context builders" waits for those builders — post-UI Context Assembly.) **Next: the Input block becomes a list.** Today Input is a single locked slot; it should be a **list of declared input items**, each configurable with its own **marker(s)** (and `injectionRisk`), because a call's input is often more than one thing — e.g. an uploaded **document** (untrusted → marked `source`, `injectionRisk: true`) *plus* a typed **comment** (the task). Each item declares its kind (text / document) and how it is framed, so the operation carries its **input contract** and the app just fills the content. This also realises the *input-data vs context* split — a document input is markable context, a comment is the task. Backend follow-ups still open: the Input / Response-Format slot renames, approach flavours + Response Style seeds, and config-io round-trip for the system-prompt selection and tool opt-in. Then the **context engine** (curated/deterministic — see [Context Assembly](#context-assembly--the-context-engine)) feeds the Context block. **Kim's decisions (2026-08-16, from the operation-surface review):** ✅ Input-becomes-a-list confirmed — item kinds **`input:upload`** (a document, untrusted/markable) and **`input:user-message`** (the typed input, i.e. the task); ✅ move the retrieval **Look-up** dials (breadth, reranking) onto the Retrieve/Context builders; ✅ build the context engine. **But "When to say it's not sure" (abstention) becomes a _discipline_, not a dial, and stays _on the operation_** — it is measured against answer support (reuses the groundedness scorer), so it belongs in the Disciplines block; the separate *retrieval-strength* abstention moves with retrieval. Plus a small **Prompt-caching copy** pass — plainer, non-developer wording. Smaller follow-ups: Kim to revisit later. **Shipped (v1.27.0):** abstention moved off the dials to become the **groundedness discipline** (`ai-core/ground-claims`) — the self-reported-confidence fallback was deleted (confidence is still *reported*, never acted on), and the threshold lives in the guardrail's config; the *retrieval-strength* abstention split out to a retrieval-scoped **`retrievalAbstentionThreshold`** Look-up dial; the **Input block became a typed list** (Message = `input`, Document upload = a marked/`injectionRisk` **context** segment riding `media`, with a seeded **`document`** marker) together with the **`task` → `input`** rename (code, plus seed-26 rewriting the persisted framework/eval-case JSON); and the **prompt-caching copy** pass. **Shipped (v1.28.0):** the retrieval **Look-up** dials (breadth, reranking, `retrievalAbstentionThreshold`) moved onto the new **Retrieve builder** (in the Builders tab) and off every operation — a normal op's Behaviour section now shows only *How it responds*. See [Context Assembly](#context-assembly--the-context-engine). **Shipped (v1.29.0):** the standalone Retrieve builder was **retired** — retrieval folded into the Context builder, whose editor is now a **block builder of retrievals** (each a method × scope × ranking against Structure). The Builders tab manages Context builders only. See [Context Assembly](#context-assembly--the-context-engine). **Shipped (v1.30.0):** the Context builder is a full **block builder** — Text / Segment (authored, trusted) · Retrieval (from Structure, hardened) · Memory / Session (P2 placeholders); the token budget became a **scale** resolving to a % of the consuming model's window (new `ai_model.contextWindow`); the tab was renamed **Context**. **Shipped (v1.31.0):** the Context editor became an **operation-style two-column block builder** (heading *"Prompt Context Segment"*, palette-left / cards-right) with **Context id** and the budget ceiling up top; the **marker moved onto each block** (a block with none inherits the consuming operation's new **overall context marker**, set on the operation's Context block), and the **query embedding model + sensitivity moved onto each Retrieval block** — `assembleContext` now resolves and embeds **per model**, gating a *sensitive* retrieval (skip + name) before its query is ever embedded. No schema change; `seed-29` backfills the per-retrieval model + sensitivity; the `tab-builders.ts` template was renamed `tab-context.ts` (`ContextTab`). **Shipped (v1.32.0):** the **Memory** and **Session** blocks are wired as **caller-supplied** context (the app passes `memory` / `session` on `aiCore.run()`; each item renders as one injection-hardened context segment under its marker), and a new **Retrieved display** block carries a per-result template (`{content}` / `{index}` / `{node}` / `{score}`) that formats each retrieved item; **Segment** was relabelled **Shared segment**.

#### Context Assembly — the context engine

The operation editor's **Context block** is stubbed "fed by the future context engine", and `ai_prompt_segment.contentSource = 'context'` (and the same field on `ai_approach`) is a **declared-but-inert foundation hook**, documented "populated by the context engine at call time", with no code path behind it yet. This is that engine. It **subsumes and supersedes** the [Context Token-Budget Stage](#context-token-budget-stage) open question below and the *retrieval half* of the [input-data / context split](#input-data--context-split-and-contextual-time) item. A July 2026 external research pass ("Designing the Context Assembly Stage") pinned the design against the industry state of the art; a follow-up mine of the old **Wysli** app and a review of the **cog stack** (Structure / cognitive-db / cog-ingest) refined it. This entry records how it lands in AI Core; the ingest/store side is specified in [Cog Ingest → Context Assembly Requirements](/modules/cog-ingest/context-assembly-requirements/).

**What it is.** Context Assembly takes *already-retrieved* Knowledge — RAG chunks from `retrieve()`, and later semantic/episodic memory and session state — and turns it into the smallest, best-ordered, best-formatted, provenance-marked set of **context segments** the prompt assembler consumes, reporting exactly what it dropped. The field names this concern precisely (Sourcegraph "Context Assembly"), distinct from *retrieval* (how you find the knowledge — already ours in `retrieve()`). The internal pipeline is **select → compress → order → format → budget**, applied only to Knowledge. Anthropic's guiding principle: "the smallest possible set of high-signal tokens that maximise the likelihood of the desired outcome" — assembly curates, it does not dump (context rot, lost-in-the-middle, and distraction ceilings all punish "throw it all in").

**Placement & the Builders family (decided).** Context assembly is a **Builder** — one of a small family of configs whose job is to *produce a slice of another operation's prompt* rather than be the end job: **System Prompt** (already a selected operation via `systemPromptOperationId`), **Retrieve** (already an embedding operation — the *finder*), and **Context** (new — the *curator*; internally selects a Retrieve). Builders are configured with the **same block-builder editor as operations** and grouped under a **"Builders"** heading in the admin; under the hood they stay operations/records — no new table, so all the existing resolver / eval / ledger machinery applies. A consuming operation **selects** its context builder via a new **`contextOperationId`** — exactly how it selects a system-prompt operation — and `run()` executes it as an **internal sub-step** (the same machinery as the nested groundedness judge and the in-retrieval rerank op), handing it the call's input to retrieve against and folding the result into the context slot. The **Context slot is first-class** (always shown, like System Prompt) and **selectable-to-None** (a pure-transform op needs no context). This **replaces the earlier "inert `contentSource='context'` hook" framing** — `contextOperationId` is the concrete seat. Output feeds the downstream run through the **existing `ConsumerSegment[]` contract**; the pure-sort assembler and per-model frameworks stay unchanged.

**Curated / deterministic — the decision.** The engine is **curated and deterministic** (embed → search → order → budget → dedupe → mark, then hand finished context to the model), **not agentic** (the model calling a retrieval tool mid-answer). For AI Core's **collaborative, human-in-the-loop** apps this is the right default: the user and app can *see and steer* what context is used, and it stays evaluable and cheap. The agentic path stays a **deferred, optional layer** whose natural home is **chat** — a conversation that also, say, logs notes in the background; it would ride the [Tools](#tools) function-tool framework when that lands, and even then feed the *same* curator. **Curated-first, probably curated-only.**

**Structure is the contract; the cognitive stack is optional.** AI Core reaches the store through **Structure's services only** (`depends` stays empty), so a **`Structure + AI Core`-only install must work**; Cog Ingest / cognitive-db are *optional producers* that populate Structure. When a producer signal is absent the engine **degrades and reports** (no summary rung → full content + a named gap; no importance → relevance+recency ordering) — never fabricates. The full ownership split is in [Cog Ingest → Context Assembly Requirements](/modules/cog-ingest/context-assembly-requirements/).

**Output contract:** `{ segments: ConsumerSegment[] (type 'context', ordered, marker-wrapped, origin/verification-tagged), mergeFields: Record<string, string> (Phase 3), feedback: Feedback (coverage { unit: 'context-tokens', covered, total }, and every dropped / summarised / budgeted-out item named in missing[]), audit (by-reference: which chunk/node ids were selected) }`.

**Most of it already exists — this is largely wiring, not greenfield.** The grounding audit found AI Core already has the majority of what the research prescribes:

- **`retrieve()`** already does embed → Structure chunk search → optional rerank op → 1-hop graph-expand → best-first scored `RetrievalContextItem[] {nodeId, chunkId, content, score}` + named `notFound[]` gaps + abstention on weak retrieval — the "found knowledge **and the shape of what it missed**" surface.
- **`ConsumerSegment`** already carries the assembly currency: `marker` (semantic axis), `injectionRisk` (security axis), `origin` (`authored|user-supplied|ingested`) + `verification` (`unverified|verified|failed`) provenance, and `timestamp` (rendered as relative contextual-time).
- Untrusted retrieved content is already **sanitised + unclosable-nonce-fenced + per-paragraph marker-interleaved** (v1.19.0 — partial spotlighting).
- **Provider formatting is already the per-model framework's job** (XML/Claude, Markdown/GPT-Gemini, plain) — the research's "format is provider-specific, frameworks wrap not author" maps 1:1.
- **Eval** already has `context-precision` / `context-recall` (deterministic) + `faithfulness` (judge) — the RAGAS "evaluate the assembled context" recommendation is buildable today.
- The **prompt-cache context boundary** (`inputCacheBreak`) already caches the stable context prefix.
- **Structure already exposes more than AI Core consumes** — the store ships **hybrid lexical+semantic search with SQL-side RRF** (`hybridSearchChunks`), **chunk spans** on every hit (`chunk.metadata`), a **topK cap**, and `observedAt` freshness. The **first, zero-new-storage win** is AI Core switching from semantic-only to `hybridSearchChunks` and reading `chunk.metadata` — the "add a lexical arm" idea is *already built one layer down*.
- **Structure already ships a single-brace `{token}` merge-field system** (`getMergeFieldSchema` → `MergeFieldToken[]`, `resolveTemplateAsync`) — AI Core's context merge-fields **match** that grammar rather than invent one.
- **cognitive-db already has pure `rrfFuse` / `mmrSelect` / `dedupe` / `budgetAssemble`** honouring the never-decay invariant — AI Core can't import them (empty `depends`) but mirrors their semantics (or they are lifted into a shared dependency-free package).

**The genuine gaps (AI Core's own work — several shrank once Structure's real surface was mapped):**
1. **No mapping** from retrieval hits (or memory/state) → `ConsumerSegment[]` — the `assembleContext` builder itself.
2. **No token budgeting** / window-fitting anywhere — the run engine even reserves the "consumer segment exceeding declared limits" coverage trigger "with retrieval on the roadmap". Input budgeting reuses the **token-learning** machinery AI Core already ships for output tokens (p95-of-historical-usage + buffer + model-clamp, reporting sample size).
3. **No in-slot ordering blend or graded dedup** — even though Structure's hybrid returns a fused rank, AI Core still owns the *final* context-slot ordering (**relevance × recency-decay × importance**) and **graded dedup** (collapse cosine>~0.9 near-duplicates, not just exact-id).
4. **No compression** — but mostly solved by a **precomputed granularity ladder** (title/summary/content rungs stored at ingest): the budget step becomes a **rung-selector** picking the richest rung that fits and *reporting the downgrade*, no hot-path summariser. Runtime summarisation is the fallback for items with no rung.
5. **No memory-tier vocabulary** — only `input|context`; the Wysli mine validates **semantic / episodic / session-state** plus a **retrieved** peer and a **voice/terminology** peer, kept separately-fetched and separately-labelled.
6. **No coverage report for *dropped input* context** (fields exist, nothing produces them) — and the rule is **degrade-per-tier but report** (Wysli degraded silently; the inverse is the invariant).
7. **Runtime context not audited by reference** — no ledger trail of *which* chunks/rows were used.
8. **No runtime merge-field resolver** — and the Wysli mine is its **defect catalogue**: after substitution scan for residual `{...}` → report/fail (never leak the literal); per-token declared default + degrade-and-report; **injection-aware single-pass** (route untrusted-origin tokens through the `injectionRisk`/marker machinery); locale/TZ-owned `{date}`; global-replace not first-occurrence; a declared token-manifest per segment.
9. **Flow-state → segments mismatch** — a flow step injects the whole state as one `JSON.stringify(state)` context segment; it can't carry multiple *distinct provenance-marked* segments across suspend/resume.

**Proposed phasing (curated-first):**
- **P1 — the quick win, then the Builder.** ✅ **Core shipped v1.28.0.** The **zero-storage win** landed: retrieval prefers Structure's **`hybridSearchChunks`** (lexical + semantic RRF) over semantic-only and reads each chunk's `tokenCount` (falling back + naming the missing lexical arm). And `assembleContext` shipped as the **Context builder** — a `builderRole:'context'` operation selected via **`contextOperationId`**, run as an internal sub-step that draws from a **Retrieve builder** (`builderRole:'retrieve'`, the finder — an embedding model + the Look-up dials, both surfaced in the new **Builders** tab), then curates: **order (relevance) → exact-dedup → token-budget (`contextTokenBudget`) → mark (`source`, injection-hardened) → fold in**, with every dropped / deduped / degraded item named in `Feedback.missing` and a by-reference chunk/node audit. Structure is the only contract; a Structure-only install degrades and names the gap. **Refined in v1.29.0:** the two-builder split was **collapsed** — the standalone Retrieve builder is retired and a Context builder now owns an ordered **`retrievals`** list, each a method × scope × ranking against Structure (hybrid/semantic **chunk** search, lexical **node** search, plus an optional 1-hop **graph** expansion). The curator now **embeds the query once** and **RRF-fuses across the retrievals** before the same dedup → budget → mark; the marker is per-builder (`contextMarker`, default `source`), and `searchNodes` joins the Structure contract (`semanticSearchChunks` / `hybridSearchChunks` / `getNodeConnections` / `getCognitiveCapabilities`). A four-lens adversarial review of the v1.29.0 change found one real degrade-not-crash bug (an unguarded capabilities probe) — fixed — plus small naming/round-trip gaps, also fixed. **One minor hardening is deferred:** a *write-time* refusal of a Context builder that binds a non-embedding model to a vector retrieval — the run already **degrades-and-names** this at resolution, so it is defence-in-depth, not a correctness gap (and a lexical-only builder legitimately carries a non-embedding placeholder model, so the check must be scoped to vector retrievals). **Extended in v1.30.0:** the Context builder became a full **block builder** — its `retrievals` list generalised to **`contextBlocks`** (kinds: **text** / **segment** authored-and-trusted, **retrieval** ingested-and-hardened, **memory** / **session** recognised P2 placeholders), so a context segment can be authored, retrieved, or a mix (this is the concrete seat for the future memory tiers). Authored blocks are always kept in canvas order; retrievals RRF-fuse into the remaining budget. The token budget became a **scale** (Minimal→Maximum) resolving to a **percentage of the consuming model's context window** (new `ai_model.contextWindow`, fixed-token fallback) — replacing the raw `contextTokenBudget` with an admin-legible control that also delivers the intent of the deferred ledger-p95 budgeting. The **Builders** tab was renamed **Context**. **Extended in v1.31.0:** the Context editor became an **operation-style block builder**, the **marker moved from the builder onto each block** (a block with none inherits the consuming operation's **overall context marker** — `ai_operation.contextMarker` repurposed from the builder to the consuming op, no schema change), and each **Retrieval block now names its OWN embedding model + sensitivity** (off the builder). `assembleContext` resolves and embeds **per embedding model**, grouped by model and gated under the strictest sensitivity of the retrievals using it, so a sensitive retrieval whose model lacks zero-retention terms is **skipped and named before the query is embedded**; the builder resolves under a lenient floor so its inert placeholder model can't block an authored-only assembly. `seed-29` backfills the per-retrieval model + sensitivity from the builder op. A four-lens adversarial review returned one low finding (a stale schema comment), fixed. **Refinements still to come inside this phase** (they need producer signals absent on a Structure-only install, so P1 degrades-and-reports rather than claiming them): the **recency × importance ordering blend** (P1 orders by relevance), the **rung-selector granularity ladder** budgeting (P1 keeps the richest prefix that fits by raw token count), **graded near-duplicate dedup** (P1 collapses exact duplicates), and **ledger-p95 input budgeting** (P1 uses `contextTokenBudget` / a default). *Fills gaps 1, 2, 3 (partly), 4 (partly), 6, 7.*
- **P2 — memory tiers + verbalization.** ✅ **First cut shipped v1.32.0** — the **Memory** and **Session** context blocks are wired as **caller-supplied** context: the app fetches and passes `memory` / `session` on `aiCore.run()` (AI Core stays stateless and holds no user data), and each supplied item renders as one injection-hardened context segment under its marker, in position, within budget; an unsupplied block is NAMED. The same release added a **Retrieved display** block — a manual per-result template (`{content}` / `{index}` / `{node}` / `{score}`) that is the first slice of **verbalization** (the automatic read-time render of source titles/dates from node metadata still awaits the Structure extension below). The fuller **Structure-backed** tiers remain: distinct **semantic / episodic / session-state** tiers plus a **retrieved** and a **voice/terminology** peer, separately-fetched and labelled with **per-tier degrade-and-report** (conservative about auto-injecting memory — surfacing the wrong memory makes the window feel like it "no longer belongs to the user"). **Verbalization is AI Core's read-time render** (a fact sentence from `observedAt` + `origin` + node id via Structure's `getNodeContext`), *not* frozen at ingest — an edge's validity is mutable. Needs the small **Structure extensions** — project `observedAt`/`origin` onto the connection DTO, a `[validFrom, validTo)` edge interval, and surface the topK cap — tracked in the [requirements doc](/modules/cog-ingest/context-assembly-requirements/). *Fills gap 5.*
- **P3 — merge fields.** A runtime resolver **matching Structure's single-brace `{token}` grammar** (reuse `getMergeFieldSchema` / `resolveTemplateAsync`; AI Core's `{{ }}` is reserved for build-time scaffold), substituting context-derived values into admin-authored segments; a token-schema surface for the block-builder's "Insert field…" picker; built against the **Wysli defect catalogue** in gap 8 (fail-loud on a residual `{...}`, per-token defaults, injection-aware single-pass). *Fills gap 8.*
- **P4 — compression + dedup, measured.** Runtime summarisation as its **own evaluable stage** — the fallback *beneath* the rung ladder; Provence-style pruning; add a `noise-sensitivity` eval metric (the one RAGAS metric AI Core lacks). *Fills the remainder of gap 4.*
- **P5 — flow integration + (optional) agentic.** Extend flow state to carry multiple provenance-marked segments across suspend/resume (today it collapses to one JSON blob); the **optional** agentic retrieval-tool layer (for chat) rides the function-tool framework and re-runs assembly per tool result. *Fills gap 9; ties to Tools.*

**Invariants carried throughout:** the **epistemic guarantee** — every dropped / summarised / ungrounded item is *named* in `Feedback.missing`, never silent (the module's one rule, applied to assembly); **provenance** to a source node id, audited by reference (extends the ledger's by-reference discipline to runtime context — a real gap today); **ordering** highest-signal first/last (lost-in-the-middle's >30% mid-context degradation; the query already sits *after* context via `CANONICAL_ORDER`); **datamarking escalation** (word-level spotlighting) stays the separate, eval-gated roadmap item — layered, never relied on alone; **formatting** stays the framework's job. **Thresholds that change the plan:** skip compression under a few-thousand-token budget (overhead outweighs benefit); tighten budgets/compress *before* enlarging the window if distraction appears (watch the model's ceiling — Databricks found ~32k for Llama 3.1 405B, ~64k for GPT-4-0125); escalate spotlighting only on real injection incidents.

**Ingest/store contract:** the producer signals this plan assumes (per-chunk summary rung, importance, hash; stamping `observedAt`) and the Structure extensions it needs are specified in [Cog Ingest → Context Assembly Requirements](/modules/cog-ingest/context-assembly-requirements/), framed so a `Structure + AI Core`-only install still works.

#### Semantic Cache

AI Core sees all provider traffic, so every consumer benefits without knowing the cache exists. Cache keys are scoped to the owner, hard — a cache keyed on similarity alone will serve one person's answer to another, and that is a real, reproduced leak. Sensitive content is not cached, or cached only where provider terms permit.

#### Re-embedding Migration

AI Core owns the embedding model, so it owns the moment the model changes: the existing vectors are invalid, and only AI Core knows it. Report the scale, then migrate in batches with progress. Silently leaving stale vectors in place is the same failure as silent truncation.

#### Review-Queue UI Polish

The review-queue table and its feed from abstentions and low-confidence outcomes shipped in v1.1.0. What remains is surface polish: the consumer-facing component a module can drop in, and the admin gaps alongside it — most visibly a Flows tab (flows have full admin CRUD endpoints but no tab of their own).

#### User Model-Choice UI

The API capability shipped in v1.1.0 — model choice is admin-granted per operation with an allow-list, default off, and a user-selected model outside the allow-list is refused. There is no user-facing UI for making the choice yet.

#### Performance — best-practice cleanups (August 2026 audit)

The audit's minor findings — no current user impact, worth doing as the catalogues grow. **Most shipped in v1.22.3** — the independent-read parallelisations (`disciplines`/`eval`/`config-io`), the `refreshCapabilities` `updateMany`, the guardrail PK getter, the `updateReviewItem` P2025 map, the FK indexes, the hot-path graph-expansion/multi-check overlaps, and the frontend derived-map memoisations. What remains deferred (with reasons):

- **`select`-narrowing on public list reads** (`listModels`, `listOperations`, `listUsage`, `listReviewItems`, and the `@db.Text`/`Json` catalogue lists): each changes a return-type / DTO contract the admin editor and config export read off, for a verified-zero-impact payload trim on a small curated set. Needs a deliberate list-vs-detail DTO split, not a blind `select`.
- **Assembler micro-hoists** (`markerPrefixes`, the `anyInjectionRisk` scan): computed twice per assembly, but each is microsecond in-memory work and the only fix threads a value through the assembler's fixed dispatch signature — churn without measurable gain.
- **`resolveOperation` branch overlap** (model branch vs discipline branch): single-digit-ms saving on a path dominated by the model call, and a naive `Promise.all` would reorder which refusal surfaces first, changing the designed ordered-refusal semantics.
- ~~**The zod-validator memo** (`providers/adapters.ts`)~~ — **done in v1.22.5**: `zodFromJsonSchema` is cached by the schema's stable serialisation instead of rebuilding the validator tree every structured run.

Grouped (the original catalogue, for reference):

- **Backend over-fetch / redundant queries (admin cold path):** add `select`s to `listModels` / `listOperations` / `listSegments` (the last reads segments twice — `select` only `id` on the first pass); replace `updateGuardrail`'s list-and-find with a `findUnique`; drop the pre-read in `updateReviewItem` (map Prisma `P2025`); collapse `refreshCapabilities`' per-model stale-flag loop into one `updateMany`; parallelise the remaining independent reads (`resolver.ts` 248, `disciplines.ts` 431, `eval.ts` 207, `config-io.ts` 354 / 865 / 1185 — the last is a per-guardrail `count()` in a delete loop, collapsible to one `groupBy`).
- **Hot-path micro-work:** memoise the per-operation zod validator (`adapters.ts` 386) instead of recompiling it every structured run; hoist `markerPrefixes` and the `anyInjectionRisk` scan in the assembler (each computed twice per assembly); thread already-fetched capabilities into `resolveEmbeddingTargetWidth` (`run.ts` 2224); parallelise the bounded graph-expansion and multi-check loops (`run.ts` 2382, 690) with `Promise.allSettled`.
- **Schema index hygiene:** the reverse-lookup FK index for the "is this in use?" model-delete check — `@@index([modelId])` on `ai_operation`, `@@index([frameworkId])` on `ai_model`; and `select` only list columns for the heavy `@db.Text`/`Json` catalogue fields. (Boolean flag columns need no index while the catalogues stay small.)
- **Frontend re-render hygiene (admin, low-traffic):** extract the dialog form state in `DisciplinesTab` and `PromptsTab` into child components (as `MarkersTab` already does); memoise `guardrailsFor` + `sorted` (`DisciplinesTab`) and `providerName` (`ModelsTab`) into `useMemo` maps; make the `ConfigTab` import textarea uncontrolled; paginate the models and golden-cases tables if those grow.

The **operation-graph cache** — caching the resolved segments / disciplines / markers / framework per `operationId`, version-stamped and invalidated on admin writes — is a larger architectural item, distinct from the response-level [Semantic Cache](#semantic-cache) above: it caches the *prompt-config records*, not model outputs. It removes the ~7 per-run reference reads entirely, but the two `Promise.all` waves and the ledger aggregate under Known Issues deliver most of the win far more cheaply, so it is lower priority.

### Long-Term / Open Questions

The design's own open questions (§21), carried here so they stay visible.

#### Discipline Authority Enforcement

The design's original question — can a registry reject a registration that conflicts with another registry's contents? — was answered in July 2026: registration-time rejection is unsound for **code registries** in the starter's ESM side-effect model, where a rule may register before the rule it conflicts with. The implementation landed somewhere stronger than the startup-barrier fallback the design reached for: disciplines are **database records**, which have no such ordering problem, so v1.1.0 enforces the hierarchy at **write time** — admin CRUD refuses violations transactionally — and re-validates at **resolution time**, where `aiCore.run` refuses an invalid set (see [Usage](/modules/ai-core/usage/)). **Superseded in v1.22.0:** the discipline `tier` and the additive authority hierarchy were **removed entirely** — the question of ordering across authorities no longer arises. What write-time and resolution-time `validateDisciplineSet` now guards is discipline-*set integrity* (every discipline known, each owning a guardrail, each model-scored guardrail naming a live scorer operation). What remains open is the CI lint for seed discipline/guardrail integrity, recorded under Known Issues. The principle underneath held throughout: **prose asks; code holds.**

#### Which Groundedness Scorer per Operation

The mechanism is settled and pluggable; which implementation runs on which operation is a per-operation decision that needs measurement, not a design argument.

#### Image and Video Provider Data-Handling Terms

Black Forest Labs and Replicate have materially weaker data-handling terms than the model providers. In v1.1.0 their provider rows are seeded **disabled** and their generation adapters are unbuilt (a call throws a typed not-implemented error) — nothing can be routed to them at all yet. Confirm their terms before they are ever enabled; the likely answer for sensitive content is that it never is, enforced by the sensitivity gate in resolution.

#### Chat Module or Generic Chat

If chat goes generic, persona has nowhere natural to live. Persona is the user's, conversation-scoped, and only chat has conversations — chat owns the records and hands AI Core a segment.

#### Multi-Breakpoint Caching

Anthropic allows up to four cache breakpoints at different change rates (seeds/disciplines stable for weeks, retrieved context daily, conversation per turn); the module emits two. The framework could emit cache-control breakpoints automatically at slot boundaries.

#### Context Token-Budget Stage

**Subsumed by [Context Assembly — the context engine](#context-assembly--the-context-engine)** (Medium-Term), which pins the full design. Kept here for the specific technique: a distinct context-builder stage — budget → compress → dedupe → edge-place (highest-ranked chunk at an edge, per "lost in the middle") — so retrieved context never overflows the window and the token budget is computed, not hoped. The assembler stays a pure sort; the context builder does the messy gathering (the industry's "context engineering as a superset of prompt engineering").

#### Discipline-Effectiveness Loop

A lightweight loop over the eval harness that prunes disciplines which don't earn their tokens — rules are versioned records, so their effect can be measured and the weak ones retired.

#### Naming Collisions

`prompt`, `token` and `model` collide with the module system's own terms. A naming pass is needed before the admin surface freezes.

---

## Completed

Items that have been done.

### v1.23.0 — Admin Redesign + Presets Removed (August 2026)

The admin page was reworked to match how the module is used, and the preset concept was removed. **Presets are gone** — the `ai_preset` table and `ai_operation.presetId` are dropped and dials come straight from the operation (per-operation `dials` are unchanged); the Presets tab, preset picker, admin routes and config-io preset handling are removed. A breaking schema + config change, picked up on `npx ai-core update` (presets were never seeded, so only admin-created ones are lost). **Operations now carry their own approaches and disciplines** as first-class slots in the editor — stored as private per-operation segments using the existing join tables (a reserved dotted key the kebab catalogue can never produce; no schema addition; the resolver and assembler treat them as ordinary segments), alongside the shared segment building blocks. **The operation editor is a full-page in-tab view** (list ↔ editor with Back), not a dialog — which also removes the per-keystroke re-render of the operations list. A dedicated **Approaches** tab was split out of the old Prompts tab (relabelled **Segments**, which hides operation-owned segments), and the **Disciplines** tab gained a per-guardrail delete. Tab order: Models · Operations · Segments · Approaches · Disciplines · Markers · Usage · Evals · Config. Template layer clean and adversarially reviewed (fixed a config round-trip double-link and a stale editor-error banner in review); verified live by install. See the [Changelog](/modules/ai-core/changelog/).

<Aside type="note" title="Doc-body sweep — done (2026-08-09)">
Several reference pages still described the removed preset concept. An [August 2026 documentation-accuracy audit](#documentation-accuracy--august-2026-audit) found the drift was wider than presets alone — stale `tier`/`authority` discipline mechanics, the old `ai_guardrail_check` table name, wrong table counts, and self-version stamps behind v1.23.0. All were corrected in the 2026-08-09 documentation pass; the record is kept under Known Issues.
</Aside>

### v1.22.4 — Truthful Embedding Coverage (August 2026)

Closed the "Embedding Coverage Is Reported as Full" known issue. An embedding value that exceeds its model's input ceiling is now reported as partially covered, making the run engine's `partial-embedding-coverage` feedback reachable — silent provider-side truncation is surfaced instead of hidden behind an always-full report. Following the design's own prescription (and the module's "everything is a record" principle), the ceiling is an admin-editable `ai_model.maxInputTokens` field — a schema change, seeded with the providers' published limits (OpenAI 8191, Gemini 2048, Voyage 32000) and editable in the Models tab — surfaced on the resolved operation and threaded into every embed / multimodal-embed call. The adapter converts it to a coarse character budget and no longer hardcodes any per-model limit; a model with no recorded ceiling reports full coverage rather than an invented one. See the [Changelog](/modules/ai-core/changelog/).

### v1.22.3 — Performance Pass (August 2026)

Acted on the August 2026 [performance audit](#performance-audit--august-2026). The one **critical** finding — the billing callback `getCurrentUsage` loading and summing the entire tenant ledger in JS — now aggregates in the database (`SUM(...) GROUP BY operationId`), turning O(rows) into O(operations). The per-`run()` hot path dropped from a ~7-deep serial DB chain to two `Promise.all` waves, `validateDisciplineSet` now hands its guardrail rows to the resolver (no second query), and the secondary review-queue write no longer blocks the response. Growable tables gained ordering indexes (`ai_usage_ledger`, `ai_eval_result`, `ai_review_item`) plus reverse-lookup FK indexes (`ai_operation.modelId`, `ai_model.frameworkId`) — a schema change, so existing installs pick them up on `npx ai-core update`. The offline eval harness runs cases through a bounded concurrency pool; the admin page sets `staleTime` / `refetchOnWindowFocus` so tab revisits serve from cache instead of a refetch storm; and several cold-path reads (config-io export/import, controller edit handlers) were parallelised or de-duplicated. Behaviour-preserving throughout; the changes are `tsc`-clean at the template layer, with live-install verification of the generated code as the remaining gate. Deferred (see the audit section): the config-io provenance double-write, the frontend interactive refactors (editor state extraction, list pagination), and a set of zero-impact `select`/hoist micro-cleanups. See the [Changelog](/modules/ai-core/changelog/).

### v1.22.0 — Prompt Model v2 (August 2026)

The prompt machinery was *simplified by removing concepts* (see the shipped item under **Prompt Model Evolution** above). Everything in a prompt is a **segment**; the two "how" kinds are their own records related into segments — **approaches** (unmeasured) and **disciplines** (measured, owning **guardrails**, `ai_guardrail_check` renamed `ai_guardrail`); an operation composes ordered segments and a **`systemWide`** operation's segments fold into every run. `important` moved onto the segment-membership join row (restated at the tail under a `key-rules` marker), `enabled` was removed from those records (membership is the switch), and the discipline tier / authority hierarchy and the `role` segment type were dropped. The declarative config JSON is **v2.0**, a clean break. Hardened through v1.22.2. **Upgrading a populated v1 install is a clean break** — the reshaped tables are reset to the v2 built-ins by the `onPrepareDatabase` hook (export custom rows via the Config tab first); requires scaffold ≥ 1.31.14. See the [Changelog](/modules/ai-core/changelog/).

### v1.9.0 — Prompt Caching (August 2026)

The first of the **cog-ingest hand-off** items to ship. An operation gains a `cache` policy (`{ instructions?, context?, ttl? }` on `ai_operation`) so it can reuse its stable prompt prefix — the system prompt, and/or the context block (a source document) — instead of paying for it on every call. Providers cache by prefix, so the two breakpoints nest: system-only across every call, system-plus-document across every chunk of one document (Anthropic's contextual-retrieval shape). Confined to the adapter; Anthropic-only for now, with a named `prompt-cache-unsupported` feedback shortfall on any other provider — it never pretends to have cached. Raised by cog-ingest's contextualise step. (The system-prompt breakpoint was corrected for AI SDK v7 in v1.11.3.) See the [Changelog](/modules/ai-core/changelog/).

**The cog-ingest hand-off.** Stage-1 (embed-at-width, language/vision operations) shipped through v1.6.0–v1.8.0. Stage-2 began with prompt caching (v1.9.0), then the **parse-QA judge / per-source summary** pair (v1.10.0): both are plain language operations the *consumer* seeds — AI Core ships no operations — so the summary needs nothing new, and the judge needed exactly one thing AI Core lacked, a way to be deterministic, delivered as the `deterministic` dial (temperature 0). Then **multimodal/visual embedding** (v1.11.0): `embedMultimodal` embeds images and text into one vector space through Voyage's multimodal models, added alongside `embed` with a `multimodal` model marker and two loud refusals. That closes the hand-off's active items. **Deferred:** a **rich transcription** operation (speakers, sentiment, tone, timestamps) — not needed for recording v1, since the upstream Docling pipeline is wired for document ingest only, not audio recordings; it waits until a recording use case is real. Callable **rerank** (item 7) shipped in v1.7.0. Truthful partial `EmbedCoverage` stays deferred under Known Issues.

### v1.3.0 — Consumer-Integration Surface (July 2026)

Closed the six raise-now gaps from cog-ingest's consumer-side readiness audit: `additionalProperties: true` free-key records in both zod builders (undeclared keys were being silently stripped — silent data loss, the exact failure this module exists to prevent); `ensureOperation` / `ensureSegment` idempotent seeding where consumers state model *requirements* rather than model names and admin edits always win; `RunOptions.maxOutputTokens` as a tighten-only cap recorded in the ledger; automatic framing for external content, with a missing framing segment reported in feedback rather than proceeding unframed; and corrected `run()` examples throughout the docs.

### v1.2.0 — Vision Input (July 2026)

Media attachments on consumer segments (`media: [{ mediaType, data }]`), handed to the provider adapter as file parts alongside the assembled input — never into the prompt text, the logs or the ledger. A model whose fetched capabilities are known to lack image input refuses the run rather than failing obscurely at the provider.

### v1.1.1–v1.1.3 — First Real Install (July 2026)

`npx ai-core init` into a real app, end to end: schema appended, feature generated, registrations wired, all six seed migrations applied (the module migration runner's first real use). Three defect classes the type harnesses could not have caught were found and fixed — Prisma 7 custom-output client imports, real starter component prop shapes, and the barrel's routes export — after which the host app type-checks the feature clean. A scaffold bug (scoped package specs mangled to `@scope/name@^4@latest`) was fixed upstream in module-starter scaffold v1.19.2.

### v1.1.0 — The Redesign Implemented (July 2026)

The whole former Short-Term column shipped as module **v1.1.0** (2026-07-23):

- **The `ai_` schema** — twelve models via the migrations runner, plus six seed migrations (providers, frameworks, segments, disciplines, checks, models). Every battery is a row: seeded, editable, resettable
- **Provider adapters on Vercel AI SDK v7** — Anthropic, OpenAI, Google, Voyage — confined to a single adapter file behind an interface AI Core defines
- **The operations resolver and `aiCore.run`/`runEmbed`** — the sensitivity gate, explicit parameter transforms, the Structure dimension gate, structured feedback on every return
- **Prompt segments, frameworks and assembly** — the computed date anchor and EXT-marked external content
- **Disciplines and guardrail checks** — authority enforced at write time and re-validated at resolution time
- **Flows with suspend/resume** and `initialState`; **the usage ledger** (references only) with the `getCurrentUsage` billing callback; **the review queue**
- **The tabbed admin page** at `/admin/ai-core` (order 30, platform section) and **the security registration** with deletion handlers
- **The legacy v2 surface deleted wholesale** — resolving all six known issues from the July 2026 audit

**Verification:** strict type harnesses over the extracted generated code — the backend against the real published SDK packages, the frontend against React 18 and TanStack Query v5. **Install verification has not yet been performed**; installing into a real starter app is the next gate, recorded under Known Issues. See the [Changelog](/modules/ai-core/changelog/) for the release detail.

### Scaffold Alignment — July 2026

Brought the repo in line with the current module contract before any redesign work:

- **Adopted module-starter scaffold v1.19.1** (contract v1.2.0).
- **Version line reconciled 2.0.0 → 1.0.0.** No consumers existed — verified across every sibling repo — so the line could be reset cleanly.
- **CLAUDE.md marker contract adopted.**
- **`docs/` became the shared documentation submodule**, and the stale bundled scaffold docs were removed.

See the [Changelog](/modules/ai-core/changelog/) for the release detail.

### Design — July 2026

The full redesign was published as this page set: [Overview](/modules/ai-core/), [Usage](/modules/ai-core/usage/), [Configuration](/modules/ai-core/configuration/), [Integration](/modules/ai-core/integration/), and [Developer Workflow](/modules/ai-core/developer-workflow/). It replaces the legacy surface wholesale and exists to prevent one failure — not *"the AI was wrong"*, but **"the AI was wrong and looked right."** Implementation followed as v1.1.0 (above).
