---
title: Roadmap
description: Known issues, planned improvements, and completed work for the Billing module
lastUpdated: 2026-07-23
sidebar:
  order: 6
---

import { Aside } from '@astrojs/starlight/components';

This page tracks known issues, planned work, and completed milestones for the Billing module. Each item carries enough context to be picked up as a standalone work item.

<Aside type="note" title="Where the 2026-07-14 audit went">
A full standards audit ran on 2026-07-14 against scaffold v1.18.0/v1.18.1 and host v1.52.1. Nearly all of its findings were resolved the same day by the v1.3.0 tenant-keyed redesign and the v1.4.x releases — those items now live under [Completed](#completed). Only the still-open remainder is listed under Known Issues.
</Aside>

---

## Known Issues

<Aside type="tip" title="2026-07-23 meta-audit — all 20 findings fixed in v1.5.0">
A four-dimension audit (standards / production / performance / security) of the generated code ran on 2026-07-23 against module v1.4.3 and host v1.54.0. It confirmed **21 findings** via adversarial verification — **20 distinct issues** after merging one duplicate pair (1 critical, 6 moderate, 13 minor). All were fixed in **v1.5.0** (see [Completed](#completed)), including the critical cross-tenant subscription-binding hole (`client-reference-id` is now HMAC-signed and verified in the webhook). The code fixes pass the module's wrapper checks and a full generated-code TS/TSX parse, and — as of v2.1.2 — a full **host type-check** in `veradai` (see the item below); the live webhook runtime leg is still pending.
</Aside>

### End-to-end host verification — type-check leg DONE (v2.1.2); runtime leg pending

- **Type-check ✅ (2026-07-24):** the full generated output — backend **and** frontend — compiles cleanly in `veradai` (host ≥ v1.54.0, React 19, Stripe SDK v22, Prisma client generated from the v2.0.0 schema). This closes a gap open since v1.4.0. It caught exactly one real bug the wrapper checks structurally cannot (Stripe v22 typing `accounts.retrieve` as requiring an id), fixed in **v2.1.2**. The admin page has also been eyeballed live.
- **Runtime ⏳ still pending:** the live webhook-delivery + test-payment loop is unverified. The connect flow refuses localhost and the three-tick verification needs a public endpoint, so this requires an actual deployment. What remains to exercise once deployed: connect the webhook, run the three-tick verification green, complete a test-card checkout with `stripe listen`/a live endpoint, and confirm the subscription syncs (and that a devtools-edited `client-reference-id` is rejected), plus the dunning/recovery transitions.
- **Where:** re-run in a deployed starter app (host ≥ v1.54.0) after `template:update` + `update-schema` + `prisma migrate dev`.
- **Severity:** Medium
- **Workaround:** None needed on ≥ v1.54.0 hosts. Verification steps: type-check both sides; boot; complete a test-card checkout with `stripe listen`; confirm the subscription syncs (and that a devtools-edited `client-reference-id` is now rejected, not honoured); exercise the dunning/recovery transitions and `/admin/billing`.

### Prisma model prefix — RESOLVED in v2.3.0

- **Resolved (2026-08-07):** `model Subscription` → `model BillingSubscription` and `enum SubscriptionStatus` → `enum BillingSubscriptionStatus`, so there is no generic name to clash in the host's shared `schema.prisma`. Both carry `@@map`, keeping the **physical** table/type named `"Subscription"`/`"SubscriptionStatus"` — which made this a **datamodel-only** change: `prisma migrate diff` on an existing install comes out empty (verified against a real host), so no data moves and it shipped as a **minor** (2.3.0), not the breaking migration first feared. Existing single-file installs migrate automatically via an `onPrepareDatabase` hook that rewrites the old blocks in place before the migrate. `BillingSettings` / `BillingPlan` were already prefixed. The client accessor is now `prisma.billingSubscription`; the HTTP wire types and the `SubscriptionStatus` React component are unchanged.

### Webhook event dedupe is per-process

- **Description:** Duplicate Stripe deliveries are deduplicated via an in-memory set of the last 1,000 event IDs. Multiple backend instances (or a restart mid-delivery) can process the same event twice. The sync operations are upserts and status transitions, so double-processing is mostly harmless, but it is not guaranteed side-effect-free forever.
- **Where:** `backend/src/features/billing/webhooks/stripe.webhook.ts` (`processedEvents`).
- **Severity:** Low (single-instance deployments are the norm for this product)
- **Workaround:** Run a single backend instance, or back the dedupe with a durable store (see Planned).

### Stripe errors leak untranslated on two member reads + the owner write path

- **Description:** `stripeToAppError` / `stripeToWriteError` normalise Stripe's HTTP status so the browser never sees a raw Stripe 401/404/429 — the module's stated invariant (backend `index.ts` ~L737). But `getPaymentMethod` (~L1641) and `getInvoices` (~L1706), both **member reads**, call Stripe with no translation, and the owner write path — `getOrCreateStripeCustomer` (~L1106), `createCheckoutSession` (~L1178), `createPortalSession`'s session create (~L1329) — skips `stripeToWriteError`. On a rotated/invalid key or a Stripe outage, `GET /payment-method` and `GET /invoices` surface Stripe's 401 to the browser; the shared `@/lib/api` client typically treats a 401 as an expired session and **logs the member out**.
- **Where:** backend `index.ts` — `getPaymentMethod`, `getInvoices`, `getOrCreateStripeCustomer`, `createCheckoutSession`, `createPortalSession`.
- **Severity:** Moderate (found 2026-08-07)
- **Fix:** wrap the Stripe calls in try/catch and rethrow via `stripeToAppError` (member reads) / `stripeToWriteError` (owner writes), matching `getPlanDetails`/`getPlanCards` and the existing write paths. Do **not** wrap the inner flow-data retrieve in `createPortalSession` (it deliberately degrades).

### An active plan can silently vanish once a Stripe account exceeds 100 prices

- **Description:** prices are versioned and never deleted (each amount change archives the old), so an account's total price count grows without bound. `getPlanCards` (~L3262), `syncPortalConfiguration` (~L3176) and `listPlans` (~L2615) call `prices.list({ limit: 100 })` with **no `active` filter** and a **single** 100-item page. Once total prices exceed 100, an active price carrying a needed `lookup_key` can fall off page 1 — `getPlanCards` then drops that plan from the customer cards (`plans.filter(p => p.prices.length > 0)`) and the portal omits it, silently. `getCatalogueReadOnly` already paginates 5 pages, proving the asymmetry.
- **Where:** backend `index.ts` — `getPlanCards`, `syncPortalConfiguration` (pass `active: true`); `listPlans` (paginate like `getCatalogueReadOnly`).
- **Severity:** Moderate (found 2026-08-07 — low likelihood, needs 100+ prices on one account, but silent and revenue-affecting)
- **Fix:** pass `active: true` to the `getPlanCards` / `syncPortalConfiguration` list calls (both use only live prices; also shrinks the payload); paginate `listPlans` with `starting_after`. Optionally warn on `has_more`.

---

## Planned Improvements

### Short-term (next version)

**Phase 3 — promotions (v2.1.0)**
- Promotions: start with *select an existing Stripe coupon* + an `allow_promotion_codes` toggle, which delivers most of the value for a fraction of the surface. The full builder (reference-B style) is a second versioning UI on near-immutable Coupons. Two hard rules when it lands: "Free Trial" must write `trial_period_days`, **not** a 100%-off coupon (which still collects a payment method and yields `active`, not `trialing`, breaking `TrialCountdown`/`TrialExpiringAlert`); and on the pinned `2024-12-18.acacia` API version promotion codes use the flat `coupon` param, not the 2025-09-30 polymorphic shape.

**Permanently out of scope** (these are per-seat/metered billing wearing a different hat — principle 4): installment payments, per-seat quantity, adjustable quantity in the Portal, product variants, and proforma-invoice line-item builders. Stripe's native Entitlements API is also *not* adopted — it is boolean feature flags with no numeric field, so `max_projects: 3` has no representation there.

**Complete the end-to-end host verification**
- Re-run the full pass in a current starter app (host ≥ v1.54.0) at v1.4.3: install, `prisma migrate dev`, type-check both sides (confirms the v1.4.1/v1.4.2 fixes and the v1.4.3 `allowNoOrigin` registration), boot, complete a test-card checkout with `stripe listen`, verify webhook sync and the dunning/recovery transitions, exercise `/admin/billing` (Stripe config save, webhook connect refusal on localhost, entitlement config save).
- *Context:* the v1.4.0 pass on `veradai` surfaced the two upstream issues, both fixed in starter v1.54.0; this item closes the loop. Record the result on this page.

### Medium-term (upcoming)

**Migrate webhook verification to the host security helpers**
- Optionally replace the handler's Stripe-SDK verification (`stripe.webhooks.constructEvent`) with the host's `verifyTimestampedWebhookSignature({ payload: req.rawBody, header, secret })` from `@/features/security` (starter ≥ v1.54.0) — replay window and rotation candidates handled host-side, one less behaviour to own.
- *Context:* deliberately **not** bundled into v1.4.3: `constructEvent` is Stripe's official implementation and already enforces a 300-second replay tolerance and checks all rotation signatures in the header, so the practical gain is small — and the swap touches the payment-critical path, so it should be made against a live ≥ v1.54.0 host (verify the backend import path and parse the event JSON explicitly after verification). Minor release when it lands.

**Durable webhook event dedupe**
- Replace the in-memory `processedEvents` set with a small `billing_webhook_event` table (event ID + processed timestamp, pruned by age), making processing idempotent across instances and restarts.
- *Context:* clears the per-process dedupe known issue; keep the "mark handled only after success" ordering so Stripe retries still work.

**Full-erasure owner flow**
- `billingService.deleteStripeCustomerForTenant(tenantId)` exists but nothing calls it. Add a deliberate, confirmed owner action (likely on `/admin/billing` in a danger section) that deletes the Stripe Customer — removing invoices from Stripe — for "delete everything" requests, distinct from the default deletion path that retains tax records.
- *Context:* two-mode deletion is the agreed design: default = cancel + retain records ('preserved'); full erasure = explicit second action.

**Meta-audit 2026-08-07 — minor hardening (v2.3.x)**
A four-dimension meta-audit (module v2.3.0) confirmed 12 findings via adversarial verification — **0 critical, 2 moderate** (see Known Issues), **10 minor**; the security dimension surfaced nothing. The minors, to batch when convenient:
- **Cache the member `getPlanCards` price list** (short TTL like `catalogueCache`) — `GET /plans` currently hits Stripe live on every request while every sibling read is cached. *(perf, backend ~L3262)*
- **Extract the duplicated `FRONTEND_URL` dev fallback** (`'http://localhost:5173'` in `createCheckoutSession` + `createPortalSession`) to one constant. *(standards, backend ~L1185/1293)*
- **Make the monotonic webhook guard atomic** — the `lastEventAt` read (~L1454) and upsert (~L1495) are a non-atomic TOCTOU, so a concurrent same-tenant delivery can let a stale event win; use a conditional `updateMany` (WHERE `lastEventAt IS NULL OR < eventCreatedAt`) or a row-locked transaction. *(production, backend)*
- **Collapse `recordWebhookDelivery` to one `updateMany`** (currently `findFirst`+`update`; the connected row is app-wide unique in single-owner installs). *(perf, backend ~L2064)*
- **Skip the per-plan portal sync during `copyCatalogueToLive`** — run `syncPortalConfiguration` once after the loop, not once per plan. *(perf, backend ~L3148)*
- **`PlanCards` should use `<Note>`** for its loading/error/empty states instead of hand-rolled `border-dashed` divs (the only three in the frontend; compose-primitives rule). *(standards, frontend ~L1289)*
- **Fix the checkout success-poll teardown** — the param-clear effect at 5s flips `checkoutSuccess` and tears down the 20s/10-attempt sync poll early; drive the poll off a mount-captured flag so a slow (>5s) webhook is still bridged. *(production, frontend ~L1795)*
- **Add `onError` UI to the admin archive/import/rebuild (+publish) mutations** — they define only `onSuccess`, so a failed action silently re-enables the button with no message. *(production, frontend ~L3244)*
- **Validate price amounts in `PlanEditor`** — `canSave` currently lets a $0 enabled interval publish a free recurring price into Stripe (no guard at any layer); require `unitAmount > 0`. *(production, frontend ~L2828)*
- **Set `staleTime` / `refetchOnWindowFocus:false` on the admin live-Stripe-backed queries** so an alt-tab doesn't re-probe Stripe. *(perf, frontend ~L3464)*

*(The `onUninstall` lifecycle hook shipped in v2.3.0 — see Completed — but as a warnings-only manual-cleanup hook: the CLI can't reach the app DB or Stripe, so it deliberately never cancels subscriptions or exports data, unlike the original plan here.)*

**Adopt the `migrations` array for any breaking schema change**
- If the model-prefix rename (or the next re-key) goes ahead, ship it via the scaffold v1.18 per-module migration runner (`npx billing migrate-db`) with expand → backfill → contract steps rather than asking apps to hand-migrate.

### Long-term / Ideas

**Multi-member tenancy readiness**
- Billing is already keyed on `tenantId`, so when the host resolves shared tenants (an invited member/VA on the same account), members inherit the owner's plan with no schema change. Remaining work is UX: the member read-only state exists on `/settings/billing`; extend the same "managed by owner" treatment anywhere else billing surfaces.
- *Context:* do **not** build tenancy into billing itself — the host owns membership resolution.

**Dashboard widget**
- A small plan/trial-status widget via `dashboardRegistry` (plan name, status badge, trial days remaining, past-due warning). Deliberately not shipped yet; the hooks make it trivial for apps to build their own.

**Usage-based pricing, if it ever returns**
- Metered billing was retired in v1.3.0 and remains out of scope. If usage pricing is ever revisited, build on Stripe's current Billing Meters APIs (the legacy usage-record APIs the old implementation used are deprecated) — treat the old code as unrecoverable prior art.

---

## Completed

Most recent first. Items moved here from the sections above as they ship.

| Item | Version | Date |
|------|---------|------|
| **`onUninstall` + `onPrepareDatabase` lifecycle hooks + data-safe model rename** — `onUninstall` prints a billing-specific manual-cleanup warning on `npx billing uninstall` (active subs keep billing after the webhook route goes; delete the orphaned webhook endpoint; Customer/invoice records retained for tax) — **warnings-only**, since the CLI can't load the host's ESM-TypeScript-source Prisma client. Model `Subscription` → `BillingSubscription` (+ enum `SubscriptionStatus` → `BillingSubscriptionStatus`) via `@@map`, keeping the physical table/type names, so `prisma migrate diff` on an existing install is **empty** (proven against `veradai`) — datamodel-only, no data moves; `onPrepareDatabase` migrates single-file hosts in place with a verify-or-leave-untouched gate. Host type-check clean (0 billing errors). Closes the generic-PascalCase-names Known Issue | v2.3.0 | 2026-08-07 |
| **Standards-alignment audit + scaffold update 1.18.2 → 1.31.10** — merged 96 commits of scaffold infra (auto-restore of deleted scaffold files, `module-contract.ts` sync, `conformance:check`/`contract:*` scripts, commander/esbuild bumps). Cleared a docs-submodule fetch blocker. Rebuilt `CLAUDE.md` with `SCAFFOLD:START/END` markers from the canonical template — fixing the stale `SettingsComponents` path (that Known Issue is now closed). Module code was already aligned (react-router v8 done, headers/`.js`/registries/self-contained schema all current); generated templates unchanged, so no host impact. Passes `conformance:check` | v2.2.2 | 2026-08-06 |
| **Action + filter hooks** — 5 subscription-lifecycle action hooks (`billing.subscription.updated/canceled`, `billing.payment.failed/recovered`, `billing.trial.will_end`) fired fire-and-forget after commit, plus the `billing.entitlements` filter; stable `BILLING_HOOKS` catalogue + context types re-exported from the barrel. Host-verified (0 billing errors in `veradai`) | v2.2.0 | 2026-07-24 |
| **Plan authoring + Stripe provisioning** — author plans on `/admin/billing`, publish to Stripe (`lookup_key` price identity, `transfer_lookup_key` versioning, archive-never-delete), `409` price-change confirmation with affected-subscriber count, import, copy-to-live, Portal configuration sync, `-1` unlimited limits, server-side trial resolution | v2.0.0 | 2026-07-23 |
| **Stripe Pricing Table retired** — replaced by `PlanCards` driving the existing `POST /checkout`; publishable key and pricing-table id removed (no Stripe JS in the app). Existing subscribers change plans via the Portal's `subscription_update_confirm` flow so Stripe owns proration | v2.0.0 | 2026-07-23 |
| Entitlements unified — plan limits are written into Stripe product metadata by the plan editor, so `BillingSettings.entitlements` demotes to the free/no-subscription fallback | v2.0.0 | 2026-07-23 |
| **Guided owner setup + webhook verification** — setup checklist derived from live probes, account/mode banner, webhook health with missing-events diff and Rebuild, three-assertion verification (probe created / signed delivery received / subscription event processed), read-only Stripe catalogue with health warnings, `lastWebhookEventAt` stamping | v1.6.0 | 2026-07-23 |
| Design principle 2 amended — "Stripe owns payment data, not the catalogue" (PCI scope stays with Stripe; use Stripe's native UI wherever a **provisionable** one exists). Research confirmed the Pricing Table has no API and is capped at 4 products, so plan authoring must replace it | — | 2026-07-23 |
| **Meta-audit (2026-07-23) — cross-tenant subscription-binding hole closed (critical):** `client-reference-id` is HMAC-signed and verified in the webhook; tenant resolution trusts a signed reference → existing customer row → customer metadata, and never rebinds a tenant to a different Stripe customer without proof | v1.5.0 | 2026-07-23 |
| Meta-audit — read-path resilience (stale-on-error cache + DB-backed degradation + Stripe-status translation to 503); monotonic webhook sync via `Subscription.lastEventAt`; `handlePaymentFailed` status guard; `connectWebhook` endpoint rollback on DB failure; `/invoices` limit floored | v1.5.0 | 2026-07-23 |
| Meta-audit — admin-page save guards (buttons gated on query success + load-error states, no more silent config wipe); settings-page post-checkout refresh/poll + copy, portal error/in-flight handling, payment-method/invoice load states, scoped param cleanup, `noopener` invoice links; pricing-table script-error + config-error states; `useEntitlements` server fallback on error | v1.5.0 | 2026-07-23 |
| Meta-audit — entitlements product cache (no live Stripe call per check) + parallel reads; Stripe SDK pinned `^22.3.1`; `getStripe`/`apiVersion` single-sourced; Australian-spelled status labels | v1.5.0 | 2026-07-23 |
| Production webhook delivery — webhook route registered with `allowNoOrigin: true` (server-to-server deliveries carry no `Origin` header; the HMAC authenticates the caller). Module now requires **starter ≥ v1.54.0** | v1.4.3 | 2026-07-16 |
| Module repo `CLAUDE.md` refreshed for v1.4.2 — tenant-keyed models, seats/metered retirement noted as permanent, admin-page config, current endpoints and env vars, `settings-blocks` import path (clears the stale-CLAUDE.md known issue) | — | 2026-07-15 |
| Scaffold merged v1.18.2 (EOL-normalised migration checksums — CRLF regression fix); all release tags v1.0.1–v1.4.2 created and pushed (release commits existed but were never tagged) | — | 2026-07-15 |
| Docs overhaul — full page set (overview, configuration, usage, integration, developer workflow, changelog, roadmap) replacing the stale v1.2.0-era overview | docs | 2026-07-15 |
| Backend templates import Prisma types from `lib/prisma`, not `@prisma/client` (host uses the `prisma-client` generator) | v1.4.2 | 2026-07-14 |
| Generated code type-clean against React 19 + current Stripe/Prisma SDKs | v1.4.1 | 2026-07-14 |
| Admin-automated webhook setup — endpoint created from `/admin/billing`, signing secret stored encrypted (AES-256-GCM), DB-first/env-fallback resolution, `STRIPE_WEBHOOK_SECRET` now optional (local dev only) | v1.4.0 | 2026-07-14 |
| Non-secret Stripe config (publishable key, pricing table ID, product ID) moved from `VITE_*` env vars to the admin page + `GET /config` | v1.4.0 | 2026-07-14 |
| Stripe API version drift tolerated — `current_period_end` read from the subscription item first (`2025-03-31.basil` moved it) with subscription fallback | v1.4.0 | 2026-07-14 |
| Tenant-keyed redesign — `Subscription` re-keyed to `tenantId @unique` (userId kept as actor); subscription owned by the account | v1.3.0 | 2026-07-14 |
| Security registration — ownership manifest + account-deletion handler (order 500): cancels the live Stripe subscription, hard-deletes local rows, retains Stripe Customer + invoices; `deleteStripeCustomerForTenant` for full erasure | v1.3.0 | 2026-07-14 |
| Seat-based and metered billing retired (`SeatManagement`, `UsageDisplay`, `/seats`, `/usage`, billing-model detection removed) | v1.3.0 | 2026-07-14 |
| Admin page (`/admin/billing`) with owner-configurable entitlements (`BillingSettings.entitlements`) merged under Stripe product metadata at runtime | v1.3.0 | 2026-07-14 |
| Admin/member route split — writes `requireAdmin`, reads member-level for feature gating; member read-only view on the settings page | v1.3.0 | 2026-07-14 |
| TanStack Query migration — `useQuery`/`useMutation` with `createModuleQueryKeys('billing')` keys throughout | v1.3.0 | 2026-07-14 |
| Structured logging — `createModuleLogger` throughout (`'billing'`, `'billing-webhook'`, `'billing-security'`), `audit: true` on financial mutations (was 11 `console.*` calls) | v1.3.0 | 2026-07-14 |
| Structure-only styling pass — semantic tokens, no colour literals / typography scale / font weights | v1.3.0 | 2026-07-14 |
| Webhook returns 5xx on processing failure so Stripe retries; in-memory event dedupe; `invoice.paid` recovers `PAST_DUE` → `ACTIVE` | v1.3.0 | 2026-07-14 |
| Typed Stripe→Prisma status map (removed `status as any` casts) | v1.3.0 | 2026-07-14 |
| Settings page imports fixed to `@/components/settings-blocks` (module previously failed to compile against host v1.52.1) | v1.3.0 | 2026-07-14 |
| Housekeeping — orphan `navigation.registration` and 13 unused placeholder exports removed; `serviceRegistry` version now `{{moduleVersion}}`; redundant `@@index` removed; Australian spelling in user-facing text | v1.3.0 | 2026-07-14 |
| Scaffold merged v1.17.0 → v1.18.1 (contract v1.2.0, per-module DB migration runner, `migrate-db` command) | — | 2026-07-14 |
| Runtime module registration with the host module registry (starter v1.48+, silent no-op on older hosts) | v1.2.1 | 2026-07-11 |
