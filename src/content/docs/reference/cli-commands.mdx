---
title: CLI Commands
description: Command reference for the starter template
---

## NPM Scripts

### Root Directory

| Command | Description |
|---------|-------------|
| `npm run dev` | Start frontend and backend |
| `npm run dev:frontend` | Start frontend only |
| `npm run dev:backend` | Start backend only |
| `npm run build` | Build both |
| `npm run type-check` | TypeScript checking |
| `npm run install:all` | Install all dependencies |

### Template Updates

| Command | Description |
|---------|-------------|
| `npm run template:check` | Check for updates |
| `npm run template:update` | Apply updates |

### Versioning

| Command | Description |
|---------|-------------|
| `npm run release:patch` | Bump patch version |
| `npm run release:minor` | Bump minor version |
| `npm run release:major` | Bump major version |

---

## Prisma Commands

Run from `backend/` directory:

```bash
# Create migration
npx prisma migrate dev --name describe_change

# Apply migrations (production)
npx prisma migrate deploy

# Generate client
npx prisma generate

# Open GUI
npx prisma studio

# Reset database
npx prisma migrate reset
```

---

## shadcn Components

```bash
cd frontend
npx shadcn@latest add button
npx shadcn@latest add card
npx shadcn@latest add dialog
npx shadcn@latest add dropdown-menu
npx shadcn@latest add input
npx shadcn@latest add switch
npx shadcn@latest add table
```

---

## Git Commands

### Template Updates

```bash
git fetch template-upstream
git merge template-upstream/main
```

### Add Remote

```bash
git remote add template-upstream https://github.com/org/unique-react-fullstack-starter.git
```

---

## Stripe CLI

```bash
# Login
stripe login

# Forward webhooks
stripe listen --forward-to localhost:3100/api/v1/billing/webhook

# Trigger test event
stripe trigger checkout.session.completed
```

---

## Troubleshooting

### Clear Caches

```bash
rm -rf node_modules
rm -rf frontend/node_modules
rm -rf backend/node_modules
npm run install:all
```

### Reset Database

```bash
cd backend
npx prisma migrate reset --force
```

### Check Ports

```bash
lsof -i :5173  # Frontend
lsof -i :3100  # Backend
```
